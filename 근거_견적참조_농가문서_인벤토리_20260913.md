# 견적참조 개별 농가 문서 인벤토리 — 90차 분석

**작성일** 2026-09-13 (90차) · **계기** 사용자 지시 "c 진행"(89차 ⑨(c) — 견적참조 개별
농가 문서 분석으로 CAPEX 표본·지역 분포 확대)
**대상** `스마트팜스펙/견적참조/` **112파일**(비카톡 **73** + 카톡 캡처 39)

> **값은 하나도 바꾸지 않았다.** 아래 금액은 **후보**이고, 등재는 원문 대조 절차를 따른다.

---

## 0. 결론

| # | 항목 | 결과 |
|---|---|---|
| 1 | 이미 엔진 표본인 농가 | `CAPEX_MAJOR` **12건** — 우민재·최혁진·이두희·윤성호·한일그린텍·이준희·맹주연·강정구·오기수·백가은·조윤정·박규현·구창회 |
| 2 | 표본 밖 문서 중 금액이 잡히는 것 | **10건 이상**(이명환 4종·논산 3사 견적·이동혁·이수근·박종훈 등) |
| 3 | 금액 추출의 함정 | 🔴 **합계 ≠ 총공사비** — 견적서가 둘 다 적고 엔진은 **총공사비(천단위 절사)**를 쓴다 |
| 4 | 검색 계층 결함 | 🔴 **case 분류 오류** — 지명·문서종류를 사람 이름으로 오인(9건) + 이름이 있는데 미상(16건) |

---

## 1. 🔴 합계 ≠ 총공사비 — 엔진 값이 맞다는 확인

요약 텍스트에서 최대 금액을 뽑아 엔진 값과 대조했더니 두 건이 어긋나 보였다.
**원문을 직접 열어 보니 둘 다 엔진이 맞았다.**

| 사례 | 원문 「합 계」 | 원문 「총공사비」 | 엔진/케이스 |
|---|---:|---:|---:|
| 한일그린텍 (이영준 설계예산서, 20p) | 480,636,**200** | 480,636,**000** | `ACTUALS` 480,636,000 ✅ |
| 이용균 (ms-9.6 설계예산서, 16p) | 506,007,**399** | 506,000,**000** | `cases/yonggyun` 506,000,000 ✅ |

한일그린텍 원문에는 **"총공사비 480,636,000 천단위 절사"**라고 절사 사실이 적혀 있고,
이용균 원문은 **"총공사비 : 일금오억육백만원정 ₩ 506,000,000"**으로 한글 금액까지 병기한다.

**교훈**: 이 말뭉치의 견적서는 **합계(세부 합)와 총공사비(절사·계약금액)를 둘 다** 적는다.
자동 추출로 "가장 큰 금액"을 집으면 **합계**가 잡히고, 그것은 계약금액이 아니다.
📌 이두희는 요약 추출값 **582,455,045**가 `ACTUALS`와 **원단위 일치**해 추출 자체의
신뢰도도 함께 확인됐다.

---

## 2. 금액이 잡히는 문서 (후보 — 등재 아님)

`content_summary`(200자 상한)에서 1천만~50억 범위 금액의 최댓값. **합계일 수 있으므로
계약금액으로 읽지 말 것**(1절).

| 금액(후보) | 문서 | 표본 |
|---:|---|---|
| 2,700,345,600 | 00-배광한 설계도서 / 00-정택근 설계도서 (동일 금액) | — |
| 1,489,000,000 | 221206 윤성호 청년스마트팜 내역서 | ✅ |
| 785,940,000 | 240510_총공사 내역서-1 | — |
| 631,693,546 | 박규현 견적서 (1) | ✅ |
| 582,455,045 | __원가설계도서_이두희(천안) | ✅ |
| 547,580,000 | 시공견적서 | — |
| 506,007,399 | 이용균(ms-9.6) 설계예산서 | — |
| 480,636,200 | 이영준 설계예산서(한일그린텍) | ✅ |
| 398,628,383 | 논산딸기 백가은 / 조윤정 (동일 금액) | ✅ |
| 383,148,248 | 산출내역서(본)_250630 | — |
| 367,867,000 | 군산 강정구 농가 | ✅ |
| 360,447,860 | 천안 맹주연님 견적서 | ✅ |
| 251,443,804 | 견적서_군산 오기수 농가 | ✅ |
| 239,800,000 | 25.12.29 군산 ㅁ75하우스견적 이명환 | — |
| 204,917,701 / 185,729,592 | 군산이명환농가 (천창랙피니언 / 커튼1중) | — |

⚠️ **동일 금액 쌍 2건**(배광한·정택근 2,700,345,600 / 백가은·조윤정 398,628,383)은
같은 템플릿을 쓴 것인지 추출 오검출인지 **확인하지 않았다**.

---

## 3. 🔴 case 분류 결함 — 검색 계층 오류

`NAME_PATTERNS`가 파일명에서 사람 이름을 집는데 세 가지로 틀린다.

### (a) 지명을 이름으로 오인
- `__원가설계도서_이두희(천안)` → **천안** (`\(([가-힣]{2,4})\)`)
- `원가계산서_이두희(천안)` → **천안**
- `설계도25 - 233 - [천안] 이두희 온실 검토서` → **천안** (`\[([가-힣]{2,4})\]`)

📌 **이두희는 `CAPEX_MAJOR` 표본 12건 중 하나**인데 그 설계예산서·원가계산서가
`case=천안`으로 잡혀 있었다 — `chunk_probe --case 이두희`가 이 문서들을 놓친다.

### (b) 앞 글자를 먹음
- `군산이명환농가-*` (3건) → **산이명환** (`([가-힣]{2,4})농가`가 "군**산이명환**농가"를 집음)

### (c) 문서 종류를 이름으로 오인
- `시공견적서.pdf` → **시공** (`^([가-힣]{2,4})\s*견적서`)
- `산출내역서(본)_250630.pdf` → **산출** (`^([가-힣]{2,4})\s*내역서`)
- `산출내역서(추가)_250630.pdf` → **추가** (`\((추가)\)`)
- `논산딸기백가은님75각 시공 견적서(최종).pdf` → **최종** — 괄호 패턴이 `님` 패턴보다
  앞에 있어 "(최종)"을 먼저 집는다(**패턴 순서** 문제)

### (d) 이름이 있는데 미상
접두사(`00-`, `1. `, `221206 `, `FIRMMIT_`)·공백(`강정구 농가`)·비한글 괄호
(`이용균(ms-9.6)`)·5글자 괄호(`(한일그린텍)`)에 패턴이 막힌다 — 배광한·정택근·강정구·
윤성호·우민재·오기수·구창회·최선동·한수진·이용균·이명환·한일그린텍 등 **16건**.

### 조치 (90차)
**`CASE_OVERRIDES` 31건 추가 + 해당 파트 무효화 후 재청킹.** (1차 28건 적용 후 재점검에서 같은 유형 3건이 더 나왔다 — `수현건설**임미라**님`의 앞 글자를 먹은 `설임미라`, 괄호 안 **업체명**을 이름으로 본 `렉창`, 문서종류를 집은 `설계`. 합쳐서 31건.) 정규식을 고치지 않은
이유는 ①패턴 수정은 전 말뭉치의 분류를 바꿔 회귀 범위가 크고 ②오버라이드는 이미
P2-16·P3-21c에서 쓰인 확립된 기구이기 때문이다. **파일명에 이름이 명확한 것만**
바로잡았고(추측 금지), 이름이 없는 문서는 잘못된 이름 대신 `견적참조-미상`으로 되돌렸다.

⚠️ `--retag-only`는 **태그만** 재계산하고 `case_name`은 바꾸지 않는다(설계 확인) —
그래서 파트 무효화 후 재청킹이 필요했다. `chunk_id`에 case명이 들어가 값이 바뀌지만
**엔진이 chunk_id를 참조하지 않음**을 확인하고 진행했다(`CAPEX_MAJOR_CASE_CHUNKS`는
금액 딕셔너리다).

---

## 4. 조사하지 않은 것

- **금액을 원문 대조하지 않았다** — 2절은 요약 추출 **후보**이고, 한일그린텍·이용균
  2건만 원문을 열었다. `ACTUALS`·`CAPEX_MAJOR` 표본 확대는 **별도 차수**(원문 대조 절차).
- 동일 금액 쌍 2건의 성격 미확인(같은 템플릿 vs 오검출).
- `견적참조` 개별 문서의 **면적·지역·공종 구조를 추출하지 않았다** — 파일명·금액
  수준까지만. 지역 분포는 89차 집계(문자열 포함 매칭)를 그대로 쓴다.
- 카톡 캡처 39장은 OCR 편입돼 있으나 **내용을 보지 않았다**.
- `NAME_PATTERNS` 자체는 고치지 않았다 — **새로 들어오는 파일은 같은 방식으로 틀린다.**
- 결과 검증: 재청킹·병합 후 `견적참조` case 분포에 **오분류 잔존 0**, 농가 case **20종**,
  미상 2,142청크(이름이 실제로 없는 문서들). 총 청크 수는 **148,424로 불변**이다
  (내용은 그대로이고 `case_name`·`chunk_id`만 바뀌었다).
