# 문서 청킹 구조 — 설계/공사비/시방서 등 원본자료 → 주제축 정리

작성일: 2026-07-24 · 상위 원칙: `작업지시서.md` 1절(엔진 단일출처·근거 명시·자동화 경계) · 관련: `설계상세_입지매핑표_OPEX분해_확정.md`

## 0. 이 문서의 위치 — 엔진과의 관계부터 명확히

이 청킹 인덱스는 **엔진의 "제2 계산 출처"가 아니다.** `smartfarm_engine.py`의 상수(SPEC_TABLE, REGION_DESIGN_LOAD, CAPEX_MAJOR_CATEGORIES 등)는 여전히 유일한 계산 근거이고, 이 문서가 설계하는 건 그 상수들의 **원본 근거자료를 찾고 인용하기 쉽게 만드는 검색·정리 계층**이다. 즉:

- 청크에서 뽑은 수치를 엔진 상수로 "승격"하려면 기존과 동일한 절차(실측 확인 → 레지스트리 등록 → 드리프트 가드 테스트)를 그대로 거친다.
- 청킹 인덱스 자체가 계산에 쓰이거나 앱이 실시간 조회하는 대상이 되지 않는다. 어디까지나 사람(컨설턴트)·다음 세션의 내가 "이 수치 근거가 어느 파일 몇 페이지에 있나"를 빨리 찾기 위한 목적이다.

## 1. 현재 보유 원본자료 인벤토리 (실측 스캔 결과)

`스마트팜스펙/` 218개 파일 (pdf 123·png 46·xls 16·xlsx 11·hwp 9·jpg 6·docx 6·doc 1) + `시설평가/` 다수. 사례별 폴더:

| 사례 폴더 | 성격 |
|---|---|
| `우민재/`, `이두희/`, `이녕연/`, `최혁진님 온실 내역서/`, `한일그린텍/` | 실제 시공 사례 1건씩 — 설계도면·내역서·시방서·구조계산서가 한 세트로 존재 |
| `견적참조/` | 다수 농가(최선동·한수진·박규현·박종훈 등)의 견적서·도면 혼재 — 세트가 불완전(도면만 있거나 견적서만 있음) |
| `2025년 청년농업인...`, `[입찰대행] 2025년...` | 정부 지원사업 표준 서식(공모계획·검토서·표준 공사비내역서) — **사례가 아니라 제도/기준 문서** |
| `노지견적/`, `노지시방서/` | 온실이 아닌 노지(관수·양액) 전용 — 기존 엔진 스코프(온실 중심) 밖일 가능성, 확인 필요 |
| `시방서(RFQ), 견적서(세부내역서QOM), 도면(설계도서)/`, `대산온실/` | 폴더명만 확인, 내용 미확인 |

**중요 제약**: 이녕연·최혁진 문서 일부는 스캔본이라 `_OCR.pdf` 버전이 별도로 존재(이미 외부에서 OCR된 것). OCR 안 된 스캔본은 이 환경에 OCR 도구가 없어(2026-07-23 확인 완료, tesseract·libreoffice·pandoc 전부 미설치) **청킹 대상에서 자동 제외**하고 "OCR 필요" 상태로 표시한다.

## 2. 문서유형 분류체계 (Doc Type)

사용자가 제시한 4쌍 + 실제 파일에 존재하는 유형을 추가로 반영했다:

| doc_type | 정의 | 실제 파일 예 |
|---|---|---|
| `설계도면` | 배치도·평면도·정면/측면/후면 골조도·주단면도·부재연결부 상세도 | `최선동 - 정면골조도.pdf`, `한일그린텍/설계도면(한일그린텍).pdf` |
| `설계예산서` | 설계 단계 공사비 추정(원가설계) | `한일그린텍/설계예산서(한일그린텍).pdf`, `원가계산서_이두희(천안).pdf` |
| `공사비도면` | 공사비 산출의 근거가 된 실행도면(설계도면과 파일이 같을 수 있음 — 링크만 별도 태깅) | 도면 파일 재사용 + `케이스 공종별 수량` 대조 |
| `공사비내역서` | 시공 확정 비용의 공종별 내역 | `우민재/1. 공사내역서 스마트팜 확대보급 시범사업.xlsx`, `혁진 스마트팜 온실 신축공사_공사비 내역서.pdf` |
| `구조계산서` | 하중조건(적설·풍하중)·부재검토 계산 과정 | `이두희/이두희_251024_구조계산서.pdf` |
| `시방서` | 공종별 시공기준·자재규격·시공방법 (일반시방/전문시방) | `우민재/5-1. 시방서(우민재).pdf`, `노지시방서/*.docx` |
| `수량산출서` | 물량 산출 근거(내역서의 앞단) | `우민재/2. 수량산출(우민재).xlsx` |
| `공사공정표` | 공종별 일정 | `우민재/4. 공사공정표(우민재).xls` |
| `검토서` | 정부지원사업 심사용 종합검토(입지+설계+사업성 혼재) | `[함평] 이준호 온실 검토서.pdf`, `[천안] 이두희 온실 검토서.pdf` |
| `사업공모/과업지시서` | 제도 기준 문서(사례 아님) | `[붙임4] 스마트팜 온실 신개축 지원사업 공모계획.pdf`, `과업지시서_제주...pdf` |
| `기자재현황/품셈/가이드라인` | 범용 참고자료(기존 `PUMSEM_ITEMS`/`EQUIPMENT_DB_META`가 이미 이 유형에서 나옴) | `202201_스마트팜 표준화_품셈.pdf`, `(붙임1)스마트팜 혁신밸리 ICT 장치 설치 가이드.pdf` |

## 3. 주제축 (Topic Axes) — 제시한 9개 + 비판적 보완

사용자가 제시한 9개(부지·기후·작물특성·냉난방·광량·CO2·생육관리·운영·유지보수)는 **농업/환경 관점**엔 맞지만, 실제 문서의 상당수(구조계산서·공사비내역서·골조도)는 이 9개 어디에도 깔끔히 안 들어간다. 억지로 끼워맞추면 태깅이 왜곡되므로 2개 축을 추가할 것을 제안한다(승인 필요):

| 축 | 포함 내용 | 왜 필요한가 |
|---|---|---|
| **구조/자재** (추가 제안) | 골조규격·피복재·연결부·하중조건 | 구조계산서·설계도면의 실제 내용 대부분이 여기 속함. 없으면 "냉난방"에 억지로 태깅하게 됨 |
| **비용/공정** (추가 제안) | 공종별 단가·수량·일정 | 공사비내역서·수량산출서·공정표가 여기 속함. CAPEX_MAJOR_CATEGORIES와 직접 연결됨 |

최종 11축(9+2) 중 어디에도 안 걸리는 콘텐츠는 태깅하지 않고 `미분류`로 남긴다(근거 없는 축 확장 금지 원칙과 동일하게, 안 쓰는 축을 미리 만들지 않는다).

## 4. 문서유형 × 주제축 매트릭스 (주 태깅 대상만 O, 부차 △)

| doc_type \ 축 | 부지 | 기후 | 작물특성 | 냉난방 | 광량 | CO2 | 생육관리 | 운영 | 유지보수 | 구조/자재 | 비용/공정 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 설계도면 | △ | | | | △ | | | | | O | |
| 설계예산서 | | | | △ | | | | | | △ | O |
| 공사비도면 | | | | | | | | | | O | △ |
| 공사비내역서 | | | | △ | △ | | | | | △ | O |
| 구조계산서 | | O | | | | | | | | O | |
| 시방서 | | | | O | △ | | △ | △ | △ | O | |
| 수량산출서 | | | | | | | | | | | O |
| 공사공정표 | | | | | | | | | | | O |
| 검토서 | O | O | O | △ | | | | O | | △ | △ |
| 기자재현황/품셈/가이드라인 | | | △ | O | O | O | △ | O | O | | O |

이 표는 "어디를 먼저 읽어야 하는지"를 정하는 용도다 — 예를 들어 냉난방 근거를 찾을 땐 시방서·품셈부터 보고 공사비내역서는 보조로 본다.

## 5. 청킹 단위 (doc_type별)

| doc_type | 청크 단위 | 근거 |
|---|---|---|
| 설계도면/공사비도면 | 도면 1매(시트) | 도면은 텍스트가 아니라 시트 단위 정보 — pdfplumber로 페이지별 텍스트/치수 주석만 추출, 도형 자체는 청크에 담지 않고 "몇 페이지에 어떤 도면"만 색인 |
| 설계예산서/공사비내역서/수량산출서 | 내역 행 1건(공종-품목-규격-수량-단가-금액) | 기존 `PUMSEM_ITEMS`/`EQUIPMENT_DB` 처리 방식과 동일 — 행 단위가 자연 단위 |
| 구조계산서 | 계산 절 1개(하중조건/부재검토/처짐검토 등) | 계산 과정은 절 단위로 전제조건이 묶여 있어 쪼개면 의미 손실 |
| 시방서 | 조항 1개(장-절-조 체계 그대로) | 시방서는 원래 조항 번호로 구조화돼 있어 그대로 따르는 게 가장 안전 |
| 공사공정표 | 공종 1행(공종명-기간-선후행) | |
| 검토서 | 섹션 1개(입지현황/설계개요/사업성 등 원문 목차 그대로) | 검토서는 이미 여러 축을 섞어 다루므로 원문 섹션 경계를 그대로 청크 경계로 사용 |
| 기자재현황/품셈/가이드라인 | 품목/조항 1개 | 기존 엔진 처리와 동일 |

## 6. 청크 메타데이터 스키마

```json
{
  "chunk_id": "문자열, 예: 이두희_구조계산서_p3_적설하중",
  "source_file": "스마트팜스펙/이두희/이두희_251024_구조계산서.pdf",
  "case_name": "이두희 | 우민재 | 최혁진 | 한일그린텍 | 이녕연 | 견적참조/최선동 | 제도문서(사례아님) | 범용",
  "doc_type": "설계도면 | 설계예산서 | 공사비도면 | 공사비내역서 | 구조계산서 | 시방서 | 수량산출서 | 공사공정표 | 검토서 | 사업공모 | 기자재현황",
  "doc_subtype": "예: 정면골조도, 적설하중검토, 냉난방설비",
  "page_or_section": "페이지 번호 또는 조항 번호",
  "topic_tags": ["부지","기후","작물특성","냉난방","광량","CO2","생육관리","운영","유지보수","구조/자재","비용/공정"],
  "content_summary": "1~2문장 요약(원문 인용 아님 — 저작권/과다인용 방지)",
  "raw_value_extracted": {"항목": "값", "단위": "..."},
  "evidence_status": "실측 | 법정기준 | 참고자료 | 확인요망",
  "engine_link": "SPEC_TABLE | REGION_DESIGN_LOAD | CAPEX_MAJOR_CATEGORIES | FR_TABLE | U_VALUE | null"
}
```

`evidence_status`는 기존 `엔진데이터_레지스트리.json`의 상태 어휘(실측/미검증/법정기준 후보 등, `작업체계_계획_비판검토.md`·최근 CAPEX 감리비 조사에서 나온 "법정 요율" 상태 포함)와 **동일한 어휘를 그대로 재사용**한다 — 두 시스템이 다른 상태 분류를 쓰면 나중에 합칠 때 혼란만 커진다.

## 7. 처리 파이프라인 (제안, 아직 미실행)

1. **파일 분류**: 파일명 패턴(정규식)으로 `doc_type`·`case_name` 1차 자동 태깅 → 사람이 표본 검수(전수 검수는 비효율).
2. **텍스트 추출**: PDF는 pdfplumber(표 있는 문서: 내역서·구조계산서) 또는 일반 텍스트 추출(시방서), docx는 python-docx, xls/xlsx는 openpyxl. hwp는 이번 세션에서 확인된 대로 이 환경에서 텍스트 추출 불가 → 건너뛰고 목록만 남김.
3. **청킹**: 6절 단위 규칙대로 분할.
4. **주제축 태깅**: 키워드 사전 기반 1차 태깅(예: "적설|풍하중|기후" → 기후, "보온커튼|열관류율|보일러" → 냉난방) → 애매한 건 `미분류`로 두고 사람이 나중에 채움. **AI가 문서 내용을 보고 자동으로 확신 없는 값을 추정해 채우지 않는다** (기존 원칙 그대로).
5. **저장**: 엔진 코드와 분리된 별도 인덱스 파일(예: `문서청킹_인덱스.jsonl`, 1행=1청크) — DB 도입은 이 규모(수백~수천 청크)에선 과설계라 보류.
6. **연결**: 나중에 특정 엔진 상수의 근거를 찾을 때 `engine_link` 필드로 역색인.

## 8. 실행 순서 제안 (승인 필요 — 아직 시작 안 함)

- 1단계: 사례 세트가 완전한 5개 폴더(우민재·이두희·이녕연·최혁진·한일그린텍)부터 파일럿으로 전체 파이프라인 실행 → 스키마가 실제로 맞는지 검증.
- 2단계: 검증되면 `견적참조/`(세트 불완전, 30여 건) 확장.
- 3단계: 제도문서(검토서·공모계획)와 범용자료(품셈·기자재현황)는 이미 상당 부분 엔진에 반영돼 있으므로 **중복 작업 여부부터 확인 후** 착수.
- 노지견적/노지시방서는 온실 스코프 밖일 가능성이 있어 **착수 전 사용자에게 스코프 포함 여부 확인 필요**.

## 9. 미해결 · 확인 필요

- [x] 11축(9+2) 확정 — **승인됨(2026-07-24)**, 아래 10절 파일럿 결과로 타당성도 실증됨
- [ ] 저장 형식(JSONL 제안) 확정 여부
- [ ] 노지(관수/양액) 자료의 스코프 포함 여부
- [x] 파일럿 실행 완료(아래 10절), 전면 확장 착수 여부는 사용자 확인 대기

## 10. 파일럿 실행 결과 (2026-07-24, 우민재·이두희·이녕연·최혁진·한일그린텍)

`build_document_chunks.py` 실행 → `문서청킹_인덱스_파일럿.jsonl`(5,475청크) + `문서청킹_파일럿_요약.txt`.
스키마가 실제로 맞는지 검증하는 게 목적이었고, **부분적으로만 성공**했다 — 아래 문제들은 전면 확장 전에 고쳐야 한다.

### 확인된 문제 (전면 확장 전 수정 필요)

1. **행 단위 청킹의 맹점(가장 중요)**: 공사비내역서/설계예산서를 표 행 단위로 쪼개니 청크의 66.1%(3,618/5,475)가 주제축 미분류로 나왔다. 원인은 상위 공종명(예: "1-1. 기초공사")이 하위 행("고리고정구 | 32용 | 조 | 236 | 800 | ...")에 안 붙어서 행 하나만 보면 문맥을 알 수 없기 때문. → **수정안**: 표 파싱 시 공종/절 헤더 행을 감지해 하위 행들의 `content_summary`에 상위 헤더를 carry-forward 해야 한다.
2. **키워드 오탐**: "배수용수중펌프"(설비명, 공사비내역서 행)가 '부지' 축으로 잘못 태깅됨 — '부지' 축 키워드 "배수"가 너무 넓어 기계설비명과 충돌. 키워드를 더 구체화하거나(예: "배수계획|배수로|암거배수" 등 복합어) 문서유형별로 키워드 세트를 다르게 적용해야 한다.
3. **구조계산서의 폰트/인증 메타데이터 노이즈**: 일부 페이지에서 실제 계산 내용이 아니라 PDF 내부 폰트·인증서 메타데이터가 텍스트로 추출됨("Certified by Gen MODEL DATA PROFILE..." 반복) — 페이지 단위 청킹만으론 걸러지지 않아 필터 로직(반복 패턴 감지 등) 필요.
4. **"완전 사례 5개"라는 1절 인벤토리 서술이 틀렸음**: 이녕연 폴더는 실제로는 도면 1건(그것도 스캔 OCR 저품질)만 있고 내역서·구조계산서·시방서가 전혀 없다 — 청크 6개만 나온 게 그 증거. 1절 인벤토리 표를 다음 갱신 때 정정해야 한다.
5. **레거시 .xls 파싱 실패 1건**: `우민재/4. 공사공정표(우민재).xls`가 xlrd로 파싱 안 됨(SUPBOOK 외부참조 레코드 관련 xlrd 자체 버그로 추정, encoding_override로도 미해결) — 수동 확인 또는 별도 도구 필요, 자동화 한계로 남긴다(값을 추정해 채우지 않음).

### 검증된 것 (그대로 유지)

- **11축 확장 결정이 타당했음**: "구조/자재"+"비용/공정" 두 축이 전체 태그 건수의 다수(1,601/약 1,857건, 약 86%)를 차지 — 추가하지 않았으면 대부분 미분류됐을 것.
- **OCR 대체 로직 정상 작동**: 원본이 스캔(텍스트 0자)이면 `_OCR.pdf` 형제 파일로 자동 대체하고 `extraction_quality=ocr_noisy`로 품질 저하를 명시하는 방식이 이녕연·최혁진 두 사례에서 의도대로 작동함.
- **중복 파일 처리 정상 작동**: 이두희의 구조계산서 중복본(`_251024 final.pdf` = `_구조계산서.pdf`와 내용 동일), 최혁진의 불필요한 OCR본(원본에 이미 텍스트 있음) 모두 청크 생성 없이 `duplicate_of`로만 표시됨.
- **pdfplumber 표 추출이 표 있는 문서에서 대체로 작동**: 3,035개 청크가 `table_extract`(표 인식 성공)로 나왔고, 실패 시 `line_fallback`(80건)으로 자연스럽게 대체됨.

### 2차 실행 (수정 후 재검증, 같은 날)

`build_document_chunks.py`에 공종 헤더 carry-forward(`section_context` 필드 신설)·부지 키워드 정교화·노이즈 필터를 반영해 동일 5개 사례로 재실행.

| 지표 | 1차 | 2차 |
|---|---|---|
| 미분류 청크 비율 | 66.1% | **48.8%** |
| 냉난방 태그 건수 | 210 | **957** (헤더 carry-forward 효과 — 상위 "냉난방설비" 공종 아래 개별 행들이 이제 함께 태깅됨) |
| 부지 태그 건수(오탐 제거 후) | 60 | 13 (배수펌프 등 오탐 제거) |
| noise_suspected(구조계산서 메타데이터 노이즈) | 미탐지 | 27건 탐지·격리 |

**개선됐지만 48.8%는 여전히 높다 — 원인을 표본 확인한 결과, 이건 파이프라인 버그가 아니라 문서 자체의 한계다.** 우민재 내역서는 "자재 177호 | 체인커플링 | 32A | 개 | 55000 | ..." 식으로 **개별 부품명 하나가 한 행 전체**인 구조라, 그 위에 걸리는 공종 헤더 자체가 없거나(코드번호가 헤더 역할을 대신함) 있어도 "체인커플링"·"유니온밸브"·"미늘엘보" 같은 개별 부품명은 11개 축 어디에도 의미상 안 걸린다 — 억지로 걸면(예: "구조/자재" 키워드를 부품명 전체로 확장) 거의 모든 행이 걸려 축이 무의미해진다.

**결론**: 행(row) 단위 청킹은 "공종 헤더가 명확하고 그 아래 몇 개 행만 있는" 문서(이두희 원가계산서, 한일그린텍 설계예산서처럼 조항형 구조)에는 잘 맞지만, **"코드번호+개별 부품명" 나열형 내역서(우민재 FIRMMIT 스타일)에는 안 맞는다.** 이걸 더 개선하려면 청킹 단위 자체를 바꿔야 한다 — 개별 행이 아니라 **공종 그룹 전체를 1청크로 묶는 방식**(그룹 내 행들을 합쳐서 그 그룹 전체 맥락으로 태깅)을 검토해야 하는데, 이는 6절 "청킹 단위" 규칙 자체의 재설계라 이번 파일럿 범위를 넘어선다.

### 다음 결정 필요 — **해결됨(2026-07-25): 전면 확장 진행**

## 11. 전면 확장 결과 (2026-07-25, `스마트팜스펙/` + `시설평가/` 전체)

파일럿 검증 후 `build_document_chunks_full.py`(자동분류, `chunking_lib.py` 공용) + `build_document_chunks_full.py`를 작성해 전체 186개 파일(노지·대산온실·이미지·hwp·.doc·락파일 제외)을 처리했다.

### 실행 중 발견·해결한 문제

1. **`견적참조/` 폴더 전체가 Git LFS 미다운로드 상태였음**: 첫 실행에서 50여 개 파일이 "No /Root object!"(PDF) 또는 "found b'version '"(xls/xlsx — LFS 포인터 텍스트의 시그니처) 오류로 실패 — 이전 세션에서 겪은 것과 같은 LFS 미다운로드 문제가 이번엔 `견적참조/`·`시설평가/` 폴더 전체에 걸쳐 있었다. `git lfs pull --include="스마트팜스펙/견적참조/*,시설평가/*"`로 해결 — 재실행 후 청크 수가 37,593 → 48,433으로 늘었다(최선동·한수진·박규현·이용균·이명환·서산 이준희 등 여러 사례의 실제 도면·견적서 내용이 그제서야 잡힘).
2. **OCR본 중복 처리**: 파일럿의 수동 로직("원본에 이미 텍스트 있으면 OCR본은 건너뜀")이 전면 확장 스크립트엔 없어서 최혁진 공사비내역서가 원본+OCR본 두 번 청크됨 — `find_non_ocr_sibling()` 체크를 메인 루프에 추가해 해결.
3. **`견적참조/`는 폴더 전체가 각기 다른 사람(농가)의 개별 자료 모음**이라는 게 실제로 확인됨 — 파일명에서 사람 이름을 정규식으로 추출하는 `guess_case_from_filename()`으로 최선동·한수진·이명환·박규현·박종훈·백가은·조윤정·맹주연·이준희·이용균·오기수·구창회·윤성호 등 13개 이상의 실제 사례로 분리됐다. 다만 이름 추출 규칙이 일부 파일에서 엉뚱한 단어를 사람 이름으로 잘못 뽑았다(`시공`·`산출`·`추가`·`최종`·`설계` 등이 "케이스명"으로 오분류됨, `case별 청크 수`에 그대로 노출) — **정확도 한계로 남겨둠**, 필요하면 다음 세션에서 수작업 재분류.
4. **여러 폴더에 걸친 완전 중복 파일**: MD5 해시 대조로 11건 발견(이두희 관련 5건, 우민재 관련 2건, 한일그린텍/이영준 관련 2건, 시설평가 품셈/시공능력평가 각 1건) — 전부 `견적참조/`에 원본 사례 폴더와 동일한 파일이 파일명만 바뀐 채 중복 존재했음. 청크 생성 없이 `duplicate_of`로만 표시.
5. **해결 못 하고 남긴 것**(전부 로그에 명시):
   - `우민재/4. 공사공정표(우민재).xls` 등 4개 레거시 .xls — xlrd 자체의 SUPBOOK 레코드 파싱 버그로 추정, 해결 안 됨
   - RFQ 폴더 PDF 2건("파파딸기/설계도면.pdf", "과수사업/설계도면.pdf")과 시설평가 PDF 1건 — LFS는 이미 받았는데도 "No /Root object!" — 진짜 손상되었거나 PDF가 아닌 다른 포맷을 확장자만 .pdf로 잘못 붙인 것으로 추정
   - 스캔본인데 OCR본이 없는 파일 다수(★공사비내역서 표준서식, 박종훈 내역서, 서산 이준희 도면 등) — 추정 채움 금지 원칙에 따라 건너뜀

### 최종 통계

| 지표 | 값 |
|---|---|
| 총 청크 수 | 48,433 |
| 처리 실패/건너뜀 | 78개 파일(사유 전부 로그에 명시) |
| 완전중복 제거 | 11건 |
| doc_type 분포 | 기자재현황/품셈/가이드라인 29,926 · 공사비내역서 12,068 · 설계예산서 2,235 · 설계도면 753 · 시방서 487 · 수량산출서 326 · 구조계산서 149 · 사업공모 142 · 미분류 2,347 |
| 미분류(태그 0개) 청크 비율 | 64.1%(31,063/48,433) — `기자재현황`(품목 리스트류)이 전체의 62%를 차지해서 파일럿의 "개별 품목명은 축에 안 걸림" 현상이 그대로 재현됨. 파이프라인 결함이 아니라 이미 파일럿에서 확인한 구조적 한계 |
| engine_link 걸린 청크 | 1,712 / 48,433 (3.5%) |

### 산출물

- `문서청킹_인덱스_전체.jsonl` — 전체 48,433개 청크(1행=1청크)
- `문서청킹_전체_요약.txt` — 파일별 처리 로그 + 통계 + 건너뛴 파일 목록
- `chunking_lib.py` — 공용 추출·태깅 로직(파일럿·전면 확장 스크립트가 공유)
- `build_document_chunks.py` — 5개 사례 수동 매니페스트(재현용으로 유지)
- `build_document_chunks_full.py` — 전체 자동분류·실행 스크립트(재실행 가능)

### 다음에 필요하면 할 일 (지금은 안 함)

- `견적참조-미상`(3,828청크)과 `시공`·`산출`·`추가`·`최종`·`설계` 같은 오분류 케이스명을 사람이 한 번 훑어서 정정
- 공종 그룹 단위 재청킹(6절에서 남긴 과제) — `기자재현황` 62% 미분류를 줄이려면 이게 유일한 실질적 개선책
- LFS 이미 받았는데도 손상된 것으로 보이는 PDF 3건 원본 재확인(발주처에 재요청 등)
