# FR_TABLE 수치 근거 후보를 찾았다 — 그리고 U_VALUE는 이미 풀려 있었다 (161차)

**작성일** 2026-09-20 (161차) · **계기** 160차가 9축 인덱스를 정본으로 확정 → P0-a 재탐색
**성격** 엔진 상수 **값 변경 0** · 케이스·견적비교 불변 · **결정 0** — 원문 대조와 ref 1건 추가

> 작업계획서(2026-07-16)가 *"`U_VALUE`·`FR_TABLE`은 **근거가 없는 상태** — P0-a는 다른 모든
> 작업보다 우선"*이라 적은 항목이다. 재탐색 결과는 **둘이 전혀 다른 상태**였다.

---

## 0. 결과

| | 계획서(7월) | 실제(161차 확인) |
|---|---|---|
| `U_VALUE` · `U_DESIGN` | *"근거 없음 · 단위 의심"* | ✅ **이미 해소돼 있었다**(99·100차) — refs 3건, 단위 환산까지 확정 |
| `FR_TABLE` **수치** | *"원출처 전무"* | 🔴 **후보를 찾았다** — 다만 **등재값과 정합하지 않는다** |

---

## 1. 🔴 먼저 — 내가 기존 기록을 읽지 않고 원문부터 봤다

`[표 3-3-30]`(PDF p335 · 인쇄 p.300)을 열어 *"등재 유리 5.3이 세 계열(6.16/5.70/5.82)
어디에도 없다"*고 적었다. **틀렸다.** `U_VALUE`의 `source_refs`가 이미 이렇게 적고 있었다:

> PDF p.336(인쇄 300) 표3-3-30 유리 1중피복 RDA **6.16 W×0.86 = 5.298** —
> 유리 5.3 '정상환산설'의 **두 번째 독립 지목(100차)**

**표는 W/㎡·K이고 엔진은 kcal/㎡·hr·℃다.** 1 W = 0.86 kcal/h이므로:

| 표 값(W/㎡·K) | ×0.86 | 등재 |
|---|---|---|
| 유리 1중 **RDA 6.16** | **5.298** | `U_VALUE["유리"] = 5.3` ✅ |
| 플라스틱 1중 **RDA 6.63** | **5.702** | `U_DESIGN["필름"] = 5.7` ✅ |

→ **계획서가 의심한 *"kcal인지 W인지"*는 이미 100차에 풀려 있었다.** `U_DESIGN`의 ref는
**`exact`**이고 *"같은 행 유리 RDA 6.16×0.86=5.30이 `U_VALUE['유리']`와 일치해 **축 동일성
보증**"*이라고까지 적는다.

**이것이 이번 차수의 첫 교훈이다** — 158차에서 *"함수는 호출해서 확인한다"*를 배웠는데,
이번엔 **등재된 근거를 읽지 않고 원문만 봤다.** 같은 유형이다.

---

## 2. ✅ `FR_TABLE`은 다르다 — 수치 근거가 정말 없었고, 후보를 찾았다

`FR_TABLE`의 기존 refs 2건은 **수치 근거가 아니다**:

| ref | 무엇인가 |
|---|---|
| `시설평가/…김평화(제공).pdf` | *"fr의 **정의·방향** 근거이지 절감률 **수치** 근거는 아니다"*(20회차 F3이 `exact`→`partial` 정정) |
| `근거_농사로_스크린보온력표_…md` | 농사로 **스크린 매수별** 보온력표(카테고리가 다르다) |

그리고 `source`는 탐색 범위를 이렇게 적었다:

> 2026-07 라운드3: **`스마트팜스펙/` 전수 검색** 결과 … **어디에도 없음**

**`스마트팜연구DB/`를 보지 않았다.** 7월 청킹 인덱스에는 `연구자료` 카테고리가 **통째로
없었고**(160차 확인), 9축(2026-09-13)에 와서야 **88,915청크**로 들어왔다.

### 찾은 것 — [표 3-3-27] (PDF p333 · 인쇄 p.298)

> **온실의 보온커튼(1층) 재료별 열절감율**
>
> | 보온피복재 | 열절감율(%) |
> |---|---|
> | 폴리에틸렌 필름 | **30~35** |
> | 염화비닐 필름 | 35~40 |
> | 부직포 | 25~30 |
> | 알루미늄 분말 혼입 필름 | 40~45 |
> | 알루미늄 증착 필름 | 50~55 |
> | 알루미늄 스크린(LSP) | 43~47 |
> | **다겹보온커튼** | **58~67** |

### 🔴 대조 — 등재 4값이 정합하지 않는다

| `FR_TABLE` | 값 | 표 3-3-27 | 판정 |
|---|---|---|---|
| `PO단일` | 0.35 | 폴리에틸렌 필름 30~35 | ⚠️ **상단과 일치**하나 표에 **PO(폴리올레핀) 항목이 없다** |
| `다겹보온` | 0.50 | 다겹보온커튼 **58~67** | 🔴 **범위 밖**(하한보다 8%p 낮다) |
| `이중커튼`·`2중커튼` | 0.70 | — | 🔴 이 표는 **「1층」 전용**이고 최대값도 67이다 |

본문이 못 박는다 — *"커튼은 복층으로 설치하면 … **2층 커튼의 조합에 의한 열관류율의
차이를 표 3-3-28에 나타냈다**"*. 즉 **2층은 열절감율이 아니라 열관류율로 제시**되므로
`이중커튼 = 0.70`을 이 표에서 **같은 단위로 끌어낼 수 없다.**

⚠️ **이 표가 `FR_TABLE`의 출처라고 단정하지 않는다.** 정합하지 않으므로 다른 출처일 수
있다. 확정된 것은 **①리포에 「보온커튼 재료별 열절감율」 표가 실재한다**(그동안 *"전무"*로
기록돼 있었다) **②그 표와 등재 4값이 맞지 않는다** **③2층은 그 표의 대상이 아니다** 셋이다.

---

## 3. 조치 — ref 1건 추가, 값은 불변

`FR_TABLE.source_refs`에 이 표를 **`near`**로 추가했다(등재값이 이 문서에서 나온 것이 아니라
**근접·불일치**를 보이므로). 이제 `verify_refs`가 이 상수를 검사한다.

⚠️ **`U_VALUE`·`U_DESIGN`·`FR_TABLE` 값은 하나도 바꾸지 않았다.** 값 교체는 회귀 기준을
움직이는 **★사용자 결정**이고, `FR_TABLE`의 경우 *"표 3-3-27을 채택할 것인가"* 자체가 판단이다
(정합하지 않으므로 채택하려면 **어느 행을 PO단일·이중커튼에 대응시킬지**를 정해야 한다).

---

## 3-1. 🔴 162차 보강 — 전수 스캔이 끝나고 두 개가 더 나왔다

161차는 인덱스가 가리킨 쪽만 열고 *"`이중커튼 = 0.70`의 출처는 **여전히 미확보**"*라 적었다.
같은 문서 **761쪽 전수 스캔**(백그라운드)이 끝나자 **2층 값이 원문에 있었다.**

### ① 2층 커튼의 열절감율 — PDF p375(인쇄 p.340)

> 플라스틱 필름 온실의 열관류율 6.75 W/m²℃(일본시설원예협회, 2007)와 비교하면 60%의
> 열절감율을 보이는 것이다. **알루미늄 증착필름+PE필름의 2층 커튼이 열절감율 55∼65%**인
> 것과 비교하면 본 실험온실은 3층 커튼을 쓰고 있으므로 …

→ **2층 커튼의 열절감율이 원문에 있다: 55~65%.**
`FR_TABLE["이중커튼"]·["2중커튼"] = 0.70`은 **그 상한보다 5%p 높다** — **여전히 범위 밖**이다.

⚠️ 161차가 *"[표 3-3-28]은 열관류율이라 단위가 달라 끌어낼 수 없다"*고 적은 것은
**그 표에 한해서는 맞지만**, **본문에 열절감율 형태의 2층 값이 따로 있었다.**
**인덱스가 가리킨 쪽만 보고 「없다」고 적은 것이 성급했다.**

### ② 「열절감율 50%」 — PDF p313(인쇄 p.278)

> TAC 1%와 2.5%의 난방설계기온 차이는 평균 1.7℃이지만, **열절감율 50%의 보온커튼**을
> 설치한 3,300m²의 연동 플라스틱 온실(보온비 0.8) 설계를 예로 들면 …

`FR_TABLE["다겹보온"] = 0.50`과 **숫자가 일치**한다. ⚠️다만 이것은 **설계 계산 예시의
가정값**이지 **재료별 값이 아니다** — 「다겹보온」에 대응한다는 근거는 없다.
**일치를 근거로 읽지 않는다.**

### 정리 — 네 값의 현재 상태

| `FR_TABLE` | 값 | 원문에서 찾은 것 | 판정 |
|---|---|---|---|
| `PO단일` | 0.35 | [표3-3-27] 폴리에틸렌 30~35 | ⚠️ 상단 일치, **PO 항목 없음** |
| `다겹보온` | 0.50 | [표3-3-27] 다겹보온커튼 58~67 / p313 설계예시 **50%** | 🔴 표 범위 밖 · 예시와는 **숫자만** 일치 |
| `이중커튼`·`2중커튼` | 0.70 | p375 2층 커튼 **55~65** | 🔴 **범위 밖**(상한 +5%p) |

**네 값 중 원문 범위 안에 드는 것은 `PO단일` 하나**이고, 그것도 표에 PO 항목이 없다.

---

## 4. 검증하지 않은 것 / 하지 않은 것

- **결정은 하나도 내리지 않았다.**
- **`FR_TABLE`이 표 3-3-27에서 왔다고 단정하지 않았다** — 정합하지 않는다.
- ~~**`이중커튼 = 0.70`의 출처는 여전히 미확보**다~~ → 🔴**162차 정정**: 2층 열절감율이
  **p375에 55~65%로 있다**. 등재 0.70은 **그 범위 밖**이다. 161차는 **인덱스가 가리킨 쪽만 보고**
  「없다」고 적었다 — **전수 스캔이 끝나기 전에 결론을 냈다.**
- **`다겹보온 0.50`을 p313 설계예시와 연결하지 않았다** — 숫자만 같을 뿐 **재료 대응 근거가 없다**.
- **`COVER_ASSEMBLIES`(표9·10·11)를 이 문서와 대조하지 않았다** — 별건이다.
- **99차의 `U_DESIGN` 5.7 채택을 재판정하지 않았다** — ref가 `exact`로 이미 서 있다.
