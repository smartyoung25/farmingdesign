# 이견 — 난방 계수(FR_TABLE 절감률 · U_DESIGN) 재검토

**작성일** 2026-09-13 (76차) · **계기** 75차에서 사용자 첨부 `(최종)온실 선정 및 비용 견적 프로그램.xlsx`
(이하 E파일) 대조 중 난방부하 3.12배 격차가 나왔고, 레드팀 19회차가 그 원인을 원문에서 규명했다.
**성격** 1절 "자료 간 모순은 본문에 섞지 않고 '이견' 항목으로 분리한다"에 따른 이견 문서다.

> **이 문서는 값을 바꾸지 않는다.** 76차 범위는 이견 정리와 영향범위 실측까지이며,
> 변경이 필요한지 자체를 아래 측정으로 먼저 답한다.

---

## 0. 결론 (먼저)

**엔진 현행값을 유지한다** — `FR_TABLE["이중커튼"/"2중커튼"]=0.70`, `U_DESIGN["필름"]=8.9`.
근거는 셋이다.

1. E파일의 0.85는 **절감률 단순 가산 오류**임이 같은 시트에서 확인됐다. 오류를 걷어내고
   복원하면 0.675가 나오는데, 이는 68차가 농사로 표로 채택한 **0.70과 근사 일치**한다.
   즉 이 발견은 현행값을 **반박하지 않고 독립 경로로 지지**한다.
2. 0.675로 바꿔도 **산출물이 움직이지 않는다** — `2중커튼`·`이중커튼` 키는 live 사용처가
   0건이다(견적비교 2건 모두 `다겹보온`). 부하 배수도 1.083에 그친다.
3. ~~`U_DESIGN=8.9`가 live인 케이스는 `uminjae` 1건이고, 실측 대조 ratio 0.89로 정상 대역
   한가운데다. 이중계상이 실제 수치를 밀어내고 있다는 징후가 없다.~~
   **→ 77차에 철회.** 사실관계(live 1건·ratio 0.89)는 그대로이나, "징후가 없다"는 판단이
   틀렸다 — `verify_heating_vs_actual`의 밴드가 0.35~1.8로 넓어 1.56배 오차를 흡수한다.
   넓은 밴드를 근거로 "징후 없음"을 말한 것이 오류였다(4절 ①).

**남는 판단성 2건**(아래 4절) — 즉시 조치 사유는 없으나 원문 확보 시 재개할 것.

> **🔴 2026-09-13 77차 갱신 — 결론이 뒤집혔다. 0절 3번은 더 이상 유효하지 않다.**
> 77차 중반까지는 ①이 "해소"됐다고 판단했으나, **레드팀 20회차가 리포 안의
> 미검색 폴더(`스마트팜연구DB/`, PDF 36개)를 찾아냈고** 그 안의 1차 원문이
> 정반대를 가리킨다 — **이견 ①은 해소가 아니라 확증됐다**(4절 ① 참조).
> 요지: `열절감율`은 **PE필름(0.08mm) 난방부하계수 5.7 kcal/㎡·h·℃ 기준의
> 상대값**으로 정의돼 있어, 식의 `난방부하계수` 자리에는 **5.7**이 와야
> `5.7 × (1 - 열절감율)`이 그 재료의 실제 난방부하계수가 된다(검산 3건 일치).
> 엔진은 그 자리에 `U_DESIGN=8.9`를 넣으므로 **1.56배 과대**일 가능성이 크다.
> ⚠️ **값은 여전히 바꾸지 않았다** — 변경은 ★사용자 판단이다(4절 ①).
> ②는 **사용자 결정으로 후보에서 제외**됐다(냉방 전용 계수, 4절 ②).

---

## 1. 무엇이 어긋났나 (75차 실측)

E파일 `시설구성내역` 시트의 자체 케이스를 엔진에 넣은 결과다.

| 항목 | E파일 | 엔진 | 비 |
|---|---:|---:|---:|
| 표면적 Aw (㎡) | 6,027.47 (`L8`) | 동일 | — |
| 난방부하계수 U | 5.7 (`L9`) | 8.9 (`U_DESIGN["필름"]`) | ×1.5614 |
| 보온 열절감률 | 0.85 (`L10`) → 노출 0.15 | 0.70 → 노출 0.30 | ×2.0000 |
| 최대난방부하 (kcal/h) | **147,905** (`M15`) | **461,879** | **×3.1228** |

**식 구조는 동일하다.** E파일의 계수(U=5.7·fr=0.15)를 엔진에 주입하면 147,905를 소수
13자리까지 재현한다. 3.1228배는 위 두 계수의 곱(1.5614 × 2.0000 = 3.1228)으로 **정확히**
분해되며 잔차가 없다.

---

## 2. 0.85의 유래가 밝혀졌다 (이번 검토의 실질 성과)

E파일 `시설구성내역!L10`=0.85 **바로 옆 칸**에 구성 내역이 병기돼 있다.

| 셀 | 내용 |
|---|---|
| `K10` | 보온피복(fr) |
| `L10` | **0.85** |
| `M10`·`N10` | 보온다겹 : **0.5** |
| `O10`·`P10` | PO : **0.35** |

0.35 + 0.5 = 0.85 — **2중 피복 절감률의 단순 가산**이다.

### 합성 모델 3종 비교

| 모델 | 식 | 결과 |
|---|---|---:|
| 단순 가산 (E파일) | 0.35 + 0.50 | **0.850** |
| 독립 곱 | 1 − (1−0.35)(1−0.50) | **0.675** |
| 직렬 열저항 (농사로, 68차 채택 경로) | U₂ = 1/(1/7 + 2/6) = 2.100 → 1 − 2.100/7 | **0.700** |

> ⚠️ **모델 동질성은 미검증[추정]이다.** 독립 곱과 직렬 열저항은 다른 모델이고, 농사로 표는
> "유리온실 + 에너지스크린 2장"이며 E파일 조합은 "PO필름 + 보온다겹커튼"이라 **대상 조합이
> 다르다**. 0.675와 0.700이 가깝다는 것은 "구간의 방향성" 근거이지 소수점 비교 근거가 아니다
> (68차가 세 출처의 수렴을 다룰 때 세운 것과 같은 유보다).

### 엔진 주석의 기존 유래 가설과의 관계

엔진은 0.85의 유래를 `[추정]`으로 이렇게 적어 두었다 — "제품 카탈로그의 **차광률**이 85~86%대라
보온율 칸에 옮겨 적었을 가능성"(시설평가/스마트온실 구축 가이드라인 p200~206).

이번 발견은 그 가설의 **1차 반례**다. 0.85가 차광률에서 온 것이 아니라 **0.35와 0.5의 가산**에서
왔음을 원자료가 자기 셀로 보여준다. 더구나 그 0.35·0.5는 리포가 NIHHS 공식 계산기 실측
(33.6%·56.3%)과 "근사 정합"으로 이미 인정한 바로 그 두 값이다.

단 **다른 경로에서 0.85가 만들어졌을 가능성을 배제하지는 못한다** — E파일이 A-5 원문서라는
증거는 없고(A-5는 여전히 미확보), 이 시트가 같은 오류를 독립적으로 저질렀을 수도 있다.
따라서 엔진 주석의 차광률 가설은 **삭제하지 않고 병기**한다.

---

## 3. 영향범위 실측 (2026-09-13)

### 3-1. 케이스 3건의 현행 난방 — 전부 정상 대역

케이스는 `curtain`이 아니라 `fr`(노출비율)을 직접 주입한다(`render_report.py:32`).

| 케이스 | 피복 | u_design | fr | 최대부하(kcal/h) | 면적당 | ratio | 판정 |
|---|---|---:|---:|---:|---:|---:|---|
| chuncheon | 유리 | 5.3 | 0.7 | 458,185 | 132.6 | 0.57 | 정상 |
| uminjae | 필름 | **8.9** | 0.7 | 372,826 | 160.5 | 0.89 | 정상 |
| wonchaewon | 유리 | 5.3 | 0.7 | 330,190 | 95.5 | 0.41 | 정상 |

(`mulhyangki`·`yonggyun`은 부분 케이스 — 설계 파라미터 없음)

### 3-2. `fr=0.7`은 노출비율로 읽는 것이 실측과 맞는다

케이스의 0.7이 **노출비율**(절감 30%)인지 **열절감률**(노출 0.3)인지는 provenance가 없어
문서상 확정되지 않는다. 두 해석을 모두 계산해 실측 대조에 걸어 봤다.

| 케이스 | 노출비율 해석(현행) | 열절감률 해석(가정) |
|---|---|---|
| chuncheon | ratio 0.57 **정상** | ratio 0.25 재확인 |
| uminjae | ratio 0.89 **정상** | ratio 0.38 정상 |
| wonchaewon | ratio 0.41 **정상** | ratio 0.18 재확인 |

**현행 해석만 3건 전부 통과한다.** 방향반전 사고(P1-9가 시그니처로 차단한 그것)가 케이스
경로에서 실제로 일어나지는 않았다는 방증이다.

> ⚠️ 이것은 **방증이지 확정이 아니다.** `verify_heating_vs_actual`의 허용 밴드는 0.35~1.8로
> 넓고(자릿수 오류 검출이 목적), wonchaewon의 0.41은 하단 0.35에 가깝다. A-5 원문이나 케이스
> 원자료에서 fr의 정의를 직접 확인하면 이 항목은 방증에서 근거로 올라간다.

### 3-3. FR_TABLE을 바꾸면 무엇이 움직이나 — 현재는 0건

| 키 | 값 | live 사용처 |
|---|---:|---|
| PO단일 | 0.35 | 0건 |
| 다겹보온 | 0.5 | **견적비교 2건**(논산딸기 3사 · 군산무화과 규격대안) |
| 이중커튼 | 0.70 | 0건 |
| 2중커튼 | 0.70 | 0건 |

0.70 → 0.675 교체 시 노출비율 0.300 → 0.325, 부하 배수 **1.083**. 그리고 그 키를 쓰는
산출물이 없으므로 **실효 영향 0**이다.

> ⚠️ 12회차 F2의 "이중커튼 입력 시 부하 2배"와 혼동하지 말 것 — **그 기록은 여전히 유효하다.**
> F2가 말한 2배는 **68차 교체**(0.85→0.70, 노출 0.15→0.30)가 이중커튼 입력에 주는 효과이고,
> 위 1.083배는 **이번에 검토한 0.70→0.675**라는 별개 변경의 효과다. 두 변경은 다른 것이다.
> 이번 실측이 재확인한 것은 F2의 후반부 — "live 사용처가 없어 실제로는 아무것도 안 움직였다".

---

## 4. 남는 판단성 (즉시 조치 사유 없음 — 원문 확보 시 재개)

### ① `U_DESIGN=8.9` 이중계상 소지

`U_DESIGN["필름"]=8.9`는 Diop et al.(2012) 핫박스 실측의 **PE필름 단일피복 고풍속** 값이다.
커튼이 있는 온실에 이 U를 쓰고 노출비율로 커튼 효과를 또 빼면 보수 마진이 두 번 들어갈 소지가
있다. 다만 —

- live 케이스는 `uminjae` 1건이고 ratio 0.89로 정상 대역 **한가운데**다.
- `heating_load`는 애초에 **최대난방부하**(장비 용량 결정용 극한조건) 계산이고, 73차가 원출처
  정의로 확정했듯 이 값은 "난방기가 최대로 공급할 수 있는 열량"이다. 극한조건에서 커튼이
  전개돼 있다고 전제할 근거가 없으므로 단일피복 U를 쓰는 것이 보수적으로 타당할 수 있다.
- 반대로 과대하면 장비 **오버사이징** 방향이라 작물 리스크는 아니다.

**판단 필요**: 최대부하 산정 시 커튼 전개를 전제할 것인가. 이는 설계 관행 영역이다.

> ### 🔴 해소 실패 — 오히려 확증됐다 (2026-09-13 77차, 레드팀 20회차 F2)
>
> 사용자 지시 "최대부하 산정 커튼 전제 진행"으로 조사를 시작했고, 77차 중반까지는
> **"해소"로 판단했다가 되돌렸다.** 경위와 최종 판정을 둘 다 남긴다.
>
> #### (1) 처음 세운 논거 — 철회
>
> 식 원출처를 리포 안에서 찾았다. `시설평가/20230627_스마트팜 시설의 구조와
> 이해_김평화(제공).pdf` **printed p.49**:
>
> > **난방부하 = 하우스표면적 \* 난방부하계수 \* (내부설정온도 - 외부기온) \* (1 - 피복 열절감률)**
>
> 여기서 "식이 난방부하계수와 열절감률을 **다른 항으로 분리**하므로 난방부하계수에
> 커튼 효과가 들어가면 안 된다 → 단일피복 U + 별도 fr은 이중계상이 아니다"라고
> 결론지었다. **이 추론은 틀렸다.** 식이 두 항을 분리한다는 관찰은 맞지만, 앞 항에
> 무엇이 들어가야 하는지를 식만 보고 단정했다 — 그 정의는 다른 문서에 있었다.
>
> #### (2) 되돌린 이유 — 리포 안에 정의 원문이 있었다
>
> 레드팀 20회차가 **내가 검색조차 하지 않은 폴더**를 찾아냈다: `스마트팜연구DB/`
> (PDF 36개). 청킹 인덱스는 `스마트팜스펙/`·`시설평가/` 2폴더만 커버하고, 내 grep은
> 루트 PDF까지만 봤다. 이 폴더는 농진청 온실 열손실·에너지 연구보고서 모음으로
> **하필 이 주제의 1순위 말뭉치**다.
>
> `스마트팜연구DB/온실열손실저감및차단기술연구.pdf` **printed p.12**(원문 확인):
>
> > 열관류율 측정값에 **0.86을 곱하여** 단위가 kcal/m2·h·℃ 인 **난방부하계수**로 환산 …
> > **PE필름(0.08mm)에 대한 난방부하계수 5.7 kcal/m2·h·℃를 기준**으로 하여
> > PE필름(0.08mm) 대비 **상대적인 열절감율**을 식(1)에 의하여 산정
> >
> > **열절감율 = (피복재 난방부하계수 - 5.7(PE필름(0.08mm)난방부하계수)) × 100 / 5.7 … (1)**
>
> 같은 쪽이 **보온재도 "단일 피복재와 동일한 방식, 동일한 5.7 기준"** 으로 산정한다고
> 명시한다. 즉 `열절감율`은 **PE 5.7 기준의 상대값**이다.
>
> #### (3) 정의가 닫힌다 — 검산 3건
>
> 정의대로라면 `5.7 × (1 - 열절감율)`이 곧 **그 재료의 실제 난방부하계수**여야 한다.
> 같은 보고서의 실측표로 검산하면 정확히 닫힌다.
>
> | 조합 | 5.7 × (1 − 절감율) | 원문 표의 난방부하계수 |
> |---|---:|---:|
> | 표 11 #5 pe-0.15 + 5겹다겹 (절감율 70%) | 1.71 | **1.7** |
> | 표 10 #12 다겹보온커튼(4겹) (53.7%) | 2.64 | **2.6** |
> | 표 10 #11 다겹보온커튼(3겹) (32.3%) | 3.86 | **3.9** |
>
> #### (4) 판정 — 이견 ①은 확증됐다
>
> 식의 `난방부하계수` 자리에는 **PE 기준값 5.7**이 와야 한다. 엔진은 그 자리에
> `U_DESIGN=8.9`(Diop 핫박스 단일피복 고풍속)를 넣고 다시 `(1 - 절감률)`을 곱하므로
> **8.9/5.7 = 1.56배 과대**일 가능성이 크다. 76차가 "이중계상 소지"로 올린 우려가
> 해소된 것이 아니라 **1차 원문으로 뒷받침됐다.**
>
> #### (5) 그래도 값을 바꾸지 않는다 — ★사용자 판단
>
> 바꾸지 않는 이유는 셋이다.
> - `U_DESIGN=8.9`와 `5.7`은 **측정 조건이 다르다**. 8.9는 고풍속 극한(장비 용량
>   설계용), 5.7은 실험실 핫박스(외기 −10℃·내부 18~20℃). "설계 극한 기준을
>   쓰겠다"는 것은 그 자체로 방어 가능한 설계 선택이며, 식 정의 위반과 설계 보수성
>   중 무엇을 택할지는 **판단성**이다.
> - 값 변경은 `uminjae` 케이스의 산출 난방부하를 바꾼다(현재 372,826 kcal/h).
> - 1절: 값 변경은 원문 확보·대조를 거쳐서만. 원문은 이제 확보됐으나 **결정은
>   사용자 몫**이다.
>
> **제시 선택지(78차)**: ⓐ `U_DESIGN`을 정의대로 5.7 계열로 교체 ⓑ 8.9를 유지하되
> "설계 극한 기준을 의도적으로 채택했다"는 근거를 명시 ⓒ `FR_TABLE`을 버리고
> 표 10·11의 **조합별 난방부하계수를 직접 조회**(절감률 곱셈 자체를 없앰 — 원문이
> 그 값을 직접 주므로 가장 정확하나 구조 변경이 크다).
>
> #### (6) 부수 — `fr` 정의는 원문으로 확정됐다(이건 유효)
>
> `fr = (1 - 열절감률) = 노출비율`이라는 정의는 김평화 p.49 자구와 RDA 정의 양쪽이
> 일치한다. 3-2의 "실측 대조 방증"이 근거로 올라가고, 2026-07-20에 물리적 추론만으로
> 세운 `curtain_exposure_ratio()`의 방향 수정이 원문과 일치함이 확인됐다.
>
> #### (7) 검색 범위 — 다시 명시한다
>
> 77차 초판은 "리포 안에서 1곳뿐"이라 쓰면서 `스마트팜연구DB/`(PDF 36)와
> `스마트팜_단계별_엑셀패키지/`(xlsx 19)를 **빠뜨렸다** — 19회차 F2 교훈을 인용하면서
> 그 교훈을 위반했다. 실제 커버리지:
> - ✅ 청킹 인덱스 = `스마트팜스펙/`·`시설평가/` 2폴더 59,509청크(**그 2폴더 파일
>   236개 중 175개만 색인**, 61개 미색인 — 이미지 PDF·`.xls`·`.hwp`·`.docx` 등)
> - ✅ 리포 루트 PDF 14개 · `기자재DB/` · `소득분석DB/` · `cases/` · `claudeguide/`
> - ✅ (20회차 보강) `스마트팜연구DB/` 36개 — **동일 자구 0건**이나 **정의 원문 발견**
> - ❌ 리포 내 `.hwp` 10 · 레거시 `.xls` 17 · 이미지 52건은 파서 부재로 **미검색**
> - ❌ 리포 밖 사용자 보유 자료 전부
>
> 따라서 "김평화 p.49 자구가 1곳뿐"은 **위 ✅ 범위 안에서만** 참이다.

### ② E파일 `L11`=1.9 — 커튼 포함 열전달률(제3안)

같은 시트 `L11`=1.9에 "㎡h℃, 2중커튼(PO필름 + 보온다겹커튼)을 가정"이라는 주기가 붙어 있다.
**커튼 효과가 이미 들어간 U**다. 적용하면 6,027.47 × 1.9 × 28.7 ≈ **328,677 kcal/h**로
E파일(147,905)과 엔진(461,879) 사이에 놓인다.

**단 이 값은 E파일 안에서 난방부하에 쓰이지 않는다** — `M16`·`M17`의 **냉방부하** 전용이다
(E파일 자신도 난방에는 5.7 × (1−0.85)를 쓴다). 즉 같은 시트가 난방과 냉방에 서로 다른 계수
체계를 병존시키고 있다. 제3안으로 승격하려면 이 값이 난방에 쓸 수 있는 것인지에 대한 별도
근거가 필요하다 — 현 상태로는 **참고 관측**이다.

> ### 🚫 후보에서 제외 (2026-09-13 77차 — 사용자 결정)
>
> 사용자가 "냉방 전용은 빼라"로 결정했다. **E파일 `L11`=1.9는 제3안 후보에서 제외한다.**
> 근거는 위에 적힌 그대로다 — 이 값은 E파일 안에서 냉방부하(`M16`·`M17`) 전용이고
> 난방에 쓸 수 있다는 근거가 없다. 재개하려면 별도 결정이 필요하다.

### ③ E파일 표면적의 관리동 누락 (엔진 쪽 이슈 아님)

`L8`이 `I5`(재배동 표면적 6,027.47)만 참조하고 관리동 844.995㎡(`I6` — 같은 측고·동고의
온실 구조)를 뺐다. 시트 자신의 합계 `I8`=6,872.47 기준이면 표면적이 약 **14% 더** 크다.
E파일의 과소산정 폭이 계수 2종 외에 하나 더 있다는 뜻이며, **엔진 계수 판단에는 영향이 없다**
(엔진은 호출부가 준 표면적을 그대로 쓴다). E파일을 실무 참조로 쓸 때의 주의사항으로만 기록한다.

---

## 5. 이 문서가 하지 않은 것

- **A-5 원문서는 여전히 미확보**다. `fr` 기본값·`HEATING_EFFICIENCY_DEFAULT` 0.85·
  `DEGREE_HOURS_DEFAULT` 10,098·`HEATING_SAFETY_FACTOR` 1.1의 원출처 문제는 그대로다.
  (⚠️ 난방기 열효율 0.85와 FR_TABLE의 0.85는 **서로 무관한 별개 상수**다 — 숫자가 같을 뿐이다.)
- E파일이 A-5의 출처인지, 아니면 같은 계열의 독립 자료인지 **확정하지 못했다**.
- 케이스 `fr=0.7`에 **provenance 항목이 없다**(`audit_traceability.py`의
  `CASE_REQUIRED_PROV`에 `fr`이 없어 감사 사각). 3-2의 실측은 방증이지 근거 등록이 아니다.
  → **77차 후보**: `fr`을 필수 provenance에 편입할지 결정(편입 시 케이스 3건에 `[추정]`
  표기가 붙는다).
- E파일의 `자가진단`·`농업인자가진단`·`온도관리`·`피복재가격` 시트는 값 대조하지 않았다.
- **🔴 `스마트팜연구DB/` 36개 PDF가 청킹 인덱스에도, 어떤 검색에도 들어 있지 않았다**
  (20회차 F1·F4). 그 안에 `온실열손실저감및차단기술연구.pdf`가 있는데, 작업지시서 7절은
  이 문서(KISTI TRKO202100009930)를 **"로그인 장벽으로 끝내 확보 실패"**로 기록해 왔다 —
  **이미 리포에 있었다.** 표 9(피복재 9종)·표 10(보온재 16종)·표 11~13(이중 24·삼중 59조합)이
  전부 난방부하계수와 열절감율 실측치를 싣는다. `FR_TABLE`·`U_VALUE`·`U_DESIGN`의 1차 출처
  후보가 통째로 미탐색 상태였다는 뜻이다. **별도 차수로 이 폴더 편입·대조가 필요하다.**

---

## 참조

- 엔진: `smartfarm_engine.py` `FR_TABLE`·`curtain_exposure_ratio`·`U_DESIGN`·`heating_load`
- 68차 교체 근거: `근거_농사로_스크린보온력표_20260819.md`
- P1-9 프로빙: 작업지시서 2절 9차 로그 · `엔진데이터_레지스트리.json` FR_TABLE source
- 레드팀 19회차: `검증절차_레드팀.md`
- E파일: `(최종)온실 선정 및 비용 견적 프로그램.xlsx` `시설구성내역` 시트
  (md5 `cc74b881c3bd48616df8f6ca088c2741` — 리포 밖 사용자 보유 자료, 경로 2벌 동일 사본)
