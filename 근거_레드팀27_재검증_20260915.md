# 레드팀 27회차 — 발견 13건 독립 재검증 (146차)

작성: 2026-09-15 146차 · 절차: `검증절차_레드팀.md` 4단계(**채택 전 메인 세션이 원문으로 재확인**)

> **이 문서는 판정하지 않는다.** 산술·코드 경로·원문만 대조했고, ★가 붙은 것은 그대로 대기다.
> 레드팀 발견도 검증 대상이다 — **5건이 거짓 양성**이었고, **타당한 8건 중 5건은 내가 직접 쓴
> 137·140·142·143·145차의 결함**이다.

---

## 0. 요약표

| # | 레드팀 주장 | 재검증 | 분류 |
|---|---|---|---|
| **[1]** | `STRUCTURE_ONLY_PYEONG` near 등급의 산술이 재현되지 않는다 | ✅ **확정** — 단 원인은 단가가 아니라 **평수 표기** | **결정론 결함** |
| **[2]** | D-12 "산출물 감응 0"에 반례가 있다 | ✅ **확정** — 순서가 **테스트로 고정**돼 있고 **HTML에 순서대로 인쇄**된다 | **결정론 결함** |
| **[3]** | 142차의 "유일한 사용처는 접두만 읽는다"가 불완전하다 | ✅ **확정** — `build_site.py:143`이 **전문**을 렌더한다 | **결정론 결함** |
| [4] | 맹주연 총계행 439,742,226이 구성 합보다 1원 적다 | ❌ **거짓 양성** — 리포가 **4곳에 이미 기록·가드** | 거짓 양성 |
| [5] | `verify_refs`의 숫자 앵커가 7자리 이상만 잡는다 | ⚠️ **사실이나 영향 0** — 앵커가 0이 되는 ref는 **없다**(6건이 일부 숫자만 놓침) | 기록만 |
| **[6]** | 기준 없는 `partial`이 **차수 숫자**로 통과한다 | ✅ **확정 — 정확히 7건** | **결정론 결함** |
| **[7]** | 오기수 집계표에 `3. 관리동커텐공사`가 **최상위 공종**이다 | ✅ **확정** — "독립 공종은 윤성호 하나뿐"의 반례 | **결정론 결함** |
| [8] | 강정구 5,597,384가 원문에 없다(키워드 줄 합 1,581,384) | ❌ **거짓 양성** — **판넬·출입문 7줄 합으로 정확히 재현**된다 | 거짓 양성(단 §8) |
| **[9]** | D-10 감응의 가정이 명시되지 않았다 | ✅ **확정**(레드팀 수치 79.3%는 틀림 — **철골공 56.8%**) | **결정론 결함** |
| **[10]** | 대장 밖에 ★가 있다 | ✅ **확정 2건**(레드팀이 지목한 `DESIGN_OUTDOOR_TEMP_TAC`은 **오지목** — D-5가 덮는다) | **결정론 결함** |
| [11] | 맹주연 12,125,520은 작업동 금액이 아니다 | ❌ **거짓 양성** — 리포가 **"(작업장포함)"이라 명시**. 박규현 26,718,545는 **리포가 인용한 적 없다** | 거짓 양성 |
| **[12]** | 작업지시서 2절에 "skip 3건인지" 잔재 | ✅ **확정** | **결정론 결함(경미)** |
| [13] | `_DOC`가 `PDF_PATH`를 캐시한다 | ❌ **거짓 양성** — **docstring이 설계로 명시**했고 `close_doc()`은 **6곳에서 호출**된다 | 거짓 양성 |

**결정론 결함 8 · 거짓 양성 5.**

---

## 1. [1] 🔴 `STRUCTURE_ONLY_PYEONG` — 702.36평과 163,470원/평은 **같은 면적에서 나올 수 없다**

원문(`스마트팜스펙/우민재/1. 공사내역서 스마트팜 확대보급 시범사업.xlsx`)을 열었다.
**평 표기는 원문에 없다** — 있는 것은 면적뿐이다.

| 위치 | 원문 표기 |
|---|---|
| `총표지 (2)!F14` | `경량철골온실(내재해형) =2,323m2` |
| `단가대비표!K5` | `2323` |
| `내역서!D88·D90·D152·D184` | `2321` (수량 행 — 94차 `2,321.87`의 계열) |

재검산(1평 = 3.3057851239669422㎡):

| 면적 기준 | 평 | 114,870,017 ÷ 평 | 등재 163,400 대비 |
|---|---|---|---|
| **2,323㎡**(표지) | **702.7075** | **163,467.75** | **+0.0415%** |
| 2,321.87㎡(내역서 계열) | 702.3657 | 163,547.31 | +0.0902% |

→ **702.36평은 2,321.87 계열이고, 163,470원/평·오차 0.04%는 2,323 계열이다.** 리포는 두
계열을 **한 식에 섞어** 5곳에 적었다. `114,870,017 ÷ 702.36 = 163,548.63`이지 163,470이 아니다.

**정정**: 등재값 `163,400`은 **건드리지 않는다**. 표지 2,323㎡ 기준으로 통일한다 —
**702.71평 · 163,468원/평 · 오차 0.04%**.

🔴 **부수 소득 — 144차 대장 D-7이 감응을 하나 빠뜨렸다.** D-7(우민재 2,323 → 2,321.87)이
채택되면 밴드 여유(159원 → 42원)만이 아니라 **이 near 등급의 오차가 0.04% → 0.09%로
2.2배가 된다**. 대장에 그 줄이 없었다.

---

## 2. [2] 🔴 D-12 "산출물 감응 0"은 **틀렸다** — 145차가 잘못 쟀다

145차는 생성기 5개 파일에서 `pumsem` 문자열이 없음을 확인하고 감응 0이라 결론했다.
**두 개의 반례가 있다.**

**반례 ①** `test_registry.py:176~181`

```python
def test_pumsem_items():
    eng = [[it.category, it.name, it.unit, it.labor_per_unit, it.equipment_hours_per_unit]
           for it in e.PUMSEM_ITEMS]
    assert eng == C["PUMSEM_ITEMS"]["value"]
```

**리스트 동일성 비교**다 — 선언 순서를 바꾸면 이 테스트가 **깨진다**. 재정렬은 레지스트리
`PUMSEM_ITEMS.value`를 **같은 커밋에서 함께 바꿔야 하는 데이터 변경**이지 "소스 정리"가 아니다.

**반례 ②** `SmartFarm_근거대장.html`이 `PUMSEM_ITEMS.value`를 **선언 순서 그대로 인쇄**한다
(레지스트리 표의 값 열). 실제 렌더 확인:

```
&#x27;철골공사&#x27;, &#x27;스틸돌리&#x27;, &#x27;개소&#x27;, {&#x27;철골공&#x27;: 0.21, ...
```

→ 재정렬하면 **산출물 HTML의 바이트가 바뀐다**. 145차의 탐색은 **호출 경로만** 봤고
**데이터 경로**(레지스트리 → 근거대장)를 보지 않았다.

**정정된 D-12 감응**: 계산 결과는 불변, **레지스트리 값 배열과 근거대장 HTML은 변한다**.
가벼운 결정이 아니라 **데이터 변경 차수**다.

📌 이것은 141차 교훈의 재발이다 — *"파서를 고칠 게 아니라 파서를 쓰지 말았어야 했다"*.
여기서도 **문자열 탐색으로 감응을 쟀다.**

---

## 3. [3] 🔴 `build_site.py`는 상태 문자열을 **두 곳**에서 쓴다 — 한 곳은 전문이다

142차는 `근거_0사유보강_20260915.md:50`에 *"build_site.py:136 — 유일한 사용처"*라 적고
접두만 읽으므로 안전하다고 결론했다. **`build_site.py:133~146`을 읽으면 같은 루프 안에
두 번째 사용처가 있다**:

```python
tag = "실측" if ev.startswith("실측") else ("추정" if ev.startswith("부분") else "미검증")
...
f"<td><span class='tag {tag}'>{esc(ev)}</span></td>"      # ← 전문
```

**`{esc(ev)}`는 1,320자 전문을 렌더한다.** 접두는 CSS 클래스에만 쓰인다.

🔴 **그래서 142·143차가 넣은 마크다운이 HTML에 리터럴로 나갔다.** `SmartFarm_CAPEX분해.html`의
`**` 리터럴 **76개**를 실측했다:

```
142차 보강: **나머지 14건이 0인 것은 &#x27;부대시설이 없...
```

**값은 틀리지 않았다 — 읽힘이 깨졌다.** 142차의 "표시 분류 불변" 결론 자체는 맞다(접두는 안
바뀌었다). 틀린 것은 **"유일한 사용처"라는 절대표현**이다(R6: 부재·유일성 주장은 탐색 범위를
병기해야 한다 — 142차는 `build_site.py:136` 한 줄만 보고 선언했다).

---

## 4. [6] 🔴 `soft_check`의 0건은 **차수 숫자가 만든 0**이다 — 실은 7건

`verify_refs.soft_criteria()`는 `_NUMA`가 잡는 2자리 이상 숫자가 있으면 `"숫자"` 기준이
있다고 본다. **`93차`·`110차`의 숫자가 거기 걸린다.**

차수 패턴을 제거하고 다시 재면:

| 상수 | 원문 | note |
|---|---|---|
| `ACTUALS_COUNT` | 한수진 - 평면도.pdf | 한수진 면적 기준 확정 근거(**93차** — 외곽 치수·관리동 위치) |
| `ACTUALS_COUNT` | 한수진 - 측면골조도.pdf | (**93차** — 길이 방향 그리드 분해) |
| `ACTUALS_COUNT` | 한수진 - 주단면도.pdf | (**93차** — 방풍벽·방풍치마 구성) |
| `ACTUALS_COUNT` | 최선동 - 평면도.pdf | (**93차** — 외곽 치수·관리동 위치) |
| `ACTUALS_COUNT` | 최선동 - 측면골조도.pdf | (**93차** — 길이 방향 그리드 분해) |
| `ACTUALS_COUNT` | 최선동 - 주단면도.pdf | (**93차** — 방풍벽·방풍치마 구성) |
| `PUMSEM_ITEMS` | 근거_비닐원가계산서_부재확정_20260914.md | 비닐 원가계산서 부재 확정(**110차**) |

**7건 / soft 58건.** 140차의 *"대조 기준이 없는 partial·near 0건"*은 이만큼 과장이었다.
차수 번호는 **경위 표시**이지 대조 기준이 아니다 — 그 숫자로는 원문을 다시 찾아갈 수 없다.

**정정**: `soft_criteria()`가 `N차` 형태를 숫자에서 제외한다 → `soft_check()` 0 → **7건**.
7건의 note를 채우는 일은 **별도 차수**다(도면 6건은 치수를, `PUMSEM_ITEMS`는 위치를 병기해야
하는데 둘 다 원문 재독이 필요하다).

---

## 5. [7] 🔴 오기수 `3. 관리동커텐공사`는 **최상위 공종**이다 — "윤성호 하나뿐"의 반례

`스마트팜스펙/견적참조/견적서_군산 오기수 농가_0803.pdf`를 열었다.

```
p2 집계표   공 종 단 위 수 량 재 료 비 노 무 비 경 비 합 계 비 고
            3. 관리동커텐공사 식 1 2,817,269 2,817,269
            합 계 164,863,706 31,834,400 2,876,832 199,574,938
p7 내역서   3. 관리동커텐공사 2,817,269 2,817,269
            관리동스크린(4.8M) PH77 ㎡ 404 4,080 1,648,320 1,648,320 환급
```

**공종 번호 `3.`이 붙은 집계표 최상위 행**이다. `auxiliary_facility`의
*"독립 부대시설 공종은 윤성호 0116 하나뿐이라 분류 규칙(독립 공종만 편입)상 0이 맞다"*는
**오기수에 적용되지 않는다** — 오기수의 관리동 계열은 **독립 공종이 맞다**.

**0이 맞는 진짜 이유는 다르다**: 이것이 **커튼 공종**이라 보온커튼 관례(57차)로
`auto_opening_system`에 귀속됐기 때문이다(엔진 주석 `smartfarm_engine.py:2713·2727`이
처음부터 그렇게 적고 있었다 — **틀린 것은 142·143차가 붙인 요약 문장뿐이다**).

→ 정정: 독립 공종 여부가 아니라 **공종 성격**이 사유다. 14건의 0은 **한 가지 규칙으로
설명되지 않는다**(혼입 · 공종 성격 · 미확인이 섞여 있다).

---

## 6. [9] D-10 감응 — 145차가 **말하지 않은 가정 하나**

145차는 *"가중 평균 공구손료율 = 노무비의 1.9802~1.9861%"*라 적었다. 그 계산은
**요율 × 인·일 계수 ÷ 전체 인·일 계수**다 — 즉 **모든 직종의 노임단가가 같다고 가정**한다.
공구손료의 분모는 **인력품(금액)**이지 인·일이 아니므로, 직종 노임이 다르면 이 비율은 달라진다.

규정 품목 29종의 직종 구성을 실측했다(계수 합 7.9260):

| 직종 | 인·일 계수 | 비중 |
|---|---|---|
| **철골공** | 4.5000 | **56.8%** |
| **조력공** | 3.1200 | **39.4%** |
| 특별인부 | 0.2760 | 3.5% |
| 내장공 | 0.0200 | 0.3% |
| 보통인부 | 0.0100 | 0.1% |

**철골공+조력공이 96.2%**로 쏠려 있다. 두 직종의 노임이 갈리면 1.98%는 그만큼 움직인다.
(레드팀이 적은 *"철골공 79.3%"*는 틀렸다 — 재집계는 56.8%다.)

**정정**: 1.98%는 *"노무비의 %"*가 아니라 **"직종 노임이 균일할 때의 노무비 %"**다.
노임단가는 시세성이므로 **주입 없이는 실제 비율을 낼 수 없다** — 감응의 한계로 명시한다.
(물량 1.0 가정은 145차가 D-11에 대해서는 적었으나 D-10에 대해서는 적지 않았다.)

---

## 7. [10] 대장이 **"대기 전량"이 아니다** — 144차가 106차와 같은 실수를 했다

144차는 106차의 *"★가 붙은 나머지는 전부 E절로 동결됐다"*가 틀렸다는 것을 발견하고
대장을 만들면서, **자기 대장에 대해 같은 절대표현을 썼다**
(`근거_결정대기대장_20260915.md` — *"이 대장은 대기 전량이다"*).

엔진의 `★사용자 결정` 줄을 전수로 훑어 대장 D-1~D-13에 매핑했다. **2건이 밖에 있다**:

| 위치 | 내용 | 대장 |
|---|---|---|
| `smartfarm_engine.py:3901` | **비닐 7종**(철골공사(비닐·파이프자재) 5 + 온실피복공사(비닐) 2)의 1차 관측자료가 부록1에 없다 → `[확인요망]`. **등재·변경은 ★사용자 결정** | ❌ 없음 |
| `smartfarm_engine.py:4057` | 품셈 `[주]` **적용 조건을 엔진에 등재하지 않았다**(★사용자 결정) — p.129 ↔ p.144 이견 포함 | ❌ 없음 |

레드팀이 지목한 `DESIGN_OUTDOOR_TEMP_TAC`은 **오지목**이다 — 그 상수에는 ★가 없고,
관련 결정은 **D-5(`t_min` 처리 — TAC 참고 열 병기)**가 이미 덮는다.

**정정**: **D-14**(비닐 7종 등재) · **D-15**(품셈 `[주]` 적용조건 등재)를 대장에 추가하고,
*"대기 전량"* → *"146차 전수 확인 시점의 대기"*로 표현을 낮춘다.

---

## 8. 거짓 양성 5건 — 그래도 하나는 구멍을 드러냈다

**[4] 맹주연 439,742,226**: 리포가 이미 네 곳에 기록·가드하고 있다 —
`smartfarm_engine.py:2577`(원문 1원 갭) · `:3110`(채택은 구성 합 439,742,227) ·
`test_cases.py:382~383`(둘 다 assert) · `test_engine.py:1188`. **조치 없음.**

**[5] 앵커 폭**: `NUM`이 7자리 이상만 잡는다. 사실이나
**exact 39건 중 앵커가 0이 되는 ref는 없다**. 6건이 4~6자리 숫자를 앵커에서 놓친다
(우민재 `456,158` · 이두희 `2,736` · 최혁진 `3,459` · 한일그린텍 `3,202` ·
한수진 `4,092` · 최선동 `3,145` — 대부분 **면적 ㎡**). `full_check`가 면적을 대조하지
않는다는 뜻이므로 **기록만** 하고 폭을 넓히지 않는다(넓히면 연도·수량이 앵커로 들어와
거짓 불일치가 는다 — 138차 P2 과잉교정과 같은 함정이다).

**[8] 강정구 5,597,384 — 재현된다.** 레드팀은 `관리동` 키워드 줄만 더해 1,581,384를 얻고
"원문에 없다"고 했다. **`판넬`·`출입문` 줄 7개의 합계열을 더하면 정확히 일치한다**:

| p | 항목 | 합계 |
|---|---|---|
| 5 | 관리동판넬부마무리 ㅁ75×75×2.0t×6m 본 4 | 211,384 |
| 8 | 판넬 100T ㎡ 54 | 1,512,000 |
| 8 | 판넬부자재 100T ㎡ 54 | 324,000 |
| 8 | 판넬출입문 4000*4000 개소 1 | 2,500,000 |
| 8 | 출입문캐노피 개 1 | 400,000 |
| 8 | 출입문쪽문 1200*2400 개소 1 | 400,000 |
| 8 | 관리동출입문 3000*3000비닐출입문 개소 1 | 250,000 |
| | **합** | **5,597,384** |

⚠️ **그래도 구멍은 실재한다**: 이 값은 **재집계**인데 **재집계 식이 어디에도 적혀 있지 않았다**.
그래서 레드팀이 재현에 실패했다. 140차가 만든 `RECOMPUTED` 규율(재집계 값은 재집계 기준을
병기한다)이 **이 값에는 적용되지 않았다**. 위 표를 `근거_0사유보강`에 편입한다.
(`관리동수평커튼-삼상0.5마력`은 p9 콘트롤박스 3,800,000 줄의 **접힌 규격 조각**이지
독립 금액이 아니다 — 원문 확인.)

**[11] 맹주연 12,125,520 / 박규현 26,718,545**: 전자는 `근거_관리동_실재점검`이 이미
**"농업용스크린(작업장포함)"**이라 적었고 §3이 *"찾은 항목은 거의 전부 다른 공종 안의 라인"*
이라 못 박았다. 후자 **26,718,545는 리포 어디에도 없다** — 레드팀이 원문에서 가져온 수인데
우리가 인용한 적이 없으므로 대조 대상이 아니다. **조치 없음.**

**[13] `_DOC` 캐시**: `_doc()` docstring이 *"호출자는 닫지 않는다 — 프로세스가 끝날 때까지
재사용한다. 명시적으로 놓아야 할 때만 `close_doc()`"*라고 **설계로 명시**하고 있고,
`close_doc()`은 `smartfarm_engine.py` 1곳 · `test_engine.py` 5곳에서 호출된다.
`PDF_PATH`가 런타임에 바뀌는 경로는 없다. **조치 없음.**

---

## 9. 이 차수가 하지 않은 것

- **결정은 하나도 내리지 않았다.** D-1~D-15 전부 ★ 대기다.
- **엔진 수치 상수·케이스·견적비교 값 변경 0.** 바뀐 것은 **서술 문자열**과
  `verify_refs.soft_criteria()`의 판정 폭이다.
- **[6]의 7건 note를 채우지 않았다** — 원문 재독이 필요해 별도 차수다.
- **[3]의 HTML 마크다운 리터럴을 지우지 않았다** — 상태 문자열에서 `**`를 빼거나
  `build_site`를 고치는 두 갈래이고, 후자는 산출물 표시를 바꾸므로 **후보로만 올린다**.
- **[5]의 앵커 폭을 넓히지 않았다** — 138차 P2 과잉교정 재발 위험.
