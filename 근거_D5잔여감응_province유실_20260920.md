# D-5 잔여 범위의 감응과, 원채원을 대조할 수 없는 진짜 이유 (159차)

**작성일** 2026-09-20 (159차) · **계기** *"남은 것 처리"* — 결정·자료를 빼고 **판단 없이 확인 가능한 것**만
**성격** 엔진 상수 값 변경 0 · 케이스·견적비교·레지스트리 값 변경 0 · **결정 0**

> 158차가 D-5의 범위를 **「기상 4표」로 좁혔다**. 그 남은 범위의 감응은 아무도 재지 않았다.
> 그리고 157차가 *"원채원 주입값 30·35는 표와 대조된 적이 없다"*고 적었는데,
> **왜 대조할 수 없는지**는 적지 않았다. 둘 다 판단이 아니라 측정이다.

---

## 1. D-5 잔여 범위(기상 4표)의 감응 — **계산 도달 0**

146차 [2]의 교훈대로 **호출 경로와 데이터 경로를 둘 다** 봤다.

### 호출 경로

표를 **함수 본문에서 읽는 것**은 다섯 개뿐이다(AST 기준):

| 표 | 읽는 함수 |
|---|---|
| `DESIGN_OUTDOOR_TEMP_TAC` | `design_outdoor_temp` |
| `HEATING_DEGREE_HOURS_1000` | `heating_degree_hours` |
| `MONTHLY_SUNSHINE_HOURS` | `monthly_sunshine` |
| `MONTHLY_MEAN_WIND_MS` | `mean_wind` · `monthly_mean_wind` |

그 **다섯 함수를 생성기 5개(`build_site`·`webapp`·`render_report`·`run_report`·`cases`)가
부르는 횟수 = 0**이다. `DEGREE_HOURS_DEFAULT`도 표가 아니라 **스칼라 기본값**(10,098 `[추정]`)이다.

### 데이터 경로

🔴 **네 표 모두 `SmartFarm_근거대장.html`에 값 열로 실린다**(레지스트리 등재 상수이므로).
D-12에서 *"산출물 감응 0"*이라 적었다가 146차에 반증된 것과 **같은 구조**다 —
다만 여기서는 **표의 값이 바뀌는 것이 아니라 조회 키가 문제**이므로,
지역명 정규화를 해도 **근거대장에 인쇄되는 표 자체는 한 글자도 안 바뀐다**.

→ **D-5(기상 4표)의 감응 = 계산 0 · 산출물 0.** 이 표들은 **아직 아무것도 계산하지 않는다**
(작업지시서 C3의 「도달성 가드」가 가리키던 상태 그대로다).

---

## 2. 🔴 측정 방법이 한 번 부풀었다 — 정규식이 모듈 선언을 삼켰다

처음에 `def <이름>\(.*?(?=\ndef )` 로 함수 본문을 잘라 표 참조를 셌더니
**`heating_load`와 `generate_rfq_package`가 표를 읽는다**고 나왔다. 확인하니 **둘 다 거짓**이다:

| 함수 | 정규식이 캡처한 줄 | 실제 본문(AST) |
|---|---|---|
| `heating_load` | **414** | **78** |
| `generate_rfq_package` | **60** | **20** |

`(?=\ndef )`는 다음 `def`까지를 본문으로 삼으므로 **그 사이의 모듈 수준 선언**
(= 표 dict 자체)을 함께 삼켰다. `generate_rfq_package`의 hit은 **주석 한 줄**이었다
(*"호출부가 [표 3-3-44] … 로 구해 넘긴다"*).

→ **AST로 다시 셌다.** 141차 *「파서를 쓰지 말았어야 했다」*·156차 「21곳」과 같은 계열이다 —
**구조를 알아야 하는 것을 문자열로 세면 부풀어난다.**

---

## 3. 🔴 원채원을 대조할 수 없는 진짜 이유 — **표에 광역이 없다**

157차는 *"원채원 주입값 30·35는 표와 대조된 적이 없다"*까지만 적었다. 이유를 확인했다.

`REGION_DESIGN_LOAD`의 값은 **`{snow_cm, wind_ms}` 둘뿐**이고 키는 **지명 하나**다.
괄호로 광역을 밝힌 것은 **동명 3건**(`고성(강원)`·`고성(경남)`·`광주(경기)`)뿐이다.

그런데 레지스트리 `source`는 원문에 대해 이렇게 적는다:

> … 172개 지역 전량 적설·풍속 두 표의 **(province, 지명) 키 집합**이 정확히 일치함을 자동 검증 후 병합

**원문에는 광역(province) 열이 있었고, 등재하면서 버렸다.**

→ 그래서 *"「충남」 소속 시군구들의 설계하중 범위"* 같은 질의가 **리포 자료만으로 불가능**하다.
원채원의 30·35가 그 범위 안인지 밖인지 **판단이 아니라 조회**로 확인할 수 있어야 하는데,
**조회할 데이터가 없다.**

⚠️ 내가 행정구역 소속을 **외부 지식으로 채워 넣지 않았다** — 1절(*"자료에 없는 수치는 만들지 않는다"*).

→ 이것은 **S-4**(농식품부 보도자료 첨부 참고2·참고3 보존본)로 풀린다. 그 첨부가 들어오면
province를 복원할 수 있고, **원채원 주입값을 소속 광역 범위와 대조**할 수 있다.
**S-1 없이 할 수 있는 최대치가 거기까지**라는 것도 이번에 확정됐다.

---

## 4. 이 차수가 하지 않은 것

- **결정은 하나도 내리지 않았다.** D-1~D-15 전부 ★ 대기다.
- **엔진 상수 값·케이스·견적비교·레지스트리 값 전부 불변.**
- **`REGION_DESIGN_LOAD`에 province를 복원하지 않았다** — 원문 없이 채우면 근거 없는 값이 된다.
- **기상 4표용 조회 함수를 만들지 않았다** — 158차와 같은 이유(정규화 규칙이 판단성)이고,
  게다가 §1에서 확인했듯 **그 표들은 아직 아무것도 계산하지 않는다**. 지금 만들면
  **쓰이지 않는 경로에 판단을 박는 것**이 된다.
