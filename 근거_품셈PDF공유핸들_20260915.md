# 품셈 PDF 공유 핸들 — 파싱 4회 → 1회 (136차)

**작성일** 2026-09-15 (136차) · **계기** 135차 ⑭후보 **(a)** — 세그폴트 관찰의 구조적 후속
**성격** 엔진 상수 값 변경 0 · 레지스트리 변경 0 · **추출 결과 비트 단위 불변**.
바뀐 것은 **PDF를 여는 횟수**뿐이다.

---

## 1. 무엇이 문제였나

`pumsem_extract.py`의 네 추출 함수가 각각 이렇게 시작했다:

```python
reader = PdfReader(PDF_PATH)
with pdfplumber.open(PDF_PATH) as pdf:
```

→ **한 pytest 프로세스 안에서 89.8MB PDF를 4회 파싱**한다.
`lru_cache`는 **폰트 인덱스와 글리프 맵에만** 걸려 있었고 **PDF 객체는 캐시되지 않았다**.

### 계기 — 세그폴트 2회

5파일 게이트 첫 실행에서 `pdfplumber` 세그폴트가 **133차·135차 각 1회** 났고
**재실행은 둘 다 정상**이었다. CLAUDE.md가 환경 특성으로 기록해 둔 현상이다.

🔴 **이 변경을 그 해결이라고 말하지 않는다.** 재현 조건을 잡지 못했고 **인과를 확인하지
않았다**. 확실한 것은 **파싱 횟수**와 **출력 불변**과 **시간**뿐이다.

---

## 2. 무엇을 했나

```python
_DOC = None

def _doc():
    """열린 (pdfplumber.PDF, pypdf.PdfReader) 쌍을 돌려준다(프로세스 공유)."""
    global _DOC
    if _DOC is None:
        import pdfplumber
        from pypdf import PdfReader
        _DOC = (pdfplumber.open(PDF_PATH), PdfReader(PDF_PATH))
    return _DOC

def close_doc():
    """공유 핸들을 닫는다. 열려 있지 않으면 아무 일도 하지 않는다."""
```

네 함수의 `reader = …` + `with pdfplumber.open(…)` 를 `pdf, reader = _doc()`로 바꾸고
블록을 내어썼다. 📌 **`_system_index()` 호출은 그대로 맨 앞에 뒀다** — 폰트가 없는 기계에서
**PDF를 열기 전에** `FontsUnavailable`이 올라야 128차 skip 규율이 유지된다.

---

## 3. ✅ 검증 — 출력이 비트 단위로 같다

이 차수의 본체는 성능이 아니라 **불변**이다. 성능을 얻자고 추출값이 한 글자라도
달라지면 **125·129차가 세운 64계수 대조가 통째로 흔들린다**.

변경 전후로 7종을 `repr()` → `sha256`으로 떠서 대조했다:

| 함수 | 전 | 후 |
|---|---|---|
| `extract_all` | `1e9d0884cdf2d994` | **동일** |
| `extract_tables` | `4dcdb9b7e623a008` | **동일** |
| `extract_notes` | `9f1f2b933ddd8298` | **동일** |
| `extract_labor_trades` | `08b677b28efc396c` | **동일** |
| `rate_rules` | `282cd312da7ed1ec` | **동일** |
| `classify_notes` | `902cc0756975b258` | **동일** |
| `note_numbers` | `9e4cc9c1b519dbce` | **동일** |

→ **7/7 일치.** 이 다이제스트를 136차 가드가 **고정**한다.

---

## 4. 📊 실측 — 파싱 4회 → 1회

`pdfplumber.open`·`pypdf.PdfReader`를 계수기로 감싸 네 함수를 연달아 호출했다:

| | `pdfplumber.open` | `pypdf.PdfReader` |
|---|---|---|
| **변경 전**(`git show HEAD:pumsem_extract.py`) | **4** | **4** |
| **변경 후** | **1** | **1** |

### 시간 — 교차 3회 실측

같은 프로세스에서 **old / new를 번갈아** 3회씩 돌렸다(캐시 조건을 맞추기 위해):

```
old(4 opens): [17.71, 19.53, 19.97]   중앙값 19.53s
new(1 open) : [15.02, 15.07, 14.93]   중앙값 15.02s   → −23%
```

📌 **분산도 줄었다**(old 2.26s 폭 → new 0.14s 폭).

**3파일 게이트**: `44.38s → 35.92s` — **가드가 1개 늘었는데도** 짧아졌다.

⚠️ 단일 세션 실측이고 **다른 기계에서 같은 비율이 나온다는 보장은 없다**.

---

## 5. 검증하지 않은 것

- 🔴 **세그폴트가 사라졌는지 확인하지 못했다.** 재현 조건을 잡지 못했으므로
  **이 변경이 그것을 고쳤다고 말할 수 없다.** 다음에 또 나면 그때 다시 본다.
- **엔진 상수 값·레지스트리는 하나도 바꾸지 않았다.**
- **메모리 사용량을 재지 않았다** — 핸들을 프로세스 끝까지 들고 있으므로
  pdfplumber가 캐시하는 페이지 객체가 **누적**된다(종전에는 함수가 끝나면 풀렸다).
  게이트에서 문제가 되지 않았으나 **측정하지는 않았다**.
- **`chunking_lib_v2` 등 다른 `pdfplumber` 사용처는 건드리지 않았다**(범위 밖).
- 다이제스트는 **이 기계의 폰트 환경**에서 뜬 값이다 — 폰트가 다른 기계에서는
  `_system_index()`가 먼저 올라 **가드가 skip**된다(128차 규율).
