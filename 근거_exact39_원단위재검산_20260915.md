# `exact` 39건 원단위 재검산 — 값은 전부 섰고, 3건은 "인쇄된 값이 아니다" (138차)

**작성일** 2026-09-15 (138차) · **계기** 137차 ⑨의 잔여 — *"exact 45건이 정말 원단위로 일치하는지
재계산하지 않았다"*
**성격** 엔진 상수 값 변경 0 · 레지스트리 **값** 변경 0. 바뀐 것은 **ref note 3건**뿐이다.

---

## 1. 무엇을 확인했나

137차가 `exact`로 확정한 45건 중 **견적 원문 39건**(법령 별표 6건 제외)에 대해,
각 ref의 `note`가 주장하는 금액을 **원문 파일에서 직접 찾거나 재계산**했다.

| 결과 | 건수 |
|---|---|
| **문서에 그 값이 인쇄돼 있다** | **24** |
| **인쇄돼 있지 않고 계산값이다** → 재계산으로 확인 | **6**(3문서 × 2상수) |
| note에 대조할 금액이 없다 | 9 |
| 확인 불가 | **0** |

→ **39건 전부 값이 섰다.** 불일치 0건.

---

## 2. 🔴 도중에 내 파서를 두 번 고쳤다

| # | 결함 | 증상 |
|---|---|---|
| **P1** | PDF가 숫자를 **자간 공백으로 쪼개** 렌더한다(`3 70,315,435`) | 실재하는 값을 *"문서에 없음"*으로 읽었다(MISS 8건) |
| **P2** | 공백을 **통째로 지웠더니** 인접한 두 숫자가 붙었다 | **OK가 22 → 11로 떨어졌다**(과교정) |

→ 132차에서 검증된 방식으로 바꿨다: **같은 줄에서 x 간격 4px 안의 숫자 토큰만 결합**한다.
→ 📌 **125차 P1~P3 · 133차 · 134차에 이은 같은 계열의 파서 결함**이다. 이번엔 **과소·과교정이
연달아** 났고, 둘 다 **원문 쪽이 아니라 내 판독기가 틀린 것**이었다(125차 P1~P3와 같다).

---

## 3. 🎯 인쇄돼 있지 않은 3건 — 재계산으로 확인했다

### 우민재 456,158,140

집계표 시트를 다시 읽으니 **문서 자신의 합계행은 453,478,913**이다.

```
01. 온실공사        297,536,847
02. 기계설비공사    155,942,066
03. 품질시험비        1,100,955   ← '합계제외'
04. 안전관리비          628,272   ← '합계제외'
05. 재해예방기술지도비  950,000   ← '합계제외'
────────────────────────────────
문서 합계행         453,478,913   (합계제외 3행을 뺀 값)
5공종 전부 합       456,158,140   = 등재 known_total ✓
차이                  2,679,227   = 합계제외 3항목
```

🔴 **차이 2,679,227은 `CAPEX_MAJOR_UNCLASSIFIED`의 우민재 값과 정확히 같다**(qa_safety).
→ 즉 **엔진의 미분류는 문서가 "합계제외"로 표시한 바로 그 집합**이다. 정합하지만
**레지스트리 서술에 문서 합계행(453,478,913)과 '합계제외' 표기가 적혀 있지 않았다.**

### 이두희 433,606,460

집계표(p1)의 13공종 합계열을 더하면 **423,454,980**이고 문서의 `계` 행과 일치한다.
거기에 **`7-1. 영세율 적용 10,151,480`**을 더하면 **433,606,460**이다 ✓
(note의 *"14개 세부공종"*은 **13공종 + 영세율 행**이다.)

### 맹주연 439,742,227

p2 원가계산서의 **직재 360,447,860 + 직노 72,269,120 + 기계경비 7,025,247**
= **439,742,227** ✓ (세 성분 모두 문서에서 확인).

---

## 4. 🔴 두 표본에서 같은 구조가 나왔다

| 표본 | 문서 자신의 총계 | 등재 known_total | 차이의 정체 |
|---|---|---|---|
| **우민재** | 453,478,913 | **456,158,140** | **'합계제외' 3행**(품질시험·안전관리·재해예방) |
| **이두희** | 423,454,980 | **433,606,460** | **영세율 적용 행** |

→ **`known_total`은 문서의 총계가 아니라 "문서가 총계에서 뺀 행까지 포함한 재집계"**다.
→ 두 경우 모두 **문서의 인쇄값만으로 정확히 재현**되므로 `exact` 등급은 유지한다.
→ ⚠️ 다만 **"그 숫자가 문서에 찍혀 있다"는 뜻은 아니므로** note에 명시했다.

---

## 5. 검증하지 않은 것

- **엔진 상수 값도 레지스트리 값도 하나도 바꾸지 않았다.** `상수 54` · `refs 148` 불변.
- **note에 금액이 없는 9건은 대조하지 못했다**(윤성호·조윤정·이두희/최혁진/한일그린텍/한수진/
  최선동의 `ACTUALS_COUNT` ref 등) — 무엇을 대조할지 note가 특정하지 않는다.
- 🔴 **"문서에 그 값이 인쇄돼 있다"는 것이 "그 값이 옳다"는 뜻은 아니다.** 24건은
  **숫자가 그 문서 안에 존재함**을 확인한 것이고, **그 숫자가 올바른 셀·행에서 왔는지**는
  각 차수의 당시 판독을 신뢰했다(전 문서 셀 단위 재대조는 하지 않았다).
- **법령 별표 6건은 이번 범위 밖**이다(견적 원문 39건만 다뤘다).
- 우민재·이두희의 **재집계 규칙이 옳은지 판정하지 않았다** — 문서가 "합계제외"·"영세율"로
  구분한 것을 포함하는 것이 타당한지는 **회계 판단**이라 엔진이 고르지 않는다.
