# 추적성 사각 (A)계열에 `source_refs` 부착 — 12 → 7 (134차)

**작성일** 2026-09-15 (134차) · **계기** 133차 ⑩후보 **(a)** — 진단의 직접 후속
**성격** 엔진 상수 값 변경 0 · **레지스트리 값 변경 0**. 바뀐 것은 **출처 포인터뿐**이다
(`source_refs` **92 → 148**, 상수 **54 불변**).

---

## 1. 무엇을 붙였나

133차가 (A)로 분류한 것 — **원문이 리포에 실재하는데 ref가 없던** 상수들이다.

| 상수 | +refs | 무엇을 가리키는가 |
|---|---|---|
| `CAPEX_MAJOR_EVIDENCE_STATUS` | **17** | 표본 16종(전부 `partial`) + 이동혁 커튼 단독견적 |
| `CAPEX_MAJOR_UNCLASSIFIED` | **16** | 15키를 뒷받침하는 표본 16파일(백가은·조윤정은 쌍 견적) |
| `PUMSEM_ITEMS` | **11** | 품셈 PDF(exact) + 전수대조·기계추출(exact) + 맥락 8종(partial) |
| `EQUIPMENT_DB_META` | **9** | `기자재DB/` CSV 8종(exact) + 책자 PDF(partial) |
| `CAPEX_CASE_CHUNKS` | **2** | 우민재·최혁진 원문 |
| `ELECTRICAL_PUMSEM_LUMP_WON_PER_HA` | **1** | 같은 품셈 PDF의 1ha 표준설계 집계표 |

📌 마지막 하나는 `참고기준`이라 133차의 **12개 사각 목록에는 없었다** — 그러나 133차 문서가
(A)로 분류했고 **같은 PDF 한 줄이면 되는 것**이라 함께 붙였다.

→ 감사 리포트의 **추적성 사각이 12 → 7**로 줄었다.

---

## 2. 🔴 `_ref_ok()`는 파일이 있는지만 본다

```python
def _ref_ok(r: dict) -> bool:
    return bool(r.get("file")) and os.path.isfile(os.path.join(ROOT, r["file"]))
```

**파일만 존재하면 통과한다.** 엉뚱한 파일을 붙여도 게이트는 green이다 —
그러면 ref는 **없느니만 못하다**(없을 때는 "검사 안 됨"이 드러나기라도 한다).

그래서 이번에는 **붙이기 전에 값과 원문의 연결을 직접 계산**했다.

### ✅ 검산 1 — `EQUIPMENT_DB_META`: CSV 행수 **8/8 원단위 일치**

등재값 `csv_row_counts`를 실제 CSV에서 다시 세었다:

| 파일 | 등재 | 실측 |
|---|---|---|
| `기업정보.csv` | 623 | **623** |
| `기업정보_자사제품.csv` | 312 | **312** |
| `장비정보.csv` | 139 | **139** |
| `장비정보_세부내용.csv` | 441 | **441** |
| `장비정보_필수구성품.csv` | 679 | **679** |
| `장비정보_선택구성품.csv` | 324 | **324** |
| `시공업체_도급순위.csv` | 84 | **84** |
| `시공업체_지역별_1_2군.csv` | 81 | **81** |

→ **이 8개 CSV가 그 값의 출처인 것이 맞다.** 런타임(`equipment_lookup()` 등)이 읽는 파일이기도 하다.

### ✅ 검산 2 — `CAPEX_CASE_CHUNKS`: 9공종 합 = 서술 총액

```
우민재 9공종 합 456,158,140 = source 서술 456,158,140 ✓
최혁진 9공종 합 694,575,784 = source 서술 694,575,784 ✓
```

### ✅ 검산 3 — 표본명 ↔ 파일 매핑을 **두 방향으로** 확인

형제 상수 `CAPEX_MAJOR_CASE_CHUNKS`의 refs 16건이 표본별 파일을 이미 갖고 있다.
그 매핑을 그대로 쓰되 **두 번 검증**했다:

1. 내가 만든 표본명→파일 사전의 **파일 집합이 형제 refs 16건과 정확히 같은가** ✓
2. 각 파일의 **형제 note가 그 표본명을 담고 있는가**(매핑이 뒤바뀌지 않았는가) ✓

🔴 **이 과정에서 오류 2건을 잡았다**:
- **조윤정이 백가은 파일로 매핑됐다** — 정규식이 *"백가은·조윤정 통합 표본의 백가은분"*을
  먼저 물어 두 이름을 같은 파일에 할당했다. 조윤정에게는 **자기 파일**
  (`논산딸기조윤정님75각 외몽골셀액분리(최종).pdf`)이 따로 있다.
- **한일그린텍이 누락됐다** — 이름이 5글자인데 정규식이 2~4글자만 받았다.

→ 📌 **주석 파싱 대신 파일명으로 식별**하도록 바꿨다. 125차 P1~P3·133차 파서 오탐과 **같은 계열**이고,
이번에도 **자기검증(집합 일치·역방향 확인)이 잡았다**.

---

## 3. 등급을 과대하게 매기지 않았다

20회차 F3의 교훈(*"match의 취지는 등재 값에 대한 근거력 표기인데 exact가 과대였다"*)을 적용했다.

| 등급 | 어디에 |
|---|---|
| **exact** | 값이 **그 문서에서 직접 나온** 것 — 품셈 PDF의 64계수 · CSV 8종의 행수 · 표본별 미분류 금액 · 9공종 합 |
| **partial** | 값의 **맥락·뒷받침**일 뿐인 것 — PUMSEM 근거문서 8종 · 기자재 책자 PDF · **`CAPEX_MAJOR_EVIDENCE_STATUS`의 표본 17건 전부** |

⚠️ **26회차 F5 정정 — 정책을 선언했지만 데이터에 적지 않았다.** 부착 56건 중 **30건이
`match` 미기재**였고, `audit_traceability.py`는 `r.get("match", "exact")`로 **기본값을 exact**로
잡는다 — 즉 등급이 **데이터에 없는 채 exact**가 됐다. 135차에 **30건 전부 `match: "exact"`로
명시**했다(의미 불변 — 기본값을 적었을 뿐이다).
📌 나머지 **56건은 134차 이전 차수의 refs**라 등급을 확인하지 않았고 **건드리지 않았다**
(여전히 암묵 exact다 — 136차 후보).

🔴 **`CAPEX_MAJOR_EVIDENCE_STATUS`는 17건 **전부** `partial`이다.** 그 값은 카테고리별
**상태 문자열**이고 **표본 전체에서 유도**된다 — 표본 하나가 상태 하나를 세우지 않는다.
개별 표본을 `exact`로 올리면 과대 표기다.

---

## 4. 남은 사각 7개 — 붙일 원문이 없거나 ref 개념이 없다

| 계열 | 상수 | 왜 못 붙이나 |
|---|---|---|
| **(B) 원문이 리포 밖** | `SPEC_TABLE`·`SPEC_COUNT` | 농사로 마스터 xlsx(249종) — ★사용자 |
| | `REGION_DESIGN_LOAD` | 농식품부 보도자료 첨부 참고2·참고3(172지역) — ★사용자 |
| | `OPEX_ITEM_CATEGORIES` | 인용 CSV가 **0바이트**(132차) — ★사용자 |
| | `OVERHEAD_RATES` | 원가계산서 6건 중 일부가 Google Drive |
| **(C) ref 개념 없음** | `CAPEX_MAJOR_CATEGORIES` | **사용자 제안 분류표**(2026-07-16 대화) — 결정 기록 |
| | `RFQ_REQUIRED_CATEGORIES_DEFAULT` | `CAPEX_MAJOR_EVIDENCE_STATUS`에서 **도출** |

⚠️ **(B)의 넷은 여전히 산출물에 닿는다** — 특히 `REGION_DESIGN_LOAD`는 `webapp`이
조회 결과에 `status="실측"`을 찍어 내보낸다. **★사용자가 원문(또는 보존본)을 넣어야 닫힌다.**

---

## 5. 검증하지 않은 것

- **엔진 상수 값도 레지스트리 값도 하나도 바꾸지 않았다.** `상수 54` 불변,
  `source_refs`만 **92 → 148**.
- 🔴 **ref는 "이 파일이 출처다"라는 표기이지 내용 대조가 아니다.** `_ref_ok()`는
  **파일 실재만** 본다 — 134차 가드가 **4개 상수에 한해** 값↔원문 검산을 추가했지만,
  `PUMSEM_ITEMS`의 64계수는 **기존 125·129차 가드**가 맡고 `CAPEX_MAJOR_EVIDENCE_STATUS`의
  상태 문자열은 **여전히 기계 검산 대상이 아니다**(판단성 서술이다).
- **partial 8종(PUMSEM 맥락 문서)의 내용이 지금도 그 서술과 맞는지 재확인하지 않았다** —
  파일이 존재한다는 것만 확인했다.
- **(B)의 원문을 구하지 않았다** — 리포 밖 자료는 ★사용자 몫이다.
- 🔴 **26회차 F6 — 빠뜨린 것이 있었다**: `ELECTRICAL_PUMSEM_LUMP_WON_PER_HA`(250,000,000원/ha)는
  **값과 원문의 연결을 계산하지 않았다.** ref가 가리키는 `<그림 7-18>`·`<그림 7-19>`
  (인쇄 p.163~164)를 리포 복원기(`pumsem_extract.page_lines`)로 읽으면 **6줄·4줄이고 전부
  미복원 글리프**다(대조군 p.165는 32줄·숫자 전량 복원). → **확인 불가(사유: 그림/미복원
  글리프 구간)**이고 **원문에 없다는 뜻이 아니다**. §2가 *"붙이기 전에 값과 원문의 연결을
  직접 계산했다"*로 시작하므로 **유일한 예외가 그 진술에 묻혀 있었다**.
- 📌 refs 148건 **전부 `loc`(쪽·셀)이 없다** — 89.8MB PDF에 대해서도 **파일 단위 포인터**뿐이다.
- `OVERHEAD_RATES`는 **원가계산서 6건 중 리포에 있는 3건만으로도 부분 부착이 가능**하지만,
  **일부만 붙이면 "6건 실측"이라는 서술과 refs가 어긋나** 보이므로 **하지 않았다**
  (남은 3건 확보 후 한꺼번에 붙이는 것이 정직하다).
