# 5단계 척도 재측정 + 단계별 회귀 앵커 (171차)

**작성일** 2026-09-20 (171차) · **요청** *"④⑤ 진행하라"*(169차 비판의 개선 ④ 단계별 회귀 앵커 ·
⑤ 5단계 척도 재측정) · **성격** 엔진 상수 **값 변경 0** · 케이스·견적비교·레지스트리 **불변** · **결정 0**

---

## 0. 결과 두 줄

1. 🔴 **166차의 ✅ 4칸 중 하나(부지 공종설계)는 과대평가였다** — 5단계로 재면 **「산출」**이고
   **판정지원까지 가지 못한다**. 그리고 **「검증」 등급은 18칸 중 0칸**이다.
2. ✅ **회귀가 1단계에서 6단계로 넓어졌다** — 새 자료 없이 **기존 케이스·표에서 앵커 5종**을 뽑았다.

---

## 1. 개선 ⑤ — 5단계 척도

### 1-1. 등급 규칙 (기계로 확인 가능한 것부터)

| 등급 | 정의 | **기계 근거** |
|---|---|---|
| **판정지원** | 불일치·결손을 **행 단위로 드러낸다** | 반환 구조에 `status`·`checks`·`overall_status`·`rows`·`counts`·`missing` 중 하나 |
| **검증** | 기준표와 대조해 **합·부를 낸다** | `verify_heating_vs_actual`(A-12) · `benchmark_check`(밴드) |
| **산출** | 입력에 **산술을 가한다** | `heating_load`·`capex_*`·`finance` 계열 |
| **조회** | 표에서 **찾아 돌려준다**(입력 산술 없음) | `siting_design_load`·`equipment_lookup`·`warranty_period` |
| **없음** | 그 칸에 함수가 없다 | 코드 5파일 전수 0건 |

> **셀 등급 = 그 칸에 배정된 함수 중 최고 역할.**
> ⚠️ 이 규칙 자체가 한계를 만든다 — **최고 역할 하나가 셀 전체를 대표**한다(§1-4).

### 1-2. 재측정한 18칸

| | 공종설계 | 품질설계 | 감리 | 타당성검증 | 운영 | 사후관리 |
|---|---|---|---|---|---|---|
| **부지** | **산출** | 조회 | 없음 | **산출** | 없음 | 없음 |
| **시설** | **판정지원** | **판정지원** | **산출** | **판정지원** | **산출** | **산출** |
| **기자재** | 조회 | **판정지원** | 없음 | **산출** | **산출** | **산출** |

| 등급 | 칸 수 |
|---|---|
| 판정지원 | **4** |
| 검증 | **0** 🔴 |
| 산출 | **8** |
| 조회 | **2** |
| 없음 | **4** |

### 1-3. 166차 3단계와 대조 — 무엇이 달라졌나

| 166차 | 칸 수 | 5단계로 다시 재면 |
|---|---|---|
| ✅ 있음 | 4 | **판정지원 3** + 🔴**산출 1**(부지 공종설계) |
| 🟡 부분 | 10 | 판정지원 **1**(기자재 품질설계) + 산출 **7** + 조회 **2** |
| ⬜ 없음 | 4 | 없음 4 (불변) |

**🔴 두 칸이 자리를 옮겼다.**

- **부지 공종설계 ✅ → 산출.** 배정 함수가 `siting_design_load`·`siting_lookup`·`select_specs`·
  `spec_crops`(전부 **조회**)와 `mean_wind`(**산출**)뿐이다. **불일치를 드러내는 함수가 없다** —
  지역이 표에 없으면 `None`을 돌려줄 뿐, *"이 부지는 무엇이 문제다"*를 행으로 내놓지 못한다.
- **기자재 품질설계 🟡 → 판정지원.** `doc_consistency_check()`가 `equipment_model`·
  `manufacturer`·`verification`을 **행에 담아 Rev 정합을 판정**한다. 166차가 이것을 *"필드는
  있으나"*로 낮춰 본 것이 과소였다. ⚠️ 단 **`verification` 칸을 채우는 것은 사람**이다.

### 1-4. 🔴 「검증」이 0칸인 이유 — 척도의 한계

`verify_heating_vs_actual()`(A-12 자릿수 검증)과 `benchmark_check()`(밴드 대조)는
**분명히 있다**. 그런데 둘 다 **같은 칸에 판정지원 함수가 함께 있어** 최고 역급에 덮인다
(시설 품질설계 = `doc_consistency_check`, 시설 타당성검증 = `reconcile_quote`).

→ **검증 0은 「검증을 안 한다」가 아니라 「셀 등급으로는 안 보인다」는 뜻이다.**
169차 §1이 *"최고 역할 하나가 셀을 대표한다"*는 문제를 예고했고, 5단계 척도는 그것을
**부분적으로만** 푼다. 완전히 풀려면 **셀당 역할 분포**(조회 n · 산출 n · …)를 적어야 한다 —
이번에는 하지 않았다.

---

## 2. 개선 ④ — 단계별 회귀 앵커

169차 §6: *"실측 회귀 기준(원채원 ROI 14.2% · Payback 7.1년 · 실질ROI 28.3%)은 전부
④타당성검증의 산출물이다. ①②③⑤⑥은 바뀌어도 벤치마크가 울지 않는다."*

→ **새 자료 없이 기존 케이스·표에서 앵커를 뽑았다.**

| 단계 | 앵커 | 고정한 값 | 출처 |
|---|---|---|---|
| **①공종설계** | **A1** 설계하중 조회 | `강원(춘천)` → **32 / 34** · `충남 천안(성환읍)` → **26 / 28** · `충남`(원채원) → **`None`** | 케이스 3건 + 172지역 표 |
| **①공종설계** | **A2** 규격 최소사양 | 적설26·풍속28 연동 → 후보 **37종**, 최소 **`18-연동(등)-01`**(폭 8m) · 적설32·풍속34 연동 → 후보 **21종**, 최소 **`22-연동(등)-03`** | `SPEC_TABLE` 249종 |
| **②품질설계** | **A3** 4축 정합 판정 | 4행 투입 → `OK 1 · MISSING_DOC 1 · MISSING_REV 1 · REV_MISMATCH 1`, 결손 문서명(**BoQ**)·결손 Rev(**시방서**)까지 | `doc_consistency_check()` |
| **③감리** | **A4** 감리 대가 | 공사비 7억 제2종 → **1.35% · 9,450,000원**(제16조① 직선보간) | 국토부고시 [별표5] |
| **④타당성검증** | **기존** | 원채원 **ROI 14.2% · Payback 7.1년 · 실질ROI 28.3%** | `ACTUALS`·벤치마크 |
| **⑤운영** | **A5** 수확량 계단 | `yield_adjustment` 경계 — **90~109%가 무감점**, 그 밖은 감점. 🔴**과잉도 감점한다**(110% → **−5%**) | `yield_adjustment()` |
| **⑥사후관리** | **A6** LCC 교체 구조 | 20년 지평 — 온풍난방기(11년) **[11]** · 배전반(12년) **[12]** · 컴퓨터서버(6년) **[6, 12, 18]** · 총 **5회** | `EQUIPMENT_SERVICE_LIFE_REFERENCE` |

### 앵커 설계에서 지킨 것

- 🔴 **가격을 앵커에 넣지 않았다.** A6은 `unit_cost_won = 1`(단위 1원)로 계산해
  **교체 연도 구조만** 고정한다 — 가격은 **시세성**이라 주입값이고, 박아 두면 그 자체가
  1절 위반이다.
- 🔴 **A1은 `None`도 고정한다.** 원채원(`region='충남'`)이 매칭되지 않는 것은 **결함이 아니라
  S-1의 그림자**다. 누군가 *"매칭되게"* 규칙을 만들면 **다른 지점의 기상값이 조용히 들어온다** —
  그 순간 앵커가 운다.
- 🔴 **A3은 「전부 OK」가 아니라 「결손을 잡는가」를 고정한다.** 정상 행만 넣으면 판정 함수가
  죽어 있어도 통과한다(139차 red self-test와 같은 이유).
- **A5는 과잉 감점을 고정한다.** *"환경이 좋을수록 좋다"*가 아니라는 것이 이 함수의 설계인데,
  경계를 박지 않으면 조용히 단조증가로 바뀔 수 있다.

---

## 3. 이제 회귀가 무엇을 지키는가

| 단계 | 169차 | 171차 |
|---|---|---|
| ①공종설계 | ⬜ | ✅ A1 · A2 |
| ②품질설계 | ⬜ | ✅ A3 |
| ③감리 | ⬜ | ✅ A4 |
| ④타당성검증 | ✅ 원채원 | ✅ 그대로 |
| ⑤운영 | ⬜ | ✅ A5 |
| ⑥사후관리 | ⬜ | ✅ A6 |

**1/6 → 6/6.** 새 자료는 하나도 쓰지 않았다.

---

## 4. 하지 않은 것 / 확인하지 못한 것

- **엔진·케이스·레지스트리를 건드리지 않았다.** 값 변경 0 · 결정 0.
- **셀당 역할 분포를 적지 않았다** — 5단계는 **최고 역할**만 본다. 그래서 「검증」이 0칸으로
  보인다(§1-4). 완전한 해소는 **분포 표기**이고 이번 범위 밖이다.
- **18칸 등급 배정의 근거는 「어느 함수가 그 칸에 배정됐나」**이고, 그 배정은 166·170차 설계서의
  것을 그대로 썼다 — **배정 자체는 판단**이다(169차 §1과 같은 한계).
- **앵커가 「값이 맞다」를 보증하지 않는다.** A1~A6은 **구조와 판정 동작**을 고정한다.
  원채원 벤치마크만이 **값의 옳음**을 지키고, 그것은 **S-1(원문 미보유)** 때문에
  여전히 리포 밖 근거에 기대고 있다.
- **169차 §0의 쏠림(앞단 70% · 뒤 8%)은 그대로다** — 개선 ⑥(과금 전 단계 확장)이
  ★사용자 결정을 기다린다.
