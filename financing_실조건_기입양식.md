# financing 실조건 기입양식 (케이스 대출상환표용)

작성일 2026-08-18 · P3-18(대출상환표) 파생 — 데이터 대기 항목 ①의 기입 준비물.
실제 대출 약정 조건이 확보되면 이 양식대로 케이스 JSON에 붙인다.
**가공값 금지**: 예시 수치는 형식 설명용이며, 실케이스에는 약정서·공고문의
실제 값만 기입한다(출처를 `note`에 반드시 남길 것).

## 1. 스키마 (케이스 JSON 최상위 `"financing"` 블록, 선택)

| 필드 | 형 | 단위/허용값 | 의미 |
|---|---|---|---|
| `loan_principal_won` | 정수 | 원 | 대출 원금(융자 실행액 — 총사업비·자부담과 구분) |
| `annual_rate_pct` | 수 | %/년, 0 이상 | 연 이율(변동금리면 약정 시점 값 + note에 변동 조건) |
| `term_years` | 정수 | 년, 1 이상 | 전체 기간(거치 포함) |
| `grace_years` | 정수 | 년, 0 ≤ 거치 < 전체 | 거치기간(이자만 납부). 없으면 0 |
| `method` | 문자열 | `"원리금균등"` \| `"원금균등"` | 상환 방식 |
| `note` | 문자열 | — | **출처 필수**(약정서/공고명·일자·조항), 변동금리·중도상환 등 특이조건 |

- 엔진 `loan_amortization()`이 연 단위 상환표를 계산하고(순수 결정론),
  `build_site`가 통합보고서 경제성 섹션에 자동 렌더한다(블록 없으면 안내문).
- 검증 규칙(엔진이 강제): 원금>0, 이율≥0, 0≤거치<전체, method 2종만.

## 2. 예시 3종 (전부 합성 예시 — 실케이스 기입 금지)

```json
// (a) 정책자금 전형 구조: 저리·장기·거치형, 원리금균등
"financing": {
  "loan_principal_won": 300000000,
  "annual_rate_pct": 1.5,
  "term_years": 25,
  "grace_years": 5,
  "method": "원리금균등",
  "note": "예시값(합성). 실기입 시: 해당 연도 정책자금 공고(예: 청년농업인
           육성자금)의 확정 금리·기간과 약정서 조항을 그대로 옮기고 출처 명기"
}
```

```json
// (b) 일반 시설자금: 무거치 원금균등
"financing": {
  "loan_principal_won": 150000000,
  "annual_rate_pct": 4.8,
  "term_years": 10,
  "grace_years": 0,
  "method": "원금균등",
  "note": "예시값(합성). ○○은행 시설자금 약정서(20XX-XX-XX) 제X조 — 식으로 기입"
}
```

```json
// (c) 무이자(지자체 이차보전 등으로 실부담 0%인 경우)
"financing": {
  "loan_principal_won": 100000000,
  "annual_rate_pct": 0,
  "term_years": 5,
  "grace_years": 0,
  "method": "원리금균등",
  "note": "예시값(합성). 이차보전으로 농가 실부담 0%인 구조면 그 근거 공고를 명기"
}
```

## 3. 기입 절차

> **착수 승인 상태(2026-08-19, 66차 — 사용자 지시 "대출 약정서 확보되면 진행해라")**:
> **약정서 확보 즉시 별도 재승인 없이 아래 절차로 진행한다.** 확보 시점의 유입 재점검은
> 필요 없다(이 문서가 트리거 기록). 66차 시점 유입 재점검 결과 **미확보 확정**
> (리포 내 금융 관련 파일은 준비물 4종[이 양식·예시·미리보기·웹폼]뿐, 청크 0건, Gmail 60일 0건).
>
> 확보 시 판단 지점 3가지(진행하며 결정):
> - **대상 케이스 선택** — 약정서의 농가와 일치하는 케이스에만 기입(원채원·춘천·우민재 중).
>   일치 케이스가 없으면 기입하지 않고 사용자에게 보고(가공값 금지).
> - **경로 선택** — 웹폼(`/entry/financing/{id}`, 33차 구현·근거 note 필수)과 JSON 직접 편집
>   중 어느 쪽이든 무방(둘 다 엔진 검증 경로 통과). 원문 대조가 끝난 실측이면 JSON 직접 편집이
>   기록에 유리.
> - **note 출처 표기** — 약정서 명칭·일자·조항을 그대로(변동금리면 약정 시점 값+변동 조건).
>
> 완료 조건: 상환표 렌더 확인 → `pytest` green → `audit_traceability.py` PASS →
> 차수 기록·커밋. 데이터 변경 차수이므로 `/redteam` 실행 권장.

1. 실조건 확보(약정서·공고문) → 아래 미리보기로 상환표 확인:

```bash
python financing_미리보기.py financing_예시.json
```

   (자기 조건 파일을 만들어 넣거나, `financing_예시.json`을 복사해 수정)

2. 확인됐으면 해당 케이스 JSON(예: `cases/uminjae.json`) **최상위**에
   `"financing": {...}` 블록으로 붙인다(`input` 안이 아님 — capex_breakdown과
   같은 레벨).
3. `python -m pytest test_cases.py -q` → `python build_site.py` 실행,
   통합보고서에 "금융조달 — 연차별 대출상환표" 섹션이 렌더되는지 눈으로 확인
   ("생성 완료" 출력 라인 확인 — 작업지시서 8절 교훈).

## 4. 한계·주의

- 연 단위 표다(농업 정책자금 관행). 월 단위가 필요해지면 별도 결정(작업지시서).
- 변동금리·금리 재산정·중도상환수수료는 모델링하지 않는다 — note에 기록만.
- 상환표는 참고 계산이며 이자 부담이 ROI/NPV 계산(`finance()`)에 자동 반영되지는
  않는다(현행 경제성 축은 대출 구조와 독립 — 통합 여부는 별도 과제).
