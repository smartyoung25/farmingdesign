# 시나리오 가정값 기입양식 (Best/Base/Worst 다단 표용)

작성일 2026-08-18 · Step6 제외항목 "시나리오 4단 표"의 가정값 주입 스키마 —
데이터 대기 항목 ④의 기입 준비물. **가정값은 판단성 영역이라 엔진이 Best/Worst
배율을 만들지 않는다** — 컨설턴트가 근거와 함께 케이스별로 기입해야 표가 생긴다.
(기존 민감도 스냅샷[판매단가·수확량 ±10%]은 "새 입력 없는 엔진 재호출"이라
가정값이 아니며, 시나리오 표와 별개로 항상 표시된다.)

## 1. 스키마 (케이스 JSON 최상위 `"scenarios"` 블록, 선택)

```json
"scenarios": {
  "note": "가정 세트 전체의 설정 근거·출처(필수)",
  "sets": [
    {
      "name": "Best",
      "assumptions": { "base_yield_kg_m2": 42.0, "price_won_per_kg": 2800 },
      "note": "이 세트의 개별 근거(필수 — 예: 농진청 소득조사 상위 20% 수확량, 최근 3년 최고 단가)"
    }
  ]
}
```

- **Base는 세트로 넣지 않는다** — 케이스 `input` 자체가 Base이며 표에 자동 포함된다.
- `assumptions`에 넣은 필드만 Base에서 바꾼다(나머지는 케이스 값 유지).
- 세트 수는 자유(2단이든 4단이든) — 이름도 자유(Best/Reality/Worst 관례 권장).

### 허용 필드(화이트리스트) — 판단성·시세성 입력만

| 필드 | 의미 |
|---|---|
| `base_yield_kg_m2` | 기준 수확량(kg/㎡) |
| `price_won_per_kg` | 판매단가(원/kg) |
| `opex` | 연간 운영비(원) |
| `fitness_pct` | 환경 적합도(%) |
| `subsidy_rate` | 보조율(0~1) |
| `total_construction_cost` | 총공사비(원) — 공사비 변동 시나리오용 |

지역·하중·피복·면적 등 물리·구조 입력은 **허용하지 않는다** — 그건 시나리오가
아니라 다른 케이스다(화이트리스트 밖 키는 빌드가 즉시 오류로 중단).

## 2. 예시 (합성 — 실케이스 기입 금지)

`시나리오_예시.json` 참고. 실기입 시 각 세트의 `note`에 근거(소득조사·시세
통계·공고 등)를 반드시 남긴다 — note 없는 세트는 빌드가 거부한다.

## 3. 기입 절차

1. 가정값 근거 확보 → 미리보기로 다단 표 확인:

```bash
python 시나리오_미리보기.py 시나리오_예시.json cases/uminjae.json
```

   (첫 인자: scenarios 블록이 든 파일 — 케이스 파일 자체에 이미 붙였으면
   케이스 파일 하나만 넘겨도 된다)
2. 확인됐으면 케이스 JSON **최상위**에 `"scenarios": {...}` 블록을 붙인다.
3. `python -m pytest test_cases.py -q` → `python build_site.py` — 통합보고서
   경제성 섹션의 "시나리오 표(가정 주입)"가 렌더되는지 육안 확인.

## 4. 원칙·한계

- 계산은 전부 엔진 재호출(`render_report.compute`) — 시나리오별로 전 지표
  (ROI·Payback·NPV·IRR·실질ROI)가 일관 재계산된다. 표는 참고정보이며 어느
  시나리오가 "실현될 것"이라는 판정은 하지 않는다.
- 확률·가중평균(기대값)은 모델링하지 않는다 — 발생 확률 부여는 근거 없는
  값 창작이 되기 쉽다(필요 시 별도 결정).
