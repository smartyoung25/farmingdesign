# 선언 건수 검수 — 8/8 일치, 그리고 "셀 수 없다"는 방법의 문제였다 (141차)

**작성일** 2026-09-15 (141차) · **계기** 140차 ⑨후보 **(g)** — *"선언 건수와 실제 열거가
맞는지는 자동으로 셀 수 없음만 확인했고 사람이 세지 않았다"*
**성격** 엔진 상수 값 변경 0 · 레지스트리 **값** 변경 0. **검수 차수**다.

---

## 1. 🔴 140차의 "셀 수 없다"는 방법의 문제였다

140차는 `CAPEX_MAJOR_EVIDENCE_STATUS`의 상태 문자열을 `—`로 쪼개 포함/제외를 가르려다
**주석 안에도 `—`가 있어** `hvac` **10건이 4건으로 오독**되자 *"자동 분류 불가"*로 접었다.

**그러나 셀 대상이 문자열이 아니었다.** 같은 레지스트리에
`CAPEX_MAJOR_CASE_CHUNKS`가 **케이스 15 × 카테고리별 금액**을 갖고 있다 —
**"그 카테고리 금액이 0이 아닌 케이스 수"를 세면 문자열을 건드릴 필요가 없다.**

📌 **틀린 도구를 쥐고 "불가능"이라 결론낸 것**이 140차의 실수다.
(125차 P1~P3·133·134·138·140차의 파서 결함 계열과 다르다 — 이번엔 **파서를 고칠 게 아니라
파서를 쓰지 말았어야** 했다.)

---

## 2. ✅ 검수 결과 — 선언 건수 8종 전부 일치

| 카테고리 | 선언 | 금액>0 케이스 | |
|---|---|---|---|
| `greenhouse_structure` | 14 | **14** | ✅ |
| `auto_opening_system` | 15 | **15** | ✅ |
| `hvac` | 10 | **10** | ✅ |
| `irrigation_fertigation` | 14 | **14** | ✅ |
| `ict_control` | 6 | **6** | ✅ |
| `electrical` | 4 | **4** | ✅ |
| `auxiliary_facility` | 1 | **1** | ✅ |
| `thermal_storage_insulation` | 2 | **2** | ✅ |

나머지 5종(`equipment_procurement`·`design_supervision_fee`·`site_preparation`·
`contingency`·`land_acquisition`)은 **미검증·참고요율이라 건수 선언이 없고**,
데이터에서도 금액이 **전부 0**이다 — 정합한다.

### 잔차도 닫힌다

**분류합 + 미분류 = `known_total`** 이 **15케이스 전부 원단위 일치**(불일치 0건).

---

## 3. 📌 세는 단위가 명시돼 있지 않았다

**"건"은 파일이 아니라 케이스**이고, **`백가은·조윤정`은 쌍 견적 통합 1건**이다.
(파일은 2개지만 표지 3줄 외 22p가 문자 단위 동일 — 59차 전문 diff.)

→ 140차가 상태 문자열의 **이름을 세어** `greenhouse_structure` 15 vs 선언 14로 어긋난 것은
**이 규칙을 몰라서**였다. 이제 적어 둔다.

---

## 4. 🔴 서술 비대칭 2건

건수를 선언한 8종 중 **6종은 "나머지가 왜 0인지"를 적는다**:

- `greenhouse_structure` — *"오기수는 설비 전용 부분 범위 견적이라 골조·피복 품목 전무(0)"*
- `hvac` — *"이두희·한일그린텍·강정구·오기수·구창회는 견적 범위에 난방설비 자체가 없어 0"*
- `irrigation_fertigation`·`ict_control`·`electrical`·`auto_opening_system`(*"전 표본"*)

**그런데 둘은 적지 않는다**:

| 카테고리 | 선언 | 언급 없는 케이스 |
|---|---|---|
| `auxiliary_facility` | 1건(윤성호) | **14건** |
| `thermal_storage_insulation` | 2건(윤성호·임미라) | **13건** |

→ ⚠️ **"나머지는 왜 0인가"가 비어 있다.** 다른 6종과 달리 독자가 **부재의 이유를 알 수 없다**
(R6 — 부재 단정에 근거 병기와 같은 계열).
→ 🔴 **값(상태 문자열)을 고치지 않았다** — 0인 이유는 **판단**이라 데이터에서 유도되지 않는다.
**142차 후보**로 남긴다.

---

## 5. 검증하지 않은 것

- **엔진 상수 값도 레지스트리 값도 하나도 바꾸지 않았다.**
- 🔴 **"금액>0 케이스 수"가 선언과 같다는 것이 "그 분류가 옳다"는 뜻은 아니다.**
  예컨대 `hvac` 10건에 최선동이 들어 있는데, **안개분무시설 18,500,000의 귀속은 여전히
  `[확인요망]`**(대장 `CM1`, hvac 이동 시 3.7%p)이다. **건수가 맞는 것과 귀속이 옳은 것은 다르다.**
- **상태 문자열 안의 개별 금액·서술(예: 박규현 170,837,702)은 대조하지 않았다** —
  건수와 잔차만 봤다.
- **`auxiliary_facility`·`thermal_storage_insulation`의 0 사유를 채우지 않았다**(판단성).
