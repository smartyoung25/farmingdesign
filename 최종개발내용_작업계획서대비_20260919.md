# 최종 개발 내용 정리 — 작업계획서 대비 (154차)

**작성일** 2026-09-19 (154차) · **기준 문서** `작업계획_컨설팅모델_입지설계운영경제성.md`(2026-07-16)
**성격** 엔진 상수 값 변경 0 · 케이스·견적비교·레지스트리 불변 · 결정 0 — **정리만 한다**

> 계획서는 2026-07-16에 **P0 → P3 로드맵**과 **컨설팅 4축**(입지·설계·운영·경제성)을 세웠다.
> 이 문서는 그 항목 하나하나를 **코드·레지스트리·산출물에서 직접 확인**해 상태를 적는다.
> ⚠️ 계획서의 기대값을 그대로 옮기지 않았다 — **지금 리포에 있는 것**만 적는다.

---

## 0. 규모

| | 값 |
|---|---|
| 기간 | 2026-07-17 ~ 2026-09-16 (첫 커밋 ~ 최종) |
| 커밋 | **158** · 차수 로그 **122**건 |
| 엔진 | `smartfarm_engine.py` **5,527줄** · 공개 함수 **55개** |
| 테스트 | `test_engine.py` **6,609줄** · 3파일 **315 passed** / 5파일 **372 passed** |
| 레지스트리 | 상수 **54개** · `source_refs` **151건** (exact 90 / partial 53 / near 8) |
| 근거문서 | `근거_*.md` **58종** |
| 원문 | `스마트팜스펙/` **218 파일** · 청킹 인덱스 **48,433 청크** |
| 산출물 | HTML **18종** |
| 레드팀 | **27회차** 완주(28회차 진행 중) |

---

## 1. 🔴 계획서 로드맵 대비 — 항목별 실제 상태

### P0 (근거 확보)

| 항목 | 계획 | 실제 | 확인 지점 |
|---|---|---|---|
| **P0-a** | `U_VALUE`(유리 5.3 단위 의심)·`FR_TABLE` 원출처 확보 또는 확정 미검증 태깅 | ⚠️ **부분** — `U_VALUE` `부분실측`(refs 2) · `FR_TABLE` `부분실측`(refs 2). **원문서는 끝내 미확보**(`스마트팜스펙/` 39문서 전수 검색 0건). 대신 **등급·대조 기준을 붙여 「근거 없는 결정론」 상태에서 뺐다**. 🔴별도로 `U_DESIGN`이 분리·신설돼 **99차 ★사용자 결정으로 8.9 → 5.7** 확정(`공공기준`, refs 3) | 레지스트리 `U_VALUE`·`U_DESIGN`·`FR_TABLE` |
| **P0-b** | 지역→설계적설심/풍속 매핑표 확보 | ✅ **완료·초과** — `REGION_DESIGN_LOAD` **172지역**. 계획 시점의 2014-78호가 **폐기 고시**임을 발견해 **2025-108호**(2025.10.31 시행)로 전면 교체 | `REGION_DESIGN_LOAD` |
| **P0-c** | 용도지역·농지전용·계통연계·용수 체크리스트 정의 | ⚠️ **부분** — 케이스 `site`에 `landuse_zone`·`grid_connection_note`·`design_load_source`·`design_load_status` 필드 존재, 보조사업 상수 3종(`SUBSIDY_APPLICATION_PROCEDURE`·`SUBSIDY_PROGRAM_TYPES_REFERENCE`·`CLUSTER_SUBSIDY_RATE_SHARED`)과 `subsidy_application_checklist()` 존재. 🔴**농지전용·용수 항목은 없다** | `cases/*.json` `site` · 엔진 |
| **P0-d** | 기존 원가계산서를 OPEX 15항목 관점으로 재추출 | ✅ **완료(항목수 초과)** — `OPEX_ITEM_CATEGORIES` **25항목**(직접경비 18 + 간접경비 7). ⚠️**원문 CSV가 0바이트**라 대조 불가(132차 발견, **S-3**) | `OPEX_ITEM_CATEGORIES` |

### P1 (엔진 모듈)

| 항목 | 계획 | 실제 | 확인 지점 |
|---|---|---|---|
| **P1-a** | `siting_lookup()` — 지명 입력 → 설계하중 | ✅ **완료** — `siting_lookup(region_name, form=None)` + `siting_design_load(region_name)` 둘 다 존재 | 엔진 |
| **P1-b** | `opex_breakdown()` — lump-sum을 항목 분해, `finance()` 연결 | ✅ **완료** — `opex_breakdown(items, known_total)` + 케이스에 `opex_breakdown` 블록(`items_won`·`known_total_won`·`unclassified_won`·`as_of`·`note`) | 엔진 · `cases/*.json` |
| **P1-c** | 케이스 스키마 v2 — 입지·운영 필드 + provenance | ✅ **완료** — 완전 케이스 3종이 `site`·`opex_breakdown`·`scenarios`·`provenance`·`as_of`·`notes`를 모두 보유. 부분 케이스 2종은 `partial`·`construction` 계열 | `cases/*.json` |

### P2 (리포트 조립)

| 항목 | 계획 | 실제 | 확인 지점 |
|---|---|---|---|
| **P2** | 컨설팅 4섹션 통합 리포트 | ✅ **완료** — `SmartFarm_통합보고서_{case}.html` **3건**이 계획서 6절 구조 그대로다: **경영자 요약 → 확인 필요 항목 → Ⅰ 입지진단서 → Ⅱ 설계적정성보고서 → Ⅲ 운영계획서 → Ⅳ 경제성분석서** | 통합보고서 HTML |
| **P2-부가** | 민감도/what-if | ✅ **완료** — 케이스 `scenarios`(Best/Base/Worst 3단, 각 가정에 **근거 note 필수** — 없으면 `build_site`가 `ValueError`) + 리포트에 민감도 스냅샷 | `build_site.py:237` |

### P3 (설명 계층)

| 항목 | 계획 | 실제 |
|---|---|---|
| **P3** | 대화형 컨설턴트 설명 계층(판단성은 초안까지만) | ❌ **미착수**. `webapp.py`(639줄·라우트 18개)가 있으나 이는 **입력·재생성 계층**(financing·scenario·newcase·quotes 폼)이지 설명 계층이 아니다 |

---

## 2. 계획서에 없던 것 — 실제로 더 지은 것

계획서는 4축 재편과 P0~P3만 적었다. 실제 엔진은 그보다 훨씬 넓다(공개 함수 55개).

| 영역 | 내용 |
|---|---|
| **견적·발주 정합** | `generate_rfq_package` · `reconcile_quote` · `compare_quotes` · `compare_design_options` — RFQ 사양서 발주와 견적서 완전성 검증 |
| **CAPEX 13분류** | `capex_major_breakdown` + 표본 **15건** · `CAPEX_MAJOR_KNOWN_TOTALS` 합 **7,918,192,671원** · 미분류 잔액까지 추적 |
| **품셈** | `pumsem_labor_days` · `pumsem_project_labor_summary` + `PUMSEM_ITEMS` **64종 전수 전사**(농어촌공사 2021 보고서 제7장 제2·3절) |
| **금융** | `loan_amortization` · `dscr_schedule` · `max_investable_capex` · `operating_breakeven` · `lcc_replacement_schedule` |
| **단지** | `cluster_economics` — 공동 CAPEX/OPEX 분담 |
| **난방 정밀화** | `heating_load_components`(관류·틈새·지중 3성분) · `transmission_share_pct` · `design_outdoor_temp`(TAC 69지역) · `heating_degree_hours` · `monthly_sunshine` · `monthly_mean_wind` · `wind_correction_factor` · `period_load_adjust_k` · `COVER_ASSEMBLIES` 49종 |
| **검증 인프라** | `audit_traceability.py`(게이트) · `verify_refs.py`(앵커 재검산) · `검증절차_레드팀.md`(27회차·112KB) · `SmartFarm_근거대장.html`(상수 54개 전량 공개) |

---

## 3. 🔴 계획서의 예측이 빗나간 지점 (기록)

| 계획서가 말한 것 | 실제 |
|---|---|
| *"SPEC_TABLE 32종 … 실측 확정"* | **249종**으로 확장. 근거였던 **2014-78호가 폐기 고시**였다 |
| *"입지축 지역 매핑표가 없다"* | 만들었으나 🔴**케이스 `region`이 기상표 키와 0/25 매칭**이다(150차) — 표는 **관측지점명**, 케이스는 **행정구역 표기**. 계획서가 예상 못 한 연결 실패 |
| *"운영축 OPEX 15항목"* | **25항목**. 단 **원문 CSV가 0바이트**라 계획서가 전제한 *"이미 확보한 원문서 재추출"*이 절반만 성립 |
| *"P0-a가 다른 모든 작업보다 우선"* | 그대로 되지 않았다. `U_DESIGN`은 99차에 ★결정으로 닫혔고 `U_VALUE`·`FR_TABLE`은 **지금도 원문서 미확보**다 — **77차부터 이월된 최대 미해결**이었다 |

---

## 4. 지금 남은 것 — 두 종류뿐이다

### (가) ★사용자 결정 15건 — 감응은 전부 측정됐다

145·149·150차에 **D-1~D-15 전 항목의 감응이 실측**됐다(`근거_결정대기대장_20260915.md`).

| 성격 | 항목 |
|---|---|
| 회귀 기준에 닿는 것 | **D-7 하나뿐**(우민재 2,323 → 2,321.87 — 밴드 여유 158원 → 42원) |
| 택일 | **D-2 ↔ D-4**(엔진이 동시 지정을 거부) · D-1은 사후 배율이라 어느 쪽에도 곱해진다 |
| 나머지 11건 | 산출물은 움직여도 회귀 기준은 건드리지 않는다 |

### (나) 자료 4건 — 사용자만 넣을 수 있다

| # | 자료 | 막고 있는 것 |
|---|---|---|
| **S-1** ⭐ | 원문 미보유 3건(공주장원리·당진이상근·**원채원**) | 🔴**원채원은 이 리포의 회귀 기준인데 원문이 없고 지역도 「충남」뿐**이라 기상표에 닿지 않는다 |
| **S-2** ⭐ | `SmartFarm_엔진데이터.md` | A-11 밴드 유도 근거 + A-12 난방실측(→ P0-a) |
| **S-3** | 농진청 OPEX CSV | **0바이트** — `OPEX_ITEM_CATEGORIES` 25항목 대조 불가 |
| **S-4** | (B)계열 보존본 3종 | 추적성 사각 (가) 3건 |

추적성 사각은 **6건이고 그중 실제 백로그는 4건**이다(152차 분류 — 나머지 2건은 파일 ref 개념이 성립하지 않는다).

---

## 5. 이 프로젝트가 배운 것 (반복된 실패 유형)

계획서는 *"모래 위 자동화"*를 경계했다. 실제로 반복된 것은 **그보다 좁고 구체적인 네 가지**다.

| 유형 | 사례 |
|---|---|
| **절대표현** — 유일한·전량·0건·전부 동일 | 106차 *"전부 동결"*(B7 누락) · 142차 *"유일한 사용처"*(전문 렌더) · 144차 *"대기 전량"*(2건 밖) · 140차 *"기준 없는 ref 0건"*(차수 숫자가 만든 0) · 151차 *"6개 문서 전부 동일"*(산재가 셋 다 다름) |
| **문자열 탐색으로 감응·부재를 잼** | 145차 D-12 *"산출물 감응 0"*(데이터 경로 누락) · 149차 D-3(연료 감소율을 OPEX 변동률로 전재) |
| **세는 문자열을 서술에 씀(자기부풀림)** | 131차 `[확인요망]` · 134차 ref note · 137차 배지 |
| **산출물을 읽는 가드가 낡은 산출물로 통과** | 148차 P1 · 151차 배지(→ **게이트 순서를 `build_site` → `pytest`로 고정**) |

→ 이 네 가지가 **레드팀 절차와 뮤테이션 규율이 존재하는 이유**다. 27회차 누적 발견 중
**상당수가 리포 자신이 쓴 서술의 결함**이었고, 기계 게이트는 그것을 잡지 못했다.

---

## 6. 이 문서가 하지 않은 것

- **엔진 상수 값·케이스·견적비교·레지스트리 값 전부 불변.** 정리만 했다.
- **결정은 하나도 내리지 않았다.** D-1~D-15 전부 ★ 대기다.
- **P3(설명 계층)를 착수하지 않았다** — 판단성 경계선이 걸린 영역이라 설계부터 ★결정이다.
- **P0-c의 농지전용·용수 체크리스트를 만들지 않았다** — 항목 정의 자체가 판단성이다.
- **153차(레드팀 28회차) 결과는 이 문서에 반영되지 않았다** — 에이전트 진행 중이고,
  결과가 오면 **별도 차수**로 반영한다. 이 문서의 수치는 `fd2c116`(152차) 시점이다.
