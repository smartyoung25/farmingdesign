# 0 사유 보강 — "부대시설이 없다"가 아니었다 (142차)

**작성일** 2026-09-15 (142차) · **계기** 141차 ⑨후보 **(g)**
**성격** ⚠️ **112차 이후 첫 엔진 상수 값 변경**이다 — 다만 **서술 필드 말미 추가**이고
**계산에 쓰이지 않으며 표시 분류도 불변**이다(아래 §3).

---

## 1. 141차가 남긴 것

건수를 선언한 8종 중 **6종은 "나머지가 왜 0인지"를 적는데**
`auxiliary_facility`(미언급 **14건**)·`thermal_storage_insulation`(**13건**)만 적지 않았다.
141차는 *"0인 이유는 판단이라 데이터에서 유도되지 않는다"*며 **값을 고치지 않았다**.

---

## 2. 🔴 그런데 리포가 이미 기록하고 있었다

`CAPEX_MAJOR_CASE_CHUNKS`의 서술을 뒤지니 **사유가 3건 나왔다** — 판단이 아니라 **기록된 사실**이다.

| 표본 | 기록된 사유 | 차수 |
|---|---|---|
| **이준희** | 재배실 4,756.32 + **관리실 648㎡**가 실재하나 `0402 재배실벽체 및 관리동지붕공사`가 **온실구조에 혼입**돼 0 | 52차 |
| **강정구** | 부속자재에 **관리동 판넬 및 출입문 부속 5,597,384원** 포함 — **독립 공종이 아니라** 분리 안 함 (**재집계** — §2-1) | 56차 |
| **오기수** | **관리동커텐 2,817,269원**이 **자동개폐로 귀속**. 🔴**146차 확인**: 이것은 오기수 집계표 p2의 **공종번호 `3.`이 붙은 최상위 공종**이다 — *"독립 공종이 아니어서 0"*이 **아니라 커튼 공종이라 보온커튼 관례(57차)로 귀속**한 것이다 | 57차 |

→ 🔴 **`auxiliary_facility`의 "실측 1건"은 "부대시설이 1건뿐"이라는 뜻이 아니다.**
최소 3건은 **관리동이 실재하는데 0**이다 — 🔴**146차 정정**: 사유가 **한 가지가 아니다**. 이준희는 **혼입**, 강정구는 **독립 공종 아님**, 오기수는 **독립 공종이지만 공종 성격(커튼)으로 귀속**이다.
상태 문자열이 그것을 적지 않아 **독자가 "나머지 14건엔 부대시설이 없다"로 읽을 수 있었다.**

⚠️ **나머지 11건의 0 사유는 확인되지 않았다** — 원문 재확인 대상이고 **판정하지 않는다**.

### 2-1. 🔴 146차 보강 — 강정구 5,597,384의 **재집계 식** (레드팀 27회차 [8])

이 값은 **원문에 인쇄돼 있지 않은 재집계**인데 **식이 어디에도 적혀 있지 않았다**. 그래서
레드팀이 `관리동` 키워드 줄만 더해 **1,581,384**를 얻고 *"원문에 없다"*고 보고했다.
140차의 `RECOMPUTED` 규율(재집계 값은 재집계 기준을 병기한다)이 이 값에는 적용되지 않았던 것이다.
원문(`스마트팜스펙/견적참조/1. 군산 강정구 농가_8.4×44×10연동_와이드_딸기.pdf`)에서 재현한다 —
**`판넬`·`출입문`이 들어간 줄 7개의 합계열**이다:

| p | 항목 | 합계 |
|---|---|---|
| 5 | 관리동판넬부마무리 ㅁ75×75×2.0t×6m 본 4 | 211,384 |
| 8 | 판넬 100T ㎡ 54 | 1,512,000 |
| 8 | 판넬부자재 100T ㎡ 54 | 324,000 |
| 8 | 판넬출입문 4000*4000 개소 1 | 2,500,000 |
| 8 | 출입문캐노피 개 1 | 400,000 |
| 8 | 출입문쪽문 1200*2400 개소 1 | 400,000 |
| 8 | 관리동출입문 3000*3000비닐출입문 개소 1 | 250,000 |
| | **합** | **5,597,384** ✅ |

⚠️ `관리동수평커튼-삼상0.5마력`(p9)은 **콘트롤박스 3,800,000 줄의 접힌 규격 조각**이지
독립 금액이 아니다 — 키워드만 세면 오히려 틀린다.
→ 종전 *"약 5,597,384원"*의 **"약"을 뺐다**. 근사치가 아니라 **정확한 합**이다.

### `thermal_storage_insulation`은 성격이 다르다

상태 문자열이 이미 *"축열탱크만 2건"*으로 **범위를 한정**하고 보온커튼의 행방(자동개폐 소속)도
적는다. 다만 **나머지 13건에 축열탱크가 실재하는지는 확인되지 않았다** —
실측 2건은 둘 다 **난방·히트펌프 블록 내 라인분리**로 찾은 것이라 **같은 방식의 전수 점검이
필요하다**. 그 사실을 병기했다.

---

## 3. ⚠️ 엔진 값을 바꿨다 — 무엇이 안전한가

`CAPEX_MAJOR_EVIDENCE_STATUS`는 **레지스트리이자 엔진 상수**다(드리프트 가드가 둘의 동일성을
검사한다). 따라서 이 보강은 **112차 이후 첫 엔진 상수 값 변경**이다.

**영향 범위를 확인했다**:

```python
# build_site.py:136
tag = "실측" if ev.startswith("실측") else ("추정" if ev.startswith("부분") else "미검증")
```

→ **접두만 읽는다.** 추가는 **말미**이므로 `auxiliary_facility`=실측 ·
`thermal_storage_insulation`=추정 **그대로**다. **계산에는 쓰이지 않는다.**

### 🔴 146차 정정 — *"유일한 사용처"*는 틀렸다 (레드팀 27회차 [3])

같은 루프 안에 **두 번째 사용처**가 있다. `build_site.py:133~146`을 끝까지 읽으면:

```python
for key, kor, desc in e.CAPEX_MAJOR_CATEGORIES:
    ev = e.CAPEX_MAJOR_EVIDENCE_STATUS[key]
    tag = "실측" if ev.startswith("실측") else (...)        # ← 접두 (CSS 클래스)
    ...
    f"<td><span class='tag {tag}'>{esc(ev)}</span></td>"   # ← 🔴 전문 렌더
```

**`{esc(ev)}`가 1,320자 전문을 HTML로 내보낸다.** 그 결과 142·143차가 넣은 마크다운이
`SmartFarm_CAPEX분해.html`에 **리터럴로** 나갔다 — `**` 실측 **76개**:

```
142차 보강: **나머지 14건이 0인 것은 &#x27;부대시설이 없...
```

**값과 표시 분류는 틀리지 않았다**(접두는 그대로다) — 틀린 것은 *"유일한 사용처"*라는
**절대표현**이다. 탐색 범위를 `build_site.py:136` 한 줄로 잡고 선언했다(R6 위반:
부재·유일성 주장은 ①탐색 범위 ②미파싱 형식 ③미열람 구간을 병기해야 한다).

⚠️ **마크다운 리터럴은 이 차수에서 지우지 않았다** — 상태 문자열에서 `**`를 빼는 길과
`build_site`가 접두만 렌더하게 바꾸는 길이 있고, 후자는 **산출물 표시를 바꾸므로 후보로만
올린다**.

📌 엔진 리터럴이 **묵시적 문자열 연결**(5줄)로 쪼개져 있어 1차 치환이 **꼬리를 중복**시켰다 —
연결 전체를 한 줄로 합쳐 고쳤고, `compile()`과 드리프트 가드로 확인했다.

---

## 4. 🔴 "언급 ≠ 근거"가 실증됐다

140차는 *"상태 문자열에 이름이 나타난다고 근거인 것은 아니다"*라고 경고하며
ref note에 **언급 카테고리 수**를 적었다. 이번에 **이름을 적자 그 수가 늘었다**:

| 표본 | 140차 | 142차 |
|---|---|---|
| 이준희 | 5 | **6** |
| 강정구 | 6 | **7** |
| 오기수 | 5 | **6** |

→ **늘어난 언급은 근거가 아니라 0 사유**다. 세 note에 그 사실을 적었다.

---

## 5. 📌 가드의 프로즈 휴리스틱을 또 고쳤다

141차 가드는 `("0)", "없어 0", "전무", "전 표본")`로 *"0 사유를 적는가"*를 쟀는데,
142차가 채운 문구(*"0인 것은"*·*"0 사유"*)를 **그 패턴이 못 잡았다**.
→ **휴리스틱을 버리고 카테고리별 표지를 명시**했다(8종 각각).

📌 125차 P1~P3 · 133 · 134 · 138 · 140 · 141차에 이어 **프로즈 자동 판정이 또 틀렸다**.
이번 교훈은 **"서술을 고치면 그 서술을 재던 휴리스틱도 같이 깨진다"**이다.

---

## 6. 검증하지 않은 것

- **수치 상수는 하나도 바꾸지 않았다.** 바뀐 것은 **상태 문자열 2종의 말미 서술**뿐이고
  `상수 54` · `refs 148` · 케이스·견적비교 수치 전부 불변이다.
- 🔴 **나머지 11건(`auxiliary_facility`)·13건(`thermal_storage_insulation`)의 0 사유를
  확인하지 않았다** — 원문 재확인이 필요하고 **판정하지 않았다**.
- **이준희·강정구·오기수의 기록을 원문으로 재확인하지 않았다** — 52·56·57차가 당시
  원문에서 읽어 적은 것을 **인용**했다.
- **`auxiliary_facility` 0인 11건에 관리동이 실재하는지 세지 않았다**(전수 점검은 별건).
