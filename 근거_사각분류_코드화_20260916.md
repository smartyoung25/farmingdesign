# 사각 분류를 코드화했다 — 선언으로 나누고 불변식으로 지킨다 (152차)

**작성일** 2026-09-16 (152차) · **계기** 사용자 지시 *"감사기 사각 분류 코드화 진행"*
**성격** 엔진 상수 **값 변경 0** · 케이스·견적비교 불변 · 레지스트리 **값 변경 0**(스키마 필드 신설) · 결정 0

> 151차가 남긴 것: *"감사기의 사각 분류를 코드로 나누지 않았다 — §3의 구분은 이 문서와
> 가드에만 있다. 게이트 출력을 바꾸는 일은 별도 차수로 둔다."* 그 차수다.

---

## 0. 결과

```
## 추적성 사각 — '실측' 계열인데 source_refs 0건 (133차 신설 · 152차 분류, FAIL 아님)

### (가) 원문이 리포에 없어 못 붙인다 — 자료가 들어오면 풀린다 (4건)
- SPEC_COUNT · SPEC_TABLE · ~~REGION_DESIGN_LOAD~~ · OPEX_ITEM_CATEGORIES
  - 🔴**163차 — `REGION_DESIGN_LOAD`가 빠져 4건 → 3건**: 고시 [별표]가 리포 안 구조검토서 2건에 전재돼 있었다(172지역 전수 대조, 하향 0건). *"원문이 리포에 없다"*는 **현행 2025-108호 전면표**에 한해 유효하다.

### (나) 파일 ref 개념이 성립하지 않는다 — 사각이 아니다 (2건)
- CAPEX_MAJOR_CATEGORIES: 실측 — `결정`
- RFQ_REQUIRED_CATEGORIES_DEFAULT: 실측 — `파생`

> 실제 백로그는 (가) 4건이다(총 6건 중).
```

---

## 1. 🔴 설계의 핵심은 "분류 근거를 어디에 두는가"였다

| 안 | 내용 | 판정 |
|---|---|---|
| ① | 감사기에 상수명 하드코딩 | ❌ **지식이 두 곳으로 갈라진다** — 레지스트리를 고쳐도 감사기가 모른다 |
| ② | `source` 서술을 파싱해 *"사용자 대화"*·*"도출"* 같은 말을 찾는다 | ❌ **141차 교훈의 반복** — *"파서를 고칠 게 아니라 파서를 쓰지 말았어야 했다"* |
| **③** | **레지스트리에 `ref_basis` 명시 선언** | ✅ 채택 |

### 신설 스키마

```json
"ref_basis_legend": {
  "결정": "값의 출처가 사용자 결정·대화라 파일이 아니다 — source_refs가 성립하지 않는다",
  "파생": "다른 엔진 상수에서 도출된 값이다 — 원문이 아니라 상수가 출처다"
}
```

| 상수 | `ref_basis` | 왜 |
|---|---|---|
| `CAPEX_MAJOR_CATEGORIES` | **결정** | 출처가 2026-07-16 **사용자 제안 분류표(대화)**다 |
| `RFQ_REQUIRED_CATEGORIES_DEFAULT` | **파생** | `CAPEX_MAJOR_EVIDENCE_STATUS`·`CAPEX_MAJOR_CASE_CHUNKS`에서 **도출**된 값이다 |

⚠️ 최상위 상수 엔트리에는 **키 화이트리스트가 없다**(`test_registry:132`는 `source_refs`
**엔트리** 키만 검증한다) — 그래서 필드 신설이 기존 가드를 깨지 않는다. 확인하고 넣었다.

---

## 2. 선언에는 불변식이 따라붙는다

선언은 **주장**이므로 검사받아야 한다. 감사기가 **hard 결함**으로 잡는다:

| 불변식 | 왜 |
|---|---|
| `ref_basis` 값은 **범례 안**이어야 한다 | 오타·임의 값으로 사각을 빠져나가는 길을 막는다 |
| `ref_basis`와 `source_refs`는 **공존할 수 없다** | 파일 ref를 붙였다면 *"구조상 0"*이라는 선언이 **틀린 것**이다 |

### 🔴 실제로 도는지 red self-test로 확인했다

위반이 0건이면 **검사가 도는지 결과로 구별되지 않는다**(139차 M5 교훈). 그래서
`audit_registry(reg=None)`에 **주입점**을 열고 합성 레지스트리로 시험했다:

```
현재            hard 0 · blocked 4 · structural 2
red① 범례 밖    ('SPEC_COUNT', "ref_basis '추측'가 ref_basis_legend에 없다")
red② 공존       ('ACTUALS_COUNT', 'ref_basis 선언과 source_refs가 함께 있다 — …')
```

---

## 3. 🔴 부수 발견 — 내 게이트 순서에 결함이 있었다

152차 착수 직후 `test_137cha`의 근거대장 배지 가드가 **실패**했다. 원인을 보니
**151차가 통과한 이유가 잘못돼 있었다**.

151차 게이트 실행 순서가 이랬다:

```
pytest(3파일) → pytest(5파일) → audit → build_site → verify_refs
                                          ↑ 여기서 HTML이 53으로 바뀐다
```

pytest가 **build_site보다 먼저** 돌아서, 배지 가드는 **151차가 refs를 3건 늘리기 전의
낡은 HTML**(부분 50)을 읽고 통과했다. 산출물이 갱신된 뒤 다시 돌리니 53이 나와 실패했다.

→ **게이트 순서는 `build_site → pytest`다.** 가드에 그 이유를 주석으로 박았다.
📌 이것은 148차 뮤테이션 P1이 *"가드가 커밋된 HTML만 본다"*로 빠져나간 것과 **같은 계열**이다 —
**산출물을 읽는 가드는 산출물이 최신일 때만 의미가 있다.**

---

## 4. 검증하지 않은 것 / 하지 않은 것

- **엔진 상수 값·케이스·견적비교·레지스트리 값 전부 불변.** 레지스트리는 **스키마 필드 신설**뿐이다.
- **(가) 4건을 줄이지 않았다** — S-3·S-4가 들어와야 풀린다.
- **`ref_basis`를 다른 상수에 붙이지 않았다** — 지금 2건이 전부이고, 늘리는 것은
  *"이 값은 파일로 검증될 수 없다"*는 **주장**이라 근거가 필요하다.
- **`refless_measured` 키는 그대로 뒀다**(6건) — 기존 계약을 깨지 않기 위해서다.
  분류는 `refless_blocked`·`refless_structural`로 **더해서** 제공한다.
- **결정은 하나도 내리지 않았다.** D-1~D-15 전부 ★ 대기다.
