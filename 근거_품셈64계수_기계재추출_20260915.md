# 64계수 기계 재추출 — 113차 육안 전수 대조의 상시화 (125차)

**작성일** 2026-09-15 (125차) · **계기** 레드팀 24회차 ⑧ / 124차 ⑨후보 **(f)**
**성격** 엔진 상수 값 변경 0. **113차의 육안 대조를 기계로 재현 — 불일치 0건.**

---

## 0. 결론

| | |
|---|---|
| ✅ **64/64 일치** | 원문 PDF **텍스트층에서 기계 추출**한 계수가 엔진과 **불일치 0건** |
| 🎯 **상시화** | `pumsem_extract.py` + `test_125cha_…` — **이 테스트가 green인 한 엔진 64계수는 원문과 같다** |
| 🔴 **파서가 틀리고 엔진이 맞았다** | 개발 중 3번 어긋났고 **3번 다 파서 결함**이었다(아래 3절) |
| ⚠️ **적용 범위** | 인쇄 **p.138~162**뿐이다. 부록1 공사일보·p.109~137은 **Type3/스캔**이라 이 경로가 통하지 않는다 |

---

## 1. 왜 지금까지 못 읽었나

이 PDF는 폰트가 **ToUnicode 없이 임베드된 서브셋**(`PGFPCD+Batang` · `PGHBIG+Gulim`)이라
`pdfplumber.extract_text()`가 `(cid:N)`만 내준다. 102차부터 여러 차수가 이를 확인하고
**렌더링 육안 판독**으로 우회해 왔다(113차의 25쪽 전수 대조가 그 정점이다).

24회차 레드팀의 fresh-context 에이전트가 **다른 길을 찾았다**:

```
CIDToGIDMap = /Identity          →  cid == GID
서브셋 glyph 아웃라인 해시  ↔  시스템 Batang/Gulim cmap 해시
                                  →  GID → 유니코드 복원
```

125차가 이것을 재현해 `pumsem_extract.py`로 만들었다. **복원 결과: 340 매핑 · 모호 0건.**

📌 한글 일부는 시스템 폰트와 아웃라인이 달라 `�`로 남는다. **숫자·소수점은 전량
복원**되므로 계수 대조에는 지장이 없다 — 그래서 이 모듈은 **품목명이 아니라 (공종, 순번,
계수 시퀀스)로 대조**한다.

---

## 2. ✅ 64/64 일치

```
엔진 64건 / PDF 64건
불일치 0건 / 대조 64건
```

대조 방식:
- 원문 **차례 순서**(목차 PDF 19 — 120차 확인)로 9공종을 훑는다. ⚠️ 엔진 **선언 순서**는
  다르다(120차 ⑦: 알루미늄이 원문 2번째 ↔ 엔진 4번째) — 대조 전에 재정렬한다.
- 각 품목의 계수 시퀀스는 `labor_per_unit` 값 + `equipment_hours_per_unit` 값이고,
  원문 표의 **인력 먼저 · 장비 나중** 순서와 일치한다.
- 값 다중집합(246개)도 엔진과 **완전히 같다**.

📌 이 대조가 113차와 **독립**인 이유: 113차는 이미지를 사람이 읽었고, 125차는 **텍스트층
바이트에서 기계가 읽는다**. 같은 실수를 반복할 경로가 없다.

---

## 3. 🔴 개발 중 3번 어긋났고 3번 다 파서 결함이었다

| # | 증상 | 원인 | 조치 |
|---|---|---|---|
| P1 | 알루미늄 1·2가 **한 품목으로 병합**, 63건만 추출 | 품목 경계를 `"1."` 순번으로 잡았는데 **앞 글자가 복원되지 않은 줄**에서 놓쳤다 | 경계를 **표 헤더 줄**(`구분/규격/단위/수량`)로 교체 — 품목당 정확히 한 번 나온다 |
| P2 | 수평스크린 10 와이어가 **`0.0003` → `3.0`** | 수량 열을 x 좌표로 잘랐는데 **자릿수가 많은 값이 왼쪽으로 삐져나온다**(오른쪽 정렬) — 첫 `0`이 잘려 `0003`이 됐다 | x 경계를 버리고 **줄의 마지막 숫자 토큰**을 취한다 |
| P3 | 비닐 5종·천창개폐 11에 **여분 값 `5.0`·`8.0`** | 규격(`지게차 / 5TON`)이 줄바꿈으로 쪼개져 `5TON` 조각이 생겼다 | **단위 열(`인`·`hr`)이 있는 줄만** 수량으로 인정 |

→ 🔴 **세 번 다 엔진이 옳고 파서가 틀렸다.** 특히 **P2는 113차 육안 판독이 맞았음을
역으로 확인**해 준다 — 사람이 `0.0003`을 정확히 읽었고 기계가 잘못 잘랐다.

⚠️ **이 사실이 기계 대조의 가치를 깎지는 않는다.** 파서 결함은 **대조 실패로 드러났고**
원문 재확인으로 즉시 판별됐다. 반대로 육안 판독의 오류는 **다시 볼 때까지 드러나지
않는다** — 116차 `가월리`(117차가 정정)·116차 `2,033`(122차가 정정)이 그랬다.

---

## 4. 상시화

| | |
|---|---|
| 모듈 | `pumsem_extract.py` — `extract_all()`이 `(공종, 순번, 계수 시퀀스)` 64건을 돌려준다 |
| 테스트 | `test_125cha_pumsem_64_reextracted_from_pdf_textlayer` (3파일 게이트에 포함) |
| 소요 | 첫 실행 **13.5초**(시스템 폰트 인덱스 구축) · 캐시 후 **1.8초** |
| skip 조건 | `pdfplumber`·`fontTools`·`pypdf` 부재 또는 시스템 Batang/Gulim 부재 |
| 덤프 | `pumsem_extract_dump.txt` — 원문에서 뽑은 값의 스냅샷 |

⚠️ **이 환경은 pip 패키지가 세션 간 유실된다**(CLAUDE.md). skip되면 **대조가 조용히
사라지므로** 게이트 실행 시 **skip 수를 반드시 확인**해야 한다.

**변이 4종**으로 실효성을 확인했다 — 엔진 계수 1개 변조 · 소수 4자리 변조 · 공종 쪽 범위
변조 · 단위 열 필터 제거. **4종 모두 검출**.

---

## 5. 한계

- 적용 범위는 **인쇄 p.138~162**(제7장 제2·3절)뿐이다. **부록1 공사일보(스캔)와 인쇄
  p.109~137(Type3 벡터 글리프)에는 이 경로가 통하지 않는다** — 116~124차가 그 구간에서
  읽은 수치는 **여전히 육안 판독 근거**다.
- 대조는 **계수 시퀀스**로 한다. **품목명은 대조하지 않는다**(한글이 일부만 복원된다) —
  품목명 정확성은 113차 육안 대조에 의존한다.
- 시퀀스 순서는 `dict` 삽입 순서에 의존한다. 엔진에서 직종 순서를 바꾸면 이 테스트가
  실패하는데, **그것이 원문과 어긋난다는 뜻은 아니다**(원문도 같은 순서일 때만 의미가 있다).
- 시스템 폰트 버전이 다른 기계에서는 매핑 수가 달라질 수 있다. **숫자 글리프가 복원되지
  않으면 추출 자체가 실패**하므로 조용히 틀리지는 않는다.
- 엔진 상수 값은 **하나도 바꾸지 않았다.**
