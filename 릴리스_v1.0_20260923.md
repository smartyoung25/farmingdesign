# v1.0 — 범위 동결 (2026-09-23 · 207차)

이 문서는 **v1.0이 무엇을 보증하고 무엇을 보증하지 않는지**를 적는다.
207차 시점의 리포를 동결 기준으로 삼고, 남은 것은 아래 **백로그**로 넘긴다.

> 🔴 **v1.0이 보증하는 것은 「이 명령으로 이 산출물이 나오고 이 회귀가 통과한다」까지다.**
> **값의 옳음은 보증하지 않는다** — 회귀 기준인 원채원 케이스조차 **원문이 리포에 없다**.
> 그 한계는 §4에 세어 두었다.

---

## 1. 실측 (207차, 전부 세어서 적었다)

| | 값 |
|---|---|
| 엔진 공개 함수 · 데이터클래스 | **74** · **30** (`smartfarm_engine.py` 6,785줄 · 533KB) |
| 레지스트리 상수 · `source_refs` | **70** · **182** |
| 케이스 | **5건**(4축 **3** · 부분 **2**) + 설계된 tombstone 1 |
| 산출물 HTML | **21건**(`index.html` 포함) |
| 근거 문서 | **75건** + 지도 1(`근거지도_20260923.md`) |
| 회귀 | 3파일 **366 passed** · 5파일 **424 passed**(skip 0) |
| 추적성 감사 | **PASS**(상수 70 · refs 182) · 추적성 사각 **2** |
| 커밋 | **212** (31~206차) |

## 2. v1.0이 하는 일

- **입지·설계·운영·경제성 4축**을 계산하고 **그 수가 어디서 왔는지**를 함께 낸다.
- 케이스 1건 → **통합보고서 · 컨설팅 패키지(D1~D24) · 판정 부록(D25~D27) · 4축 리포트**.
- **주입되지 않은 것은 「무엇이 없는지」로** 나온다 — 자료 요청서가 곧 산출물이다.
- **웹 콘솔**로 새 케이스를 기입→미리보기→저장→산출물까지 끌 수 있다(206차에 관통 확인).
- `python build_site.py`·`python audit_traceability.py`는 **외부 패키지 없이** 돈다.

## 3. v1.0이 **하지 않는** 일 (1절 불변 원칙)

- **작목·부지·업체를 고르지 않는다.**
- **시세를 조회하지 않는다** — 단가·노임·유가·금리는 **주입만** 받는다.
- **근거 없는 수를 만들지 않는다** — `[추정]`·`[확인요망]` 표기와 출처 병기.
- **판정·추천을 자동화하지 않는다.** 단 **규칙 적용형 등급·판정**은 네 조건
  (레지스트리 등재 · 결정론 · 항목별 근거 행 · 임계값 주입 가능)을 모두 지킬 때만 예외이고,
  그 산출물은 **본문이 아니라 「판정 부록」**에 따로 실린다.
- **투자·보험·시공 판정은 하지 않는다.** 최종 판단은 **사람과 계약**이 한다.

## 4. 동결 시점에 **열려 있는 것** (백로그)

닫지 않고 **세어서** 넘긴다. 닫힌 척하는 것이 가장 나쁘다.

| 갈래 | 수 | 어디에 |
|---|---|---|
| **★사용자 결정 대기** | **17** | `근거_결정대기대장_20260915.md` |
| **확인요망**(자료가 와야 답한다) | **8** | `근거_확인요망대장_20260915.md` |
| 자기 점검에서 **열린 채 관리** | **1**(3위 — 가드의 실측 추종) | `근거_자기점검대장_20260922.md` |
| 추적성 사각 | **2**(`SPEC_COUNT`·`SPEC_TABLE` — 원문이 리포에 없다) | 감사 리포트 |
| 상수 중 `추정`·`확인요망` | **5** · **3** | 레지스트리 · 근거대장 |

🔴 **가장 큰 구멍 셋**

1. **원채원 원문이 리포에 없다** — 회귀 기준(ROI 14.2% · Payback 7.1년)의 **원문이 없다**.
2. **재무 가정 세 칸**(할인율 5% · 평가기간 10년 · 내용연수 15년)은 **주입되지 않은
   엔진 기본값**이다. 자리와 입구는 열려 있고 산출물이 그 사실을 표기한다.
3. **`FR_TABLE` 4값**이 원문 범위 밖이다 — H4 검측의 합격 기준이 **근거 없이 서 있다**.

## 5. 어떻게 확인했는가

```bash
python build_site.py          # 산출물 21건 — "생성 완료" 줄 확인
python audit_traceability.py  # PASS
python verify_refs.py
python -m pytest test_engine.py test_registry.py test_cases.py -q                       # 366
python -m pytest test_engine.py test_registry.py test_cases.py test_chunking_v2.py test_webapp.py -q   # 424
```

- **게이트 순서**: `build_site.py` → `pytest`(산출물을 읽는 가드가 있다).
- **매 차수 뮤테이션**: 가드가 *헛도는지*를 잰다 — 201차부터 **기계가 요구**한다
  (갱신 이력 최신 5차수에 `N/N` 기록이 없으면 실패).

## 6. v1.0 이후에 손대려면

- **엔진 상수 값**을 바꾸려면 **원문 확보·대조가 선행**이고, 회귀 벤치마크를 함께 본다.
- **차수 기록**은 `차수로그.md` 맨 위에, **착수점**은 `작업지시서.md` 머리 한 줄.
- 새 근거 문서를 쓰면 **`근거지도_20260923.md`에 올려야** 가드가 통과한다.
- 새 테스트는 **줄여도 되지만 이유를 적어야** 한다(200차 — 테스트 수는 **하한**이다).

---

⚠️ **이 문서도 썩는다.** 가드가 §1의 수치를 **실측과 대조**하므로, 리포가 바뀌면
여기 수치도 함께 고쳐야 통과한다.
