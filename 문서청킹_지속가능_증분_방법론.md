# 스마트팜 스펙 문서 청킹 — 지속가능(증분) 방법론 구조화

작성일: 2026-07-24 · 작성 근거(먼저읽기): `00_인수인계서_먼저읽기.md`, `작업체계_계획_비판검토.md`, `문서청킹구조_설계공사비시방서.md`(1차 설계+파일럿 결과), `chunking_lib.py`, `build_document_chunks_full.py`, `엔진데이터_레지스트리.json`
상위 원칙: `작업지시서.md` 1절(엔진 단일출처 · 근거 명시 · 자동화 경계) · 관련: `설계상세_입지매핑표_OPEX분해_확정.md`

> **이 문서의 성격**: 새 파이프라인을 지금 실행하는 문서가 아니라, "스마트팜 구축·운영을 위한 설계 문서를 **끊기지 않고 계속 청킹해 나갈 수 있는 방법**"을 구조로 못 박는 설계문서다. 산출물 형태는 사용자 승인(2026-07-24)에 따라 **방법론 설계문서(.md)**이며, 코드는 실행이 아니라 **설계(스키마·의사코드·절차)** 수준까지만 규정한다.

---

## 0. 전제 — 바꾸지 않는 것 (기존 원칙 재확인)

재구조화를 하더라도 이 프로젝트가 지켜온 4가지는 그대로 상속한다. 이걸 흔들면 청킹이 "제2의 계산 출처"로 변질돼 근본문제(숫자 신뢰성)를 되살린다.

1. **엔진이 유일한 계산 출처.** 이 청킹 인덱스는 `smartfarm_engine.py` 상수의 **원본 근거를 찾고 인용하기 쉽게 만드는 검색·정리 계층**일 뿐이다. 청크에서 뽑은 값을 엔진 상수로 승격하려면 기존 절차(실측 확인 → 레지스트리 등록 → 드리프트 가드 테스트)를 그대로 거친다. 인덱스가 계산에 직접 쓰이거나 앱이 실시간 조회하지 않는다.
2. **근거 없는 값 금지.** 파싱·추출이 실패하면 값을 추정해 채우지 않고 "건너뜀 + 사유"로 명시한다(예: OCR 불가 스캔본, .hwp/.doc 미지원, 레거시 .xls 파싱 실패).
3. **자동화 경계 = 에피스테믹 3분류.** 결정론(자동화 O) / 시세성(주입만) / 판단성(초안까지). 청킹 자동태깅은 **결정론적 키워드 매칭까지만** 하고, 애매하면 `미분류`로 남긴다. AI가 문서를 읽고 확신 없는 축을 추정해 채우지 않는다.
4. **상태의 외부화.** 컨텍스트에 다 담지 않고 durable artifact(인덱스 파일·매니페스트·요약)로 밀어낸다. 각 세션은 필요한 조각만 로드한다.

---

## 1. 왜 재구조화가 필요한가 — 파일럿의 두 한계

`문서청킹구조_설계공사비시방서.md` 10절에 기록된 파일럿·2차 실행 결과가 드러낸 문제는 두 갈래다.

### 1-1. 구조적 한계 — 행 단위 청킹의 미분류 48.8%
2차 실행에서 공종 헤더 carry-forward·키워드 정교화·노이즈 필터까지 넣고도 미분류가 48.8%였다. 원인은 파이프라인 버그가 아니라 **청킹 단위 자체**다. 우민재 FIRMMIT식 내역서는 "자재 177호 | 체인커플링 | 32A | 개 | 55000 | …"처럼 **개별 부품명 하나가 한 행 전체**라, 그 행만 보면 11개 축 어디에도 의미상 안 걸린다. 억지로 걸면 축이 무의미해진다. → **행이 아니라 공종 그룹을 청크 단위로 올려야** 맥락이 산다(6절 규칙 재설계 필요, 파일럿 범위를 넘어선 미결 항목이었음).

### 1-2. 운영적 한계 — "1회성 배치"라 지속이 불가능
현재 `build_document_chunks_full.py`는 매 실행마다 `스마트팜스펙/`·`시설평가/` **전체를 처음부터 다시 훑어** 인덱스를 통째로 덮어쓴다. 그래서:

- 새 사례 폴더 1개가 들어와도 전체를 재청킹해야 하고(수백~수천 청크 재생성),
- 이전에 사람이 손으로 채운 `미분류` 보정이나 축 승인 결과가 **덮어쓰기로 날아가며**,
- 어디까지 처리됐는지·무엇이 새로 들어왔는지 추적할 상태가 없다.

즉 "**청킹을 지속적으로 진행**"하려면 청킹 단위 재설계(1-1)와 **증분·재개 가능한 처리 상태 기계(1-2)** 두 가지를 동시에 세워야 한다. 이 문서의 핵심은 후자다.

---

## 2. 목표와 성공기준

| 속성 | 정의 | 측정 |
|---|---|---|
| **지속성(Continuity)** | 새 문서가 폴더에 추가되면 전체 재실행 없이 그 문서만 추가 청킹 | 재실행 시 처리 파일 수 = 신규/변경분에 한정 |
| **증분성(Incremental)** | 무변경 파일은 건너뛰고, 변경 파일만 재청킹, 삭제 파일은 tombstone 처리 | 매니페스트 diff 4상태(신규/변경/삭제/무변경) 집계 |
| **멱등성(Idempotency)** | 같은 입력으로 두 번 돌려도 인덱스가 동일 | `chunk_id`·`content_hash` 안정, 중복 append 없음 |
| **재현성·추적성** | 모든 청크가 (출처파일, 페이지/조항, 지문, 처리 run_id)로 역추적 | 스키마 v2 필수필드 100% 채움 |
| **보존성(Non-destructive)** | 사람이 손보정한 태그·축 승인은 재실행에도 보존 | 오버레이(수동 보정) 파일 분리 병합 |
| **커버리지 가시성** | 8개 설계도메인별로 얼마나 채워졌는지·엔진 근거가 비었는지 리포트 | 도메인 커버리지 표(8절) |

---

## 3. 이중 축 체계 — Layer A(농업/환경 11축) + Layer B(설계도메인 8축) 병렬 태깅

사용자 승인(2026-07-24): **기존 11축은 유지**하고, 이번에 제시된 **8개 설계도메인 축을 제2 분류 레이어로 추가**한다. 청크 1개는 두 레이어에 **동시에** 태깅될 수 있다(예: "보온커튼 설치 단가" 행 → Layer A: `냉난방`,`비용/공정` + Layer B: `시설구축`).

두 레이어는 **관점이 다르다**:
- **Layer A(농업/환경)** = "이 내용이 *무엇에 관한 것*인가"(냉난방·광량·생육…). 기존 파일럿 자산·`engine_link` 매핑을 그대로 승계.
- **Layer B(설계도메인)** = "스마트팜을 *짓고 돌리는 라이프사이클의 어느 단계*인가"(부지선정→구축→운영→장애대응). 이번 요청의 8개 도메인.

### 3-1. Layer B 8도메인 정의 · 라이프사이클 위치 · 결정론성

| # | 도메인(Layer B) | 라이프사이클 | 핵심 내용 | 에피스테믹 유형 |
|---|---|---|---|---|
| B1 | **부지선정** | 사전 | 지반·진입로·구획·경사·배수·필지, 지역별 적설/풍속 등 입지 적합성 | 결정론(입지 하중)+판단성(적합도) |
| B2 | **시설구축** | 구축 | 골조·기초·피복·구조계산·공사비내역·수량·공정 (온실 하드웨어 건설) | 결정론(규격/하중)+시세성(단가) |
| B3 | **기자재도입** | 구축/도입 | ICT 장치·센서·양액기·냉난방기·품셈 기반 기자재 사양·수량 | 결정론(품셈)+시세성(단가) |
| B4 | **전기** | 인프라 | 수전·분전반·배선·용량 산정·접지·비상전원 | 결정론(용량)+판단성(설계) |
| B5 | **통신** | 인프라 | 네트워크·게이트웨이·유무선·프로토콜(Modbus/RS-485 등)·원격접속 | 판단성(구성) |
| B6 | **구동** | 인프라/운영 | 개폐기·모터·펌프·밸브·구동부(천창/측창/보온커튼/차광)·액추에이터 제어 | 결정론(구동사양)+판단성(제어로직) |
| B7 | **데이터활용** | 운영 | 계측·수집·로깅·대시보드·제어이력·분석·의사결정 활용 | 판단성 |
| B8 | **장애대응** | 유지/운영 | 하자·고장·경보·페일세이프·백업·유지관리·내구연한·보증 | 판단성+법정기준(보증) |

### 3-2. Layer B 도메인별 키워드 사전(1차, 결정론 매칭용) · engine_link 기대

키워드는 **문서유형별로 다르게** 적용할 수 있게 설계한다(파일럿 교훈 2: "배수"가 기계설비명과 충돌한 것처럼, 넓은 단일어는 복합어로 좁힌다).

| 도메인 | 1차 키워드(정규식 초안, 복합어 우선) | engine_link(현행 상수) | 근거상태 기대 |
|---|---|---|---|
| B1 부지선정 | `부지\|지반\|진입로\|구획\|성토\|절토\|경사\|필지\|배수계획\|배수로\|용배수\|입지` | `REGION_DESIGN_LOAD`(부분) | 실측/판단 혼재 |
| B2 시설구축 | `골조\|기초\|피복\|트러스\|파이프\|구조계산\|하중\|공사비\|내역서\|수량산출\|공정표\|KDS` | `SPEC_TABLE`,`REGION_DESIGN_LOAD`,`CAPEX_MAJOR_CATEGORIES` | 실측 다수 |
| B3 기자재도입 | `기자재\|ICT\|센서\|양액기\|관수\|제어기\|품셈\|장치\|설비\|환기팬\|유동팬` | `PUMSEM_ITEMS`,`EQUIPMENT_DB_META` | 부분/실측 |
| B4 전기 | `수전\|분전반\|배선\|전력\|kW\|접지\|차단기\|누전\|비상전원\|전기설비\|인입` | **null(엔진 미모델링)** | 공백 — 확보 필요 |
| B5 통신 | `통신\|네트워크\|게이트웨이\|공유기\|RS-?485\|Modbus\|LoRa\|무선\|유선\|프로토콜\|원격` | **null** | 공백 |
| B6 구동 | `개폐기\|모터\|감속기\|액추에이터\|천창\|측창\|보온커튼\|차광\|권취\|구동부\|밸브\|펌프` | **null(부분: EQUIPMENT_DB에 일부)** | 공백/부분 |
| B7 데이터활용 | `계측\|수집\|로깅\|데이터\|대시보드\|모니터링\|이력\|분석\|클라우드\|API` | **null** | 공백 |
| B8 장애대응 | `하자\|고장\|경보\|알람\|페일세이프\|백업\|유지관리\|내구연한\|보증기간\|A/S\|점검` | **null(보증은 법정기준 후보)** | 공백/법정 |

> **중요 발견(8절에서 재론)**: B4~B8(전기·통신·구동·데이터·장애대응) 5개 도메인은 **현재 엔진에 대응 상수가 사실상 없다.** 이건 청킹의 실패가 아니라, 이 5개 도메인이 지금까지 엔진 스코프(온실 하드웨어·경제성 중심) 밖이었다는 **커버리지 갭의 증거**다. 청킹은 이 갭을 숨기지 말고 "engine_link=null, 근거상태=공백"으로 드러내 다음 확장의 착수점을 만든다.

### 3-3. Layer A ↔ Layer B 교차 매핑(중복 태깅 가이드)

| Layer A(11축) | 주로 함께 걸리는 Layer B 도메인 |
|---|---|
| 부지 | B1 부지선정 |
| 기후 | B1 부지선정, B2 시설구축(하중조건) |
| 작물특성 | (Layer B 약함 — 재배계획 문서. 매핑 없으면 미매핑 유지) |
| 냉난방 | B3 기자재도입, B4 전기(열원 전력), B6 구동(커튼) |
| 광량 | B3 기자재도입(보광), B6 구동(차광) |
| CO2 | B3 기자재도입, B7 데이터활용(농도 계측) |
| 생육관리 | B7 데이터활용, B3 기자재도입(양액) |
| 운영 | B7 데이터활용, B4 전기(전기요금), B8 장애대응 |
| 유지보수 | B8 장애대응 |
| 구조/자재 | B2 시설구축 |
| 비용/공정 | B2 시설구축, B3 기자재도입 |

이 표는 자동태깅을 강제하는 규칙이 아니라 **교차검증용 참고표**다(둘 다 결정론 키워드로 독립 태깅하되, A와 B가 서로 모순되면 표본 검수 대상으로 플래그).

---

## 4. 청킹 단위 재설계 — 그룹 단위 도입(파일럿 한계 1-1 해소)

파일럿의 doc_type별 청킹 단위(설계문서 6절)를 유지하되, **표형 문서(내역서·설계예산서·수량산출서)에 "그룹 단위" 계층을 추가**한다.

### 4-1. 3층 청크 계층 (표형 문서)
| 레벨 | 단위 | 목적 | 태깅 기준 |
|---|---|---|---|
| L0 문서 | 파일 1개 | 지문·매니페스트 추적 | (태깅 안 함, 메타만) |
| **L1 그룹(신설)** | **공종/절 1묶음**(헤더 + 하위 행 전체) | **주제축 태깅의 1차 단위** | 그룹 텍스트 합본으로 Layer A/B 태깅 |
| L2 행 | 내역 행 1건 | 수치 역추적(단가·수량) | 태깅 안 함, L1 그룹에 소속(`parent_group_id`) |

**핵심 변경**: 태깅은 L1(그룹)에서 한다. 개별 부품 행(L2)은 그룹에 매달아 두되 축 태깅을 시도하지 않는다 → 48.8% 미분류의 주원인(부품명 단일행) 자연 소멸. 수치가 필요할 땐 `parent_group_id`로 L2 행을 역참조한다.

**그룹 경계 판정**: 기존 `is_header_row()`/`SECTION_HEADER_PATTERN`(예: `1-1. 기초공사`)을 **그룹 시작 신호**로 재사용. 헤더가 없는 순수 부품 나열형(코드번호가 헤더 역할)은 "**연속된 무헤더 행 구간**"을 1그룹으로 묶고 `doc_subtype=코드나열` 표시(태깅은 시도하되 대부분 미분류로 남는 게 정상 — 억지 태깅 금지).

### 4-2. doc_type별 최종 청킹 단위 (v2)
| doc_type | v1 단위 | v2 단위(변경점) |
|---|---|---|
| 설계도면/공사비도면 | 도면 1매 | 유지(페이지 색인) |
| 공사비내역서/설계예산서/수량산출서 | 행 1건 | **그룹(L1) 태깅 + 행(L2) 역참조** ← 변경 |
| 구조계산서 | 계산 절 1개 | 유지 + 노이즈필터 강화 |
| 시방서 | 조항 1개 | 유지(전기/통신/구동 조항은 Layer B로 새로 걸림) |
| 공사공정표 | 공종 1행 | 유지 |
| 검토서 | 섹션 1개 | (전면확장에서 구조계산서로 흡수됨 — 실제 열어보니 구조계산서 템플릿) |
| 기자재현황/품셈/가이드라인 | 품목/조항 1개 | 유지(B3~B8 도메인 주요 공급원) |

---

## 5. 청크 메타데이터 스키마 v2

v1(설계문서 6절) + `chunking_lib.py`의 실제 필드에 **Layer B·그룹·지문·버전·수명** 필드를 추가한다. 기존 필드는 이름 그대로 보존(하위호환).

```json
{
  "chunk_id": "우민재_공사비내역서_그룹_기초공사_p2t1g3",   // 안정적·재현적(멱등성 키)
  "chunk_schema_version": "2.0",
  "source_file": "스마트팜스펙/우민재/1. 공사내역서...xlsx",
  "source_sha256": "…",            // 신규: 파일 지문(증분 diff 핵심)
  "source_mtime": 1784242583682,    // 신규: 보조 지문
  "case_name": "우민재",
  "doc_type": "공사비내역서",
  "doc_subtype": "기초공사 | 코드나열 | 냉난방설비 …",
  "chunk_level": "group",           // 신규: group | row | page | section | item
  "parent_group_id": null,          // 신규: L2 행이 소속 그룹을 가리킴
  "group_member_rows": ["p2t1r4","p2t1r5"],  // 신규: 그룹이 품은 행들
  "page_or_section": "p2-t1-g3",
  "section_context": "1-1. 기초공사",       // v1 유지(헤더 carry-forward)
  "topic_tags_A": ["구조/자재","비용/공정"], // Layer A(기존 11축, 필드명만 명확화)
  "domain_tags_B": ["시설구축"],             // 신규: Layer B(설계도메인 8축)
  "content_summary": "…(1~2문장, 원문 과다인용 금지)",
  "content_hash": "…",              // 신규: 내용 지문(멱등 병합·변경감지)
  "text_len": 812,
  "raw_value_extracted": {"항목":"값","단위":"…"},   // v1 유지(선택)
  "evidence_status": "실측",         // 어휘는 5-1에서 레지스트리와 통일
  "extraction_quality": "table_extract | xlsx_row | ocr_noisy | noise_suspected | line_fallback | skipped_duplicate",
  "engine_link": "CAPEX_MAJOR_CATEGORIES | SPEC_TABLE | REGION_DESIGN_LOAD | PUMSEM_ITEMS | EQUIPMENT_DB_META | null",
  "duplicate_of": null,
  "first_seen_run": "2026-07-24T01",  // 신규: 처음 등장한 run
  "last_seen_run": "2026-07-24T01",   // 신규: 마지막으로 확인된 run(삭제감지용)
  "manual_overlay": false             // 신규: 사람이 손보정했는지(재실행 보존 대상)
}
```

### 5-1. evidence_status 어휘 통일 (드리프트 방지)
파일럿 설계문서는 `실측/법정기준/참고자료/확인요망`을, 실제 `엔진데이터_레지스트리.json`의 `status_legend`는 `실측/부분/미검증`을 쓴다 — **두 어휘가 갈라져 있다.** 합칠 때 혼란을 막기 위해 **레지스트리 어휘를 정본으로 채택**하고 확장한다:

| evidence_status | 정의 | 출처 |
|---|---|---|
| `실측` | 출처 확인된 실측/공인값 | 레지스트리 legend |
| `부분` | 대표/일부만 수록(전체 원본 필요) | 레지스트리 legend |
| `미검증` | 값은 있으나 근거문서 미확보 | 레지스트리 legend |
| `법정기준` | 법·고시·표준(KDS 등) 근거 | 확장(구조계산·보증) |
| `확인요망` | 자동추출 신뢰 낮음, 사람 검수 필요 | 확장(OCR 저품질·노이즈) |

---

## 6. 지속(증분) 처리 메커니즘 — 이 문서의 핵심

"1회 배치"를 "언제든 다시 돌려도 신규·변경분만 처리하고 나머지는 보존"하는 상태 기계로 바꾼다.

### 6-1. 매니페스트(상태 레지스트리) 도입
인덱스(`*.jsonl`)와 **별도로** 파일 단위 처리상태를 기록하는 매니페스트를 둔다: `청킹_매니페스트.json`.

```json
{
  "manifest_version": "1.0",
  "last_run": "2026-07-24T01",
  "files": {
    "스마트팜스펙/우민재/1. 공사내역서...xlsx": {
      "sha256": "…", "mtime": 1784242583682, "size": 726357,
      "status": "processed",           // processed | skipped | error | tombstoned
      "doc_type": "공사비내역서", "case_name": "우민재",
      "chunk_count": 138, "first_seen_run": "…", "last_seen_run": "…",
      "skip_reason": null
    }
  }
}
```

### 6-2. 재실행 시 4상태 diff (파일 지문 기준)
매 실행에서 폴더를 스캔하고 **각 파일의 `sha256`(+보조 mtime/size)** 를 매니페스트와 대조해 4가지로 분기한다:

| 상태 | 판정 | 처리 |
|---|---|---|
| **무변경(unchanged)** | 경로·sha256 동일 | **건너뜀**(재청킹 X). 해당 청크 `last_seen_run`만 갱신 |
| **신규(new)** | 경로가 매니페스트에 없음 | 청킹 → 인덱스 append, 매니페스트 등록 |
| **변경(changed)** | 경로 동일·sha256 다름 | 기존 그 파일 유래 청크 **무효화 후 재청킹**(경로 기준 교체), 매니페스트 갱신 |
| **삭제(deleted)** | 매니페스트엔 있으나 폴더에 없음 | 청크를 지우지 않고 `tombstoned` 표시(추적성 보존) + 요약에 보고 |

핵심: **덮어쓰기(truncate) 금지.** 인덱스는 append/merge로만 자란다. 무변경 파일은 손대지 않으므로 사람이 채운 `manual_overlay=true` 청크가 보존된다.

### 6-3. 멱등성 보장
- `chunk_id`는 (case, doc_type, doc_subtype, page/group)로 **결정론적**으로 생성 → 같은 파일 재청킹 시 같은 id.
- 병합 시 `chunk_id` 충돌하면 `content_hash` 비교: 같으면 skip, 다르면(=파일 변경) 교체.
- 따라서 "같은 입력 두 번 실행 = 동일 인덱스"가 성립.

### 6-4. 수동 보정 오버레이 분리
사람이 `미분류`를 손으로 채우거나 태그를 고친 결과는 **원본 인덱스에 직접 쓰지 않고** `청킹_오버레이.jsonl`(chunk_id → 보정필드)에 따로 쌓는다. 최종 뷰 = 자동 인덱스 ⊕ 오버레이(오버레이 우선). 재실행이 자동 인덱스를 갱신해도 오버레이는 불변 → **손보정 영구 보존**.

### 6-5. 재개(resume)와 부분 실패 격리
- 파일 단위로 처리하고 **한 파일 처리 후 매니페스트를 즉시 커밋**(체크포인트) → 중단돼도 다음 실행이 `processed`부터 건너뛰고 이어감.
- 한 파일 예외는 그 파일만 `error`+사유로 격리하고 전체를 멈추지 않는다(기존 `W.skip` 철학 계승).

### 6-6. 실행 버전 태깅
각 실행은 `run_id`(예: `2026-07-24T01`)를 부여받고 모든 신규/변경 청크에 `first_seen_run`/`last_seen_run`으로 새긴다 → "이 청크가 언제 들어왔나 / 아직 살아있나"를 감사 가능.

---

## 7. 파이프라인 절차 (v2, 증분)

```
[0] 스캔      스마트팜스펙/·시설평가/ 워크 → 파일 목록 + 지문(sha256/mtime/size)
[1] diff      매니페스트 대조 → {신규, 변경, 삭제, 무변경} 4버킷
[2] 분류      신규·변경 파일만: 파일명 규칙으로 doc_type·case 자동 태깅(guess_doc_type/classify 재사용)
              └ 제외 규칙 그대로: .hwp/.doc/이미지(OCR無), 노지·대산온실 스코프제외, 락파일, MD5 완전중복
[3] 추출      pdf→pdfplumber(표/페이지), xlsx→openpyxl, xls→xlrd, docx→python-docx
              └ 스캔본은 _OCR 형제파일 자동대체, 없으면 skip(추정 금지)
[4] 청킹      doc_type별 단위(4절): 표형은 그룹(L1)+행(L2), 도면=페이지, 시방=조항, 구조=절
[5] 태깅      Layer A(11축)·Layer B(8도메인) 병렬 결정론 키워드 매칭 → 미매칭은 미분류 유지
              └ 노이즈 필터(NOISE_PATTERN) → noise_suspected 격리
[6] 병합      append/merge(멱등): 무변경 skip, 변경 교체, 삭제 tombstone. 오버레이는 건드리지 않음
[7] 커밋      매니페스트 갱신(파일별 체크포인트) + 인덱스 flush
[8] 리포트    요약: diff 집계, doc_type/case/quality 분포, Layer A·B 태그 분포,
              8도메인 커버리지·engine_link 공백, 미분류율, 신규 대비 처리율
```

기존 `chunking_lib.py` 함수(`chunk_pdf_table_or_lines`, `chunk_xlsx`, `tag_topics`, `engine_link_for`)는 대부분 재사용한다. 신설이 필요한 것만 정리하면:

| 신설/변경 | 내용 |
|---|---|
| `manifest.py`(신설) | 지문 계산·4상태 diff·체크포인트 커밋 |
| `tag_domains_B()`(신설) | Layer B 8도메인 키워드 매칭(3-2 사전) |
| 그룹 청킹(변경) | 표형에서 L1 그룹 청크 생성 + L2 행에 `parent_group_id` 부여 |
| `overlay.py`(신설) | 오버레이 병합(자동 인덱스 ⊕ 손보정) |
| `write_outputs`(확장) | Layer B 분포·도메인 커버리지·engine_link 공백 리포트 추가 |

---

## 8. 새 도메인의 근거 공백 리포트 (B4~B8)

증분 파이프라인이 매 실행에서 반드시 출력해야 하는 **커버리지 표**. 목적은 "전기·통신·구동·데이터·장애대응 설계 근거가 지금 스펙 말뭉치에 얼마나 있고, 엔진엔 왜 비어있나"를 가시화하는 것.

| 도메인(B) | 청크 수(예상 초기) | engine_link | 해석·다음 액션 |
|---|---|---|---|
| B1 부지선정 | 중 | REGION_DESIGN_LOAD(부분) | 검토서·구조계산서에서 확보 가능 |
| B2 시설구축 | 다 | SPEC/CAPEX(실측) | 이미 강함 — 그룹청킹으로 정밀화 |
| B3 기자재도입 | 다 | PUMSEM/EQUIPMENT | 품셈·기자재현황이 공급원 |
| **B4 전기** | **소~공백** | **null** | 시방서 전기공 조항·내역서 전기공종에서만 확보. 엔진 미모델링 → 확장 후보 |
| **B5 통신** | **공백 유력** | **null** | ICT 가이드·시방서에 산발. 표준 부재 → 판단성, 자동화 경계 밖 |
| **B6 구동** | **소** | **null(부분)** | 기자재/구동부 사양에 존재. EQUIPMENT_DB 일부 → 승격 검토 |
| **B7 데이터활용** | **공백 유력** | **null** | 운영 단계 문서가 말뭉치에 거의 없음 → 원본 자료 확보 필요 |
| **B8 장애대응** | **소** | **null(보증=법정 후보)** | 시방서 보증·하자, 유지관리 조항. 보증기간은 법정기준 태깅 |

> 이 표 자체가 "다음에 어떤 원본자료를 더 확보해야 하는가"의 지도가 된다. **근거가 비어 있으면 비어 있다고 리포트할 뿐, AI가 값을 지어내 채우지 않는다**(원칙 2·3).

---

## 9. 품질 게이트 · 지속 운영 루틴

### 9-1. 매 실행 품질 게이트(요약에 자동 출력)
- 미분류율(Layer A / Layer B 각각): **그룹청킹 후 목표 = 표형 문서 미분류율 30% 이하**(파일럿 48.8% 대비). 초과 시 원인이 "문서 한계"인지 "키워드 부족"인지 표본 검수 플래그.
- 도메인 커버리지(8절 표)와 engine_link 공백 목록.
- diff 집계(신규/변경/삭제/무변경)와 error·skip 목록(사유 포함).
- 키워드 오탐 감시: Layer A `부지` vs 기계설비명 충돌 같은 알려진 오탐 패턴 카운트.

### 9-2. 언제 다시 돌리나 ("지속적으로 진행"의 운영 정의)
청킹은 **이벤트 기반 증분 재실행**으로 지속한다:
1. `스마트팜스펙/`·`시설평가/`에 **새 사례/자료 폴더가 추가될 때**(가장 흔한 트리거).
2. OCR·품셈 등 **추출 도구가 새로 생겨** 기존 skip 파일을 재시도할 수 있을 때.
3. **Layer B 키워드 사전을 개정**했을 때(태깅만 재수행하는 "재태깅 전용" 모드 — 추출·청킹은 캐시 재사용).
4. 정기 점검(예: 월 1회) — diff가 0이면 즉시 종료(무변경이면 비용 0).

트리거 3은 중요하다: 키워드를 고쳤을 때 전체 재추출은 낭비이므로, **추출 결과(L1/L2 텍스트)는 그대로 두고 태그 필드만 다시 계산**하는 경로를 파이프라인에 별도로 둔다(`--retag-only`).

### 9-3. 자동화 경계(재확인)
- 결정론 키워드 매칭·지문 diff·그룹 경계 판정 = **자동화 O**.
- 애매한 축 귀속, B5·B7 같은 판단성 도메인의 의미 해석 = **미분류로 남기고 사람이 오버레이로 보정**.
- 새 도메인 값의 엔진 상수 승격 = **기존 실측 절차(레지스트리+드리프트 테스트) 필수**, 청킹이 대신하지 않음.

---

## 10. 실행 로드맵 · 미해결

### 10-1. 로드맵(승인 후 착수 순서 제안)
1. **P0 — 매니페스트+증분 골격**(`manifest.py`, 4상태 diff, append/merge, 오버레이 분리). 기존 전체배치를 이 골격 위로 이식. → "지속성" 확보.
2. **P1 — 그룹청킹(L1/L2)**: 표형 3종에 그룹 단위 적용. 우민재(부품나열형)·한일그린텍(조항형) 두 대비 사례로 미분류율 개선 검증.
3. **P2 — Layer B 8도메인 태깅**: `tag_domains_B()` + 커버리지/engine_link 공백 리포트. B4~B8 공백을 가시화.
4. **P3 — 재태깅 전용 모드**(`--retag-only`)와 정기 트리거.
5. (엔진팀과 별개 트랙) B4~B8 근거자료 확보 → 필요 시 엔진 신규 모듈+테스트로 승격.

### 10-2. 미해결(사용자/다음 세션 확인 필요)
- [ ] 그룹청킹 미분류율 목표(30%)가 부품나열형에서도 현실적인지 — P1 검증 후 재조정.
- [x] ~~노지(관수/양액) 자료 스코프 포함 여부(설계문서 9절부터 계속 미결). B3·B6과 겹쳐 재부상.~~ → **2026-08-16 사용자 결정: 스코프에서 제외 확정.** `EXCLUDE_DIRS = {"노지견적","노지시방서","대산온실"}` 유지, 해당 35개 파일(9.0MB)은 LFS 다운로드도 하지 않는다(의도적 미판독). 근거: 온실 중심 엔진 스코프와 불일치. 파생 결론 — `기자재구매` CAPEX 공백은 말뭉치가 아니라 `기자재DB/`에서 조달.
- [ ] B7 데이터활용·B5 통신은 **운영 단계 원본자료 자체가 말뭉치에 거의 없음** — 자료 확보 트랙을 별도로 열지.
- [ ] 매니페스트·오버레이·인덱스 3파일 체계의 저장 위치·백업(Git 관리 범위) 확정.
- [ ] `.hwp`/`.doc`/레거시 `.xls`/스캔본 등 현재 skip군의 재처리 도구 도입 시점.

---

### 부록 A. 산출물 파일 구성(제안)
| 파일 | 역할 | 갱신 방식 |
|---|---|---|
| `문서청킹_인덱스_전체.jsonl` | 자동 청크(1행=1청크) | append/merge(증분) |
| `청킹_매니페스트.json` | 파일별 처리상태·지문 | 파일단위 체크포인트 |
| `청킹_오버레이.jsonl` | 사람 손보정(태그·미분류 채움) | 수동, 재실행 불변 |
| `문서청킹_전체_요약.txt` | 실행 리포트(diff·분포·커버리지·공백) | 매 실행 재생성 |
| `문서청킹_지속가능_증분_방법론.md` | (이 문서) 방법 정의 | 규칙 변경 시 개정 |

### 부록 B. 파일럿 대비 변경 요약(한눈에)
| 항목 | 파일럿(v1) | 이 방법론(v2) |
|---|---|---|
| 실행 방식 | 전체 배치·덮어쓰기 | 증분·append/merge·재개 |
| 상태추적 | 없음 | 매니페스트(지문 4상태 diff) |
| 축 체계 | 11축(농업/환경) | 11축 + 8도메인(설계) 병렬 |
| 표형 청킹 | 행 단위(미분류 48.8%) | 그룹(L1)+행(L2) |
| 손보정 | 재실행 시 유실 | 오버레이로 영구 보존 |
| 새 도메인 근거 | 미가시 | engine_link 공백 리포트 |
| evidence 어휘 | 문서/레지스트리 불일치 | 레지스트리 어휘로 통일 |
