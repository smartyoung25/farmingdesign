# 도면 6종 + 품셈 범위 문서에 대조 기준을 채웠다 — weak 7건 → 0건 (147차)

**작성일** 2026-09-16 (147차) · **계기** 146차 레드팀 27회차 [6]이 연 구멍
**성격** 엔진 상수 값 변경 0 · 레지스트리 **값** 변경 0(`source_refs` note 7건만 보강) · 결정 0

> 146차가 `verify_refs.weak_check()`를 신설해 **"기준이 차수 번호뿐인 ref 7건"**을 드러냈다.
> 그때는 *"원문 재독이 필요해 별도 차수"*로 미뤘다. 이 차수가 그 7건을 **원문을 열어** 닫는다.

---

## 0. 결과

| | 146차 | 147차 |
|---|---|---|
| `soft_check()` (기준 전무) | 0건 | 0건 |
| `weak_check()` (기준이 차수 번호뿐) | **7건** | **0건** |

7건 전부가 `strict` 모드에서도 **숫자·위치·인용 세 종류를 모두** 갖는다.

🔴 **부수 소득이 더 크다** — 도면이 등재 면적을 **정확히 재현한다**(§2).

---

## 1. 무엇이 문제였나

`93차 — 외곽 치수·관리동 위치` 같은 note는 **경위**만 적고 **그 문서에서 무엇을 보면
되는지**를 적지 않았다. `93차`의 숫자가 `_NUMA`에 걸려 "숫자 기준 있음"으로 통과했을 뿐이다.
**차수 번호로는 원문을 다시 찾아갈 수 없다.**

---

## 2. 🔴 도면이 등재 면적을 **정확히 재현한다**

평면도 2장에서 치수를 읽었다. 도면에 **면적은 인쇄돼 있지 않으므로 이것은 재집계**다.

| | 한수진 | 최선동 |
|---|---|---|
| 전장 | **93,000** | **85,000** |
| 3,000 피치 | **31스팬**(=93,000) | **28스팬**(=84,000) + 1,000 |
| 그리드 | Y1~**Y32** | Y1~**Y29** |
| 연동 폭 | **7,000 × 6 = 42,000** | **7,000 × 5 = 35,000** |
| 외곽 폭 | **44,000** (= 42,000 + 1,000×2) | **37,000** (= 35,000 + 1,000×2) |
| 재집계 면적 | 93 × 44 = **4,092㎡** | 85 × 37 = **3,145㎡** |
| 등재 `ACTUALS` | **4,092** | **3,145** |
| | ✅ **정확히 일치** | ✅ **정확히 일치** |

→ 등재 면적의 기준은 **전장 × 외곽폭**이다(연동폭이 아니다). 93차가 *"외곽 치수"*라 적은
것이 바로 이 뜻이었으나, **어느 치수인지**는 적혀 있지 않았다. 이제 적혀 있다.

⚠️ **등급은 `partial` 그대로 둔다.** 값의 출처는 여전히 **견적서 사업량 표기**이고, 도면 쪽
숫자는 **인쇄된 면적이 아니라 곱셈 결과**다(140차 `RECOMPUTED` 규율 — 재집계는 재집계 식을
병기하고 등급을 올리지 않는다). 등급 분포 **exact 90 / partial 50 / near 8**은 불변이다.

### 🔴 세로쓰기 치수는 뒤집혀 추출된다

`44,000`·`42,000`·`7,000`·`1,000`은 도면에서 **세로로 쓰여 있고**, `extract_text()`는 이를
**`000,44`처럼 뒤집어** 내놓는다. 자릿수를 그대로 믿으면 틀린다.

**확인 방법**: 문자의 `upright` 속성으로 회전 문자만 모아 `(x0, -top)` 순으로 재배열한다.

```
한수진 평면도  회전 47자 → 44,000 42,000 7,000×6 1,000   (upright True 861 / False 47)
최선동 평면도  회전 42자 → 37,000 35,000 7,000×5 1,000   (upright True 735 / False 42)
```

138차가 배운 *"PDF가 숫자를 공백으로 쪼갠다"*의 **도면판**이다 — 이번엔 쪼개는 게 아니라
**뒤집는다**. 공통 교훈: **글자 좌표를 보기 전에는 문자열을 믿지 않는다.**

### ⚠️ 문자열 카운트도 오염된다

`"3,000"`을 그냥 세면 한수진 평면도가 **32회**로 나오는데, 그중 하나는 **`"93,000"` 안의
부분문자열**이다(실제 스팬은 31). 같은 이유로 최선동의 `"7,000"` 6회 중 하나는 `"37,000"`
안에 있다(실제 5). **부분문자열은 항목이 아니다** — 131차 자기부풀림과 같은 계열이다.

---

## 3. 두 농가를 가르는 표기 (대조할 때 쓰는 지문)

| | 한수진 | 최선동 |
|---|---|---|
| 평면도 실내 표기 | **관리동** · 출입문 | **작업동** · 출입문 |
| 측면골조도 높이 치수 | 1,500 · **3,000** | **1,000** · 1,500 |
| 주단면도 스크린 | AL스크린 55% | AL스크린 55% + **스크린 10%** |
| 주단면도 서까래 지름 | `*1.7T@3000` (Ø 없음) | `Ø31.8*1.7T@3000` |

⚠️ 마지막 행은 **원문 표기 차이**다. 같은 시공사(다온팜) 도면인데 한쪽만 지름이 빠져 있다 —
누락인지 생략인지 **판정하지 않고 기록만** 한다.

📌 표제란 「다온팜 / TEL (041)-741-2739 / 충청남도 논산시 채운면 채운로 398-8」은
**시공사 주소이지 현장 주소가 아니다.** 6장 전부 동일하므로 현장 식별에 쓸 수 없다.

---

## 4. `PUMSEM_ITEMS` ← `근거_비닐원가계산서_부재확정_20260914.md`

이 ref는 **제7장 범위 정정의 근거**다. 대조 기준으로 §3의 목차표(원문 PDF 19)를 지정했다.

| 목차 항목 | 인쇄 쪽 |
|---|---|
| 제7장 온실공사품셈 정립 | **109** |
| 제2절 경량철골유리온실 | **138** (철골138·알루미늄141·피복144·천창146·수평151·측벽156·행잉158) |
| 제3절 경량철골비닐온실 | **160** (철골(파이프)160·피복162) |
| 제8장 | 171 |

🔴 **이 9개 시작 쪽이 `pumsem_extract.SECTION_PAGES`의 9공종과 1:1로 일치한다** —
138·141·144·146·151·156·158·160·162. 그래서 이 문서는 "경위"가 아니라 **대조 가능한 기준**이다.

인용 앵커: 「제7장은 109~167이다 … 전사한 64개 품목이 온 자리는 정확하나 이름이 틀렸다」

⚠️ 같은 절에 **원문 결함 2건**이 기록돼 있다 — 인쇄 p.163에 목차와 다른 「제3절 온실공사품셈
적용 단가 산정」이 또 나오고, 인쇄 163~167은 목차에 등재돼 있지 않다. **기록만 한다.**

---

## 5. 🔴 마크다운을 쓰지 않았다 — 그리고 더 큰 것을 쟀다

146차 [3]이 밝힌 대로 **레지스트리 note는 `SmartFarm_근거대장.html`에 그대로 렌더되고
`build_site`는 마크다운을 해석하지 않는다.** 그래서 이번 7건은 `**` 없이 「」로만 썼다.

측정해 보니 문제의 크기가 CAPEX분해보다 훨씬 크다:

| 산출물 | 리터럴 `**` (146차 시점) | 147차 note 정리 후 |
|---|---|---|
| `SmartFarm_CAPEX분해.html` | 80 | 80 (손대지 않았다) |
| **`SmartFarm_근거대장.html`** | **3,757** | **3,745** (이번 7건에서 12개 제거) |

146차는 80개만 보고 *"제거는 후보"*라 했는데, **실제 규모는 3,757개**다. 이 차수는 여전히
**지우지 않는다** — 두 갈래(레지스트리 문자열에서 `**`를 빼기 / `build_site`가 마크다운을
렌더하게 하기) 중 후자는 **산출물 표시를 바꾸는 결정**이라 ★사용자 판단이다.
다만 **선택지의 크기를 이제 안다.**

---

## 6. 검증하지 않은 것 / 하지 않은 것

- **엔진 상수 값·레지스트리 값·케이스·견적비교 전부 불변.** 바뀐 것은 `source_refs` note 7건뿐이다.
- **등급을 올리지 않았다** — 도면이 면적을 정확히 재현해도 값의 출처는 견적서다(`partial` 유지).
- **결정은 하나도 내리지 않았다.** D-1~D-15 전부 ★ 대기다.
- **도면의 나머지 정보를 등재하지 않았다** — 부재 규격(Ø31.8*1.7T 등)은 이 리포가 쓰지 않는다.
- **`**` 리터럴 3,757개를 지우지 않았다**(§5) — ★사용자 결정 대기.
- 도면은 **각 1쪽뿐**이고 6장 전부를 읽었다. 그 밖의 한수진·최선동 문서(견적서 xls)는
  이번 범위가 아니다.
