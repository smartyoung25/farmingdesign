# 3×6 설계는 적정한가 — 비판적 검토 (169차)

**작성일** 2026-09-20 (169차) · **요청** *"3×6 설계의 적정성을 비판적으로 분석하여 개선할 수
있도록 하라"* · **성격** 엔진 상수 **값 변경 0** · 케이스·견적비교·레지스트리 **불변** · **결정 0**

> 🔴 이 설계는 **내가 만들었다**(166차). 자기 검토는 편향된다. 그래서 **주장 대신 측정**으로
> 시작했고, 경계가 만든 값을 보고하지 않도록 **정규식을 두 번 고쳐 다시 쟀다**(159차 교훈).

---

## 0. 결론 — 6단계로 그렸지만 무게는 2단계에 있다

| 단계 | 설계서가 배정한 엔진 함수 | 비중 |
|---|---|---|
| ①공종설계 | **14** | 31% |
| ②품질설계 | 4 | 9% |
| ③감리 | **2** | 4% |
| ④타당성검증 | **17** | 38% |
| ⑤운영 | 6 | 13% |
| ⑥사후관리 | **2** | 4% |
| | 중복 포함 **45** | |

**공종설계 + 타당성검증 = 31/45 (69%).** 감리 + 사후관리 = **4/45 (9%)**.
→ 이 서비스는 **「생애주기 컨설팅」 모양으로 그려졌지만 실제로는 「설계·견적 회사」**다.
6단계 프레임이 **실제 역량 분포를 감추고 있다.**

---

## 1. 🔴 척도가 틀렸다 — 「함수 실재 = 커버리지」

166차는 *"전용 엔진 함수가 있으면 ✅있음"*이라 했다. 그런데 함수의 **역할이 다르다**.

| 함수 | 하는 일 | 166차 등급 |
|---|---|---|
| `equipment_lookup()` | 표에서 **찾아서 돌려준다** | 기자재 공종설계 🟡 |
| `select_specs()` | 조건으로 **거른다** | 부지·시설 공종설계 ✅ |
| `reconcile_quote()` | 4검사로 **불일치를 드러낸다** | 타당성검증 ✅ |

**조회·필터·판정지원이 같은 등급으로 세어졌다.** 측정한 것은 **코드의 존재**이지
**업무 수행 능력**이 아니다. 「있음」이 4칸이라는 말은 *"그 칸에 함수가 있다"*는 뜻일 뿐이다.

### 개선 ① — 척도를 5단계로 바꾼다

```
없음  <  조회(표를 돌려준다)  <  산출(계산한다)  <  검증(기준과 대조한다)  <  판정지원(불일치를 드러낸다)
```
18칸을 이 척도로 다시 매기면 **「부분」 10칸(56%)의 내부 편차**가 드러난다 —
지금은 `env_fitness`(가중치 `[추정]`)와 `warranty_period`(법정 원문 확보)가 **같은 🟡**이다.

---

## 2. 🔴 대상 3축이 배타적이지 않다 — 166차가 인정하지 않은 모호성

166차는 *"단계 분류 자체는 판단이다"*라고 적었다. **대상 축에 대해서는 적지 않았다.**
그런데 경계가 더 모호하다.

| 품목 | 시설인가 | 기자재인가 | 실제 |
|---|---|---|---|
| 온풍난방기 | `heating_load()`가 용량을 낸다 | `EQUIPMENT_SERVICE_LIFE_REFERENCE`에 11년 | **둘 다에 있다** |
| 보온커튼 | `FR_TABLE`·`COVER_ASSEMBLIES` | KS X 3265 구동기 9종에 포함 | **둘 다에 있다** |
| 관수모터 | — | KS X 3265 · 조달청 내용연수 | 기자재 |

→ **18칸 집계(4/10/4)는 배정 규칙 없이 만들어진 수**다. 규칙이 바뀌면 수가 바뀐다.

### 개선 ② — 귀속 규칙을 명문화한다

> **구조체·피복·기초는 시설. 전원에 연결돼 동작하는 것은 기자재. 난방기처럼 둘에 걸치는
> 품목은 「설계 하중을 만드는 면(시설)」과 「내용연수·KS 적합성 면(기자재)」으로 나눠 센다.**

규칙을 적으면 집계가 재현 가능해진다. 규칙 없이 센 수는 **다음 사람이 다시 세면 달라진다.**

---

## 3. 🔴 ★결정 지점을 1곳으로 그렸다 — 실제는 15건 + 자료 4건

166차 게이트 흐름도는 **★를 「업체·사양 확정」 한 곳**에만 놓았다.
`근거_결정대기대장_20260915.md` 실측: **★결정 D-1 ~ D-15 (15건)** · **자료 S-1 ~ S-4 (4건)**.
게다가 167·168차가 **새 결정 지점을 두 개 더 만들었다**:

- **감리 대가 체계 선택** — 국토부 [별표5] 1.35% vs 산업부 [별표1] 비상주 1.66%
- **`FR_TABLE` 계열 채택** — 국내 원문(55~65) vs 국제 일반(30~50) vs 제품 스펙(70~72)

→ 흐름도는 *"기계가 넘지 않는 지점"*을 **과소 표시**한다. 고객에게 *"한 번만 정하면 된다"*는
인상을 준다면 **그 자체가 거짓**이다.

### 개선 ③ — ★를 단계마다 찍고, D-1~D-15를 단계에 매핑한다

흐름도는 결정을 **세는 도구**여야 한다. 단계별 ★ 개수가 곧 **고객 부담**이고,
그것을 먼저 보여주는 것이 정직하다.

---

## 4. 🔴 과금 근거가 6단계 중 2단계뿐

167차 §5-3 과금표 **8행 중 `[협의]`가 5칸**이다. 근거가 있는 것은:

| 단계 | 근거 |
|---|---|
| ②공종설계 | B계층 — 실비정액가산(제경비 110~120% · 기술료 20~40%) + [별표1] 도로 요율 |
| ⑤감리 | A계층 — 국토부 [별표5] 법정 요율 (엔진이 산출) |
| ⑤-b 커미셔닝 | C계층 — 문헌 0.5~2% |

→ **①입지진단 · ③품질설계 · ④타당성검증 · ⑥운영·사후관리는 값을 매길 근거가 없다.**
산출물 밀도가 가장 높은 **④타당성검증(함수 17개)이 과금 근거가 없다**는 것이 특히 이상하다.

### 개선 ④ — B계층을 전 단계로 확장한다

실비정액가산은 **업무 종류를 가리지 않는다**(직접인건비 기반). ①③④⑥도
**투입 인·일 × 등급별 노임 + 제경비 + 기술료**로 산정할 수 있다.
막는 것은 근거가 아니라 **우리가 각 단계의 표준 투입 인·일을 정하지 않은 것**이다.
`pumsem_project_labor_summary()`가 **시공 인력**에 하는 일을 **컨설팅 인력**에 하면 된다.

---

## 5. 🔴 산출물↔단계 매핑이 한 곳에만 있고, D11이 빠졌다

- §3 단계별 본문에 **D번호가 0건** — 단계를 읽어도 무엇이 나오는지 알 수 없다.
- 매핑은 **§8 게이트 흐름도 한 곳**에만 있다.
- 그 흐름도에 **D1~D10만 있고 `D11 근거대장`이 없다**.

→ 근거대장은 **이 서비스의 차별점**(상수 54 · `source_refs` 154)인데 **흐름도에서 빠졌다.**
가장 팔 만한 것을 배치하지 않았다.

### 개선 ⑤ — 산출물을 단계 본문에 박고, D11을 전 단계에 걸친다

`D11 근거대장`은 한 단계의 산출물이 아니라 **모든 단계에 따라붙는 레이어**다.
흐름도에서 **세로로 관통하는 띠**로 그려야 맞다.

---

## 6. 🔴 회귀가 보호하는 것은 6단계 중 1단계다

실측 회귀 기준은 **원채원 ROI 14.2% · Payback 7.1년 · 실질ROI 28.3%** — 전부
**④타당성검증**의 산출물이다. 3파일 테스트에서 **한 번도 호출되지 않는 공개 함수는 2개**
(`apply_overheads` · `monthly_sunshine`)로 적지만, *"호출된다"*와 *"산출물이 고정된다"*는 다르다.

→ ①②③⑤⑥의 산출물은 **바뀌어도 벤치마크가 울지 않는다.**

### 개선 ⑥ — 단계별 회귀 앵커를 하나씩 둔다

케이스 하나에 대해 **D1(설계하중) · D2(규격코드) · D4(4축 상태) · D10(교체주기)**의
값을 고정하면, 지금 ④만 지키는 회귀가 **6단계를 지킨다**. 새 자료는 필요 없다 —
**이미 있는 케이스에서 뽑으면 된다.**

---

## 7. 설계서가 엔진의 17%를 설명하지 않는다

공개 함수 **53개 중 44개**가 설계서에 인용됐다. **인용되지 않은 9개**:

`construction_company_list` · `spec_crops` · `cover_assembly_options` · `mean_wind` ·
`transmission_share_pct` · `npv` · `irr` · `py_to_m2` · `m2_to_py`

- 뒤 4개는 **하위 유틸**이라 문제가 아니다(`npv`·`irr`은 `finance()` 안에서 쓰인다).
- 🔴 앞 5개는 **업무 기능**이다. 특히 `construction_company_list()`(시공업체 도급순위)는
  **④타당성검증의 업체 비교와 직결**되는데 설계서에 없다 — *"업체를 추천하지 않는다"*는
  금지선과 **가장 가까이 붙어 있는 함수**를 설명하지 않은 것이다.

---

## 8. 🔴 「하지 않는 것」은 6문장인데 「대신 주는 것」은 목록뿐이다

금지선 6문장은 정교하다. 그런데 *"판정을 안 한다면 고객은 무엇으로 판정하나"*에 대한
설계가 **D1~D11 산출물 목록**밖에 없다. 리포에는 이미 **결정을 돕는 자산**이 있다 —
`근거_결정대기대장` · D-1~D-15 **감응 측정** 문서 · 레드팀 **반대근거** 기록.
**설계서가 그것을 산출물로 세우지 않았다.**

### 개선 ⑦ — `D12 결정 지원 패키지`를 신설한다

| 구성 | 이미 있는 자산 |
|---|---|
| 결정 대기 목록 + 선행조건 | `근거_결정대기대장_20260915.md` |
| 각 결정의 **감응도**(바꾸면 얼마나 움직이나) | D-1~D-15 감응 측정 문서 |
| **반대근거** | 레드팀 회차 기록 |
| 결정 로그(누가·언제·무엇을 근거로) | ⬜ 신설 |

→ 이것이 *"추천은 없지만 결정은 쉬워진다"*의 **구체적 형태**다. 지금은 말로만 있다.

---

## 9. 개선 우선순위

| # | 개선 | 비용 | 효과 |
|---|---|---|---|
| **1** | **D12 결정 지원 패키지**(§8) | 낮음 — 자산이 이미 있다 | 금지선이 **가치 제안**이 된다 |
| **2** | **대상 귀속 규칙 명문화**(§2) | 낮음 — 문서 한 절 | 18칸 집계가 **재현 가능**해진다 |
| **3** | **★를 단계마다 표시 + D-1~D-15 매핑**(§3) | 낮음 | 고객 부담을 **정직하게** 보여준다 |
| **4** | **단계별 회귀 앵커**(§6) | 중간 — 케이스에서 뽑는다 | 회귀가 **1단계 → 6단계**를 지킨다 |
| **5** | **5단계 척도로 재측정**(§1) | 중간 | 「부분 10칸」의 **내부 편차**가 보인다 |
| **6** | **B계층 과금을 전 단계로**(§4) | 중간 — 표준 투입 인·일 필요 | 6단계 중 **2 → 6**이 값을 갖는다 |
| **7** | 미인용 5함수 배치(§7) | 낮음 | 설계서가 엔진을 **다 설명한다** |

**1·2·3·7은 문서 작업이고 오늘 할 수 있다. 4·5는 측정이 필요하고, 6은 판단성(표준 투입
인·일 = 우리 원가)이라 ★사용자 결정이 앞선다.**

---

## 10. 비판이 닿지 않은 곳 / 하지 않은 것

- **엔진·케이스·레지스트리를 건드리지 않았다.** 값 변경 0 · 결정 0.
- **개선안을 적용하지 않았다** — 이 차수는 **진단**이다. 적용은 다음 차수의 일이고,
  그중 **6번은 ★사용자 결정**(우리 원가·요율 = 시세성)이 선행한다.
- **외부 벤치마크와 비교하지 않았다** — 다른 컨설팅사의 단계 구분이 어떤지 조사하지 않았다.
  **국내외 컨설팅 서비스 분류 체계를 대조하면 3×6 자체가 흔들릴 수 있다**(검증 대상).
- **고객 인터뷰가 없다** — *"6단계가 고객이 사고 싶은 단위인가"*는 **자료 없이 답할 수 없다.**
  §0의 *"설계·견적 회사 모양"* 진단도 **시장 적합성 판단이 아니라 내부 역량 분포 측정**이다.
- **함수 개수를 역량의 대리지표로 썼다** — 함수 2개가 함수 17개보다 **덜 중요하다는 뜻이 아니다**.
  `warranty_period()` 하나가 법정 하자 전체를 덮는다. 이 한계는 §1 개선안(5단계 척도)이
  부분적으로만 해소한다.
