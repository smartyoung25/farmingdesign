# ⓐ 얽힘 4건의 감응을 측정했다 — 그리고 대장 한 행이 틀렸다 (149차)

**작성일** 2026-09-16 (149차) · **계기** 사용자 지시 *"여전히 남은것 처리"*
**성격** 엔진 상수 값 변경 0 · 케이스·견적비교 불변 · 레지스트리 값 변경 0 · **결정 0**

> **결정은 제가 내릴 수 없습니다**(1절 — 판정·추천 자동화 금지). 대신 **결정을 막고 있는 것**을
> 치웁니다. 144차 대장이 스스로 적은 한계가 그것이었다 — *"나머지 10건의 감응은 여전히
> 제기 차수가 적은 값을 인용한 것이다"*. 이 차수는 **D-1~D-4(ⓐ 얽힘 블록)**를 잰다.
> 이 블록은 **106차 이래 동결**돼 있고 *"근거 없이 순서를 정하는 것이 1절이 금한 판단성 자동화"*가
> 동결 사유였다.

---

## 0. 결과 — 세 가지가 뒤집혔다

| | 대장(144차)이 적은 것 | 149차 실측 |
|---|---|---|
| **D-3의 파괴력** | 🔴*"OPEX가 20~33% 움직여 원채원 ROI 14.2%가 직접 깨진다"* | ❌ **깨지지 않는다.** `sunshine_k`는 **연료소비량만** 움직이고 그 값은 **어떤 산출물에도 실리지 않는다** |
| **ⓐ의 성격** | *"순서와 조합이 곧 결과다"* | ❌ **D-2와 D-4는 순서 문제가 아니라 배타다** — 엔진이 동시 지정을 **거부**한다 |
| **경제성 영향** | (적혀 있지 않음) | ✅ **D-1~D-4 전부 ROI·Payback·실질ROI 불변** |

**그래서 ⓐ 블록은 "회귀 기준을 깨뜨릴까 봐" 동결할 이유가 없다.** 동결의 실제 근거는
**난방 지표(11개 산출물에 실리는 최대부하)** 쪽이다.

⚠️ 그래도 **결정하지 않는다** — 어느 값이 옳은지는 원문·판단의 몫이고, 이 문서는 **크기만** 잰다.

---

## 1. 🔴 왜 회귀가 깨지지 않는가 — 경제성은 난방과 연결돼 있지 않다

`render_report.compute()`를 읽었다.

```python
hr  = e.heating_load(...)                    # 난방
fin = e.finance(revenue, inp.opex, ...)      # 경제성 ← inp.opex(케이스 입력)
```

**ROI는 `inp.opex`에서 나온다.** `opex`는 케이스 JSON의 입력값이고 provenance가
*"운영비 산정치"*·*"baseline 동일값"*·*"면적비로 축소 추정"*이라 적는 **[추정] 입력**이다.
**연료소비량에서 유도되지 않는다.** `opex_breakdown()`은 *이미 아는 총액*을 항목으로
쪼갤 뿐 총액을 만들지 않는다.

실측(3개 완전 케이스, 전 설정 동일):

| 케이스 | ROI | Payback | 실질ROI | OPEX |
|---|---|---|---|---|
| **wonchaewon**(회귀 기준) | **14.16%** | **7.06** | **28.32%** | 186,420,000 (케이스 입력) |
| chuncheon | 14.16% | 7.06 | 28.32% | 186,420,000 (baseline 동일값) |
| uminjae | (불변) | (불변) | (불변) | 125,300,000 (면적비 추정) |

D-1·D-2·D-3·D-4 **어느 것을 걸어도 이 세 숫자는 한 자리도 안 움직인다.**

---

## 2. 🔴 대장 D-3 행이 틀렸다 — 연료 범위를 OPEX 범위로 옮겨 적었다

대장 §3은 이렇게 적는다:

> **D-3** | 일조 k를 케이스에 걸면 OPEX가 **20~33% 움직여** 원채원 ROI 14.2%가 직접 깨진다

실측하면 `sunshine_k`가 움직이는 것은 **연료소비량 하나뿐**이다(최대부하·난방기용량 불변):

| `sunshine_k` | 일조 | 원채원 연료 | 대비 |
|---|---|---|---|
| (없음) = 3600 | — | 27,039.8 | — |
| 3020 | 3.0h | 22,683.3 | **−16.1%** |
| 2820 | 4.5h | 21,181.1 | **−21.7%** |
| 2620 | 6.0h | 19,678.9 | −27.2% |
| 2420 | 7.5h | 18,176.7 | **−32.8%** |
| 2220 | 9.0h | 16,674.5 | **−38.3%** |

🔴 **대장의 "20~33%"는 `k=2820`과 `k=2420`의 연료 변동률과 소수점까지 같다**
(−21.7% · −32.8%). 즉 **연료 감소율을 OPEX 변동률로 그대로 옮겨 적은 것**이다.
그러려면 **연료비가 OPEX의 100%**여야 하는데, 원채원 OPEX 186,420,000에는
종묘·비료·농약·인건비가 함께 들어 있다.

그리고 **실제 OPEX 효과는 산출할 수 없다** — 유가는 **시세성**이라 주입만 받는다(1절).
연료 **ℓ**까지가 한계다.

### 게다가 연료소비량은 어디에도 실리지 않는다

| 확인 | 결과 |
|---|---|
| `compute()`가 돌려주는 dict | `max_load`·`load_per_m2`·`heater_capacity`·`ref`·`ratio`·`status` — **`fuel_consumption` 없음** |
| 생성기 5개(`build_site`·`webapp`·`render_report`·`run_report`·`cases`)의 `fuel` 언급 | **전부 0회** |
| 산출물 HTML의 연료 수치 | **0건** |

⚠️ **146차 [2]의 교훈대로 두 경로를 다 봤다** — 호출 경로(문자열 탐색)뿐 아니라
**데이터 경로**(`compute()` 반환 dict에 실리는가 · 케이스 `opex`가 유도되는가)까지.
D-12에서 데이터 경로를 빠뜨려 틀렸던 실수를 반복하지 않으려는 것이다.

→ **정정**: D-3의 감응은 *"연료소비량 −16.1~−38.3%(전 표 범위), 산출물·경제성 도달 0"*이다.
대장의 −21.7~32.8%는 **전 표가 아니라 일조 4.5~7.5h 구간**만 본 수치였다(범위 표기 없음).

---

## 3. 🔴 D-2와 D-4는 "얽힘"이 아니라 **배타**다

대장 ⓐ는 *"서로 얽혀 있다 — 순서와 조합이 곧 결과다"*라 적고, D-4를 *"위 셋의 적용 지점
자체를 바꾼다"*고 했다. 실제로 걸어 보면 **엔진이 거부한다**:

```
D-2+D-4 → ValueError: assembly와 u_design/u_period를 함께 줄 수 없다 —
          조합 계수에 보온 효과가 이미 포함돼 있어 이중 지정이 된다
```

→ **순서를 정하는 문제가 아니라 둘 중 하나를 고르는 문제**다. 이것은 결정을
**쉽게** 만든다(조합 폭발이 없다). 남는 겹침은 **D-1 하나뿐**이다 — 사후 배율 1.0881이라
어느 선택 위에도 곱해진다.

---

## 4. ⚠️ 자기 정정 — D-2는 **필름 계열만**이다

첫 측정에서 원채원(유리)에 `u_design=8.9`를 걸어 *"+67.9%"*를 얻었는데, **그것은 과적용**이다.

```
U_DESIGN 키 = ['필름', '불소필름', '단동']      ← 유리 없음
U_VALUE  키 = ['유리', '필름', '불소필름', '단동', '필름_이중']
```

D-2는 99차가 `U_DESIGN`을 8.9 → 5.7로 바꾼 건의 재검토이므로 **대상은 필름 계열**이다.
**원채원·춘천(유리)은 D-2 대상이 아니다.** 인자로 강제 주입하면 유리에도 먹지만,
그건 결정의 내용이 아니다. → **D-2의 실측 대상은 uminjae(필름) 한 건**이다.

---

## 5. 네 항목의 감응 — 최대부하 기준 (11개 산출물에 실린다)

**uminjae(필름, 현행 최대부하 238,776 kcal/h)**

| 결정 | 최대부하 | 대비 | 성격 |
|---|---|---|---|
| 현행 | 238,776 | — | |
| **D-1** 3성분 8% 보정 | 259,812 | **+8.8%** | 사후 배율 — **어느 선택에도 곱해진다** |
| **D-2** `u_design` 8.9 | 372,826 | **+56.1%** | 필름 계열만 |
| **D-4** 최소절감 `PE필름(0.08)` | 341,109 | **+42.9%** | D-2와 **배타** |
| **D-4** 최대절감 `po-0.10+5겹다겹` | 83,781 | **−64.9%** | D-2와 **배타** |
| **D-3** 일조 k | **238,776 (불변)** | 0% | 연료만 움직인다 |

**D-4의 폭이 압도적이다 — 조합 49종 사이에서 최대부하가 4.07배 갈린다.**
그리고 **유리 케이스에도 적용된다**(assembly가 cover를 덮어쓴다):

| 케이스 | 피복 | 현행 | D-4 최소 | D-4 최대 | 배율 |
|---|---|---|---|---|---|
| uminjae | 필름 | 238,776 | 83,781 (−64.9%) | 341,109 (+42.9%) | **4.07배** |
| wonchaewon | 유리 | 330,190 | 124,600 (−62.3%) | 507,300 (+53.6%) | **4.07배** |
| chuncheon | 유리 | 458,185 | 172,900 (−62.3%) | 703,950 (+53.6%) | **4.07배** |

---

## 6. 🔴 부수 발견 — 실측 이중검증이 이 변동을 잡지 못한다

`verify_heating_vs_actual()`은 원채원에서 이렇게 답한다:

| 설정 | 부하/㎡ | 기준 | 비 | 상태 |
|---|---|---|---|---|
| 현행 | 95.5 | 231.0 | 0.410 | **정상** |
| D-1 ×1.0881 | 104.0 | 231.0 | 0.450 | **정상** |
| (유리에 8.9 강제) | 160.4 | 231.0 | 0.690 | **정상** |

부하가 **+68%** 움직여도 상태는 그대로 **정상**이다. 이 가드는 **자릿수 오류를 잡는 넓은 띠**이지
결정의 옳고 그름을 가려 주지 않는다 — **결정을 검증기에 맡길 수 없다**는 뜻이다. 기록만 한다.

---

## 7. 이 차수가 하지 않은 것

- **결정은 하나도 내리지 않았다.** D-1~D-15 전부 ★ 대기다.
- **엔진 상수 값·케이스·견적비교·레지스트리 값 전부 불변.** 측정은 **인자 주입**으로만 했다
  (상수를 바꾸지 않았다).
- **OPEX 효과를 산출하지 않았다** — 유가가 **시세성**이라 주입 없이는 낼 수 없다. 연료 **ℓ**까지가 한계다.
- **D-5~D-9·D-13~D-15의 감응은 여전히 미측정**이다(ⓑ 국소 4건 + ⓒ 7건 중 D-10~D-12만 145차에 측정됐다).
- **`verify_heating_vs_actual`의 띠를 좁히지 않았다** — 좁히는 것은 판정 기준 변경이라 ★사용자 결정이다.
- **원문 미보유 3건**(원채원 최우선)·`SmartFarm_엔진데이터.md`·농진청 OPEX CSV는 **여전히 사용자만 넣을 수 있다**.
  🔴 그중 **OPEX 원문이 들어오면 이 차수의 결론이 바뀐다** — OPEX가 연료에서 유도되기 시작하면
  D-3이 비로소 회귀 기준에 닿는다.
