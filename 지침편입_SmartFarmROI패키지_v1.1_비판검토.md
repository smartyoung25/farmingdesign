# 외부 지침 비판검토 — SmartFarm ROI / Design Assurance Claude Package v1.1

**작성일** 2026-09-13 (79차) · **계기** 사용자 지시 "전체 서비스 지침 및 스킬을 가져와서 녹여라"
**대상** `SmartFarm_ROI_Claude_Package_v1.1_FULL.zip` (사용자 제공) — 지침·스킬 19종 + 참고자료 12종
**보존본** `외부지침/SmartFarm_ROI_Claude_Package_v1.1/` (md·json 19종, 67KB)
**선례** `통합작업체계_컨설팅8단계_엔진스코프경계.md` 1절 — GPT 지침 v1.1~v3.0++ **전량 폐기** 검토

> ⚠️ **이 폴더의 문서는 참고자료이지 이 리포의 실행 지침이 아니다.** 패키지의
> `CLAUDE.md`는 하위 폴더에 그대로 두면 프로젝트 지침으로 자동 로드될 위험이 있어
> `_원본_CLAUDE_프로젝트지침.md`로 개명했다(내용 불변). 이 리포의 지침은 루트
> `CLAUDE.md` 하나뿐이다.

---

## 0. 결론 (먼저)

**부분 채택한다.** GPT 지침 v1.1~v3.0++와 달리 **전량 폐기가 아니다.**

| 판정 | 건수 | 요지 |
|---|---:|---|
| **채택** | 7영역 | 컨설팅 도메인 체크리스트·분류체계·불확실성 원칙 — 1절과 정합하거나 이미 같은 방향 |
| **조건부 채택** | 2영역 | "판정"을 떼고 **[확인요망] 체크리스트**로 격하하면 채택 가능 |
| **거부** | 2계열 | ①판정·추천 자동화 ②Claude 직접 계산(병렬 계산기) — **1절 정면 위배** |

**왜 GPT 지침과 다른가.** 그때의 폐기 사유 4가지를 이 패키지에 다시 걸어 보면 —

| 폐기 사유(당시) | 이 패키지 |
|---|---|
| ①엔진 단일출처 위배(GPT가 직접 계산) | **동일 문제 있음** → 거부 대상(3절) |
| ②근거 없는 값 자동 추정("누락 시 산업 평균값 자동 추정") | **정반대다.** "확인되지 않은 숫자를 사실처럼 만들지 않는다", "계수 근거가 약하면 임의 생성하지 말고 Low/Base/High" |
| ③출처 없는 의사과학적 공식(GDD·VPD 동역학) | **없다.** 계산식이 전부 표준 재무·물리 정의 수준 |
| ④산출물이 빈 파일이었다 | **실물이 있다.** 참고자료 12종(205MB) 실재 |

즉 ②③④는 해당하지 않고 ①만 남는다. ①은 "계산을 누가 하느냐"의 문제라 **지침을 버릴
사유가 아니라 계산 주체를 엔진으로 돌리면 되는 문제**다.

---

## 1. 무엇을 검토했나

| 파일 | 성격 |
|---|---|
| `_원본_CLAUDE_프로젝트지침.md` | 프로젝트 인스트럭션 — 목표·판단원칙·자료우선순위·필수지표·Hard Stop·Skill Router |
| `00_MASTER_INSTRUCTIONS.md` | 시스템 정체성·입력 데이터 모델(C1~C6)·라벨링(D)·단위(E)·계산식(F)·Reality Factor(G)·보조금(H)·평가체계(I)·불확실성(J)·문서검증(K)·보고서등급(L)·최종판정(M) |
| `01`~`12` Skill 12종 | 입지 / 설계검토 / QOM·BOQ / 견적감사 / CAPEX / OPEX·LCC / 생산·매출 / 금융·ROI / 시나리오 / RFQ·업체 / 시운전·실적 / 보고서 |
| `ALL_IN_ONE_CLAUDE_INSTRUCTIONS.md` | 위 13개의 **단순 연결본**(고유 내용 없음 — 목차 대조로 확인) |
| `README` · `BOOTSTRAP_PROMPT` · `REFERENCE_MANIFEST` · `package_index.json` | 설치·부트스트랩·참고자료 목록 |

**전문을 읽었다.** 다만 `references/` 12종(205MB PDF·xlsx)은 **목록만 확인했고 내용은 열지
않았다** — 별도 차수(5절).

---

## 2. 채택 (7영역)

### 2-1. `00_MASTER` K절 — 문서 검증 순서 ⭐ 최우선

> `도면 수량 ↔ 시방서 규격 ↔ QOM 수량 ↔ 견적 단가/금액`
> 검출: 누락·중복·과다·규격불일치·과사양·저사양·단위오류·포함/제외 모호·설치/운반/시운전 중복

**엔진에 이 계층이 통째로 없다.** `reconcile_quote`는 견적↔RFQ 대사이고, 도면·시방서·규격서
Rev 정합성은 다루지 않는다. 첨부 자료 A(수출단지 체크리스트 v1.2)의 `P2_정합성매트릭스`가
같은 요구를 Rev 문자열 일치 비교로 구현하고 있어 **둘이 같은 사양을 가리킨다** — 순수
문자열 비교라 결정론이고 엔진 편입 자격이 있다. → `doc_consistency_check()` 사양으로 채택.

### 2-2. `03_QOM_BOQ` — 20 WBS 분류체계

가설/토목/기초/골조/피복/천창·측창/보온·차광/환기/난방/냉방·제습/용수/양액/전기/ICT/
재배베드/작업·선별/부대시설/설계·감리/시운전·교육/예비비.

엔진의 `CAPEX_MAJOR_CATEGORIES`(13종)·`CAPEX_CATEGORIES`(9종)보다 **세분**돼 있다.
**값이 아니라 taxonomy만 채택**하고, 실제 매핑(20↔13)은 케이스 데이터가 그 해상도를
지원할 때만 한다 — 지금 분해하면 근거 없는 세분이 된다.

### 2-3. `04_QUOTE_AUDIT` — 판정코드 8종

`OK / CHECK / HIGH / LOW / MISS / DUP / SPEC / ALT`

`reconcile_quote`의 기존 신호와 성격이 같고 어휘가 더 명확하다. **단 "판정"이라는 말에
속지 말 것** — 이 코드들은 *사실 분류*(누락·중복·차이)이지 *가치 판단*(적합·추천)이
아니다. 그래서 채택 가능하다. 3절의 거부 대상과 갈리는 지점이 정확히 여기다.

### 2-4. 불확실성·범위 원칙 (여러 파일에 분산)

이 패키지에서 **가장 값어치 있는 부분**이고 1절과 정확히 같은 방향이다.

| 출처 | 원문 |
|---|---|
| `CLAUDE.md` §2 | "확인되지 않은 숫자를 사실처럼 만들지 않는다" |
| `00_MASTER` §G | "각 계수는 명확한 근거가 없으면 임의의 소수점 값으로 만들지 말고, Low/Base/High 범위를 사용한다" |
| `00_MASTER` §J | "단일 숫자보다 범위와 민감도를 우선한다", "제조사 홍보상의 절감률을 검증 없이 적용하지 않는다" |
| `04` §단가 | "기준단가가 시점·지역·수량에 민감하면 단일 정상가격이 아니라 범위를 제시한다" |
| `05` §소규모 보정 | "자동으로 고정 비율을 적용하지 말고 … 범위로 제시한다" |
| `06` §보정 | "계수 근거가 약하면 계수값을 임의 생성하지 말고 Low/Base/High로 시뮬레이션한다" |
| `07` §기술효과 | "증수효과를 중복 적용하지 않는다. 제조사 주장만 있는 경우 별도 민감도로 둔다" |
| `02` §금지 | "구조계산서 없이 구조안전을 확정하지 않는다", "냉난방부하 계산 없이 장비 용량을 적정하다고 단정하지 않는다" |
| `05` §예비비 | "예비비를 숨겨서 공종단가에 분산시키지 않는다" |

📌 **이 리포의 진행 중 이견(난방 계수)에 그대로 적용된다** —
`근거_난방계수_이견_20260913.md`가 `FR_TABLE` 4키를 단일값으로 들고 있는데, 두 1차 출처가
서로 다른 값을 준다(표 3-3-27 다겹보온커튼 58~67 vs RDA 표10 4겹 53.7). §G·§J 원칙대로면
**단일값이 아니라 범위**가 정직한 표기다. → 79차 이후 검토 대상으로 넘긴다.

### 2-5. `00_MASTER` H절 — Project / Equity 경제성 분리

> 1. Total Project Economics: 보조금 전 사업 자체
> 2. Equity Economics: 보조금·대출 반영 후 사업주 기준

엔진 `finance()`는 하나만 낸다(`subsidy_rate`로 `real_roi_after_subsidy`를 덧붙일 뿐).
**보조금 지급 지연 시 브릿지 금융비용**까지 보라는 것도 `loan_amortization`과 연결된다.
→ 엔진 공백(4절).

### 2-6. `07_YIELD_REVENUE` — 총면적 ≠ 순재배면적

> "총면적이 아닌 순재배면적"

엔진 `production_kg(area_m2, ...)`는 호출부가 주는 면적을 그대로 쓴다. 케이스의
`area_m2`가 총면적인지 순재배면적인지 **정의가 문서화돼 있지 않다** — E파일 대조에서도
재배동 2,016㎡ vs 부대 포함 3,202㎡가 갈렸다. 실제 오차 요인이라 채택.

### 2-7. `10_RFQ_CONTRACTOR` — "최저가 자동선정 금지"

> "가격이 싸다는 이유만으로 대체안을 추천하지 않는다"(`04`)
> "최저가 자동선정 금지"(`10`)

**엔진이 이미 그렇게 설계돼 있다** — `compare_quotes`는 `lowest_cost_vendor`를
"참고정보일 뿐, 추천 아님"으로 명시하고, 38차 레드팀 F5가 동점 표기 누락을 잡아 고쳤다.
`compare_design_options`(75차)는 아예 순위 필드를 두지 않았다. **외부 지침이 같은 결론에
독립적으로 도달했다는 교차확인**으로만 취급한다.

---

## 3. 거부 (2계열)

### 3-1. 판정·추천 자동화 — 1절 "판정·추천 자동화 금지" 정면 위배

| 위치 | 내용 |
|---|---|
| `00_MASTER` I절 | 100점 평가체계(12항목 가중) + **등급 자동 판정**("85 이상 적합 / 75~84 조건부 / 65~74 재설계 / 65 미만 재검토") |
| `00_MASTER` M절 | **최종 판정 문구 4종**("적합 / 조건부 적합 / 재설계 필요 / 사업 재검토") |
| `CLAUDE.md` §5 | Hard Stop 14항목 → **"사업 보류 또는 재설계를 우선 권고한다"** |
| `01` §출력 4) | **Site Score 0~100** |
| `02` §판정 | **설계 종합판정** + 분야별 점수 + "적합/조건부/부적합" |
| `10` §최종 추천 | **"Best Value Contractor 관점에서 추천한다"** |
| `12` §Executive Summary | 표의 마지막 행 **"최종판정"** |

**거부 사유.** 1절 신뢰유형 표가 "내재해 지정값 해석·작목 선정·부지 최종선정"을 판단성으로
분류하고 "보조/초안까지만(자동화 X)"으로 못박았다. 사업 추진 여부·설계 적합 판정·업체
추천은 전부 그 범주다. 더구나 이 프로젝트는 같은 계열을 이미 두 번 거부했다 —
GPT 지침의 자동보정(통합작업체계 1절), 그리고 첨부 C파일(경제성 시뮬레이터)의
`종합_요약!D12` 등급→보조율 자동 추천(75차).

⚠️ **점수 체계 자체가 문제가 아니다.** 문제는 **점수→등급→추진 여부**로 이어지는
자동 결론이다. 가중치 12항목은 컨설턴트가 수기로 채우는 **평가 서식**으로는 유용하다.

### 3-2. Claude 직접 계산 — 1절 "엔진이 유일한 계산 출처" 위배

| 위치 | 내용 |
|---|---|
| `CLAUDE.md` §4 | "가능한 경우 **항상 다음을 계산한다**" — CAPEX/OPEX/LCC/생산량/매출/EBITDA/ROI/NPV/IRR/Payback/BEP/DSCR/Max CAPEX |
| `00_MASTER` F절 | 핵심 계산식 9종을 **LLM이 수행할 식으로** 제시 |
| `08_FINANCE_ROI` | Maximum Investable CAPEX **탐색 절차**(제약 만족 최대 CAPEX 탐색) |

**거부 사유.** LLM은 결정론적이지 않다 — 같은 입력에 다른 값을 낼 수 있다. 채택하면
`smartfarm_engine.py`와 별개의 제2 계산 출처가 생긴다(GPT 지침 폐기 사유 ①과 동일).

**다만 식 자체는 거부 대상이 아니다.** 전부 표준 정의이고 결정론이다. →
**계산 주체만 엔진으로 돌려서 채택**한다(4절). 이것이 이 패키지와 GPT 지침의 결정적 차이다.

---

## 4. 조건부 채택 (2영역)

### 4-1. Hard Stop 14항목 → **[확인요망] 체크리스트로 격하**

원문은 "사업 보류 권고"라는 **판정**이라 그대로는 거부다. 그러나 목록 자체는 실무적으로
값어치가 크고 **전부 사실 확인 항목**이다.

토지 사용권·인허가 / 구조안전 검증 / 침수·강풍·적설 / 용수 확보·수질 / 전력 인입·증설 /
배수계획 / 작물-시설 정합 / **난방·냉방 용량 산정 근거** / 도면·시방·견적 수량 불일치 /
핵심 공종 누락 / 시장·판매근거 / Reality·Stress 현금고갈 / 보조금 선집행 자금 /
시운전·성능검증·하자보증 조건

**격하 방식**: "보류 권고"를 떼고 **"미확인 시 [확인요망] 표기 + 해당 지표 산출 보류"**로
바꾼다. 판정은 컨설턴트 몫으로 남기되, **미확인 사실이 산출물에 드러나게** 한다.
📌 8번 항목("난방·냉방 용량 산정 근거 없음")은 지금 이 리포의 미해결 이견과 정확히 겹친다.

### 4-2. `00_MASTER` D절 — 데이터 라벨 8종

`[USER] [DOC] [DB] [WEB] [CALC] [ASSUMPTION] [ESTIMATE] [RECOMMENDATION]`
+ "근거가 없는 숫자에는 `[DOC]`,`[DB]`,`[WEB]` 라벨을 붙이지 않는다"

**조건부인 이유**: 이 리포엔 이미 provenance status enum 8종이 있다
(`실측/부분실측/법정기준/공공기준/참고기준/추정/확인요망/미검증`). 두 체계는 **축이 다르다** —
패키지는 *출처 종류*, 리포는 *근거 강도*. 나란히 쓰면 **이중 분류체계**가 되어
감사기(`audit_traceability`)와 웹앱 근거 칩이 갈린다.

**채택 조건**: 새 enum을 만들지 않고, 필요하면 기존 status에 *출처 종류* 필드를
**별도 키로** 덧붙인다. enum 교체는 하지 않는다(44차 F5·12회차 F3 선례 — 표기 체계
변경은 감사 전반에 파급된다).

---

## 5. 엔진 공백 지도 (구현 대기 — 우선순위)

`dir(smartfarm_engine)` 실측 대조 결과다.

| # | 항목 | 현황 | 성격 | 비고 |
|---|---|---|---|---|
| 1 | **`doc_consistency_check()`** | ❌ 없음 | 결정론 | 2-1절. Rev 문자열 비교 — 판정 없음 |
| 2 | **DSCR** | ❌ 없음 | 결정론 | `CADS / Debt Service`. `loan_amortization` 재사용 |
| 3 | **Maximum Investable CAPEX** | ❌ 없음 | 결정론 | 목표 IRR·Payback·DSCR 제약 하 CAPEX 상한 역산. **"얼마에 지을 수 있나"가 아니라 "얼마 이하로 지어야 하나"** — 이 패키지의 핵심 아이디어 |
| 4 | **Project / Equity 분리** | 부분(`real_roi_after_subsidy`) | 결정론 | 2-5절 |
| 5 | 월별 매출 | ❌ 없음 | 결정론 | 단가·출하량 **주입** 전제(시세성) |
| 6 | 냉방부하 | ❌ 없음 | 결정론 | 첨부 E파일 `시설구성내역`이 식 보유 |
| 7 | 상품화율(Marketable Rate) | ❌ 없음 | 결정론 | `production_kg`에 없음. A파일 KPI와 연결되나 **운영 축은 리포 밖 유지 결정**(75차) |
| 8 | 순재배면적 정의 | 미문서화 | 표기 | 2-6절 |

**이미 있어서 신설 불요**(19회차 F2 재발 방지 — 부재 단정 전 리포 전역 확인):

| 패키지 요구 | 리포 현황 |
|---|---|
| `09` 시나리오·민감도 | `build_site.scenario_rows()` + 4케이스 `scenarios` 블록 + `webapp /entry/scenario` (50차 종결) |
| `06` LCC 교체 | `lcc_replacement_schedule()` |
| `08` 대출·상환 | `loan_amortization()` (P3-18) |
| `04` 견적 정규화·비교 | `reconcile_quote()` · `compare_quotes()` |
| `10` 업체 데이터 | `기자재DB/시공업체_*.csv` · `construction_company_list()` |
| `11` Plan vs Actual | **8단계 경계상 리포 밖** — `smartfarm-consulting-report` 스킬 소관(75차 사용자 결정) |
| `02` 설계 대안 비교 | `compare_design_options()` (75차) |

---

## 6. 하지 않은 것 / 확인하지 않은 것

- **`references/` 12종(205MB)은 목록만 봤고 내용을 열지 않았다.** 그중
  `온실공사업체_2025_온실설치공사시공능력평가결과(수시분).pdf`는 **이미 리포 루트에 있고
  md5 `5115a74270432674658ef46c6c6c6240`·198,172바이트로 동일 파일임을 확인했다**
  (79차 대조). 따라서 실제 신규 편입 대상은 **11종**이다 — 별도 차수.
- **이 문서는 코드를 바꾸지 않았다.** 5절은 구현 지도이지 구현이 아니다.
- 패키지 계산식의 **수치 검증을 하지 않았다** — 표준 정의라 판단했을 뿐 각 식을
  원전과 대조하지는 않았다.
- `package_index.json`·`BOOTSTRAP_PROMPT.md`는 설치 편의 파일이라 판정 대상에서 제외했다.
- 패키지가 `1평 = 3.305785㎡`로 적는데 엔진 `PYEONG_TO_M2 = 3.3058`이다(정확값
  400/121 = 3.3057851…). 상대오차 4.5e-6으로 실무 영향은 없으나 **레지스트리가
  "확정 환산값 400/121"이라 적어 두고 반올림값을 저장**하고 있다 — 정밀화 후보로만
  기록한다(값 변경이라 임의로 손대지 않는다).

---

## 7. 한 줄 요약

> **분류체계·체크리스트·불확실성 원칙은 가져오고, 판정과 계산은 가져오지 않는다.**
> 계산은 엔진이 하고, 판정은 사람이 한다 — 이 패키지를 녹이는 유일한 방법이다.
