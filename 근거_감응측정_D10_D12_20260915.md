# D-10~D-12 감응 측정 — 결정의 선행조건을 채웠다 (145차)

**작성일** 2026-09-15 (145차) · **계기** 144차 ⑪후보 **(a)** — *"D-10~D-12의 감응을 모른다"*
**성격** 엔진 상수 값 변경 0 · 레지스트리 값 변경 0. **측정 차수**다. **결정은 내리지 않았다.**

---

## 0. 왜 측정인가

144차가 ★대기 13건을 모으며 남긴 한계: **D-10~D-12는 *"등재하면 무엇이 얼마나 움직이는가"*가
측정된 적이 없다.** 얼마나 움직이는지 모르는 채 결정하라고 두는 것이 병목의 실질이다.

⚠️ **시세성(노임단가·재료단가·크루 규모)은 주입만 받는다** — 그래서 **금액이 아니라 비율·구조**까지만 잰다.

---

## 1. 🔴 D-10 — 후보 설계 자체가 부정확했다

129차 F2는 *"★등재 후보가 플래그가 아니라 **품목별 요율값(0/2%/3%/5%)**이어야 한다"*고 적었다.
원문 `[주]`를 분모까지 읽으니 **요율은 두 계열이고 분모가 다르다**:

| 계열 | 분모 | 요율 | 품목 |
|---|---|---|---|
| **공구손료·경장비 기계경비** | **인력품(노무)** | **2% · 3%** | **29** |
| **잡재료·소모재료(실리콘)** | **주재료비** | **5%** | **2**(천장우레탄판넬·샌드위치판넬) |

> 온실피복공사 #4 샌드위치판넬 원문:
> ③ 공구손료 … 기계경비는 **인력품의 3%**로 계상한다
> ④ 잡재료 및 소모재료(실리콘)는 **주재료비의 5%**로 계상한다

→ 🔴 **한 품목이 두 요율을 갖지만 모순이 아니다** — **다른 항목, 다른 분모**다.
→ 🔴 **"품목별 요율값" 필드 하나로는 담을 수 없다. 두 필드가 필요하다.**
  (`공구손료율`[노무 기준] · `잡재료율`[재료 기준])

### 감응

- 공구손료 규정이 있는 품목 **29 / 64**, 그 품목들이 **인·일 계수의 66.5%**를 차지한다.
- **가중 평균 공구손료율 ≈ 노무비의 1.98%**.
- 🔴 **선홈통 모순(2% vs 3%)의 감응은 0.0059%p다.** 113차부터 `[확인요망]`으로 끌어온
  그 항목은 **집계 수준에서 결정을 막지 않는다** — 품목 단위 정확도에만 영향한다.
- ⚠️ **잡재료 5%는 재료비 기준이라 인·일로 비중을 잴 수 없다** — 건수(2건)까지가 한계다.

---

## 2. D-11 — 크루는 공기를 **반비례로만** 움직인다

`pumsem_project_labor_summary`에 **전 품목 물량 1.0**을 넣으면 총 **11.920 인·일**(직종 7종:
철골공 5.678 · 조력공 5.727 · 특별인부 0.373 · 철근공 0.041 · 내장공 0.040 · 보통인부 0.031 ·
유리공 0.030).

| 크루 | 공기(= 총 인·일 ÷ 크루) |
|---|---|
| 1.0명 | 11.92일 |
| 8.0명 | 1.49일 |
| 11.9명 | 1.00일 |
| 16.4명 | 0.73일 |

→ **구조는 단순 반비례**다. 📌 **124차 실측 크루 범위 1.0~16.4** → 같은 물량에서 공기가
**16.4배** 갈린다. **즉 크루 값을 고르는 것이 곧 공기를 정하는 것**이고, 그래서 판단성이다.

⚠️ 물량 1.0은 **절대 공기가 아니다** — 구조를 보이는 수치다.

---

## 3. ✅ D-12 — 산출물 감응이 **0**이다

| 확인 | 결과 |
|---|---|
| 엔진 선언 순서 vs 원문 차례 | **9개 중 3자리가 다르다**(2·3·4번째에서 알루미늄·온실피복·천창개폐가 회전) |
| 집합 | **같다** |
| `pumsem_*` 호출 | `build_site.py`·`webapp.py`·`render_report.py`·`run_report.py`·`cases.py` **전부 0회** |
| `pumsem_project_labor_summary` 출력 순서 | **입력 dict 순서**를 따른다 — 선언 순서가 아니다 |

→ **재정렬해도 산출물은 한 글자도 안 바뀐다.** 움직이는 것은 **엔진 소스의 줄 순서**와
**125·129차 가드가 비교하는 순번 매핑**뿐이다.

⚠️ 그래서 D-12는 **"산출물 결정"이 아니라 "가독성·대조 편의 결정"**이다. 재정렬하면
**`(공종, 순번) → 품목` 매핑이 원문과 1:1이 되어** 이번 D-10 측정 같은 작업이 쉬워진다.

---

## 4. 세 항목의 성격이 서로 다르다

| # | 성격 | 결정에 필요한 것 |
|---|---|---|
| **D-10** | 🔴 **후보 설계를 고쳐야 한다**(한 필드 → 두 필드) | 설계 승인. 감응은 **노무비의 약 2%**로 측정됐다 |
| **D-11** | **값을 고르는 것이 곧 결과** — 크루 1.0~16.4로 공기가 16.4배 | 크루 값 선택(판단성) |
| **D-12** | **산출물 감응 0** — 소스 정리에 가깝다 | 가벼운 결정 |

---

## 5. 검증하지 않은 것

- **엔진 상수 값도 레지스트리 값도 하나도 바꾸지 않았고, 결정도 내리지 않았다.**
- 🔴 **금액을 산출하지 않았다** — 노임단가·재료단가가 **시세성**이라 주입만 받는다.
  D-10의 "노무비의 1.98%"는 **비율**이지 금액이 아니다.
- **D-11의 절대 공기를 구하지 않았다** — 물량 1.0 가정이다. 실제 공기는 **표준설계 물량**이
  있어야 나오고, 그 물량은 **품셈 표준설계(1ha)** 기준이라 케이스에 그대로 쓸 수 없다.
- **크루 1.0~16.4의 출처는 124차 공사일보 실측**이고 **이번에 재확인하지 않았다**.
- **D-12 재정렬을 실제로 해보지 않았다** — 하면 125·129차 가드의 순번 매핑이 함께 움직인다.
- `(공종, 순번) → 품목` 매핑은 **선언 순서 = 원문 순번**을 전제한다 — 125차 가드가 그것을
  보증하지만 **재정렬 후에는 다시 확인해야 한다**.
