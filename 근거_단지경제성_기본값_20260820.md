# 근거: 단지(클러스터) 경제성 기본값 2종 — 조사 결과 근거 미확보

**작성일**: 2026-08-20 (74차)
**대상**: `CLUSTER_SCALE_SAVING_RATE`(0.15) · `CLUSTER_SUBSIDY_RATE_SHARED`(0.7)
**계기**: 16회차 F8 지적 → 17회차 F1 재지적 → 사용자 지시 "cluster_economics 2건도 등재해라"

---

## 1. 왜 이제야 등재하는가

두 값은 `cluster_economics()`의 **함수 기본인자**로만 존재해, 모듈 상수를 훑는 점검에
걸리지 않았다. 그 결과 레지스트리에도 없었고 **대조가능성 감사의 검사 대상도 아니었다.**

- 16회차 F8: "'전수 점검'이 모듈 상수만 훑어 함수 기본인자 2건 누락"
- 17회차 F1: "73차가 PYEONG_TO_M2를 '마지막 미등재 수치 상수'라 닫았으나 이 2건이 남아 있다"

72차가 난방 3종(`HEATING_EFFICIENCY_DEFAULT`·`DEGREE_HOURS_DEFAULT`·`HEATING_SAFETY_FACTOR`)을
승격한 사유가 그대로 적용된다. 이번 74차로 **함수 기본인자 계열의 미등재는 해소된다**
(※ 이번에도 "마지막"이라 단정하지 않는다 — 17회차 F1의 교훈이다. 현재 확인 범위는 아래 §4).

## 2. 출처 조사 결과 — 두 값 모두 근거 0건

| 검색 대상 | 결과 |
|---|---|
| 청킹 인덱스 "규모의 경제" | 0건 |
| 청킹 인덱스 "공동시설 보조" | 0건 |
| 청킹 인덱스 "보조율" | 0건 |
| 청킹 인덱스 "집적화" | 0건 |
| 청킹 인덱스 "공동이용시설"·"클러스터"·"규모화"·"단지화"·"공동경영체" | 각 0건 |
| 청킹 인덱스 "절감률" | 1건 — 난방 **열**절감률(김평화 p.49)이라 무관 |
| 엔진 주석 | 없음(승격 전까지 주석 0줄) |

검색어를 8종으로 넓혀 재확인했다(16회차 F9 교훈 — "리포 내 근거 0건"이 검색어가 좁아서
나온 잘못된 결론이었던 사례가 있다). **0.15·0.7을 지지하는 근거는 여전히 0건**이다.

⚠️ **위 표는 청킹 인덱스만 검색한 결과다** — 18회차 F1·F3이 이를 반증했다.
청킹 인덱스는 `스마트팜스펙/`(49,122청크)·`시설평가/`(64,295청크) **2개 폴더만** 커버하고
— `스마트팜연구DB/`·`기자재DB/`·`소득분석DB/`·`스마트팜_단계별_엑셀패키지/`·루트 문서는
**아예 포함되지 않는다** — 본문도 **200자로 절단**되므로,
**인덱스 0건은 "리포에 없음"을 뜻하지 않는다.** 실제로 리포를 직접 훑으면 `0.7`과 값이 같은
기록이 나온다:

| 발견 위치 | 내용 |
|---|---|
| `스마트팜_단계별_엑셀패키지/17_정책별_자기부담금_시뮬레이터.xlsx` | **청년창업형 스마트팜 보조금 비율 70%**(확산 50·에너지 60·현대화 40) |
| `smartfarm_engine.py` L2440 | "그 시트가 예시로 든 보조율 수치(50%·**70%**·50~70%)…" |
| `통합스마트팜 ROI 지침_250914.docx` | "정부지원 시나리오: 보조율 30~50%" |

**0.15 쪽에도 관련 관측이 있었다**(18회차 F9 — 초판이 한쪽만 닫은 **비대칭**이 실제 결함):

| 발견 위치 | 내용 |
|---|---|
| `스마트팜연구DB/농업부문에너지이용실태.pdf` p33·p40·p78 | "재배규모가 클수록 단위면적당 사용량은 감소하여 **규모의 경제**가 실현" |
| 같은 문서 p40 표 3-15 | 오이 농가 규모구간별 단위 경유 사용량: **20.4 → 17.5 → 16.4 → 17.5 → 12.4** |

⚠️ **근거로 승격하지 않는다**: ①이것은 **단일 농가 재배면적**의 규모 경제이지 **단지
공동운영**의 경제가 아니다(72차 F1이 지적한 범주 확장을 반복하지 않는다) ②계열이 단조롭지
않아 인접 구간 -6~-14%·최대 구간 -39%로 **0.15를 사이에 두고 벌어진다** — 반증도 아니다.

즉 **두 값 모두 "값의 출처"는 여전히 미확보**이나, "근거 없음"이라는 초판 서술은 거짓이었다.
값 일치가 출처 증명이 아니듯(69차 교훈) 관련 문헌의 존재도 유래 증명은 아니지만, **관측을
한쪽에만 싣는 것**은 그 자체로 편향된 서술이다.

## 2-A. 🔴 자기모순 관측 (18회차 F1 파생)

위 `smartfarm_engine.py` L2440 주석의 전문은 이렇다:

> 그 시트가 예시로 든 보조율 수치(50%·70%·50~70%)는 **공모 회차마다 바뀌는 값이라
> 여기 옮기지 않는다** — 근거 없는 값 금지.

**그런데 `CLUSTER_SUBSIDY_RATE_SHARED = 0.7`이 정확히 그 옮기지 않기로 한 값이다.**
1절 원칙의 "시세성 값(단가·노임·유가·금리)은 조회하지 않는다 — 인자로 주입만 받는다"에
보조율이 해당한다고 보면, **이 상수는 기본값을 갖는 것 자체가 재검토 대상**이다
(인자 필수화 등). 74차 승인 범위는 "등재"까지이므로 값·시그니처는 손대지 않고 관측만 남긴다. **값은 바꾸지 않았다** — 근거 없는 값을 다른 근거 없는
값으로 교체하는 것은 개선이 아니다(71차 `ENV_WEIGHTS` 선례, 1절 원칙 "근거 없는 값 금지").

### ⚠️ 관측 하나 — 근거도 반증도 아님

리포에서 확인되는 **유일한** 보조율은 「2026년 스마트농업 육성 시행계획」(농식품부, 리포 보존)
**p12**의 다음 한 줄이다:

> ** (보조율) 국비 25%, 지방비 30%, 융자 25%, 자부담 20%

(PDF 12쪽 = 인쇄면 "- 8 -", 오프셋 +4. 줄 앞의 `**`는 각주 표시로 **스마트팜ICT융복합확산
사업**에 걸려 있다 — 18회차 F5)

보조 합계는 **55%**다. 검색을 넓히자 **두 번째 보조율**도 나왔다 —
「[붙임4] 스마트팜 온실 신개축 지원사업 '25년 사업자 공모계획」 **p4**:

> (지원비율) 국비 20%, 지방비 30, 융자(이차보전) 30, 자부담 20

이쪽은 보조 **50%**다. 즉 리포에서 확인되는 보조율은 **55%와 50%** 두 건이고 **둘 다 엔진의
0.7과 다르며, 사업마다 값이 다르다**는 사실 자체가 드러난다. 위 둘은 **농가 개별사업** 보조율이고 이 상수는 **단지 공동시설** 몫이라 성격이 다르다.
초판은 "공동시설 쪽은 리포에서 확인되지 않는다"고 적었으나 **이 역시 사실이 아니다**
(18회차 F2) — 단지 기반조성 재원분담 1차 자료가 2건 실재한다:

| 문서 | 재원분담 |
|---|---|
| `스마트팜스펙/장성군 스마트농업 육성지구 조성사업 … 과업설명서.pdf` p2 | 사업비 39,149 / 국비 10,925 / 지방비 24,909 / 자부담 3,315 → **보조 91.5%** (기반조성 13.11ha, 공동육묘장·공동 축열조 포함) |
| `스마트팜스펙/과업지시서_제주 지역특화 임대형 스마트팜 … .pdf` p2 | 24,200 / 14,000 / 10,200 / 자부담 - → **보조 100%** (기반조성 10.67ha) |

둘 다 0.7 그 자체는 아니지만 **0.7보다 높은** 값이라, 초판이 유일 관측으로 세운 55%(0.7보다
낮음)와 방향이 반대다 — 한쪽만 싣는 서술은 균형을 잃은 것이었다. 따라서 이 관측은 **0.7의
근거로도, 반증으로도 쓰지 않고** 사실만 기록한다(17회차 교훈: 확인한 것보다 넓게 말하지 않는다).

## 3. 도달성 — 수치 영향 0

12~14회차가 세 차수 연속 지적한 항목이라 **먼저 확인했다**:

```
cluster_economics 참조 (전 모듈 grep):
  smartfarm_engine.py        ← 정의(줄번호는 주석 삽입으로 이동하므로 적지 않는다)
  test_engine.py:478-479     ← 테스트 1건
```

`build_site.py`·`webapp.py`·`render_report.py`·템플릿 어디에도 호출이 없다. 즉 두 값은 현재 **계산에 참여하지 않으며**(등재 이후 근거대장 HTML에는 상수 행으로
렌더된다 — 18회차 F6), 이번 등재의 효과는 "다음에 이 함수를 쓸 때
근거 상태를 알 수 있게 하는 것"에 한정된다.

## 4. 확인 범위와 남는 과제

이번 조사는 `smartfarm_engine.py`·`build_site.py`·`webapp.py`·`render_report.py`의
**함수 기본인자**를 AST로 훑어 근거가 필요한 수치를 찾은 것이다(자명값 0/1/2/100,
수치해법 파라미터 `irr(tol/it)`, 표시 폭 `_trunc(n)`은 제외).

- 두 값의 원출처 — 확보 시 status 재판정
- 공동시설 보조율의 공공 근거(공모지침 계열) — 리포에 해당 문서 없음
- 위 4개 모듈 밖(스크립트·파서 계열)의 리터럴은 이번 범위에 넣지 않았다
