# 대조 기준 보강 — 검산되지 않던 9건, 그리고 재검산기를 리포 도구로 (139차)

**작성일** 2026-09-15 (139차) · **계기** 138차 ⑩후보 **(g)**
**성격** 엔진 상수 값 변경 0 · 레지스트리 **값** 변경 0. 바뀐 것은 **ref note 9건**과
**신설 도구 `verify_refs.py`**다.

---

## 1. 왜 이것이 문제였나

138차는 `exact` 39건을 대조하면서 **9건은 대조하지 못했다** — `note`에 **금액이 하나도 적혀
있지 않아 무엇과 맞춰야 할지 알 수 없었다**.

`audit_traceability.py`의 `_ref_ok()`는 **파일 실재만** 본다. 그래서 대조 기준이 없는 ref는
**게이트도 그 사실을 모르고**, 결과적으로 **앞으로도 영원히 검산되지 않는다.**

---

## 2. 9건에 앵커를 적었다 — 적기 전에 원문에서 확인했다

📌 **138차 교훈**: 적어 두기만 하면 **검산된 척**이 된다. 그래서 **먼저 원문에서 찾고** 적었다.

| 상수 | 원문 | 앵커 | 확인 |
|---|---|---|---|
| `ACTUALS_COUNT` | 이두희 | **582,455,045**(2,736㎡) | ✅ |
| `ACTUALS_COUNT` | 최혁진 | **930,000,000**(3,459㎡) | ✅ |
| `ACTUALS_COUNT` | 한일그린텍 | **480,636,000**(3,202㎡) | ✅ |
| `ACTUALS_COUNT` | 한수진 | **618,001,000**(4,092㎡) | ✅ |
| `ACTUALS_COUNT` | 최선동 | **613,782,000**(3,145㎡) | ✅ |
| `CAPEX_MAJOR_CASE_CHUNKS` | 윤성호 | **1,162,078,090** | ✅ |
| `CAPEX_MAJOR_CASE_CHUNKS` | 조윤정 | **398,628,383**(쌍 견적 통합) | ✅ |
| `CAPEX_CATEGORY_OBSERVED_RANGE` | 최혁진 | **694,575,784** | ✅ |
| `CAPEX_CATEGORY_OBSERVED_RANGE` | 우민재 | **456,158,140** | ⚠️ **재집계**(138차 기록) |

→ **8건은 원문에 인쇄돼 있고**, 1건은 138차가 밝힌 **재집계값**이라 그 사실을 함께 적었다.

### 전수 재검산이 닫혔다

```
138차: OK 24 · 재계산 6 · 대조할 금액 없음 9 · 확인 불가 0
139차: OK 32 · RECOMPUTED 7 · MISS 0 · UNK 0   ← 대조 못 한 건 0
```

---

## 3. ✅ 재검산기를 리포 도구로 남겼다 — `verify_refs.py`

138·139차의 판독 로직이 **임시 스크립트로 흩어지면 다음 차수가 다시 짠다**(그러면 파서
결함도 다시 난다). 모듈로 고정했다.

```
python verify_refs.py          # 정적 검사 — 앵커가 있는가(빠르다, 게이트에서 돈다)
python verify_refs.py --full   # 원문 16종을 열어 전수 재검산 → verify_refs_dump.txt
```

📌 **판독 규율을 모듈 docstring에 박아 뒀다**(138차에 두 번 고친 것):
PDF가 숫자를 **자간 공백으로 쪼개** 렌더하고(`3 70,315,435`), 공백을 **통째로 지우면 인접한
두 숫자가 붙는다**. → **같은 줄에서 x 간격 4px 안의 숫자 토큰만** 결합한다.

🔴 **인쇄돼 있지 않은 앵커는 note가 재집계임을 선언해야 한다** — 도구가 그 선언을 요구하고,
없으면 `MISS`로 뜬다. 선언 없이 조용히 통과하는 길을 막는다.

---

## 4. 🔴 가드가 도구를 신뢰만 하고 있었다 — 변이가 잡았다

변이 5종 중 **2종이 처음에 통과**했다:

| 변이 | 왜 통과했나 | 조치 |
|---|---|---|
| **M3** `RECOMPUTED` 표지에 **빈 문자열** 추가 | 빈 문자열은 **모든 note에 매칭**돼 인쇄되지 않은 값이 전부 통과한다 | 표지에 빈 값이 없는지 검사 |
| **M5** `static_check` 무력화 | **앵커 없는 ref가 0건이라 결과로는 구별되지 않는다** | **red 자기검증** — 합성 입력(앵커 없는 ref 1건)을 먹여 도구가 실제로 잡는지 본다 |

→ 📌 **M5는 26회차 F3(*"가드가 한 시트만 봤다"*)과 같은 계열**이다: **가드가 지킨다고 적힌
것을 실제로는 안 지키고 있었다.** `test_cases.py`가 `_ref_ok`에 쓰던 **red 자기검증 선례**를
가져왔다.

---

## 5. 검증하지 않은 것

- **엔진 상수 값도 레지스트리 값도 하나도 바꾸지 않았다.** `상수 54` · `refs 148` 불변.
- 🔴 **앵커가 "그 문서 안에 있다"는 것이 "올바른 셀·행에서 왔다"는 뜻은 아니다**(138차와 같은
  한계). 예컨대 `618,001,000`이 한수진 파일 어딘가에 있음은 확인했으나 **그것이 총공사비 행에서
  왔는지**는 91·92차의 당시 판독을 신뢰했다.
- **`ACTUALS_COUNT`의 면적값(2,736·3,459·3,202·4,092·3,145)은 앵커로 넣지 않았다** —
  금액만 넣었다(면적은 26회차 F4·`AC5`가 다루는 별건이다).
- **법령 별표 6건과 `partial`/`near` 58건은 범위 밖**이다 — `exact` 39건만 다뤘다.
- `verify_refs.py --full`은 **게이트에서 돌지 않는다**(원문 16종을 열어 느리다). 게이트는
  **정적 검사 + 스냅샷 대조**만 하고, 전수 재검산은 **앵커를 바꿀 때 사람이 돌린다**.
