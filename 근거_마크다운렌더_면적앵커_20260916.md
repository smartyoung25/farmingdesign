# 남긴 둘을 닫았다 — 마크다운 리터럴 3,833 → 2 · 면적 앵커 (148차)

**작성일** 2026-09-16 (148차) · **계기** 사용자 지시 *"확인하지 못한 것 남긴 것도 개선하라"*
**성격** 엔진 상수 값 변경 0 · 케이스·견적비교 불변 · 결정 0.
바뀐 것은 **생성기(`build_site.py`)·검산기(`verify_refs.py`)와 ref note 1건**이다.

> 146차 [3]과 [5]는 *"후보로만 올린다"* · *"기록만 한다"*로 남아 있었다. 둘 다 닫는다.
> 🔴 **[5]를 열자 곧바로 결함이 하나 나왔다** — 이두희 면적 2,736의 근거 표기가 부정확했다(§3).

---

## 0. 결과

| | 146차 | 148차 |
|---|---|---|
| 산출물 HTML의 리터럴 `**` | **3,833** | **2** |
| `full_check` | OK 32 · RECOMPUTED 7 | **OK 31 · RECOMPUTED 8 · MISS 0** |
| exact ref 앵커 총수 | 51 | **57** |

남은 2개는 **코드 스팬 안**(`` `<그림 7-3> 비닐 온실평면도(**연동형**)` ``)이라 **리터럴이 맞다** —
코드 안의 마크다운은 렌더하지 않는 것이 올바른 동작이다.

---

## 1. [3] `build_site`가 마크다운을 렌더하지 않았다

146차가 밝힌 원인: 레지스트리 `note`·`source`와 `CAPEX_MAJOR_EVIDENCE_STATUS`는
**마크다운으로 쓰여 있는데** 생성기가 `html.escape`만 걸어 내보냈다.
146차는 CAPEX분해 80개만 보고 *"제거는 후보"*라 했고, 147차가 **근거대장 3,745개**를 더 쟀다.

### 고른 길과 고르지 않은 길

| 안 | 내용 | 판정 |
|---|---|---|
| (가) | 레지스트리·엔진 서술 문자열에서 `**`를 뺀다 | ❌ **원문이 손상된다.** 강조는 저자 의도이고, 엔진 문자열 변경은 112차 이후 민감한 조작이다 |
| **(나)** | **생성기가 렌더하게 한다** | ✅ **버그가 생성기에 있으므로 고칠 자리도 생성기다.** 데이터는 한 글자도 안 건드린다 |

### `md()` — 보수적으로 셋만

```python
_MD = re.compile(
    r"`([^`]{1,200})`"                  # `코드`
    r"|\*\*(.{1,200}?)\*\*"             # **굵게**
    r"|\*(&quot;.{1,200}?&quot;)\*"     # *"인용"*
)

def md(s):
    return _MD.sub(_md_sub, esc("" if s is None else str(s)))
```

**원칙 — 이스케이프가 먼저다.** 렌더러는 escape된 문자열 위에서만 돌고, 우리가 넣는
태그 외에 `<`를 만들지 않는다(원문의 `<`는 이미 `&lt;`다).

🔴 **일반 `*이탤릭*`은 지원하지 않는다.** 레지스트리에 `ㅁ60*60*2.3T`·`4000*4000` 같은
**치수 표기가 21곳** 있어서, 단일 별표를 이탤릭으로 읽으면 **규격이 깨진다**. 그래서
이탤릭은 `*"…"*`(큰따옴표를 낀 인용 관용구)만 잡는다.

설계를 실측 위에 세웠다:

| | 실측 |
|---|---|
| `**굵게**` 스팬 | **1,887** (안에 단일 별표가 낀 것 **1건** · 줄바꿈 **0** · 최장 85자) |
| `` `코드` `` 스팬 | 443 |
| `*"인용"*` 스팬 | 130 (그중 **23은 안에 굵게를 품는다** → 한 겹 재귀로 처리) |
| `**` 개수가 홀수인 문자열 | **0** (짝이 맞는다) |

교대(alternation) **하나로 왼쪽에서 오른쪽으로 한 번만** 훑으므로 코드 스팬 안에서
굵게가 시작되는 겹침이 생기지 않는다.

### 자르면 짝이 깨진다 — `md_cut()`

`_trunc(s, 160)`이 `**` 쌍 가운데를 자르면 리터럴 별표 하나가 화면에 남는다
(148차 실측: `U_DESIGN` status_note 1건). 자른 뒤 **짝이 안 맞는 `**` 하나를 버린다.**

### 동작 확인

| 입력 | 출력 |
|---|---|
| `**굵게** 보통` | `<b>굵게</b> 보통` |
| `*"인용 안에 **굵게**가 있다"*` | `<i>&quot;인용 안에 <b>굵게</b>가 있다&quot;</i>` |
| `ㅁ60*60*2.3T 와 4000*4000` | **그대로**(치수 보존) |
| `<script>alert(1)</script>` | `&lt;script&gt;alert(1)&lt;/script&gt;` |
| `짝 안 맞는 ** 하나` | **그대로**(깨진 HTML을 만들지 않는다) |
| `**…160***이다` | `<b>…160</b>*이다` |

적용한 자리는 **서술 필드만** 17곳이다. ⚠️`title=` 속성에 들어가는 `esc(note)`는
**바꾸지 않았다** — 속성 안에서는 태그가 리터럴로 보이므로 escape가 맞다.

---

## 2. [5] 앵커 폭 — 146차가 *"영향 0"*이라 했으나 **영향이 있었다**

146차는 *"`NUM`이 7자리 이상만 잡는다. 사실이나 앵커가 0이 되는 ref는 없다"*며 넘겼다.
맞는 관찰이지만 **결론이 좁았다**: 앵커가 0이 되지 않을 뿐, **면적이 검산 대상에서 빠져 있었다**.

### ① 단위가 붙은 것만 잡는다

```python
AREA = re.compile(r"(?<![\d,.])\d{1,3}(?:,\d{3})(?![\d,])(?=\s*(?:㎡|m2|m²|평))")
```

폭을 **일반적으로** 넓히지 않았다 — 그러면 연도(`2021년`)·수량(`1,200개`)이 앵커로 들어와
거짓 불일치가 는다(**138차 P2 과잉교정과 같은 함정**). 단위가 **바로 뒤에** 붙은 것만 잡는다.

문서 쪽(`NUM_DOC`)은 넓혀도 안전하다 — 찾은 숫자 집합이 커지면 MISS가 OK로 바뀔 뿐
**새로운 요구를 만들지 않는다**(요구는 note의 앵커가 만든다).

### ② 🔴 `\b`가 한글 접미사 앞에서 실패하고 있었다

만들면서 더 큰 것이 나왔다. 종전 패턴은 `\b\d{1,3}(?:,\d{3}){2,}\b`인데,
**한글은 `\w`**라서 끝의 `\b`가 성립하지 않는다:

| 문자열 | 종전 | 지금 |
|---|---|---|
| `40,093,200원` | ❌ **못 잡음** | ✅ `40093200` |
| `4,092평` | ❌ 못 잡음 | ✅ `4092` |
| `2,736㎡` | ❌ 못 잡음 | ✅ `2736` |
| `456,158,140` 안의 `456,158` | (배제) | ✅ 여전히 배제 |
| `2021년`·`1,200개` | (미포함) | ✅ 여전히 미포함 |

경계를 `(?<![\d,.])` … `(?![\d,])`로 바꿨다. **금액 뒤에 「원」이 붙은 앵커가 통째로
보이지 않던 것**이 이것 때문이다. exact ref 앵커 총수가 **51 → 57**로 늘었다.

---

## 3. 🔴 앵커를 넓히자 나온 것 — 이두희 2,736은 **역산값**이다

`--full`을 돌리자 **MISS 1건**이 떴다.

```
MISS  ACTUALS_COUNT  스마트팜스펙/이두희/이두희 천안 20251028.pdf
      인쇄 없음 ['2,736'] — note에 선언이 없다
```

원문 53쪽을 직접 훑었다 — **`2,736`은 어떤 표기(공백·소수점 변형 포함)로도 0건**이다.
그 문서에서 `㎡`가 붙은 숫자는 `5,117 · 3,750 · 6,185 · 8,727 · 1,670 · 835 · 105 · 242.4` 계열뿐이다.

**리포는 이미 알고 있었다.** `smartfarm_engine.py:3226·3255`가 이렇게 적는다:

> 이두희 2,736은 규격 8m×6연동×57m 역산값이라 **골조 순면적(방풍 미포함)**이고 …

`8 × 6 × 57 = 2,736` ✅

**틀린 것은 139차가 쓴 ref note**다 — *"ACTUALS 총공사비 582,455,045(면적 2,736㎡) — 문서에서
확인"*이라고 **금액과 면적을 한 괄호로 묶었다**. 금액은 문서에서 확인한 것이 맞지만
**면적은 역산**이다. 근거가 다른 둘을 한 문장으로 선언했다.

→ note를 정정하고 **역산임을 선언**했다(140차 `RECOMPUTED` 규율). 분류가
**MISS → RECOMPUTED**로 바뀌며 닫혔다. 합은 39로 같다(OK 32·R 7 → OK 31·R 8).

⚠️ **면적값 2,736은 바꾸지 않았다.** 역산 근거가 엔진에 기록돼 있고, 94차가 이미
*"ACTUALS의 면적 기준이 표본마다 같지 않다"*(이두희만 방풍 미포함)를 ★사용자 결정으로
올려 뒀다. **이 차수는 표기만 고쳤다.**

---

## 4. 같은 함정을 두 번 밟았다 — 레지스트리에 생따옴표

146차에 이어 이번에도 **정정문에 `"`를 그대로 넣어 JSON을 깨뜨렸다**
(`*"문서에서 확인"*`). 레지스트리는 **JSON 문자열 안**이라 생따옴표가 들어가면 파싱이 죽는다.
두 번 다 즉시 발견했지만(파일을 쓰기 전 `json.load` 검증), **관례로 굳힌다**:

> 레지스트리·엔진 문자열에 인용을 넣을 때는 **「」를 쓴다.** `"`는 쓰지 않는다.

가드로 고정했다(`test_148cha_*` ③).

---

## 5. 검증하지 않은 것 / 하지 않은 것

- **엔진 상수 값·케이스·견적비교 전부 불변.** 레지스트리는 **ref note 1건**만 고쳤다.
- **면적 기준 통일(이두희 방풍 미포함)은 손대지 않았다** — 94차 이래 ★사용자 결정이다.
- **일반 `*이탤릭*`을 지원하지 않는다** — 치수 표기 21곳이 깨지기 때문이다(§1).
- **`title=` 속성의 escape는 그대로** 뒀다 — 속성 안에서는 태그가 리터럴이다.
- **도면 부재 규격은 여전히 등재하지 않았다**(147차와 같음) — 이 리포의 계산이 쓰지 않는
  값을 상수로 올리면 검증 부채만 는다.
- **등급은 올리지 않았다** — 분포 exact 90 / partial 50 / near 8 불변.
- **결정은 하나도 내리지 않았다.** D-1~D-15 전부 ★ 대기다.
