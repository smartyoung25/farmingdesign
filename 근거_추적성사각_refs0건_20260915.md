# 추적성 사각 — `source_refs` 0건 상수 16개 점검 (133차)

**작성일** 2026-09-15 (133차) · **계기** 132차 ⑨후보 **(f)** — OP1이 드러낸 감사 사각이
그 상수 하나만의 문제인지 확인
**성격** 엔진 상수 값 변경 0 · 레지스트리 **값** 변경 0. **감사기가 사각을 드러내도록 고쳤다.**

---

## 1. 🔴 기제 — ref가 없으면 검사도 없다

`audit_traceability.py`의 레지스트리 검사는 이렇게 생겼다:

```python
for r in ent.get("source_refs", []):
    if not _ref_ok(r):
        hard.append((key, f"원문 소실 {r.get('file')}"))
```

**빈 리스트는 0회 순회한다.** 즉 `source_refs`가 없는 상수는 **원문 실재 검사를 한 번도
받지 않는다.** 게이트가 검사하는 것은 `status`가 enum 안에 있는지뿐이다 —
그리고 `"실측"`은 enum 안에 있다.

→ **`status: "실측"`이라고 적기만 하면 게이트를 통과한다.**

---

## 2. 전수 — 54개 상수 중 16개가 `source_refs` 0건

| status | refs 있음 | refs 없음 |
|---|---|---|
| 공공기준 | **16** | 0 |
| 법정기준 | **5** | 0 |
| 실측 | 5 | 🔴 **11** |
| 부분실측 | 4 | 🔴 **1** |
| 참고기준 | 2 | 2 |
| 추정 | 4 | 1 |
| 확인요망 | 2 | 1 |

### 🔴 패턴이 정확히 거꾸로다

**법령·고시에서 온 21개(공공기준·법정기준)는 전부 ref가 붙어 있다.**
**우리가 원문서를 읽어 전사한 "실측" 16개 중 11개(69%)는 0건이다.**

→ **전사 오류가 가장 일어나기 쉬운 계열이 검사를 가장 안 받는다.**

---

## 3. 12개 상수가 "실측"을 표방하며 검사를 0회 받는다

| 상수 | 무엇인가 | 산출물 도달 |
|---|---|---|
| `SPEC_TABLE` | 내재해형 규격 **249종** | ✅ `select_specs()` → `render_report.py`·`run_report.py` |
| `SPEC_COUNT` | 그 종수(249) | 파생 |
| `REGION_DESIGN_LOAD` | 지역별 설계적설심·풍속 **172지역** | ✅ `siting_design_load()` → **`webapp.py`** |
| `PUMSEM_ITEMS` | 품셈 **64계수** | ✅ `pumsem_labor_days()` |
| `OVERHEAD_RATES` | 제경비율(보험·안전·일반관리·이윤) | ✅ `apply_overheads()` |
| `OPEX_ITEM_CATEGORIES` | OPEX 25항목 | 🔴 **132차: 원문 0바이트** |
| `CAPEX_CASE_CHUNKS` | 케이스별 공종 원자료 | 관측 범위의 산출 근거 |
| `CAPEX_MAJOR_CATEGORIES` | 13 카테고리 구조 | 분류 골격 |
| `CAPEX_MAJOR_EVIDENCE_STATUS` | 카테고리별 근거 상태 | 근거 표기 |
| `CAPEX_MAJOR_UNCLASSIFIED` | 미분류 잔액 | 잔차 대조 |
| `RFQ_REQUIRED_CATEGORIES_DEFAULT` | RFQ 필수 공종 | `generate_rfq_package()` |
| `EQUIPMENT_DB_META` | 기자재DB 메타(부분실측) | `equipment_lookup()` 등 |

⚠️ **`REGION_DESIGN_LOAD`가 가장 날카롭다** — `webapp.py:348`이 조회 결과에
`status: "실측"`을 **찍어서 내보낸다**. 그 "실측"의 근거는 **레지스트리 서술뿐**이고,
원문(농식품부 보도자료 첨부표)은 **리포에 없으며**, 감사기는 **검사하지 않는다**.

---

## 4. 성격은 셋으로 갈린다 — 전부가 결함은 아니다

### (A) 🔴 붙일 수 있는데 안 붙었다 — **원문이 리포에 실재한다**

| 상수 | 리포 내 원문 |
|---|---|
| `PUMSEM_ITEMS` | `시설평가/202201_스마트팜 표준화_품셈.pdf` **(89,821,538 B)** + 근거문서 **9종** |
| `ELECTRICAL_PUMSEM_LUMP_WON_PER_HA` | **같은 품셈 PDF**(1ha 표준설계 집계표) — 서술이 파일명을 안 적었다 |
| `CAPEX_CASE_CHUNKS`·`CAPEX_MAJOR_EVIDENCE_STATUS`·`CAPEX_MAJOR_UNCLASSIFIED` | **형제 상수가 이미 refs를 갖고 있다** |
| `EQUIPMENT_DB_META` | `기자재DB/` **CSV 8종**(런타임에 실제로 읽는 파일) |

🔴 **형제 상수끼리 갈린다**: 같은 견적 원문에서 나온
`CAPEX_MAJOR_KNOWN_TOTALS`는 **refs 15건**, `CAPEX_MAJOR_CASE_CHUNKS`는 **16건**인데,
**같은 문서를 근거로 삼는** 세 형제는 **0건**이다.

→ **원문이 없어서가 아니라 붙이지 않아서다.**

### (B) 원문이 리포 밖이다

| 상수 | 원문 | 상태 |
|---|---|---|
| `SPEC_TABLE`·`SPEC_COUNT` | 농사로 마스터 xlsx(2025-108호, 249종) | 🔴 리포에 없다 |
| `REGION_DESIGN_LOAD` | 농식품부 보도자료 첨부 참고2·참고3 | 🔴 리포에 없다 |
| `OPEX_ITEM_CATEGORIES` | 농진청 코드 CSV | 🔴 **0바이트**(132차) |
| `OVERHEAD_RATES` | 원가계산서 6건 중 일부는 Google Drive | 부분적으로 밖 |
| `HEATING_VERIFY_REF_KCAL_H_M2` | `SmartFarm_엔진데이터.md` A-12 | ✅ **부재가 명시돼 있다** |

→ **웹·클라우드에서 읽고 리포에 보존본을 남기지 않은 계열**이다.
📌 다른 웹 출처는 보존본을 남겼다 — `FR_TABLE`은 농사로 웹표를
`근거_농사로_스크린보온력표_20260819.md`로 보존하고 **거기에 ref를 건다**.
**그 관례가 이 셋에는 적용되지 않았다.**

### (C) 원문 ref 개념이 없다 — 파생·결정

| 상수 | 성격 |
|---|---|
| `CAPEX_MAJOR_CATEGORIES` | **사용자 제안 분류표**(2026-07-16 대화) — 결정 기록이지 원문서가 아니다 |
| `RFQ_REQUIRED_CATEGORIES_DEFAULT` | `CAPEX_MAJOR_EVIDENCE_STATUS`에서 **도출** |
| `SPEC_COUNT` | `SPEC_TABLE`의 **길이** |
| `ENV_WEIGHTS` | ✅ `추정` + *"근거문서 미확보"* **명시** |
| `SUBSIDY_APPLICATION_PROCEDURE` | `참고기준` — 수치 미포함 업무절차 |

→ **(C)는 결함이 아니다.** 다만 **(A)·(B)와 섞여 있어 구분이 안 됐던 것**이 문제다.

---

## 5. ✅ 133차가 한 것 — 게이트가 드러내게 했다

`audit_traceability.py`에 **`추적성 사각` 절을 신설**했다(판정 의미는 **바꾸지 않았다** —
`hard_failures`가 아니라 **FAIL 아님** 목록이다. (C)처럼 정당한 0건이 섞여 있기 때문이다).

```
## 추적성 사각 — '실측' 계열인데 source_refs 0건 (133차 신설, FAIL 아님)
> ref가 0건이면 원문 실재 검사가 한 번도 돌지 않는다. 아래 상수의 출처는
> 이 게이트가 검사한 적이 없다 — 서술만 읽고 믿는 상태다.
- SPEC_COUNT: 실측 … (12개)
```

→ **이제 매 게이트 실행마다 12개가 보인다.** 종전에는 리포트 어디에도 없었다.
→ 가드가 **그 12개를 고정**한다: **새 상수가 ref 없이 '실측'으로 들어오면 실패**한다.

---

## 6. 검증하지 않은 것

- **엔진 상수 값은 하나도 바꾸지 않았다.** 레지스트리 **값·`source_refs`도 바꾸지 않았다**
  (`상수 54 · refs 92` **불변**).
- 🔴 **refs를 붙이지 않았다** — (A) 5개는 **지금 붙일 수 있는데** 이번 차수에 하지 않았다.
  붙이면 `refs 92`가 움직이고 **각 ref가 정말 그 값의 출처인지 건별 확인**이 필요하다
  → **134차 과제**로 남긴다(진단과 대량 데이터 변경을 한 차수에 섞지 않는다).
- **(B)의 원문을 구하지 않았다** — 리포 밖 자료는 ★사용자 몫이다.
- **12개 상수의 값이 옳은지 확인하지 않았다.** 이 문서가 말하는 것은
  **"검사받은 적이 없다"**이지 **"틀렸다"**가 아니다.
- `_ref_ok()`가 **파일 실재만 보고 내용 대조는 하지 않는 것**은 종전 그대로다(범위 밖).
