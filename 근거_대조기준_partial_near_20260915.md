# `partial`·`near` 58건에 대조 기준 — 148건 전부가 닫혔다 (140차)

**작성일** 2026-09-15 (140차) · **계기** 139차 ⑨후보 **(g)** — *"`exact`만 닫혔다"*
**성격** 엔진 상수 값 변경 0 · 레지스트리 **값** 변경 0. 바뀐 것은 **ref note 24건**과
**`verify_refs.py` 확장**이다.

---

## 1. `partial`에는 금액 앵커가 성립하지 않는다

139차가 `exact` 39건에 **금액 앵커**를 붙여 닫았다. 그러나 `partial`은 그 문서가
**값의 일부·해석 근거**일 뿐이라 *"이 금액과 맞춰 보라"*가 성립하지 않는 경우가 많다.

→ 기준을 넓혔다: **①숫자 ②위치(쪽·표·시트·절·행) ③인용 문구** 중 하나 이상.
→ 뜻은 하나다 — **그 문서에서 무엇을 보면 되는지**.

### 현황 → 결과

```
140차 전: 기준 보유 34 · 🔴기준 없음 24   (partial 50 · near 8)
140차 후: 기준 보유 58 · 기준 없음 0
```

---

## 2. 🔴 자동 유도를 한 번 접었다

`CAPEX_MAJOR_EVIDENCE_STATUS`의 상태 문자열은 `실측(N건: A·B·C … — 제외 사유)` 꼴이라,
`—`로 쪼개면 **포함 표본과 제외 표본을 자동으로 가를 수 있어 보였다**. 실제로 돌리니:

| 카테고리 | 선언 | 내 분할기가 센 수 |
|---|---|---|
| `hvac` | 10건 | 🔴 **4건** |
| `auto_opening_system` | 15건 | 🔴 **0건**(*"전 표본"*이라 이름이 없다) |
| `greenhouse_structure` | 14건 | 15개(백가은·조윤정이 **쌍 견적 통합 1건**이다) |

→ **주석 안에도 `—`가 있어** 분할이 앞에서 끊긴다. **자동 분류를 포기**하고,
기준을 **"이름이 몇 개 카테고리에 나타나는가 + 포함·제외가 섞여 있다"**로 적었다.

🔴 **이것이 140차의 실질 발견**이다: 134차가 17건에 붙인 일괄 문구
*"13 카테고리 상태 문자열의 **근거** 표본 중 1건"*은 **부정확할 수 있다** —
예컨대 **오기수는 `greenhouse_structure`에 "설비 전용 부분 범위 견적이라 골조·피복 품목
전무(0)"라는 제외 사유로 등장**한다.

📌 125차 P1~P3 · 133 · 134 · 138차에 이어 **또 텍스트 자동 분류가 틀렸다.**
이번에는 **틀린 결과를 쓰지 않고 접었다**는 점이 달라진 것이다.

---

## 3. 채운 24건

| 대상 | 건수 | 대조 기준 |
|---|---|---|
| `CAPEX_MAJOR_EVIDENCE_STATUS` | **16** | 표본명이 나타나는 **카테고리 수**(상수 자신의 값에서 계산) + 포함·제외 혼재 경고 |
| `U_DESIGN` ← 난방계수 이견 | 1 | §0 결론 — `FR_TABLE[…]=0.70` · `U_DESIGN["필름"]=8.9` |
| `FR_TABLE` ← 농사로 보존본 | 1 | 머리말 기준 — **유리온실 U=7 · 온도차 15℃** · §1 인용 표 |
| `FINANCE_DEFAULTS` ← 법인세법 별표5 | 1 | **제3호 내용연수범위 15~25년**의 하한 |
| `PUMSEM_ITEMS` ← 표준설계 면적구성 | 1 | §0 *"벤로타입은 유리온실에만"* · p.112 `<그림 7-3>` |
| `PUMSEM_ITEMS` ← 요약본 6.2 추적 | 1 | 23회차 F4 배너 — `<그림 4-15>` **250/250** · **4,196/3,509** |
| `HEATING_EFFICIENCY_DEFAULT` | 1 | §3-A **철회 사유**(순발열량 조합 유지) · 고시 **제2020-10호** |
| `CLUSTER_*` ← 단지경제성 | 2 | 머리말 대상 줄 — **0.15** · **0.7** |

---

## 4. `verify_refs.py` 확장

```
soft_refs()      partial·near 전량(58건)
soft_criteria()  숫자·위치·인용 중 무엇을 갖췄는가
soft_check()     기준이 없는 ref — 140차 실측 0건
```

`python verify_refs.py`가 이제 **exact 앵커와 partial·near 기준을 함께** 본다.

---

## 5. 검증하지 않은 것

- **엔진 상수 값도 레지스트리 값도 하나도 바꾸지 않았다.** `상수 54` · `refs 148` 불변.
- 🔴 **기준이 "있다"는 것이 "그 기준이 옳다"는 뜻은 아니다.** 140차는 **그 문서를 열어
  기준이 실제로 거기 있는지**를 8건(보존본·법령)만 확인했고, **`EVIDENCE_STATUS` 16건은
  상수 자신의 값에서 계산**했다 — **원문 견적서를 다시 열지는 않았다**.
- **`CAPEX_MAJOR_EVIDENCE_STATUS`의 13개 상태 문자열이 옳은지 판정하지 않았다.**
  선언 건수(예: `hvac` 10건)와 실제 열거가 맞는지는 **자동으로 셀 수 없음만 확인**했고
  사람이 세지 않았다 — **별건**이다.
- 134차가 붙인 *"근거 표본 중 1건"* 문구를 **지우지 않았다**. 뒤에 경고를 덧붙였을 뿐이다
  (이력을 지우지 않는다는 대장 규칙과 같다).
