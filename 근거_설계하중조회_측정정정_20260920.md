# 「0/15 매칭」은 틀린 측정이었다 — 설계하중 조회는 이미 작동한다 (158차)

**작성일** 2026-09-20 (158차) · **계기** Ⅰ섹션의 「설계기준 출처」를 계산으로 바꾸려다 발견
**성격** 엔진 상수 값 변경 0 · 케이스 **값** 변경 0(서술 1건 정정) · 견적비교·레지스트리 불변 · 결정 0

> 🔴 **150차와 157차에서 내가 잰 것이 틀렸다.** *"케이스 `region`이 기상표 키와 0/25(→0/15)
> 매칭"*이라며 **D-5를 「측정 이전에 막힌 항목」**으로 올렸는데, 나는 `region in table`이라는
> **직접 키 매칭**을 쟀고 **`siting_design_load()`를 호출해 보지 않았다**. 그 함수는
> **부분 매칭을 이미 내장**하고 있고 3건 중 **2건을 정상 조회**한다.

---

## 1. 🔴 함수는 처음부터 이 문제를 풀고 있었다

```python
def siting_design_load(region_name):
    """... 케이스의 region 필드는 "강원(춘천)"·"충남 천안(성환읍)"처럼 자유 서술형이라
    정확히 일치하는 키를 먼저 찾고, 없으면 REGION_DESIGN_LOAD의 시군구명이
    region_name에 부분 포함되는지로 찾는다. 단 지명 중복 항목("고성(강원)" 등)은
    부분매칭 후보에서 제외한다 — 애매하면 None을 반환한다(모래 위 자동화 금지)."""
```

**docstring이 내가 「발견」했다는 것을 그대로 적고 있었다.** 나는 그 함수를 호출하지 않고
사전 조회만 해 보고 *"표는 관측지점명, 케이스는 행정구역 표기 — 0/25"*라 결론했다.

### 올바른 측정

| 케이스 | `region` | 케이스 주입 | `siting_design_load()` | |
|---|---|---|---|---|
| chuncheon | `강원(춘천)` | 32 · 34 | **32 · 34** | ✅ 일치 |
| uminjae | `충남 천안(성환읍)` | 26 · 28 | **26 · 28** | ✅ 일치 |
| wonchaewon | `충남` | 30 · 35 | **None** | 표에 없음 |

→ **조회 성공 2 / 조회 불가 1.** 「0/15」는 **직접 키 매칭**의 수이지 함수의 성능이 아니다.

⚠️ **다만 그 0이 전부 거짓은 아니다.** 기상 4표(TAC·난방도일·일조·풍속)에는 **그런 조회 함수가
없다** — 그쪽은 지금도 직접 키만 쓴다. 즉 150차가 본 문제는 **설계하중 표에는 해당하지 않고
기상 4표에만 해당**한다. D-5의 범위가 그만큼 좁아진다.

---

## 2. 🔴 케이스 서술이 함수 반환을 잘못 적고 있었다

`chuncheon`의 `site.design_load_source`:

> … `siting_design_load('강원(춘천)')` = `{'snow_cm': 32, 'wind_ms': 32}`

**실제 반환은 `{'snow_cm': 32, 'wind_ms': 34}`다.** 같은 문장 뒤쪽이
*"2025-108호 개정에 따라 wind_ms만 32→34로 재교체"*라고 적고 있으므로 **서술은 갱신됐는데
앞의 함수 호출 결과 표기만 낡은 채 남았다.**

147차 note *「그대로 렌더된다」*가 148차에 거짓이 된 것과 **같은 유형** —
**손으로 쓴 코드 동작 주장이 낡는다.**

---

## 3. 그래서 Ⅰ섹션을 계산으로 바꿨다

종전 Ⅰ섹션의 「설계기준 출처」는 **케이스에 적힌 문장을 220자로 잘라 인쇄**했다. 그 문장이
*"함수가 이렇게 답한다"*는 주장을 담고 있었고, 위처럼 **틀릴 수 있다.**

이제 생성기가 **실제로 호출한다**:

```
설계기준(적설·풍속)   32cm · 34m/s
매핑표 대조          siting_design_load('강원(춘천)') = 32cm · 34m/s — 주입값과 일치
```

⚠️ **값의 권위는 그대로 케이스 주입값이다.** 조회 결과는 **대조 표시**일 뿐 값을 대체하지
않는다 — 원채원처럼 `None`인 경우의 **fallback 설계 선택을 만들지 않기 위해서**다.
`None`이면 「매핑표에 없음(region 해상도 부족)」이라 적는다.

📌 이로써 **C3 「전부 계산 미연결(도달성 가드)」의 설계하중 부분이 해소**된다.
`siting_design_load()`가 생성기에서 **실제로 호출**되고, 불일치가 생기면 **산출물에 드러난다**.

---

## 4. 검증하지 않은 것 / 하지 않은 것

- **엔진 상수 값·케이스 입력값·견적비교·레지스트리 값 전부 불변.** 케이스는 `chuncheon`의
  `design_load_source` **문자열 1건**만 고쳤다.
- **기상 4표(TAC·난방도일·일조·풍속)에는 조회 함수를 만들지 않았다** — 어떤 정규화 규칙을
  쓸지는 **판단성**이고(잘못 이으면 다른 지점의 기상값이 조용히 들어온다), 그 표들은
  아직 계산에 연결돼 있지도 않다.
- **원채원은 여전히 `None`이다** — region이 「충남」(시도 수준)이라 시군구명이 없다.
  **S-1**이 있어야 풀린다. 그 주입값 30·35는 **표와 대조된 적이 없다**.
- **`siting_lookup()`(규격 후보까지 잇는 상위 함수)은 여전히 호출하지 않는다** — 규격 선정은
  이미 `select_specs`로 Ⅱ섹션에서 나오고, 중복 경로를 만들 이유가 없다.
- **결정은 하나도 내리지 않았다.** D-1~D-15 전부 ★ 대기다.
