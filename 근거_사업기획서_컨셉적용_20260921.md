# 사업기획서의 컨셉을 3×6 설계에 얹었다 (185차)

**작성일** 2026-09-21 (185차) · **요청** *"기획서의 컨셉을 세부적으로 녹여서 적용할 수 있는
부분을 적용하라"* · **출처** `근거_사업기획서_투자검증설계보증플랫폼_20260921.docx`
(사용자 제공 · 리포 사본 보존 · 323문단 · 표 7)
**성격** 엔진 상수·함수 변경 **0** · 케이스·견적비교·레지스트리 **불변** · **결정 0**

---

## 0. 한 줄

기획서의 **3단계(투자실사 → 설계검증 → 성능보증)** 중 **2단계는 이미 서 있었고**,
**1·3단계는 일부만 설 수 있다**. 설 수 없는 자리를 **덮은 척하지 않고 세었다** —
**실사 6영역 중 2개**, **성능 5항목 중 3개**가 엔진 밖이다.

---

## 1. 기획서 3단계 ↔ 리포 3×6 대응

| 기획서 단계 | 내용 | 리포의 대응 | 상태 |
|---|---|---|---|
| **1단계 투자 전 실사** | 6영역(기술시스템·단위경제성·운영프로세스·경영진 역량·시장위치·리스크)을 문서검토 → 기술평가 → 보고서로 심사 | **④타당성검증** + **D23 투자 실사 카드**(185차 신설) | 🟡 **4/6 영역** |
| **2단계 설계 적정성 검증** | 설계문서 검토 → 시공 중 감리 → 준공 커미셔닝 | **②품질설계 `D4`** → **③감리 `D13`** → **`D14`·`D15`** | ✅ **이미 서 있다**(173차) |
| **3단계 준공 후 성능보증** | 실측이 설계값에 미달하면 보상 | **D24 설계값–실측 대조표**(185차 신설) | 🟡 **2/5 항목** |

📌 **2단계는 기획서가 요구하는 3프로세스가 그대로 있다**: `doc_consistency_check()`
(도면·시방서·BoQ·규격서 4축) → `inspection_checklist()`(시방서 9단계 검측) →
`commissioning_plan()`·`completion_docset()`(시운전·준공). 기획서가 든 준거
**DNV Owner's Engineer의 전주기 개입**과 **TÜV Design Review**의 구조와 같은 모양이다.

---

## 2. 1단계 — 실사 6영역 중 **2개는 엔진 밖이다**

| 영역 | 엔진 함수 | 상태 |
|---|---|---|
| 기술시스템 | `select_specs` · `verify_heating_vs_actual` · `equipment_reconcile` | ✅ |
| 단위경제성 | `operating_breakeven` · `benchmark_check`(ROI·NPV·IRR은 `D8`·`D9`) | ✅ |
| 운영프로세스 | `service_life_reference`(점검·검측은 `D16`·`D13`) | 🟡 |
| **경영진 역량** | — | ⬜ **영농경력·기술이해도·재무역량은 사람 자료다** |
| **시장위치** | — | ⬜ **판로·경쟁강도는 시세성·시장자료** — 1절이 조회를 금한다 |
| 리스크 평가 | `dscr_schedule` · `site_permit_checklist`(하자·기후는 `D17`·`D1`) | ✅ |

🔴 **덮지 못하는 둘을 드러내는 것이 이 카드의 값어치다.** 기획서도 6영역을
*「문서검토 → 기술평가 → 보고서」*로 적어 **문서검토를 사람이 한다**고 전제한다 —
`D23`은 그 사람 몫을 `dd_documents` **주입 슬롯**으로 세워 둔다.

---

## 3. 3단계 — 성능 5항목 중 **3개는 편차를 잴 함수가 없다**

| 항목 | 편차 함수 | 실측 주입 | 상태 |
|---|---|---|---|
| 난방부하 | `verify_heating_vs_actual()` | `actual_load_per_m2` | ✅ |
| 공사비 | `benchmark_check()` | — (등재값 대조) | ✅ |
| **수확량** | — | `actual_yield_kg` | ⬜ **설계값은 `production_kg()`이 내지만 편차 함수가 없다** |
| **에너지효율** | — | `actual_energy` | ⬜ |
| **가동률** | — | `actual_uptime_pct` | ⬜ |

🔴 **만들지 않고 없다고 적었다.** 편차를 재려면 *"무엇을 몇 % 미달로 볼 것인가"*를
정해야 하고 그것은 **보증 상품 설계(판단성)**다. `D24`는 **설계값과 실측을 나란히 놓는
데까지**다 — 기획서 §5.3의 보상은 **보험·금융 판단**이라 1절 밖이다.

---

## 4. 🔴 적용하지 않은 것 — 기획서와 1절이 부딪히는 자리

| 기획서 | 1절 | 처리 |
|---|---|---|
| **K-SFID 등급 부여**(PVEL Top Performer 방식 준용) | *"판단성 값(최종판정)은 초안·참고까지만. 판정·추천 자동화 금지"* | **등급을 매기지 않는다.** 엔진은 **검증 항목과 결손**까지 |
| **성능보증 지급 판정·보험료 산정** | *"시세성 값은 조회하지 않는다 — 인자로 주입만"* | **판정하지 않는다.** 설계값 대 실측 **대조까지** |
| **투자 성공률·수익률 보장** | *"근거 없는 값 금지"* | 다루지 않는다 |

📌 **이것은 기획서가 틀렸다는 뜻이 아니다.** 기획서의 등급·보증은 **사람과 보험사가 지는
책임**이고, 이 엔진은 **그 판단에 들어갈 자료를 결손까지 드러내 만드는 계층**이다.
둘은 층이 다르다 — 그래서 **섞지 않고 경계를 적었다**.

---

## 5. 기획서가 준 것 중 **이미 리포에 있던 것**

- **NGMA Structural Design Manual**(기획서 §3.1, 2단계 구조 안전기준 준거) →
  리포는 **내재해형 규격 고시 249종**(`SPEC_TABLE`)과 **172지역 설계하중**
  (`REGION_DESIGN_LOAD`)으로 이미 구조 검증을 한다. 기준의 출처가 다를 뿐 자리는 같다.
- **정량 KPI 기준 실증평가**(NARO, §3.1) → `yield_adjustment()`의 **90~109% 무감점 계단**과
  `benchmark_check()`의 **피복별 밴드**가 같은 성격이다.
- **기성관리(Progress Certification)**(부동산 PF, §3.2) → `inspection_checklist()`의
  시방서 9단계가 **검측 시점**을 준다. 다만 **기성률 산정은 없다**.

---

## 6. 하지 않은 것 / 확인하지 못한 것

- **엔진 상수·함수를 하나도 바꾸지 않았다.** 케이스·견적비교·레지스트리 **불변**.
- **기획서의 수치를 엔진에 넣지 않았다** — 하자 407건·사업비 6,900억원·예산 2,348억원 등은
  **기획서가 인용한 보도·정책자료**이고, 리포는 **원문 확보 전까지 등재하지 않는다**.
- **벤치마킹 출처 URL 60여 건을 열어보지 않았다.** 기획서 §11 스스로
  *"제출 전 각 출처의 최신성과 내용 일치 여부를 재확인할 것을 권장한다"*고 적는다.
- **K-SFID 인증번호 체계를 설계하지 않았다** — 등급이 빠지면 번호만으로는 의미가 없고,
  등급은 **★사용자 결정**이다.
- **수확량·에너지·가동률 편차 함수를 만들지 않았다** — 기준(몇 % 미달)이 **보증 상품 설계**다.
- **보증 재원·보험료·수수료 구조를 다루지 않았다** — 전부 **시세성**이다.
