# D-4의 적용 범위와 성격을 확정했다 — 149차 과적용 정정 (157차)

**작성일** 2026-09-20 (157차) · **계기** 사용자 지시 *"결정 1건"* → 조사 중 전제 두 개가 뒤집힘
**성격** 엔진 상수 값 변경 0 · 케이스·견적비교·레지스트리 값 변경 0 · **결정 0**

> 사용자께서 D-2↔D-4 택일을 보시려 했고, **결정은 사용자 몫**이다(1절).
> 이 차수는 그 결정을 **내리지 않고**, 결정 전에 바로잡아야 할 **사실 두 가지**만 고친다.

---

## 1. 🔴 149차가 D-4를 유리 케이스에 과적용했다

`COVER_ASSEMBLIES` 49종의 구성을 전수로 봤다.

| 표 | 종수 | 층수 | `coef_kcal` |
|---|---|---|---|
| 표9 | 9 | 1층 | 4.4 ~ 5.7 |
| 표10 | 16 | 1층 | 1.7 ~ 5.2 |
| 표11 | 24 | **2층** | 1.4 ~ 3.0 |

**「유리」를 포함한 조합은 0건**이다 — 전부 필름·부직포·보온커튼 계열이다.

149차는 *"D-4 감응: 최대부하 4.07배 … **유리 케이스에도 적용된다**(assembly가 cover를 덮어쓴다)"*라고
적고 원채원 124,600~507,300 · 춘천 172,900~703,950을 표로 실었다. **그것은 필름 조합을
유리 케이스에 강제로 주입한 결과**다. 엔진이 막지 않을 뿐 **표 근거가 없다.**

🔴 같은 149차에서 나는 *"D-2는 필름 계열만이다 — 원채원에 `u_design=8.9`를 건 첫 측정은
과적용이었다"*고 **자기 정정까지 했다**. **바로 옆 항목에서 같은 오류를 반복했다** —
한 축(U값)의 적용 범위를 확인하고도 다른 축(조합표)의 적용 범위는 확인하지 않았다.

### 정정된 적용 범위

| 케이스 | 피복 | D-2 | D-4 |
|---|---|---|---|
| wonchaewon | 유리 | ❌ `U_DESIGN`에 유리 없음 | ❌ **조합표에 유리 없음** |
| chuncheon | 유리 | ❌ | ❌ |
| **uminjae** | **필름** | ✅ | ✅ |

→ **현재 완전 케이스 3건 중 D-2·D-4가 닿는 것은 `uminjae` 하나뿐이다.**
(부분 케이스 mulhyangki·yonggyun은 필름이나 `surface_area_m2`·`fr`이 없어 `heating_load` 대상이 아니다.)

---

## 2. 🔴 D-4의 「어느 조합인가」는 판단이 아니라 **원문 조회**였다

우민재 공사설명서(`스마트팜스펙/우민재/3. 공사설명서(우민재).pdf` p.1 「4) 피복공사」):

> **온실상부·측면: 장기성 PO필름 0.15T(외피) / 내부: 장기성 PO필름 0.1T(내피)**

공사내역서 수량(0.15t **4,157㎡** + 0.1T **1,118㎡**)과 정합한다.

그리고 조합표 **표11**에 **정확히 그 조합**이 있다:

```
('po-0.15', 'po-0.10')   coef_kcal 2.5 · 절감 56.0% · 표11(2층)
```

→ **「49종 중 무엇을 고를까」는 물음이 아니었다.** 원문이 조합을 지정하고 표가 그 계수를 준다.

📌 케이스 `provenance`가 이미 예고하고 있었다 —
*"이 케이스만 따로 `heating_load()`를 재계산할 땐 cover 인자에 '필름_이중'을 넘겨 실제 이중층
구조를 반영할 것 — 표준 파이프라인은 아직 Cover enum 기반이라 층수 분기를 자동 지원하지
않음(**향후 설계 과제**)"*. **D-4가 바로 그 과제다.**

---

## 3. 감응 — uminjae 하나에 대해

현행 238,776 kcal/h (u_design 5.7 · fr 0.70)

| 선택지 | 최대난방부하 | 대비 | 근거 |
|---|---|---|---|
| **D-1** ×1.0881 | 259,812 | +8.8% | 101차 [표3-3-51] |
| **D-2** `u_design` 8.9 | 372,826 | **+56.1%** | 99차에 5.7로 내린 ★결정을 되돌리는 건 |
| **D-4** `(po-0.15, po-0.10)` | **149,609** | **−37.3%** | ✅ **원문과 일치** |
| (참고) 단층 `PO필름(0.15)` | 263,312 | +10.3% | 원문은 **2층**이라 해당 없음 |
| (참고) `cover='필름_이중'` U_VALUE 1.82 | 76,241 | −68.1% | **다른 경로**(U값 이원화) — 섞지 않는다 |

🔴 **D-4를 채택하면 근거 없는 `fr=0.70`이 함께 사라진다.** 조합과 `fr`은 **배타**이고
(82차 ★사용자 결정 ⓒ로 신설된 경로), 조합 계수에는 보온 효과가 **이미 포함**돼 있어
`fr=1.0`으로 처리된다. 케이스 provenance는 `fr=0.70`에 대해
*"산출 근거가 문서화돼 있지 않아 별도 확인 필요"*라고 **스스로 적고 있다**.

실측 이중검증: 부하/㎡ **64.4** · 기준 180.0 · 비 **0.360** · 상태 **정상**.

---

## 4. 🔴 이 차수가 **하지 않은 것** — 결정

**D-2·D-4·D-1 중 무엇을 채택할지 정하지 않았다.** 1절이 금하는 판단성이다. 특히:

- **D-4는 부하를 −37.3% 낮춘다** → 난방기 용량 추천이 함께 내려간다.
  케이스 provenance가 *"향후 이 케이스의 난방장비 용량 추천 시 **과소산정 여부 별도 확인 권장**"*
  이라고 경고해 둔 지점이다. **설계 책임이 따르는 판단**이다.
- **D-2는 99차에 사용자가 8.9 → 5.7로 내린 ★결정을 되돌리는 것**이다.
- `cover='필름_이중'`(U값 경로)과 D-4(조합 경로)는 **서로 다른 두 길**이고,
  어느 쪽이 이 케이스에 맞는지도 **판단**이다. 이 문서는 둘을 나란히 적었을 뿐 고르지 않았다.

**엔진 상수 값·케이스·견적비교·레지스트리 값 전부 불변**이다.

---

## 5. 부수 확인 — 케이스 주입 설계하중은 표와 일치한다

D-5(지역명 정규화)의 감응을 재려다 확인한 것:

| 케이스 | region | 주입(적설·풍속) | `REGION_DESIGN_LOAD` | 규격 선정 |
|---|---|---|---|---|
| chuncheon | `강원(춘천)` | 32 · 34 | **32 · 34** | 동일 |
| uminjae | `충남 천안(성환읍)` | 26 · 28 | **26 · 28** | 동일 |
| **wonchaewon** | **`충남`** | 30 · 35 | **None** | — |

→ **D-5/D-6을 결정해도 산출물 수치는 바뀌지 않는다**(부분 매칭 2건이 이미 표와 일치).
실익은 *"어긋나면 잡힌다"*는 **자동 검증 경로**뿐이고, 그것은 **가드로 충분하다**(이 차수에 넣었다).

🔴 **원채원만 표에 닿지 않는다.** region이 「충남」(시도 수준)이라 조회가 `None`이고,
그 주입값 30·35는 **표와 대조된 적이 없다**. **회귀 기준 케이스의 설계하중이 미검증**이다 —
**S-1(원문 확보)** 없이는 풀 수 없다.

⚠️ `siting_lookup()`·`siting_design_load()`는 **생성기에서 호출되지 않는다**(`build_site`의
`siting` 변수는 f-string 이름일 뿐이다). 통합보고서 Ⅰ섹션은 케이스 주입값을 인쇄하며
출처 필드에 *"`siting_design_load('충남')`는 여전히 None 반환"*이라 **스스로 미연결을 밝히고 있다**.
**배선은 하지 않았다** — 원채원이 `None`이라 fallback 처리라는 **설계 선택**이 생기기 때문이다.
