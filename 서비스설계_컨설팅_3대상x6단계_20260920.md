# 스마트팜 구축 컨설팅 — 서비스/솔루션 설계 (3대상 × 6단계)

**작성일** 2026-09-20 (166차) · **요청** *"부지·시설·기자재에 대한 공종설계·품질설계·감리·
타당성검증·운영·사후관리 컨설팅을 위한 서비스/솔루션으로 구체적으로 설계하라"*
**성격** 설계 문서 — 엔진 상수 **값 변경 0** · 케이스·견적비교·레지스트리 **불변** · **결정 0**

> ⚠️ 이 문서의 **커버리지 표기(있음/부분/없음)는 실측**이다 — `smartfarm_engine.py`를 AST로
> 파싱해 **공개 함수 53개·데이터 클래스 30개**를 전수 확인했고, 부재는 코드 5파일
> (`smartfarm_engine.py`·`webapp.py`·`build_site.py`·`cases.py`·`render_report.py`)
> 전수 검색으로 **0건임을 확인**한 것만 「없음」이라 적었다.
> ⚠️ **단계 분류 자체는 판단이다** — 어떤 함수를 「품질설계」로 볼지 「감리」로 볼지는
> 내 배치이고, 다르게 볼 수 있다.

---

## 0. 설계 제약 — 이 서비스가 **하지 않는 것**부터 고정한다

`CLAUDE.md` 1절이 그은 선이다. 서비스 설계가 이 선을 넘으면 산출물이 **팔 수 없는 물건**이 된다.

| 선 | 내용 | 서비스에서의 뜻 |
|---|---|---|
| **단일 계산 출처** | 엔진이 유일한 계산 출처. 엑셀 수식·JS·앱 계층 산술 금지 | 컨설턴트가 엑셀로 덧붙인 수치는 **산출물이 아니다** |
| **시세성 주입** | 단가·노임·유가·금리는 **조회하지 않는다 — 인자로 받는다** | 견적·재무 산출은 **고객이 준 단가 위에서만** 성립한다 |
| **판단성 비자동화** | 작목·부지·업체 선정, 최종판정은 **초안·참고까지만** | 「추천」이 아니라 **「나란히 놓기」**가 산출물이다 |
| **근거 없는 값 금지** | 확보 전까지 `[추정]`/`[확인요망]` + 출처 병기 | 모든 셀에 **출처 배지**가 붙는다 |
| **실측 회귀 기준** | 원채원 ROI 14.2%·Payback 7.1년·실질ROI 28.3% | 엔진이 흔들리면 **작업을 멈춘다** |

---

## 0-c. 사업기획서 3단계를 3×6에 얹는다 (185차 신설)

사용자 제공 **「스마트팜 투자검증·설계보증 플랫폼 사업기획서」**(리포 사본 보존)의
3단계 모델을 이 설계 위에 얹었다. 상세: `근거_사업기획서_컨셉적용_20260921.md`.

| 기획서 단계 | 리포의 대응 | 상태 |
|---|---|---|
| **1단계 투자 전 실사**(6영역) | ④타당성검증 + **D23 투자 실사 카드** | 🟡 **4/6 영역** — 경영진 역량·시장위치는 **엔진 밖** |
| **2단계 설계 적정성 검증**(문서검토→감리→커미셔닝) | **D4 → D13 → D14·D15** | ✅ **이미 서 있다**(173차) |
| **3단계 준공 후 성능보증** | **D24 설계값–실측 대조표** · ✅**D26 성능보증 판정서**(188차) | ✅ **5/5 항목** — 188차에 편차 3함수를 붙였다 |

### 🔴 189차 정정 — 185차가 「적용하지 않는다」고 적은 둘은 **적용됐다**

| 185차에 적은 것 | 지금 |
|---|---|
| ~~K-SFID 등급 부여 → **등급을 매기지 않는다**~~ | ✅ **187차 ★사용자 결정으로 적용**(`D25`) — 등급을 **낸다** |
| ~~성능보증 지급 판정·보험료 산정 → **판정하지 않는다**~~ | ✅ **188차 적용**(`D26`) — 지급 대상과 보상액을 **낸다** |
| 기획서가 인용한 보도 수치를 엔진에 넣지 않았다 | **그대로 유지** — 원문 확보 전까지 등재하지 않는다 |

🔴 **이 문장들을 지우지 않고 취소선으로 남긴다.** 185차에 *「1절이 금한다」*고 적은 것이
**틀린 판단은 아니었고**, 바뀐 것은 **규칙이 정해졌다는 사실**이다 — 경위가 지워지면
*"왜 갑자기 판정하게 됐는가"*를 아무도 답할 수 없다.

### ⚠️ 그래서 지금 「하지 않는 것」은 무엇인가

- **투자·보험·시공 판정은 하지 않는다.** `D25`의 등급은 **검증 항목 통과 수**이고
  `D26`의 판정은 **계약이 정한 규칙을 적용한 결과**다 — 둘 다 *"이 사업이 좋은가"*가 아니다.
- **임계값을 엔진이 고르지 않는다.** 주입으로 덮어쓸 수 있고 반환의 `rule`이 적용값을 드러낸다.
- **미검증을 통과로 세지 않는다.** 면책이 정해지지 않으면 **편차만 내고 판정하지 않는다**.
- **보증 재원·재보험·청구 심사는 다루지 않는다.**

📌 **기획서가 틀렸다는 뜻이 아니었다.** 기획서의 등급·보증은 **사람과 보험사가 지는 책임**이고
이 엔진은 **그 판단에 들어갈 자료를 결손까지 드러내 만드는 계층**이다. 187·188차에 엔진이
**규칙 적용까지** 맡게 됐지만 **책임의 층은 그대로**다 — 최종 판단은 사람과 계약이 한다.

✅ **190차 — 1절의 모순이 닫혔다**(★사용자 결정). ~~`CLAUDE.md` 1절의
*「판정·추천 자동화 금지」*가 문자 그대로 살아 있어 새 세션이 그 문장만 읽고 시작한다~~ →
**금지는 그대로 두고 단서를 달았다**:

> **규칙 적용형 등급·판정은 예외**다. 네 조건을 **모두** 지킬 때만 —
> ①규칙·임계값이 레지스트리에 status **「결정」**으로 등재 ②**같은 입력이면 같은 결과**
> ③등급·판정마다 **항목별 근거 행** ④임계값을 **주입으로 덮어쓸 수 있고 반환이 적용 규칙을
> 드러낸다**. **미검증을 통과로 세지 않는다.**
> 여전히 금지: **투자·보험·시공 판정** · 업체·작목·부지 **추천** · **임계값을 엔진이 고르는 것**.

🔴 **가드가 문구만 보지 않는다** — 네 조건을 **엔진에서 직접** 잰다(등재 status ·
난수·시각 의존 0 · 근거 행 · 임계값 주입과 `rule` 노출 · 미검증 비산입), 그리고
**여전히 금지된 것**이 산출물 데이터에 없는지도 함께 본다.

✅ **191차 — 엔진은 분리하지 않는다**(★사용자 결정 2026-09-22). 189차 자기 점검이 5위로 든
~~*「엔진이 판정 계층을 품었다(6,116 → 6,464줄) — 구조 변경이 필요하다」*~~ 는 **처방이
철회됐다**. 쪼개면 판정 모듈이 엔진을 되부르거나 설계값을 복제해 **제2의 계산 출처**가 된다 —
**1절 첫 줄이 금하는 바로 그것**이다.

🔴 **대신 경계를 파일이 아니라 「호출 방향」으로 잰다**(191차 실측 · AST):

| 방향 | 측정 | 뜻 |
|---|---|---|
| **계산 → 판정** | **0건** | 판정이 계산 결과에 **섞여 들어가지 않는다** |
| **판정 → 계산** | **0건** | 판정 입력은 **인자로 주입**된다 — 엔진이 스스로 설계값을 집어오지 않는다 |
| 판정 → 판정 | 1건(`guarantee_assessment` → `performance_shortfall`) | 허용 — **같은 계층**이다 |
| **잇는 것** | `consulting_package.py`가 `heating_load()`·`production_kg()` 값을 `D26`에 넣는다 | **조립 계층**의 일이다(188차) |

📌 그래서 `PERF_GUARANTEE_SPEC`의 `design_from`은 **주석이 아니라 계약**이다 —
*「`heating_load()`의 fuel_consumption」*은 **조립 계층이 가져다 넣으라**는 뜻이지
**판정 함수가 스스로 부르라**는 뜻이 아니다.

⚠️ ~~**점검 4위는 그대로다.** 판정이 **산출물에 실리는 설계 자체**는 189차의 고지로만
대응돼 있다~~ → **194차에 닫혔다**(아래).

✅ **194차 — 판정을 부록으로 분리했다**(★사용자 결정 2026-09-22).
189차가 점검 4위로 든 것을 **구조로** 옮겼다. 189차가 든 이유는 그대로다 —
등급·보상액이 공유 산출물 **본문**에 실리면 **분쟁 시 증거로 읽힐 수 있다**.

| | 담는 것 | 고지 |
|---|---|---|
| **본문** `SmartFarm_컨설팅패키지_*` | **D1~D24 — 대조 자료** | *「등급·지급 판정을 싣지 않는다 · 판정은 부록에 있다」* + 부록 링크 |
| **부록** `SmartFarm_판정부록_*` | **D25~D27 — 규칙 적용 결과** | 전문(무엇이 아닌지 · 적용된 규칙과 근거 행 · **미검증 비산입**) |

🔴 **빼는 것이지 지우는 것이 아니다.** 부록은 그대로 나오고, **떼어 보내거나 빼고
보낼 수 있다**. 그래서 가드는 **본문에 없는가**와 **부록에 있는가**를 **둘 다** 잰다 —
한쪽만 보면 *「지워서 통과」*하는 길이 열린다. 본문·부록은 **케이스마다 짝**이고,
새로 난 부록 3건도 **실명 0 검사 대상**이다(산출물 18 → **21건**).

⚠️ **남은 것은 5위다**(엔진이 판정 계층을 품었다) — 191차 ★결정으로 **분리하지
않기로 정해졌으므로** 이쪽은 *닫힌 것이 아니라 **정해진 것***이다.

✅ **192차 — 이번엔 엔진 **밖**을 쟀다: 표시 계층이 계산하고 있었다.**
191차가 엔진 **안**의 호출 방향을 쟀다면, 192차는 1절이 명시적으로 금한
*「앱 계층 산술」*을 **처음으로** 전수 측정했다. 181차 가드는 `consulting_package.py`
**하나만** 보고 있었다.

🔴 **`build_site.py`는 머리글에 *「계산은 전적으로 smartfarm_engine 에 위임. 이 파일은
렌더/조립만 한다」*고 적어 두고** 네 가지를 스스로 만들고 있었다:

| 자리 | 무엇을 만들었나 | 왜 문제인가 |
|---|---|---|
| `consulting_report_page` | `capex * subsidy_rate` · `capex - 보조금` | `finance()`가 **속으로 이미 쓰는 양**을 **다른 식으로** 다시 만들었다 |
| `consulting_report_page` | `영업이익 + 감가상각` | 엔진이 현금흐름에 쓰는 바로 그 식의 **복제** |
| `_sensitivity_snapshot` | `prod * price` | **바로 위 `_scenario_rows`는 `rr.compute`를 다시 부른다** — 같은 파일 안에 올바른 방식과 복제가 나란히 있었다 |
| `render_report.compute` | `prod * price` | 매출 산식이 **두 곳**에 있었다 |

📌 **부동소수점이 이것을 이론 문제로 두지 않는다.** `capex - capex×r`과 `capex×(1-r)`은
**다른 수**다(7억·30%에서 갈린다). 논산딸기 `known_total`(10.1억)·20%에서는
`capex×r + capex×(1-r)`이 **capex와 같지 않다**. 그래서 **자부담을 정본**으로 두고
**보조금을 차액**으로 낸다 — 합이 CAPEX와 어긋나지 않는다.

🔴 **고친 방식은 「회수」다.** 엔진에 `revenue_won()`을 두고 `FinanceResult`에
`subsidy_won`·`self_funded_won`·`operating_cash_flow`를 **내보냈다**(속으로 이미 쓰던 양이다).
공개 함수가 69 → **70**이 된 것은 **기능이 늘어서가 아니다**.
**산출물 수치는 한 자리도 바뀌지 않았다** — 바뀐 것은 `economics`에 **키 셋이 늘어난 것**뿐이다.

⚠️ **자의 한계를 적어 둔다.** 이 가드는 *오른쪽이 숫자 상수인 연산*을 지나친다
(`roi*100`·`npv/1e8` 같은 **표시 환산**을 허용하기 위해서다). 그래서
`price * 1.1` 같은 **시나리오 폭**은 잡히지 않는다 — **잡힌다고 적지 않는다**(159차 교훈).
남은 예외 3건(대사 차이 2 · 미분류 비율 1)은 **항목마다 사유**를 달았고,
**쓰이지 않는 예외가 있으면 실패**한다 — 아무것도 가리지 않는 예외는 나중에 문이 된다.

✅ **193차 — 이름을 댄 출처를 전수로 찾아봤다.**
132차 가드는 **백틱 안의 전체 경로**(`폴더/파일.csv`)만 본다. 그래서 **경로 없이
파일명만** 댄 인용은 **아무도 열어보지 않았다** — `audit_traceability.py`도
`실측`·`부분실측` 계열만 사각으로 세므로 `참고기준`·`확인요망`의 인용은 그 집계 밖이다.

🔴 **자를 두 번 고쳤다 — 첫 수는 내가 만든 수였다.**

| 자 | 결과 | 무엇이 틀렸나 |
|---|---|---|
| 1차(공백에서 끊음) | 「없음」 **21종** | **파일명 꼬리**를 셌다 — `내역서.pdf`·`시범사업.xlsx`는 실제 파일명의 **뒤 토막**이다 |
| 2차(공백 포함·접미 일치) | 「없음」 5종 | `chunking_lib_v2.hwp_extract_text()`라는 **함수 이름**을 파일로 읽었다 |
| 3차(확장자 뒤 경계) | **84종 인용 · 4종 미확인** | 이 수를 쓴다 |

📌 **21을 보고하지 않았다**(159차 교훈 — 정규식 경계가 만든 수는 결과가 아니다).

🔴 **미확인 4종 중 셋은 이미 「없다」고 적혀 있었다**: `SmartFarm_엔진데이터.md`는
*「리포에 존재하지 않는다」*, 기자재 DB 원본과 견적 프로그램은 **외부 드라이브 경로**로
적히고 리포에는 변환본(`기자재DB/` CSV)만 있다. **한 건만 적혀 있지 않았다** —
`SUBSIDY_APPLICATION_PROCEDURE`가 시트명까지 대면서 `source_refs`는 0건이었다.
읽는 사람은 파일명을 보고 **열어볼 수 있다고 믿는다** → 서술에 부재를 명시했다
(값·status 변경 없음).

⚠️ **남은 한계**: 잘린 인용을 받아들이려고 **접미 일치**로 찾는다. 「있다」는
*그 이름으로 끝나는 파일이 리포에 있다*는 뜻이지 *그 파일이다*라는 뜻이 아니다.

✅ **196차 — 등재명이 엔진에 없었다.**
193차가 *「이름을 댄 출처」*를 찾아봤다면, 196차는 **레지스트리가 이름을 댄 상수**를
찾아봤다. 등재 **65개 중 6개**가 엔진에 **그 이름이 없었다**.

| 등재명 | 실제로 어디에 있나 |
|---|---|
| `ACTUALS_COUNT` · `SPEC_COUNT` | **세어지는 값** — `ACTUALS`(9) · `SPEC_TABLE`(249)의 건수다 |
| `OVERHEAD_RATES` | 데이터클래스 **`OverheadRates`** — 이름 형태가 다르다 |
| `ENV_WEIGHTS` | **`env_fitness()` 안의 상수** — 레지스트리가 그렇게 적는다 |
| `EQUIPMENT_DB_META` | 값이 **`기자재DB/` CSV**에 있다 |
| 🔴`FINANCE_DEFAULTS` | **아무 데도 없었다** — `finance()`의 **기본인자 리터럴**뿐 |

🔴 **마지막 하나를 상수로 승격했다**(105차 선례 — *「하드코딩 리터럴이라 감사가 볼 수
없던 값」*과 같은 계열). **값은 바꾸지 않았다**(useful_life 15 · discount_rate 0.05 ·
years 10). 이제 등재명이 **실재**하고, 남은 5개는 **예외마다 무엇으로 대신 확인되는지**를
가드가 함께 잰다 — 대신할 것이 사라지면 실패한다.

### 그리고 NPV는 세 가정 위에 서 있었다

케이스 **어느 것도** 할인율·평가기간·내용연수를 **주입하지 않는다**(0건). `render_report.compute`도
넘기지 않으므로 **엔진 기본값이 그대로 쓰인다**. 그런데 산출물에는 NPV·IRR이 실리고
**「할인율」이라는 말이 한 번도 없었다** — 읽는 사람은 **확정값으로 읽는다**.

> 🔴 할인율 **5%** · 평가기간 **10년** · 감가상각 내용연수 **15년**은 **주입되지 않은
> 엔진 기본값**이다. 엔진 주석의 실측: 할인율 0.05→0.03이면 NPV **427,042,081 →
> 545,256,258**, 평가기간 10→20년이면 IRR **16.2% → 20.3%**.

리포트 3건·통합보고서 3건 **여섯 면 전부**에 이 표기를 싣고, 가드가 **케이스가 이 셋을
주입하기 시작하면 실패**한다 — 그때는 표기가 **거짓**이 되기 때문이다.

✅ **197차 — 그런데 주입할 자리가 없었다.**
1절은 시세성 값을 *「인자로 **주입만** 받는다」*고 하는데, 재무 세 가정은 **주입할 방법이
없었다** — 바꾸려면 **엔진을 고쳐야** 했다. 196차가 산출물에 *「주입되지 않았다」*고 적어
두었으니 읽는 사람은 **주입하면 된다고 생각한다**. 그 간극을 메웠다.

| | 197차 이전 | 지금 |
|---|---|---|
| 자리 | **없다** — 엔진 서명을 고쳐야 했다 | `FarmInput`의 선택 필드 3(기본 `None`) |
| 기본값 | 엔진 | **엔진 하나뿐**(`FINANCE_DEFAULTS`) — 자리는 기본값을 갖지 않는다 |
| 표기 | `injected: False` 하나 | **키별 진실** — `할인율 3%[주입] · 평가기간 10년[기본값]` |
| 문구 | 두 면이 각자 | **`render_report.assumption_html()` 한 곳**에서 만든다 |

🔴 **합성 입력으로 실제로 주입해 본다.** 가드가 ①엔진까지 닿아 **NPV가 바뀌는가**
②`ROI`는 그대로인가(엔진 주석의 실측 — 할인율은 NPV·IRR만 움직인다) ③주지 않은 것은
여전히 **기본값이라 말하는가** ④**셋 다 주입되면 기본값 민감도 문구가 빠지는가**를 잰다.

⚠️ **자리만 열었고 값은 넣지 않았다.** 기존 케이스 3건은 이 키를 두지 않으므로
**산출물 수치는 한 자리도 바뀌지 않는다** — 가드가 그것도 잰다.

✅ **198차 — 접미 일치가 틀린 이름을 통과시켰다.**
193차는 **레지스트리 서술**의 인용만 봤다. **코드가 사람에게 읽으라고 가리키는 문서**는
아무도 열어보지 않았고, `기입양식.md`를 가리키는 자리가 **3곳** 있었다 — 리포에 **그
이름의 파일은 없다**(실제는 `시나리오_가정값_기입양식.md`·`financing_실조건_기입양식.md`).
사용자는 그 오류 메시지를 보고 **없는 문서를 찾는다**.

🔴 **193차의 자를 그대로 쓰면 이것을 놓친다.** 그쪽은 **잘린 인용**을 받아들이려고
**접미 일치**로 찾는데, `시나리오_가정값_기입양식.md`가 `기입양식.md`로 **끝나므로**
틀린 이름이 통과한다. **코드 인용은 잘리지 않는다** — 완전 일치가 맞는 자다.

| 자 | 결과 | 무엇이 틀렸나 |
|---|---|---|
| 확장자 전부 훑기 | 「없음」 **49종** | **테스트 픽스처**(`good.json`)와 **서술 꼬리**(`손상 .xls`)가 섞였다 |
| `(`를 이름에 허용 | 조각 다수 | `되고(CLAUDE.md` 같은 **문장 조각**이 생겼다 |
| `.md` 뒤 경계 없음 | +1 | `bsmod.md(...)`라는 **모듈 속성 접근**을 파일로 읽었다 |
| **고친 자** | **82종 인용 · 미확인 2종** | 둘 다 예외(공시된 부재 · 모듈 속성) |

📌 **49를 보고하지 않았다**(159차 교훈). 그리고 **지목한 절이 실재하는지**도 함께 잰다 —
이름을 고쳐도 **1절이 없으면** 사용자는 또 헤맨다(두 문서 모두 `## 1.`이 있다).

✅ **199차 — 열어 둔 자리에 입구가 없었다.**
197차가 `FarmInput`에 재무 가정 **세 칸**을 열었는데 **웹 기입 마법사는 그것을 몰랐다** —
주입 자리는 **JSON을 직접 고치는 사람만** 쓸 수 있었고, **아무 가드도 울지 않았다**.
같은 스키마를 **두 곳이 각자 아는** 구조다(192차의 *「같은 것을 두 곳이 만든다」*와 같은 계열).

| | 197차 직후 | 지금 |
|---|---|---|
| `FarmInput` | 20필드 | 20필드 |
| 기입 폼이 채우는 키 | **17** | **20** |
| 대조 | **아무도 하지 않는다** | 가드가 **양방향**으로 — 폼이 안 채우는 필드는 **사유와 함께** 선언 |

🔴 **빈칸을 넣지 않는다.** 비워 둔 칸을 기본값으로 채워 넣으면 산출물의
*「주입되지 않았다」*가 **거짓**이 되고, **비운 것과 기본값을 적어 넣은 것이 구별되지
않는다**. 가드가 **합성 입력을 실제로 파서에 넣어** ①빈칸은 케이스에 들어가지 않는가
②넣은 값은 그대로 들어가는가 ③**하나만 채웠을 때 나머지까지 들어가지 않는가**를 잰다.

⚠️ 폼에 **비우면 어떻게 되는지**를 적었다 — 기본값이 있다는 사실을 모르면 빈칸을
*「값이 없는 것」*으로 읽는다. 내용연수는 **ROI·회수기간까지 움직인다**는 경고도 함께 둔다.

✅ **200차 — 1절의 마지막 한 줄을 기계가 재게 했다.**
1절 여섯 줄 중 **다섯**은 가드가 붙어 있다 — 병렬 계산기(191·192) · 근거 없는 값
(193·196) · 시세성 주입(197·199) · 판정 단서(190) · 회귀 벤치마크(상시).
남은 한 줄 *「**테스트를 지우거나 skip으로 바꿔 통과시키지 않는다**」*만은
**아무도 재지 않았다** — *「내가 그러지 않았다」*는 말에 기대 왔다. **말은 측정이 아니다.**

| 무엇을 | 어떻게 | 지금 |
|---|---|---|
| 테스트가 줄지 않았는가 | **하한**으로 — 늘면 아무 일 없고 **줄면 실패** | 총 **418**(46·33·287·28·24) |
| 덮으려고 끄는 skip | `@pytest.mark.skip/skipif/xfail` **0건** | **0** |
| 환경 skip | **사유와 함께 등재** — *무엇이 없어서* 못 도는지 | **6건**(원문 PDF 1 · 말뭉치 2 · 시스템 폰트 3) |
| 조용한 skip 경로 | `importorskip` 모듈 목록 **고정** | 파일별 목록 대조 |

🔴 **세는 방향이 다르다.** 195차가 3위로 남긴 *「가드가 실측 추종으로 기운다」*와 반대다 —
**추종은 바뀐 값을 따라가는 것**이고 **하한은 따라가지 않는 것**이다. 테스트가 늘어도
갱신할 일이 없고, **줄었을 때만** 이유를 적게 만든다.

🔴 **skip은 두 종류다.** 환경·자료가 없어 **못 도는 것**과 실패를 **덮으려고 끄는 것**.
앞은 사유와 함께 등재하고, 뒤는 **0이어야 한다** — 사유 없는 skip은
**실패를 끄는 것과 구별되지 않는다**.

✅ **202차 — 같은 리포트 안에 두 할인율이 공존할 수 있었다.**
197차가 주입 자리를 열고 199차가 입구를 이었는데, **조립 계층의 `D9`는 여전히
`e.npv(0.05, cf)`로 할인율을 리터럴로** 넘기고 있었다. 케이스가 0.03을 주입해도
**4축 리포트는 0.03, D9는 0.05**로 돌았다. `max_investable_capex()`도 세 가정을
넘기지 않아 **엔진 기본값**으로 돌았다(197·199차가 두 번 *「별건」*으로 미룬 자리다).

| 자리 | 이전 | 지금 |
|---|---|---|
| `D9` 시나리오 NPV | **리터럴 `0.05`** | 케이스가 정한 할인율 |
| `D9` CAPEX 상한 역산 | 세 가정 **미전달** → 엔진 기본값 | 셋 다 전달 |
| `D9` 가정 표기 | **없다** | 값 + **키별 주입 여부** |

🔴 **문구로 재지 않는다.** 가드가 **합성 케이스에 실제로 0.03을 주입해** ①D9의 가정
표기가 따라오는가 ②**시나리오 NPV가 다른 수가 되는가**를 잰다 — 리터럴이 되살아나면
②가 **움직이지 않는다**.

📌 **리터럴 하나는 남겼다**: `lcc_replacement_schedule(…, 20)`의 **20년 지평**은
*시세성 가정이 아니라 **구조 선택***이고, 회귀 앵커 **A6**가 교체 연도 구조를 그 지평
위에서 고정한다 — **사유와 함께 예외로 선언**했고, 쓰이지 않게 되면 예외도 지우게 한다.

⚠️ **잇기만 했고 값은 넣지 않았다** — 기존 케이스 3건은 여전히 미주입이라
**산출물 수치는 바뀌지 않았다**(D9 데이터에 `가정` 항목이 **는 것**뿐이다).

---

## 1. 서비스 한 줄 정의

> **「고객이 준 입력과 리포에 있는 원문만으로, 대조 가능한 산출물을 만든다」**
> — 판정은 사람이 하고, 기계는 **판정에 필요한 것을 빠짐없이 갖춰 놓는다.**

### 전달 채널 4종 (모두 구현돼 있다)

| 채널 | 실체 | 쓰임 |
|---|---|---|
| **엔진 API** | `smartfarm_engine.py` 공개 함수 **70**(166차 53 → … → 188차 69 → 🔴192차 70 — `revenue_won()`은 **신기능이 아니라 표시 계층에서 회수한 산식**이다) | 컨설턴트·연동 시스템이 호출 |
| **컨설팅 패키지**(181차 신설) | `consulting_package.py` → `SmartFarm_컨설팅패키지_C*.html` | 케이스 1건 → **D1~D22 한 장** + **자료 요청서** |
| **정적 리포트** | `build_site.py` → 케이스 리포트·통합보고서·비교뷰·CAPEX분해·벤치마크 | 고객 제출본 |
| **웹 콘솔** | `webapp.py` — `/entry/newcase`·`/entry/quotes`·`/entry/financing`·`/entry/scenario` | 입력 수집·미리보기·저장 |
| **근거대장** | `SmartFarm_근거대장.html` + `엔진데이터_레지스트리.json`(상수 **65** · `source_refs` **178** — 188차 실측) | *"이 수는 어디서 왔나"*에 대한 답 |

---

## 1-b. 대상 귀속 규칙 (170차 신설 — 개선 ②)

🔴 166차는 *"단계 분류 자체는 판단이다"*라고만 적고 **대상 축의 모호성은 적지 않았다**.
그런데 경계가 더 모호하다 — 난방기는 `heating_load()`가 용량을 내면서
`EQUIPMENT_SERVICE_LIFE_REFERENCE`에 11년으로도 올라 있다. **규칙 없이 센 18칸은 재현되지 않는다.**

> **R1. 구조체·피복·기초는 시설이다.** 골조·서까래·피복재·보온재·기초 — 하중을 받거나
> 열을 막는 것.
> **R2. 전원·신호에 연결돼 동작하는 것은 기자재다.** 센서·구동기·제어기·펌프·모터.
> **R3. 두 면을 가진 품목은 면별로 센다.** 난방기는 **「설계 하중·용량을 만드는 면」은 시설**,
> **「내용연수·KS 적합성·검정 대상인 면」은 기자재**다.
> **R4. 부지는 대지 경계 안의 지반·배수·진입로·인허가**다. 그 위에 놓이는 것은 시설이다.

### R3이 실제로 갈라내는 품목

| 품목 | 시설 면 | 기자재 면 |
|---|---|---|
| 온풍난방기 | `heating_load()` 최대난방부하 → 용량 | 내용연수 11년(조달청 고시) |
| 보온커튼 | `FR_TABLE` 열절감률 · `COVER_ASSEMBLIES` | **KS X 3265** 구동기 9종 |
| 관수 펌프 | — | KS X 3265 · 내용연수 |
| 천창·측창 | 구조 개구부 | **KS X 3265** 구동기 |

⚠️ **이 규칙은 집계를 재현 가능하게 만들 뿐, 18칸 등급을 다시 매기지는 않았다**(개선 ⑤ 몫).

---

## 2. 커버리지 매트릭스 (실측)

**✅있음** = 전용 엔진 함수가 있다 · **🟡부분** = 인접 함수로 일부만 · **⬜없음** = 코드 5파일 전수 검색 0건

| | 공종설계 | 품질설계 | 감리 | 타당성검증 | 운영 | 사후관리 |
|---|---|---|---|---|---|---|
| **부지** | ✅ | 🟡 | ⬜ | 🟡 | ⬜ | ⬜ |
| **시설** | ✅ | ✅ | 🟡 | ✅ | 🟡 | 🟡 |
| **기자재** | 🟡 | 🟡 | 🟡 | 🟡 | 🟡 | 🟡 |

**18칸 중 — 있음 4 · 부분 **11** · 없음 **3**.**(173차 — 기자재 감리 ⬜ → 🟡) 강한 축은 **시설**이고, 약한 축은 여전히 **감리**와 **부지 운영·사후관리**다.

### 🔴 171차 — 5단계 척도로 다시 재면 두 칸이 자리를 옮긴다 *(좁은 읽기 · **171~179차 기록** — 현행은 아래 180차 절이다)*

등급 규칙: **없음 < 조회 < 산출 < 검증 < 판정지원**. 셀 등급 = 배정 함수 중 **최고 역할**.
「판정지원」의 기계 근거는 **반환 구조에 `status`·`checks`·`rows`·`counts`·`missing`이 있는가**다.

| | 공종설계 | 품질설계 | 감리 | 타당성검증 | 운영 | 사후관리 |
|---|---|---|---|---|---|---|
| **부지** | **산출** | 조회 | 없음 | **산출** | 없음 | 없음 |
| **시설** | **판정지원** | **판정지원** | **산출** | **판정지원** | **산출** | **산출** |
| **기자재** | 조회 | **판정지원** | **산출** | **산출** | **산출** | **산출** |

**판정지원 4 · 검증 0 · 산출 9 · 조회 2 · 없음 3.**(173차 — 기자재 감리 없음 → 산출)

⚠️ **감리를 「판정지원」으로 올리지 않았다.** `commissioning_plan()`의 `needs_criteria`와 `completion_docset()`의 `four_axis`는 **결손을 드러내지만**, 170차에 세운 기계 규칙은 **반환 데이터클래스의 필드**를 본다. 규칙을 중간에 넓히면 척도가 무너지므로 **보수적으로 「산출」에 둔다**.

- 🔴 **부지 공종설계는 ✅였는데 「산출」로 강등된다** — 배정 함수가 전부 조회(`siting_design_load`·
  `siting_lookup`·`select_specs`·`spec_crops`)와 산출(`mean_wind`)뿐이라 **불일치를 행으로
  드러내는 함수가 없다**. 지역이 표에 없으면 `None`을 돌려줄 뿐이다.
- ✅ **기자재 품질설계는 🟡에서 「판정지원」으로 올라간다** — `doc_consistency_check()`가
  `equipment_model`·`manufacturer`·`verification`을 **행에 담아 Rev 정합을 판정**한다.
- ⚠️ **「검증」 0칸은 「검증을 안 한다」가 아니다** — `verify_heating_vs_actual()`·
  `benchmark_check()`는 있으나 **같은 칸의 판정지원에 덮인다**. 상세: `서비스설계_5단계척도_회귀앵커_20260920.md`

### 🔴 179차 — 집계가 **두 문서에 있고 값이 다르다**

| 문서 | 집계 | 시점 |
|---|---|---|
| `서비스설계_5단계척도_회귀앵커_20260920.md` §1-2 | 판정지원 4 · 검증 0 · **산출 8** · 조회 2 · **없음 4** | **171차에 멈춰 있다** |
| **이 문서** §2(위 표) | 판정지원 4 · 검증 0 · **산출 9** · 조회 2 · **없음 3** | 173차 갱신 |

차이는 **정확히 한 칸**이다 — **기자재 × 감리**(173차에 ⬜ → 「산출」). 앵커 문서는 **171차
날짜가 박힌 기록**이라 그대로 두는 것이 맞지만, 🔴**171차 가드가 고정하고 있던 것은 그
날짜 박힌 쪽이고 살아 있는 이 표는 아무도 재지 않았다**. 173차 이후 **174·175·176차가
함수를 5개 더하는 동안 집계를 다시 잰 적이 없다**(「낡은 산출물로 통과하는 가드」 계열).

**다시 쟀다 — 5함수는 칸을 옮기지 않는다.**

| 함수 | 차수 | 반환 | `status`·`checks`·`rows`·`counts`·`missing` | 칸 |
|---|---|---|---|---|
| `inspection_checklist()` | 173 | `dict` | 없음 | 감리 — 「산출」 유지 |
| `commissioning_plan()` | 173 | `dict` | **`rows` 있음** | 감리 — 「산출」 유지(아래 이견) |
| `completion_docset()` | 173 | `dict` | 없음(`four_axis`) | 감리 — 「산출」 유지 |
| `maintenance_schedule()` | 174 | `dict` | **`rows` 있음**(+`needs_interval`) | 사후관리 — 「산출」 유지(아래 이견) |
| `defect_tracking()` | 174 | `dict` | **`rows` 있음** | 사후관리 — 「산출」 유지(아래 이견) |
| `site_permit_checklist()` | 175 | `dict` | 없음(`missing_inputs`) | 부지 공종설계 — 이미 「산출」 |
| `equipment_reconcile()` | 175 | `dict` | **`rows` 있음** | 기자재 품질설계 — 이미 「판정지원」 |
| `service_life_reference()` | 176 | `dict` | 없음 | 사후관리 — 이미 「산출」 |

→ **위 표의 「산출 9 · 없음 3」은 지금도 맞다.** 179차가 바꾼 것은 **수가 아니라 「그 수를
누가 재는가」**다 — 두 매트릭스 표를 파싱해 칸을 세고 본문 집계와 대조하는 가드를 세웠다.

### ⚠️ 이견 — 규칙 문구와 가드 구현이 다르다

- **문구**(앵커 문서 §1-1): 「판정지원」의 기계 근거는 *"**반환 구조**에 `status`·`checks`·
  `overall_status`·`rows`·`counts`·`missing` 중 하나"*
- **구현**(171차 가드): **데이터클래스 필드**만 본다 — `dict` 키는 세지 않는다
- 173차가 이 좁은 쪽을 명시적으로 택했다(*"규칙을 중간에 넓히면 척도가 무너진다"*)

🔴 **넓게 읽으면 네 칸이 올라간다.** 8함수를 호출해 반환 키를 재니 **`rows`를 가진 것이
4개**다 — `commissioning_plan()` · `maintenance_schedule()` · `defect_tracking()` ·
`equipment_reconcile()`. 이들이 걸리면 **감리 2칸(시설·기자재)과 사후관리 2칸(시설·기자재)이
「산출」 → 「판정지원」**으로 올라가고 집계는 **판정지원 8 · 검증 0 · 산출 5 · 조회 2 · 없음 3**이 된다.
📌**자기 정정**: 이 절의 1차 작성에서 `maintenance_schedule()`·`defect_tracking()`을 *"`rows` 없음"*
으로 적었는데 **호출이 실패한 것을 「없음」으로 읽은 것**이었다 — 인자를 맞춰 다시 호출해 정정했다.
**★ 어느 읽기를 택할지는 결정이었다 — 임의로 고르지 않았다.** → ✅**180차에 사용자가 넓은 읽기를 채택했다**(아래 절).


### ✅ 180차 — ★ **넓은 읽기를 채택했다**(사용자 결정 2026-09-20)

**개정된 규칙**: 「판정지원」의 기계 근거는 **반환 구조**에 `status`·`checks`·`overall_status`·
`rows`·`counts`·`missing` 중 하나가 있는가다 — **데이터클래스 필드와 `dict` 키를 함께** 센다.

🔴 **신설 8함수만 다시 재면 틀린다.** 엔진 **공개 함수 62개를 AST로 전수** 재니 판정 필드를
가진 것이 **18개**다 — 데이터클래스 경유 6(`compare_design_options`·`compare_quotes`·
`doc_consistency_check`·`dscr_schedule`·`heating_load_components`·`reconcile_quote`)에
**`dict` 경유 9**(`benchmark_check`·`commissioning_plan`·`consulting_fee_estimate`·
`defect_tracking`·`equipment_reconcile`·`lcc_replacement_schedule`·`loan_amortization`·
`maintenance_schedule`·`verify_heating_vs_actual`)가 더해진다. 179차가 이름을 댄 4개는
**그중 일부**였다.

#### 배정표 — 이제 매트릭스를 **손으로 세지 않는다**

⚠️ **칸 배정 자체는 판단이다**(166차 관례). 아래 표가 그 판단이고, **등급과 집계는 이 표에서
기계가 다시 계산한다** — 가드가 배정 함수의 판정 필드 보유를 AST로 재서 **「하나라도 있으면
판정지원, 하나도 없으면 판정지원이 아니다」를 양방향으로** 검사한다.

| 칸 | 배정 함수(판단) | 판정 필드 보유(실측) | 등급 |
|---|---|---|---|
| **부지 × 공종설계** | `siting_design_load()` · `siting_lookup()` · `select_specs()` · `spec_crops()` · `mean_wind()` · `design_outdoor_temp()` · `heating_degree_hours()` · `monthly_mean_wind()` · `monthly_sunshine()` · `site_permit_checklist()` | 없음 | 산출 |
| **부지 × 품질설계** | `siting_design_load()` · `ksfid_grade()` | `ksfid_grade` | 판정지원 |
| **부지 × 감리** | — | 없음 | 없음 |
| **부지 × 타당성검증** | `finance()` | 없음 | 산출 |
| **부지 × 운영** | — | 없음 | 없음 |
| **부지 × 사후관리** | — | 없음 | 없음 |
| **시설 × 공종설계** | `select_specs()` · `cover_assembly_lookup()` · `cover_assembly_options()` · `heating_load()` · `heating_load_components()` · `curtain_exposure_ratio()` · `period_load_adjust_k()` · `wind_correction_factor()` · `generate_rfq_package()` · `compare_design_options()` | `heating_load_components`· `compare_design_options` | 판정지원 |
| **시설 × 품질설계** | `doc_consistency_check()` · `verify_heating_vs_actual()` · `benchmark_check()` · `transmission_share_pct()` · `ksfid_grade()` | `doc_consistency_check`· `verify_heating_vs_actual`· `benchmark_check`· `ksfid_grade` | 판정지원 |
| **시설 × 감리** | `design_supervision_fee_reference()` · `warranty_period()` · `inspection_checklist()` · `commissioning_plan()` · `completion_docset()` · `progress_certification()` | `commissioning_plan`· `progress_certification` | 판정지원 |
| **시설 × 타당성검증** | `greenhouse_total_estimate()` · `structure_only_estimate()` · `capex_breakdown()` · `capex_major_breakdown()` · `apply_overheads()` · `pumsem_labor_days()` · `pumsem_project_labor_summary()` · `reconcile_quote()` · `compare_quotes()` · `finance()` · `npv()` · `irr()` · `loan_amortization()` · `dscr_schedule()` · `operating_breakeven()` · `max_investable_capex()` · `cluster_economics()` · `subsidy_application_checklist()` · `construction_company_list()` | `reconcile_quote`· `compare_quotes`· `loan_amortization`· `dscr_schedule` | 판정지원 |
| **시설 × 운영** | `env_fitness()` · `yield_adjustment()` · `production_kg()` · `opex_breakdown()` · `improvement_roi()` | 없음 | 산출 |
| **시설 × 사후관리** | `warranty_period()` · `lcc_replacement_schedule()` · `maintenance_schedule()` · `defect_tracking()` · `performance_shortfall()` · `guarantee_assessment()` | `lcc_replacement_schedule`· `maintenance_schedule`· `defect_tracking`· `guarantee_assessment` | 판정지원 |
| **기자재 × 공종설계** | `equipment_lookup()` · `equipment_component_prices()` | 없음 | 조회 |
| **기자재 × 품질설계** | `doc_consistency_check()` · `equipment_reconcile()` · `ksfid_grade()` | `doc_consistency_check`· `equipment_reconcile`· `ksfid_grade` | 판정지원 |
| **기자재 × 감리** | `inspection_checklist()` · `commissioning_plan()` | `commissioning_plan` | 판정지원 |
| **기자재 × 타당성검증** | `equipment_component_prices()` · `capex_major_breakdown()` | 없음 | 산출 |
| **기자재 × 운영** | `opex_breakdown()` | 없음 | 산출 |
| **기자재 × 사후관리** | `lcc_replacement_schedule()` · `maintenance_schedule()` · `service_life_reference()` · `performance_shortfall()` · `guarantee_assessment()` | `lcc_replacement_schedule`· `maintenance_schedule`· `guarantee_assessment` | 판정지원 |

📌 `commissioning_plan()`은 **시설·기자재 두 칸에 모두** 배정했다 — 시운전은 설비 인수와 장비
합격을 함께 다루고, §1-b **R3**(*"두 면을 가진 품목은 면별로 센다"*)가 그 선례다.
📌 **칸 밖 함수 7개**: `consulting_fee_estimate()` · `guarantee_fee()` · `m2_to_py()` · `py_to_m2()` · `ksfid_number()` · `ksfid_validity()` · 🔴`revenue_won()`(192차) — 단위 변환·**과금/수수료**(§3-c)와 **식별번호·유효기간**(등급이 아니라 발급 사무), 그리고 **매출 산식**이라 3×6 칸에 속하지 않는다. ⚠️`revenue_won()`이 늘어난 것은 **기능이 늘어서가 아니다** — 표시 계층에 복제돼 있던 산식을 엔진으로 **회수**한 것이다(1절 병렬 계산기 금지).

#### 개정된 18칸

| | 공종설계 | 품질설계 | 감리 | 타당성검증 | 운영 | 사후관리 |
|---|---|---|---|---|---|---|
| **부지** | 산출 | **판정지원** | 없음 | 산출 | 없음 | 없음 |
| **시설** | **판정지원** | **판정지원** | **판정지원** | **판정지원** | 산출 | **판정지원** |
| **기자재** | 조회 | **판정지원** | **판정지원** | 산출 | 산출 | **판정지원** |

**판정지원 9 · 검증 0 · 산출 5 · 조회 1 · 없음 3.**

✅ **187차 — ★ 등급 부여가 들어와 한 칸이 더 올라갔다**: `ksfid_grade()`가 `rows`를
가지고 **부지·시설·기자재 품질설계**에 배정돼 **부지 품질설계가 조회 → 판정지원**이 됐다
(집계 `[8,0,5,2,3]` → **`[9,0,5,1,3]`**). 🔴**규칙이 넓어져서가 아니라 함수가 생겨서**다 —
180차 규칙은 그대로다.

**180차에 옮겨진 것은 네 칸**이었다 — **감리 시설·기자재**(`commissioning_plan`의 `rows`)와
**사후관리 시설·기자재**(`lcc_replacement_schedule`·`maintenance_schedule`·`defect_tracking`).
🔴 **없음 3칸은 그대로다** — 규칙을 넓혀도 **부지의 감리·운영·사후관리는 함수가 0개**다.
넓은 읽기는 **있는 것을 다시 볼 뿐 없는 것을 만들지 않는다.**

⚠️ **「검증」은 여전히 0칸**이고 이유가 하나 더 늘었다 — `verify_heating_vs_actual()`·
`benchmark_check()`가 `status`를 반환하므로 **개정 규칙에서는 그 둘 자체가 「판정지원」**이다.
셀당 역할 분포를 적지 않는 한 「검증」 등급은 계속 보이지 않는다.

---

## 3. 단계별 상세 설계

각 단계는 **입력 → 처리(엔진 함수 실명) → 산출물 → 검증 게이트 → 사람 개입**으로 정의한다.

### 3-1. 공종설계 (Work-type Design)

목적: *"이 부지에 무엇을 지을 수 있는가"*를 **규격·부하·공종 분해**로 확정한다.

| | 부지 | 시설 | 기자재 |
|---|---|---|---|
| **입력** | 행정구역명(자유입력) | 면적·형식·피복·커튼·설정온도 | 표준 장치명 / 모델명 |
| **처리** | `siting_design_load()` → 172지역 {적설심, 풍속} · `siting_lookup()` · `select_specs(crop=)` · `spec_crops()`(작목특화형 목록) · `mean_wind(region, months)` · **`site_permit_checklist()`**(175차 — 인허가·제출물) | `select_specs()` → 규격 249종 중 후보 · `cover_assembly_lookup()` 49종 · `cover_assembly_options()`(표기가 표마다 달라 **사용 전 키를 확인한다**) · `heating_load()`·`heating_load_components()`(원문 식 3-3-1 3성분) · `curtain_exposure_ratio()` · `period_load_adjust_k()` · `wind_correction_factor()` | `equipment_lookup()` · `equipment_component_prices()` |
| **산출물** | 설계하중 카드(지역·적설·풍속·출처) | **RFQ 사양서** `generate_rfq_package()` — 구조화 사양표(CAD 도면 아님) · 설계 대안 비교표 `compare_design_options()` | 구성품·표준가격 목록 |
| **게이트** | 지역 미매칭 시 `None` — **추정하지 않는다** | `verify_heating_vs_actual()` — **자릿수(10배·0.1배) 검증** · `select_specs` 하중 미충족 규격 제외 | 미등록 장치명은 `None` |
| **사람** | **부지 선정은 판단성** — 후보 나열까지 · **인허가 해당 여부는 지자체** | 작목·형식 확정 | 제조사 선정 |
| **상태** | ✅ (지반·측량·인허가는 ⬜, 아래 7절) | ✅ | 🟡 조회만 — **선정·정합 검증 없음** |
| **산출물** | **D1** | **D2·D3** | — |
| **★** | `D-5`·`D-6`·`D-9` | `D-1`~`D-4`·`D-14`·`FR_TABLE` 계열 | — |

🔴 **`mean_wind()`는 금지선이 코드로 굳은 자리다** — docstring이 *"`months`의 **기본값을 두지 않는다**"*라고 적는다. 원문이 *'동절기'*를 정의하지 않았기 때문이고, 그 정의가 바로 **★`D-9`**다. **기계가 임의로 고르지 않는다**는 원칙이 **함수 시그니처에 들어가 있다.**

### 3-2. 품질설계 (Quality Design)

목적: *"무엇을 만족해야 합격인가"*를 **문서 정합 + 성능 기준**으로 못 박는다.

- **문서 4축 정합**: `doc_consistency_check()` — **도면·시방서·BoQ·규격서**의 식별자·Rev를
  행 단위로 분류한다(`DocRefRow`에 `equipment_model`·`manufacturer`·`verification` 필드까지 있다).
  → **기자재 품질설계가 여기 걸려 있다** — 모델·제조사·검증방법이 4축과 같이 관리된다.
- **성능 기준**: `heating_load()` 결과 대비 `verify_heating_vs_actual()`(A-12 실측 기준).
- **원가 품질**: `benchmark_check()` — 단가가 실측 밴드 안인가.
- **부하 구성 검증**: `transmission_share_pct(group)` — [표 3-3-51]의 **관류열부하 비율**(계 91.9%)과 대조해 3성분 조립이 원문 구조에서 벗어나지 않았는지 본다.
- **기자재 정합**(175차): `equipment_reconcile(quoted_models, ks_declared, attachments_by_model)` — **3열 대조**(등재 DB · 규격 선언 · 재료승인 첨부 6종). 🔴시방서가 *「K.S규격에 합격한 신품 … 부득이한 경우 **감독자가 인정하는 동등품 이상**」*이라 적으므로 **적합 판정을 하지 않는다**.
- **산출물**: **D4** · **D19 기자재 대조표** · **★**: 없음
- **상태**: 시설 ✅ · 부지 🟡(설계하중 충족 여부만) · 기자재 🟡(4축 필드는 있으나 **성능시험·
  시운전 절차 0건**).

### 3-3. 감리 (Supervision)

목적: *"설계대로 시공됐는가"*.

- **있는 것**: `design_supervision_fee_reference()` — 건축공사감리 **대가 참고 추정**(결정론,
  CAPEX 불산입) · `warranty_period()` — 공종 → **법정 하자담보책임기간**(20공종 1차 출처 확보).
- ✅ **173차 — 감리 행위가 구현됐다**(P1): `inspection_checklist(work_type)` ·
  `commissioning_plan(equipment_names, acceptance_criteria)` · `completion_docset(report)`.
- 🔴 **기준을 창작하지 않았다.** 167차가 *"검측 기준 원문이 리포에 있는지 확인하지 않았다"*고
  남긴 것을 다시 재니 — **파일명 전수는 0건이지만 9축 인덱스에 455청크·36종**이 있었다.
  리포의 `chunking_lib_v2.hwp_extract_text()`로 **공사시방서 3종을 직접 추출**해
  절차 **9단계**를 전사했고(`INSPECTION_PROCEDURE_SPEC`), 각 단계에 **확인된 사본 수**를 달았다.
- **전사한 절차**: 1-11 제출물 계획서 → 1-10 종합공정표·시공계획서 → 1-13 재료승인 요청서 →
  1-13 **반입 재료 검사**(*"K.S규격품은 검사를 생략할 수 있다"*) → 1-14 **공시체·시험편 채취**
  (*"감독자의 입회하에 채취하고 봉인 … 독립된 공인기관에서 시험"*) → 1-14 재료시험 성적결과보고서 →
  1-16 **준공도**(*"준공 검사원 제출 7일전"*·원도 A3 3부·USB 1개) → 1-16 유지관리지침서·**시운전**
  (*"시공자 비용으로 이의없이 시행"*) → 1-16 사용검사 신청(4자 날인).
- **엔진 대조 지점 4곳**을 붙였다 — 규격(`select_specs`) · 피복(`cover_assembly_lookup`) ·
  난방(`verify_heating_vs_actual`) · 문서 4축(`doc_consistency_check`).
- ⚠️ **장비별 합격 기준은 원문에 없다** — `commissioning_plan()`이 **주입받고**, 미주입은
  `needs_criteria`로 **드러낸다**(만들지 않는다).
- **산출물**: **D13 검측 체크리스트 · D14 시운전 계획 · D15 준공 서류철** ·
  **★**: `D-11`(크루 구성 = **공기가 갈린다**) · **감리 대가 체계 선택**(국토부 [별표5] vs 산업부 [별표1])
- **판정**: 🟡 → **기능은 섰다.** 남은 공백은 **부지 감리**(지반 검측)와
  `성능시험`·`검사체크` 같은 **세부 항목표**다.

### 3-4. 타당성검증 (Feasibility Verification)

목적: *"이 돈으로 될 일인가, 견적이 사양과 맞는가"*. **엔진에서 가장 두꺼운 축이다.**

| 갈래 | 함수 | 산출물 |
|---|---|---|
| 개산 | `greenhouse_total_estimate()`(평단가×면적) · `structure_only_estimate()`(골조 단독) | 2가지 방법의 병렬 제시 |
| 원가 분해 | `capex_breakdown()` · `capex_major_breakdown()`(13 상위 카테고리, 잔차를 `unclassified`로 **남긴다**) | 공종별 비중 + **미분류 명시** |
| 간접비 | `apply_overheads()` (`OverheadRates` 7종) | 재·노·경 → 총원가 |
| 품셈 | `pumsem_labor_days()` · `pumsem_project_labor_summary()` | 직종별 총 인·일 |
| 견적 정합 | `reconcile_quote()` — **완전성/면적/규격코드/금액밴드 4검사** · `compare_quotes()` | `match_score_pct` + 업체 나란히 |
| 재무 | `finance()` · `npv()` · `irr()` · `loan_amortization()` · `dscr_schedule()` · `operating_breakeven()` · `max_investable_capex()`(CAPEX 상한 역산) · `cluster_economics()` | ROI·Payback·NPV·IRR·DSCR·손익분기 |
| 보조금 | `subsidy_application_checklist()` — **6단계 체크리스트, 계산·자동판정 없음** | 신청 절차표 |
| 업체 후보 | `construction_company_list(region)` — 도급순위 리스트를 **소재지로 거르기만** 한다 | 후보 명단(**추천 아님**) |

- **게이트**: `benchmark_check()` 밴드 이탈 · `capex_major_breakdown`의 잔차 · `reconcile_quote`의 4검사.
- **사람**: **업체 선정은 판단성** — `compare_quotes()`는 `lowest_cost_vendor`·
  `highest_match_score_vendor`를 **표시**할 뿐 **추천하지 않는다**.
- **산출물**: **D5·D6·D7·D9** · **★**: `D-7`·`D-8`·`D-10`·`D-12`·`D-13`·`D-15` **6건**(전 단계 중 최다)
- 🔴 **`construction_company_list()`가 금지선 ③의 실례다** — 리스트를 주되 *"업체선정 자체는 컨설턴트 몫"*이라고 **docstring이 스스로 적는다**. 166차 설계서는 **이 함수를 설명하지 않았다**(169차 §7 지적).
- **상태**: 시설 ✅ · 부지 🟡(토지비는 `finance(land_cost=)`로 **주입만**) · 기자재 🟡.

### 3-5. 운영 (Operation)

목적: *"지어진 뒤 얼마나 나오나, 얼마나 드나"*.

- **생산**: `env_fitness()`(광 0.5·온 0.2·습 0.2·CO₂ 0.1 — ⚠️**가중치가 `[추정]`이다**) →
  `yield_adjustment()` → `production_kg()`.
- **비용**: `opex_breakdown()` — lump-sum OPEX를 항목별로 나누고 **근거를 남긴다**
  (`OPEX_ITEM_CATEGORIES` 25항목). **연료는 `heating_load()`의 기간부하에서 유도**한다.
- **개선 투자**: `improvement_roi(annual_saving, invest)`.
- ✅ **182차 — 운영에 전용 산출물이 생겼다**: `D21 운영 진단표`. 181차까지 ⑤운영의 산출물은 **`D8` 링크뿐**이었고 `env_fitness()`·`opex_breakdown()`·`improvement_roi()`는 **어느 산출물에도 닿지 않았다**.
- **산출물**: **D8** · **D21 운영 진단표** · **★**: 없음(가중치는 결정이 아니라 **자료 대기 `S-3` 계열**)
- **상태**: 🟡 — **`env_fitness` 가중치가 [추정]**이고, 유가는 **시세성이라 주입만** 받는다.

### 3-6. 사후관리 (After-care)

목적: *"언제 무엇을 갈아야 하나, 언제까지 책임지나"*.

- `warranty_period()` — 공종별 법정 하자담보책임기간(전기 1년·통신 1년 등 **원문 확보**).
- `lcc_replacement_schedule(items, horizon_years)` — **LCC 교체주기(결정론)**.
- `EQUIPMENT_SERVICE_LIFE_REFERENCE` — 기자재 내용연수 참조.
- ✅ **174차 — 점검·보수 운영체계가 섰다**(P3): `maintenance_schedule(items, interval_months_by_item)` ·
  `defect_tracking(records, completion_date)`.
- 🔴 **하자는 리포 원문에서 왔다.** 공사시방서 3종(**3/3 사본**)의 하자보수 조항 —
  *"하자보수 기간내에 발생한 하자는 **시공자부담**으로 즉시 재시공 또는 보수"* ·
  *"제반 발생비용은 **하자보수 보증금에서 공제**"* · 🔴*"**하자 보수 기간은 해당 하자보수
  공사 완료때 까지 자동 연장**되는 것으로 한다"*. 마지막 조항이 `defect_tracking()`의 핵심이다 —
  보수가 끝나지 않은 건은 `expiry_extended=True`로 표시하고 **연장된 만료일을 만들지 않는다**
  (완료일이 없으면 확정할 수 없다). 담보기간은 `WARRANTY_STATUTORY` 등재값이고
  **미등록 공종은 지어내지 않는다**.
- 🔴 **점검 주기는 리포에 없다** — 「단동 비닐하우스 유지관리 개선방안 연구」는 **실태조사**이고
  주기표가 아니다. 그래서 `maintenance_schedule()`은 **주기를 주입받고** 미주입은
  `needs_interval`로 **드러낸다**. 교체 시점만 `EQUIPMENT_SERVICE_LIFE_REFERENCE`에서 유도한다.
- **외부 참고**`[외부출처]`: 난방기 **연 1회** 전문업체 점검(UMass Extension 계열) ·
  환기 모터·가동부 점검·윤활·교정 **주기 미명시** · 국내 농촌진흥청 **겨울철** 농업시설 관리요령
  (피복 보수·보온커튼 틈새). → **엔진에 넣지 않았다**(167차 경계).
- ✅ **176차 — 내용연수 3출처 대조**: `service_life_reference(name)`이 **조달청 고시**(공공물품 불용 기준) · **농진청 준용**`[추정]` · **세법**(별표6 구분2 농업 5년)을 나란히 놓고 `disagreement`만 표시한다. 🔴**어느 값을 쓸지 정하지 않는다** — 원본 파일의 권고(*「감가상각은 세법, 경제성분석은 농진청」*)도 **권고이지 판정이 아니다**.
- **산출물**: **D10** · **D16 점검 일정표** · **D17 하자 추적표** · **D20 내용연수 대조표** · **★**: 없음
- **상태**: 🟡 → **기능은 섰다.** 남은 공백은 **점검 주기의 국내 기준**이다.

---

## 3-b. ★ 결정 지점은 단계마다 있다 (170차 신설 — 개선 ③)

🔴 166차 게이트 흐름도는 ★를 **「업체·사양 확정」 한 곳**에만 놓았다. 실측은
**★결정 15건 + 자료 4건**이고, 167·168차가 **두 건을 더 만들었다**. 고객에게
*"한 번만 정하면 된다"*는 인상을 주면 **그 자체가 거짓**이다.

| 단계 | ★ 결정 | 건수 |
|---|---|---|
| **①공종설계** | `D-1` 3성분 8% 보정 · `D-2` `u_design` 5.7→8.9 · `D-3` 일조계수 k · `D-4` `COVER_ASSEMBLIES` 이관 · `D-5` `t_min` 처리 · `D-6` 마산↔창원 별칭 · `D-9` 동절기 개월 정의 · `D-14` 비닐 7종 등재 · **`FR_TABLE` 계열 채택**(168차) | **9** |
| **②품질설계** | — | 0 |
| **③감리** | `D-11` 크루 구성(=**공기가 갈린다**) · **감리 대가 체계 선택**(167차 — 국토부 [별표5] 1.35% vs 산업부 [별표1] 1.66%) | **2** |
| **④타당성검증** | `D-7` 우민재 2,323 · `D-8` 무인방제 카테고리 이관 · `D-10` 공구손료 등재 · `D-12` 공종 선언 순서 · `D-13` 부대시설 이관 · `D-15` 품셈 `[주]` 적용조건 | **6** |
| **⑤운영** | — (`env_fitness` 가중치는 `[추정]` — 결정이 아니라 **자료 대기**) | 0 |
| **⑥사후관리** | — | 0 |
| | **합계** | **17** |

### 자료 대기 S-1~S-4는 다른 성격이다

| | 막고 있는 것 | 단계 |
|---|---|---|
| **S-1** ⭐ | 원채원·공주장원리·당진이상근 **원문 미보유** — 회귀 기준의 원문이 없다 | ④(+①) |
| **S-2** ⭐ | `SmartFarm_엔진데이터.md` — 밴드 유도 근거 **+** A-12 난방실측(→`D-2` 판정) | ④·① |
| **S-3** | 농진청 OPEX 코드 CSV(0바이트) — 🔴**178차에 절반 풀렸다**: 조사표 `소득분석` 시트로 **명칭 7건·용어 대응 6쌍**을 독립 확인했다. **코드(`3010104`·`3010117`)는 29시트 전수 0건**이라 여전히 대조 불가 | ⑤ |
| **S-4** | (B)계열 보존본 3종 — 들어오면 **추적성 사각 2 → 0**(178차에 3 → 2 · 남은 것은 `SPEC_COUNT`·`SPEC_TABLE`) | ① |

📌 **★는 「고르면 되는 것」이고 S는 「자료가 와야 고를 수 있는 것」이다.**
`D-2`처럼 **S-2가 와야 판정되는 ★**도 있다 — 순서를 뒤집으면 근거 없이 고르게 된다.

---

## 3-c. 단계 과금은 어떻게 서는가 (172차 신설 — 개선 ⑥)

169차 §4: *"과금 근거가 6단계 중 2단계뿐이고, 함수가 가장 많은 ④타당성검증이 값을 못 매긴다."*
→ **막고 있던 것은 근거가 아니라 「우리가 투입을 정하지 않은 것」**이었다.

`consulting_fee_estimate(mandays_by_grade, wage_by_grade, overhead_rate, tech_fee_rate)`를
신설했다 — 실비정액가산 산식(제경비 = 직접인건비 × r · 기술료 = (직접인건비+제경비) × r)을
그대로 구현하되 **요율·노임단가에 기본값을 두지 않는다**. 고시는 **범위**만 정하고
그 안의 선택은 **협의(시세성)**이기 때문이다 — `mean_wind()`가 `months`에 기본값을
두지 않는 것과 같은 관용이다.

| 단계 | 과금 계층 |
|---|---|
| ①입지진단 · ③품질설계 · ④타당성검증 · ⑥운영·사후관리 | **B 실비정액가산** — `consulting_fee_estimate()` |
| ②공종설계 | **B** + **A** 공사비요율([별표1] 기본설계 3.78% · 실시설계 6.16%, 비고5 도로 열) |
| ⑤감리 | **A 법정 요율 두 체계 병렬** — 국토부 [별표5] 1.35% / 산업부 [별표1] 비상주 1.66% |

✅ **182차 — 과금에도 산출물이 생겼다**: `D22 컨설팅 대가 산출`. 172차에 함수를 만들고도 **어느 산출물에도 붙이지 않았다**.

⚠️ **투입 인·일은 `[제안]`**(우리 원가 = 판단성)이고 **노임단가는 주입**(시세성)이다.
상세·대조: `서비스설계_과금_전단계확장_20260920.md`

---

## 3-d. 조립 계층 — 함수를 산출물로 잇는다 (181차 신설)

🔴 **측정하고 나서야 보였다**: 180차까지 엔진 공개 함수는 **62개**인데 산출물 계층
(`build_site`·`webapp`·`cases`·`render_report`)이 부르는 것은 **17개**뿐이었다.
**D13~D20(173~176차 신설 8함수)은 생성 경로가 아예 없었다** — 함수는 있는데
**고객에게 가는 길이 없었다**.

`consulting_package.py`가 케이스 1건을 받아 **D1~D20을 한 장으로** 세운다.

| | 181차 | **182차** |
|---|---|---|
| 산출물 계층 도달 범위 | 17 → 46 / 62 | **18 → 70 / 70**(192차) |
| 패키지가 이름을 대고 **실제로 부르는** 엔진 함수 | 43종 | **68종** |
| 아직 어느 산출물에도 닿지 않는 함수 | 16개 | **0개** |
| 산출물 카탈로그 | D1~D20 | **D1~D27**(182차 2 · 185차 2 · 187차 ★등급 1 · 🔴188차 성능보증·기성 2) |
| 케이스 1건의 상태 분포 | 생성 8 · 부분생성 3 · 링크 3 · 주입대기 6 | 생성 **8** · 부분생성 **10** · 링크 **3** · 주입대기 **6** |
| 미해결 주입 슬롯 | 17종 | **38종**(이름·성격을 밝혀 둔다 — 188차 실측) |

### 🔴 이 계층이 지키는 경계 셋

1. **계산하지 않는다** — 산술 연산자(`*`·`/`·`-`·`//`·`%`·`**`)가 **한 줄도 없다**.
   가드가 AST로 검사한다. 4축 수치는 이미 있는 단일 경로 `render_report.compute`를
   **재사용**한다. 여기서 다시 계산하면 그것이 곧 **병렬 계산기**다(1절).
   📌집합 차집합도 `-`가 아니라 `.difference()`로 쓴다 — **예외를 하나 두면 그것이 문이 된다**.
2. **채우지 않는다** — 주입이 없는 산출물은 `주입대기`로 남고 `data`가 `None`이다.
   생성된 산출물 안에서도 미주입은 살아 있다: 점검 주기는 `needs_interval`,
   시운전 기준은 `needs_criteria`, 용도지역은 `missing_inputs`로 **드러난다**.
   🔴**빈칸을 기본값으로 덮는 것이 가장 조용한 창작이다** — 가드가 그 셋을 각각 고정한다.
3. **고르지 않는다** — 반환 어디에도 `recommended`·`selected`·`rank`·`추천`·`최종판정`이
   없다. `compare_quotes()`의 최저가·최고정합도 **표시일 뿐**이고,
   `construction_company_list()`는 **소재지로 거른 명단**이다. RFQ 기준 **형식 선택조차
   주입 슬롯**(`rfq_form`)이다 — 판단성이라 기계가 고르지 않는다.

### 📌 자료 요청서가 곧 산출물이다

주입이 없어 서지 못한 칸은 **빈칸이 아니라 「무엇이 없는지」**로 나온다. 페이지 맨 앞의
**「아직 주입되지 않은 것 17종」**이 그것이고, 각 항목에 **왜 없는지**가 붙는다 —
시세성(단가·금리)인지, 고객 문서(도면·견적·하자 기록)인지, ★결정 대기(`winter_months`는
`D-9`, `curtain`은 `FR_TABLE` 계열)인지, **리포에 국내 기준이 없는 것**(`inspection_intervals`)인지.

### ✅ 182차 — 남은 16함수를 연결했다

181차가 **세어 두었던 16개**를 모두 붙여 **도달 범위가 62 / 62**가 됐다. 늘린 방법은
**숫자를 채우는 것이 아니라 빠진 칸을 찾는 것**이었다 —

| 빠져 있던 것 | 붙인 자리 |
|---|---|
| `wind_correction_factor` · `period_load_adjust_k` | **D1** — 동절기 평균풍속에서 풍속 보정계수로, 일조시간에서 기간부하 보정 k로 이어진다 |
| `cover_assembly_lookup` · `curtain_exposure_ratio` · `py_to_m2` · `equipment_lookup` · `equipment_component_prices` | **D2** |
| `transmission_share_pct` · `heating_load_components` | **D4** — 원문 관류열부하 비율(91.9%)과 3성분 분해 |
| `npv` · `irr` · `cluster_economics` | **D9** — 시나리오 현금흐름·단지화 |
| `revenue_won`(🔴192차 신설) | **D8**·**D9** — 매출 = 생산량 × 단가. **신기능이 아니라 회수**다: 이 한 줄이 `render_report.compute`와 `build_site._sensitivity_snapshot` **두 곳에 복제**돼 있었다(1절 병렬 계산기) |
| `env_fitness` · `opex_breakdown` · `improvement_roi` | **D21 운영 진단표**(신설) |
| `consulting_fee_estimate` | **D22 컨설팅 대가 산출**(신설) |

🔴 **두 칸이 비어 있었다**: **⑤운영에는 산출물이 `D8` 링크뿐**이었고, **172차에 만든 과금
함수는 어느 산출물에도 붙어 있지 않았다**. 함수를 이어 붙일 곳을 찾다가 **카탈로그의 구멍**을
찾은 셈이다.

⚠️ **미해결 주입이 17 → 27종으로 늘었다.** 연결이 늘면 **필요한 자료도 같이 드러난다** —
줄어드는 것이 좋은 수가 아니다(줄었다면 어디선가 값을 지어냈다는 뜻이다).

🔴 **`장비정보.csv`는 행을 그대로 옮기지 않았다** — 그 파일에는 `농장명/업체명`·`농장주`·
`계약 금액` 열이 함께 있다. 패키지는 **표준 장치명별 등재 수만 센다**.

---

## 3-e. 산출물에서 실명을 뺀다 (183차 신설)

케이스 5건은 **실제 농가·기관 이름**을 달고 있다. 산출물은 팀과 공유되므로
**생성 HTML과 파일명에서** 실명을 뺀다 — `case_display.py`.

| | 내용 |
|---|---|
| 표시 코드 | **C1~C5** · 라벨은 식별에 필요한 것만(지역 · 작목 · 피복 · 형식 · 성격) |
| 지역은 남긴다 | 설계하중·기상 조회의 **근거**라서 가리면 숫자를 읽을 수 없다. **행정구역명은 실명이 아니다** |
| 파일명 | `SmartFarm_통합보고서_C2.html` — **URL이 곧 노출**이라 파일명도 코드다 |
| 미등재 케이스 | 웹앱으로 새로 저장된 케이스는 `case_id` **해시**로 `CX-xxxx` — 이름으로 물러서지 않는다 |
| 적용 범위 | **184차에 산출물 전체 18건**(근거대장·CAPEX분해·벤치마크·견적비교 포함) — **실명 0** |

🔴 **내부 데이터는 손대지 않았다.** `cases/*.json`의 `case_id`·`title`, 엔진 출처 표기,
레지스트리, 테스트 앵커는 **그대로**다 — 이름은 **값이 아니라 출처**이고 바꾸면
**추적성이 끊긴다**(1절: 출처를 파일명·시트·행까지 병기한다).

### ✅ 184차 — ★ 범위를 **산출물 전체**로 넓혔다(사용자 결정)

183차는 근거대장 계열의 **실측 출처 이름**을 남겨 두었다(*「가리면 근거대장이 근거대장이
아니게 된다」*). 사용자가 **가리라**고 정했다.

🔴 **지우지 않고 코드로 바꾼다.** 근거대장은 **추적성 산출물**이라 이름을 지우면 출처가
사라진다 — **한 실체 = 한 코드**로 치환하고 **비-실명 출처 정보는 그대로 둔다**.

| 코드 | 무엇 | 출처 |
|---|---|---|
| **C1~C5** | 케이스 | — |
| **A1~A7** | `ACTUALS` 실측 9건 중 **케이스가 아닌 7건** | 원채원=**C2** · 우민재=**C3**(한 실체 = 한 코드) |
| **S1~S11** | `CAPEX_MAJOR_CASE_CHUNKS` 표본 등 개인·농가 | |
| **V1~V15** | 시공·견적 업체, `SPEC_TABLE` 개발사, 공표 보고서 수행사 | |
| **`[소재지]`** | **번지 수준 주소 7건** — 이름을 가려도 주소가 대신 식별시킨다 | 시·군·읍·면은 **남긴다** |

🔴 **공공기관·정부부처·지자체는 남긴다** — 농촌진흥청·조달청·국토교통부·한국농어촌공사·
성주군농업기술센터는 **법령·고시·공표자료의 주체**이고 가리면 **인용이 불가능해진다**.
**사기업·개인만 가린다.**

**남은 것으로 대조가 되는가**: 근거대장에 `별표` 128 · `고시` 42 · `.pdf` 147 · `.xlsx` 27 ·
`.hwp` 21 · `시행령` 31 · `p.` 214가 그대로 있다. 같은 코드가 **모든 페이지에서 같은 원본**을
가리키므로 원문 대조는 여전히 가능하다. **원본 이름 ↔ 코드 매핑은 `case_display.py`
(리포 내부)에만 있고 산출물에 싣지 않는다.**

### 🔴 이번에 실제로 걸린 것 둘

1. **실명 조각** — `그린팜`을 항목으로 두자 `그린팜스글로벌`이 **`V7스글로벌`**로 잘렸다.
   리포에서 `그린팜`은 **5회 전부 그 긴 이름 안**에 있어 **항목으로 두면 안 된다**.
2. **순수 인명 누락** — `이동혁`·`오기수`·`맹주연` 등은 **업체 접미 기반 후보 추출이 놓쳤다**.
   목록은 **엔진의 권위 있는 키**(`ACTUALS` · `CAPEX_MAJOR_CASE_CHUNKS` ·
   `SPEC_TABLE.developer`)에서 뽑아야 한다 — 가드가 그 셋을 전수 대조한다.

📌 **주소는 정규식으로 훑지 않았다**: `천창유리 136`·`일반관리203`처럼 **「리」로 끝나는
일반어 + 숫자**가 그대로 걸린다(실측). **명시 목록 7건**만 가린다.

📌 **고아 파일이 11건 있었다**: 이름을 바꾼 뒤 옛 이름의 산출물이 리포에 남아 있었고
**git이 추적하고 있었다**. 지웠고, 가드가 **파일명 전수**를 다시 센다.

---

## 4. 산출물 카탈로그 — 고객에게 무엇을 주는가

| # | 산출물 | 생성 | 근거 표기 |
|---|---|---|---|
| D1 | **입지 진단 카드** (설계하중·기상 4종) | `siting_lookup` + `design_outdoor_temp`·`heating_degree_hours`·`monthly_mean_wind`·`monthly_sunshine` | 고시 [별표] 172지역 · 표3-3-38/42/44/45 |
| D2 | **RFQ 사양서** (구조화 사양표) | `generate_rfq_package()` | 규격 249종 · 피복조합 49종 |
| D3 | **설계 대안 비교표** | `compare_design_options()` | 대안별 부하·개산 병렬 |
| D4 | **문서 4축 정합 리포트** | `doc_consistency_check()` | 식별자·Rev 행 단위 |
| D5 | **견적 정합·업체 비교표** | `reconcile_quote()` · `compare_quotes()` | 4검사 + `match_score_pct` |
| D6 | **원가 분해표**(13 상위 + 미분류) | `capex_major_breakdown()` | 실측 표본 대비 밴드 |
| D7 | **품셈 인력 산출표** | `pumsem_project_labor_summary()` | 표준품셈 |
| D8 | **4축 통합 컨설팅 리포트** | `build_site.consulting_report_page()` | 케이스 JSON + provenance |
| D9 | **재무 시나리오**(NPV·IRR·DSCR·손익분기·CAPEX 상한) | `finance`·`dscr_schedule`·`operating_breakeven`·`max_investable_capex` | 주입 단가 명시 |
| D10 | **LCC·하자 관리표** | `lcc_replacement_schedule()` · `warranty_period()` | 법령 조문 |
| D11 | **근거대장** | `build_site.registry_page()` | 상수 **65** · refs **178** · 배지 |
| **D12** | **결정 지원 패키지**(170차 신설) | 아래 4구성 | ★17건 + 자료 4건 |
| **D13** | **검측 체크리스트**(173차 신설) | `inspection_checklist()` | 시방서 9단계 + 엔진 대조 4곳 + 하자기간 |
| **D14** | **시운전 계획**(173차 신설) | `commissioning_plan()` | 장비별 기준 **주입**, 미주입은 드러낸다 |
| **D15** | **준공 서류철**(173차 신설) | `completion_docset()` | 시방서 열거 5종 + 4축 정합 상태 |
| **D16** | **점검 일정표**(174차 신설) | `maintenance_schedule()` | 주기 **주입**, 미주입은 드러낸다 |
| **D17** | **하자 추적표**(174차 신설) | `defect_tracking()` | 법정 담보기간 대조 + **자동 연장** |
| **D18** | **인허가 체크리스트**(175차 신설) | `site_permit_checklist()` | 시방서 4단계 + 확인 항목 4 + **비용 이견 2건** |
| **D19** | **기자재 대조표**(175차 신설) | `equipment_reconcile()` | 등재 DB · 규격 선언 · 첨부 6종 3열 |
| **D20** | **내용연수 대조표**(176차 신설) | `service_life_reference()` | 조달청 · 농진청`[추정]` · 세법 3출처 + 이견 |
| **D21** | **운영 진단표**(182차 신설) | `env_fitness()` · `opex_breakdown()` · `improvement_roi()` | 가중치 `[추정]` 명시 · OPEX 잔차 `unclassified` 유지 |
| **D22** | **컨설팅 대가 산출**(182차 신설) | `consulting_fee_estimate()` · `design_supervision_fee_reference()` | 노임·요율 **주입 전용** · `in_notice_range`는 **대조 결과이지 판정이 아니다** |
| **D23** | **투자 실사 카드**(185차 신설 — 기획서 1단계) | `select_specs()` · `benchmark_check()` · `dscr_schedule()` · `site_permit_checklist()` | 6영역 중 **2개는 엔진 밖**이라 그렇게 적는다 · **등급 없음** |
| **D24** | **설계값–실측 대조표**(185차 신설 — 기획서 3단계) | `verify_heating_vs_actual()` · `benchmark_check()` · `production_kg()` | 5항목 중 **3개는 편차 함수가 없다** · **보증 지급 판정 없음** |
| **D25** | **K-SFID 검증 등급·인증서**(187차 신설 — ★사용자 결정) | `ksfid_grade()` · `ksfid_number()` · `ksfid_validity()` | 검증 항목 **7종**(실격 2) · 등급마다 **근거 행** · **미검증은 통과로 세지 않는다** |
| **D26** | **성능보증 판정서**(188차 신설 — ★사용자 결정) | `performance_shortfall()` · `guarantee_assessment()` · `guarantee_fee()` | 면책 밖 미달만 지급 대상 · **손실액·요율은 주입** · 보험가액 상한 |
| **D27** | **기성 확인서**(188차 신설) | `progress_certification()` | 공종별 기성률 · 계획 밖 공종은 **합계에 넣지 않는다** · **지급 승인 없음** |

### D12 — 「추천은 없지만 결정은 쉬워진다」의 구체적 형태 (개선 ①)

169차 §8: *"「하지 않는 것」은 6문장인데 「대신 주는 것」은 목록뿐이다."*
**자산은 이미 리포에 있었다** — 세우지 않았을 뿐이다.

| 구성 | 실체 | 상태 |
|---|---|---|
| **① 결정 대기 목록 + 선행조건** | `근거_결정대기대장_20260915.md` (17KB) — ★15건 · 자료 4건, 각 항목의 **선행조건과 얽힘**까지 | ✅ 있다 |
| **② 감응도** — *바꾸면 얼마나 움직이나* | `근거_감응측정_D10_D12_*.md` · `근거_얽힘4건_감응측정_*.md` · `근거_잔여결정8건_감응측정_*.md` · `근거_D5잔여감응_*.md` (합 34KB) | ✅ 있다 |
| **③ 반대근거** | `검증절차_레드팀.md` (**120KB**) — fresh-context 대조 28회차 기록, 발견·거짓양성·조치 | ✅ 있다 |
| **④ 결정 로그** — *누가·언제·무엇을 근거로* | — | ⬜ **신설 대상** |

**왜 이것이 상품인가.** 금지선은 *"최종 판정을 하지 않는다"*고 말한다(189차 — 187·188차에
**규칙 적용형 등급·지급 판정**이 들어왔으므로 *"아무 판정도 안 한다"*는 더는 참이 아니다).
그러면 고객은 무엇으로
판정하는가 — **D12가 그 답이다**: 무엇을 정해야 하는지(①), 정하면 숫자가 얼마나
움직이는지(②), 반대 의견은 무엇인지(③). **판정을 대신하지 않고 판정을 가능하게 한다.**

⚠️ **④ 결정 로그는 아직 없다.** ①②③은 세우기만 하면 되고, ④는 만들어야 한다.

---

## 5. 근거·품질 체계 (서비스의 방어선)

| 게이트 | 명령 | 무엇을 보증하나 |
|---|---|---|
| 회귀 | `pytest test_engine.py test_registry.py test_cases.py -q` → **333 passed** | 실측 벤치마크 불변 |
| **단계별 앵커**(171차) | 같은 명령 | **A1** 설계하중(`None` 포함) · **A2** 규격 최소사양 · **A3** 4축 결손 판정 · **A4** 감리 요율 보간 · **A5** 수확량 계단(**과잉도 감점**) · **A6** LCC 교체 연도 — **6단계를 전부** 덮는다 |
| 전체 | 5파일 → **390 passed** | 청킹·웹앱 포함 |
| 추적성 | `python audit_traceability.py` → **PASS** | 모든 ref의 **원문 실재** |
| 재검산 | `python verify_refs.py` | exact ref의 **금액 원단위 재계산** |
| 산출물 | `python build_site.py` → *"생성 완료"* | 리포트 최신화 |

🔴 **게이트 순서는 `build_site` → `pytest`** — 산출물을 읽는 가드가 **낡은 HTML로 통과**한다(151차 실사고).

---

## 6. 자동화 금지선 — 서비스 계약서에 그대로 써야 할 문장

1. **작목 선정을 하지 않는다.** 작목별 규격 후보는 낸다(`select_specs(crop=)`).
2. **부지를 고르지 않는다.** 부지별 설계하중·규격 후보를 **나란히** 낸다.
3. **업체를 추천하지 않는다.** `compare_quotes()`는 최저가·최고정합을 **표시**만 한다.
4. **최종 판정을 하지 않는다.** 밴드 이탈·미분류·미매칭을 **드러내고 멈춘다**.
5. **시세를 조회하지 않는다.** 단가·노임·유가·금리는 **고객이 준 값**으로만 계산한다.
6. **근거 없는 수를 만들지 않는다.** `[추정]`/`[확인요망]`로 표기하고 출처를 병기한다.

→ 이 여섯이 **서비스의 상품성**이다. *"추천해 드립니다"*가 아니라
*"이 판단에 필요한 것이 전부 여기 있고, 빠진 것은 빠졌다고 적혀 있습니다"*.

---

## 7. 미구현 백로그 — 측정된 공백 (우선순위)

**~~P1 — 감리~~ ✅ 173차 완료**
- ~~`inspection_checklist(work_type)`~~ · ~~`commissioning_plan(equipment_list)`~~ ·
  ~~`completion_docset()`~~ — **전부 구현**(§3-3).
- 🔴 **선행조건이 이미 충족돼 있었다.** *"검측 기준 원문이 리포에 있어야 한다"*고 적어 두고
  **재지 않았는데**, 9축 인덱스에 **455청크·36종**이 있었고 **공사시방서 3종**에서 절차를
  전사했다. 167·168차와 같은 계열의 **탐색 범위 문제**다.
- **남은 것**: **부지 감리**(지반 검측 — 그 부지의 지내력 값이 필요하다) ·
  `성능시험`·`검사체크` **세부 항목표**(원문에 장비별 기준이 없다).

**~~P2 — 부지 전단(前段)~~ ✅ 175차 완료**
- ~~`site_permit_checklist()`~~ — **구현**(§3-1). 시방서 「관공서, 기타민원에 대한 인허가
  수속 및 협의」 절 **4단계**를 전사했고, 🔴**비용 부담이 사본마다 달라 이견으로 분리**했다.
- 🔴 **개별 인허가 조문은 여전히 리포에 없다** — 웹에서 읽은
  *「건축법 시행령 제15조⑤ 연면적 100㎡」*·*「골조 PVC+비닐이면 신고 면제(유리·FRP 제외)」*는
  **원문 미확보**라 엔진에 넣지 않았고, 함수는 **해당 여부를 판정하지 않는다**(지자체 사항).
- **남은 것**: 지반·지내력 — **그 부지의 값**은 지반조사 성과품에서만 나온다.

**~~P3 — 사후관리 운영~~ ✅ 174차 완료**
- ~~`maintenance_schedule(items)`~~ · ~~`defect_tracking()`~~ — **둘 다 구현**(§3-6).
- 🔴 **하자는 리포 원문(시방서 3/3)으로 전부 채웠고, 점검 주기는 리포에 없어 주입 슬롯**으로 뒀다.
- **남은 것**: **점검 주기의 국내 기준** — 외부 자료도 *"난방기 연 1회"* 수준이고
  나머지는 **주기 미명시**다.

**~~P4 — 기자재 정합~~ ✅ 175차 완료**
- ~~`equipment_reconcile(...)`~~ — **구현**(§3-2). **3열 대조**(등재 DB · 규격 선언 ·
  재료승인 첨부 **6종**)이고 **적합 판정은 하지 않는다**.
- 🔴 **부수 발견**: `온풍난방기`는 `EQUIPMENT_SERVICE_LIFE_REFERENCE`(내용연수 11년)에는
  있는데 `equipment_lookup()` DB에는 **없다** — **두 등재가 서로 다른 목록**이다.
  그래서 `not_in_equipment_db`는 *"결함"*이 아니라 **"등재 범위 밖"**이라고 적는다.

---

### ✅ 백로그가 전부 닫혔다 (P1 173차 · P3 174차 · P2·P4 175차)

남은 것은 **구현 대상이 아니라 자료**다 — 지반조사 성과품 · 점검 주기 국내 기준 ·
개별 인허가 조문 · `FR_TABLE` 계열 채택(★).

---

## 8. 단계 게이트 (서비스 운영 흐름)

```
  D11 근거대장 ─────────────────────────────────────────────┐ 전 단계 관통
  D12 결정지원 ─────────────────────────────────────────────┤ (대장·감응도·반대근거)
                                                            │
  ①입지 진단(D1)          ★3  D-5·D-6·D-9                  │
     │  gate: 지역 매칭 성공?                                 │
  ②공종설계(D2·D3)        ★6  D-1~D-4·D-14·FR_TABLE 계열   │
     │  gate: select_specs 후보 ≥ 1?                         │
  ③품질설계(D4)           ★0                                │
     │  gate: 4축 Rev 불일치 0?                              │
  ④타당성검증(D5·D6·D7·D9) ★6  D-7·D-8·D-10·D-12·D-13·D-15 │
     │  gate: 밴드 이탈·미분류 확인                            │
  ⑤감리                   ★2  D-11(공기) · 대가 체계 선택    │
     │  P1 미구현 — H1·H4·H6·H7 신설 대상                    │
  ⑥운영(D8)·사후관리(D10)  ★0                                │
                                                            ┘
```

🔴 **★는 17곳이다.** 166차 흐름도는 ★를 **한 곳**으로 그렸다 — 그것이 169차 §3의 지적이고,
이 표가 그 정정이다. `D-2`처럼 **자료 `S-2`가 와야 판정되는 ★**도 있으므로 순서가 있다.
⚠️ **★ 지점은 기계가 넘지 않는다.** 그 앞까지가 솔루션이고, 그 뒤는 컨설턴트의 일이다.
⚠️ **각 단계의 기간·대가는 이 문서에서 정하지 않았다** — 상업 조건은 판단성이고 **사용자 몫**이다.

---

## 9. 하지 않은 것 / 확인하지 못한 것

- **코드를 한 줄도 바꾸지 않았다.** 엔진·케이스·레지스트리 **값 변경 0** · **결정 0**.
- 🔴 **170차 — 169차 비판의 개선 ①②③⑦을 적용했다**(D12 신설 · 귀속 규칙 R1~R4 · ★ 17곳 단계 매핑 · 미인용 5함수 배치). **개선 ④⑤⑥은 적용하지 않았다** — ④⑤는 측정이 필요하고 **⑥은 표준 투입 인·일(=우리 원가)이라 ★사용자 결정이 선행**한다.
- **D12의 ④ 결정 로그는 아직 없다** — ①②③은 기존 자산을 세운 것이고 ④는 신설 대상이다.
- **7절 백로그를 구현하지 않았다** — 특히 **감리 4함수는 검측 기준 원문이 리포에 있는지
  확인하지 않았다**(파일명 전수 검색은 하지 않았고, 코드 5파일 내 키워드 0건만 확인했다).
- **단계 분류는 내 판단이다** — `design_supervision_fee_reference`를 「감리」에 둘지
  「타당성검증」에 둘지 같은 배치는 달라질 수 있다.
- **기간·대가·과금 구조를 설계하지 않았다**(상업 조건 = 판단성).
- **`env_fitness` 가중치가 `[추정]`이라는 한계는 그대로다** — 운영 축 산출물의 정밀도 상한이다.
- **커버리지 「부분」의 정도를 계량하지 않았다** — 있음/부분/없음 3단계까지만 구분했다.
