# `match` 등급 확정 — 미기재 56건, 이제 148건 전부 기록된다 (137차)

**작성일** 2026-09-15 (137차) · **계기** 레드팀 26회차 **F5**의 잔여
**성격** 엔진 상수 값 변경 0 · 레지스트리 **값** 변경 0. 바뀐 것은 **등급 표기와 note**뿐이다.
⚠️ 다만 **산출물(근거대장 HTML)의 배지가 11개 늘었다** — 등급은 독자에게 보이는 값이다.

---

## 1. 왜 등급이 비어 있으면 안 되나

```python
# audit_traceability.py
if r.get("match", "exact") not in ("exact", "near", "partial"):
```
```python
# build_site.py:611
_match_badge = {"near": " <b>[근접]</b>", "partial": " <b>[부분]</b>"}
```

**등급을 안 적으면 감사기에서는 `exact`로 간주되고, 배지에서는 `exact`와 똑같이 무배지가 된다.**
즉 **적지 않은 것이 "정확히 일치한다"는 주장으로 바뀐다.**

134차가 자기 부착분 **30건**을 명시했고, 137차가 **나머지 56건**을 확정한다.

---

## 2. 판정 기준 (20회차 F3 — *"match의 취지는 등재 값에 대한 근거력 표기"*)

| 등급 | 뜻 |
|---|---|
| `exact` | 값이 그 문서에서 **직접** 나왔다 |
| `near` | 값이 문서 값과 **근접하지만 같지 않다**(반올림·확대·근사 일치) |
| `partial` | 문서가 값의 **일부·맥락·해석 근거**일 뿐이다 |

---

## 3. 결과 — exact 45 · near 4 · partial 7

### 🔴 near 4건 — "값이 원문과 다르다"가 이번 차수의 수확이다

| 상수 | 등재값 | 원문값 | 차이 |
|---|---|---|---|
| `STRUCTURE_ONLY_PYEONG` | **163,400** | 우민재 철골 114,870,017 ÷ **702.7075평**(표지 2,323㎡) = **163,468** | 0.04% |
| `BENCHMARK_BANDS` 필름 상한 | **240,000** | 우민재 557,152,000 ÷ 2,323 = **239,842** | 올려 잡은 경계 |
| `ACTUALS_COUNT` 우민재(공사설명서) | **2,323** | p1 면적표 **2,321.87** | 0.05% |
| `ACTUALS_COUNT` 우민재(도면) | **2,323** | 배치도 면적개요 **2,321.87** | 0.05% |

📌 **`STRUCTURE_ONLY_PYEONG`이 특히 그렇다** — `source`가 *"엔진값과 오차 0.04%로 거의 정확히
일치해 실측 확정"*이라 적는다. 즉 **값이 이 문서에서 나온 것이 아니라 A-8 엔진값을 이 문서로
근사 확인**한 것이다. 등급 없이 두면 **"원문 전사"로 읽힌다.**

📌 **우민재 2건은 26회차 F4가 올린 `AC5` 그 자체**다(등재 2,323 vs 원문 2,321.87 —
적용 시 필름 밴드 상한과의 여유가 159원 → 42원). 이제 **배지로도 드러난다.**

⚠️ `BENCHMARK_BANDS`의 이 ref는 **상한만** 뒷받침한다 — 하한 115,000은 공주 계열이고
**그 원문은 리포에 없다**.

### partial 7건

| 대상 | 왜 |
|---|---|
| 한수진·최선동 **도면 6종**(평면도·측면골조도·주단면도) | 도면은 **면적 기준의 해석 근거**이고 등재된 면적값(4,092·3,145)은 **견적서 사업량 표기**에서 왔다 |
| `SUBSIDY_PROGRAM_TYPES_REFERENCE` 시행계획 원문 | 사업 명칭 **4건 확인·2건 미확인** — 등재값 전체를 뒷받침하지 않는다 |

### exact 45건

- **법령·고시 별표 원문 6건** — 에너지법 별표(순발열량·총발열량) · 법인세법 별표5·6 ·
  건축사대가 별표5 · 조달청고시 별표1. **값이 그 별표에서 직접 나온다.**
- **견적 원문 39건** — `CAPEX_MAJOR_CASE_CHUNKS` 16 · `CAPEX_MAJOR_KNOWN_TOTALS` 15 ·
  `ACTUALS_COUNT` 원문 대조 6 · `CAPEX_CATEGORY_OBSERVED_RANGE` 2.
  각 파일이 **자기 표본의 값을 정확히** 뒷받침한다(134차 `CAPEX_MAJOR_UNCLASSIFIED`와 같은 논리).

---

## 4. 레지스트리 전체 등급 분포

| | exact | near | partial | 계 |
|---|---|---|---|---|
| **137차 전** | 45(+암묵 56) | 4 | 43 | 148 |
| **137차 후** | **90** | **8** | **50** | **148** |

→ **미기재 0건.** 137차 가드가 그것을 고정한다.

---

## 5. ⚠️ 산출물이 바뀌었다

`SmartFarm_근거대장.html`의 배지가 늘었다:

```
[근접]  4 → 8   (+4)
[부분] 43 → 50  (+7)
```

→ 부여한 **near 4 · partial 7**과 정확히 일치한다.

### 🔴 131차 자기증식을 또 되풀이했다

레지스트리 `source`에 로그를 쓰면서 **`[근접]`·`[부분]` 문자열을 그대로 썼더니**
그 서술이 근거대장 HTML로 렌더돼 **배지 수가 9·50으로 부풀었다**(실제는 8·50).
→ 서술에서 **대괄호 표기를 빼** 되돌렸다.

📌 **131차(`[확인요망]` 집계 자기증식) · 134차(ref note의 같은 실수)에 이은 세 번째**다.
**세는 문자열을 서술에 쓰면 그 서술이 집계를 늘린다** — 배지도 같은 성질이다. **값은 하나도 안 바뀌었고 표기만 늘었다** —
그러나 **독자가 보는 근거력 표시가 달라졌으므로 산출물 변경 차수로 취급한다.**

---

## 6. 검증하지 않은 것

- **엔진 상수 값도 레지스트리 값도 하나도 바꾸지 않았다.** `상수 54` · `source_refs 148` 불변.
- 🔴 **등급은 각 ref의 `note`와 그 상수의 `source` 서술을 근거로 판정한 것**이고,
  **원문을 새로 열어 대조하지는 않았다**(경계 3건 — `BENCHMARK_BANDS`·
  `CAPEX_CATEGORY_OBSERVED_RANGE`·`STRUCTURE_ONLY_PYEONG` — 만 `source` 원문을 다시 읽었다).
- **exact 45건이 정말 원단위로 일치하는지 재계산하지 않았다** — 각 차수가 당시 확인한
  기록(`note`의 known_total 등)을 신뢰했다.
- **137차 이전부터 `partial`/`near`였던 47건은 건드리지 않았다**(등급 재검토는 별건).
- `AC5`(우민재 2,323 → 2,321.87 **값 교체**)는 **하지 않았다** — 회귀 기준 변경이라 ★사용자 결정.
