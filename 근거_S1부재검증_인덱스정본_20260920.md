# S-1 「원문 미보유」를 자료로 확정했다 — 그리고 낡은 인덱스를 봤다 (160차)

**작성일** 2026-09-20 (160차) · **계기** 사용자 지시 *"자료가 있는 내용만 진행"*
**성격** 엔진 상수 값 변경 0 · 케이스·견적비교·레지스트리 값 변경 0 · **결정 0** · 인덱스 **재생성 없음**

> 남은 것이 결정과 자료뿐이라면, **「자료가 없다」는 주장 자체는 자료로 검증할 수 있다.**
> 146차 [8]에서 레드팀이 *"강정구 5,597,384는 원문에 없다"*고 했는데 **원문엔 있었다** —
> 부재 판정이 탐색 범위에 달려 있다는 것이 그때 드러났다(R6).

---

## 1. ✅ S-1 「원문 미보유 3건」은 사실이다 — 탐색 범위를 병기한다

| 탐색 | 원채원 | 공주장원리 | 당진이상근 |
|---|---|---|---|
| 파일명 전수(`.git`·스코프 제외 폴더 뺀 저장소 전체) | **0** | **0** | **0** |
| 청킹 인덱스 `_전체.jsonl`(48,433청크) | **0** | — | — |
| **청킹 인덱스 `_전체_9축.jsonl`(148,424청크)** | **0** | — | — |
| 금액 문자열 `702,030,000`·`702030000` | 6파일 — **전부 리포 자체 기록**(인수인계서·작업계획서·작업지시서·`cases/`) | — | — |

원채원 이름이 붙은 파일 4건은 **전부 리포가 생성한 산출물**이다
(`SmartFarm_리포트_wonchaewon.html`·`SmartFarm_리포트_원채원.html`·`SmartFarm_통합보고서_wonchaewon.html`·`cases/wonchaewon.json`).

→ **회귀 기준 케이스의 원문은 리포에 없다.** 이제 그 주장이 **탐색 범위와 함께** 기록됐다(R6 충족).

---

## 2. 🔴 나는 낡은 인덱스를 보고 있었다

저장소에 청킹 인덱스가 **세 개** 있다:

| 파일 | 크기 | 갱신 | 청크 |
|---|---|---|---|
| `문서청킹_인덱스_파일럿.jsonl` | 3.4MB | 2026-07-24 | — |
| `문서청킹_인덱스_전체.jsonl` | 31.6MB | **2026-07-25** | **48,433** |
| **`문서청킹_인덱스_전체_9축.jsonl`** | **124.8MB** | **2026-09-13** | **148,424** |

**9축이 최신이고 3배 크다.** 그런데 이 차수 초반에 나는 `_전체.jsonl`(7월판)을 조회했고,
**147차에서도 같은 파일을 봤다**(우민재 원문 검색 때).

### 그래서 처음 낸 측정이 틀렸다

7월판으로 재니 CAPEX 표본 15건 중 **9건이 청크 0~9건**이라 *"커버리지가 심하게 비대칭"*이라
결론했다. 9축으로 다시 재면:

| 표본 | 7월판 | 9축 |
|---|---|---|
| 이두희 | 3 | **1,432** |
| 윤성호 | 0 | **735** |
| 한일그린텍 | 2 | **553** |
| 강정구 | 0 | **184** |
| 임미라 | 0 | **477** |
| 한수진 | 9 | **325** |
| 최선동 | 9 | **371** |

**희박은 9건이 아니라 사실상 1건**이다(아래 §3).

📌 **146차 [8]의 인과도 여기 있었을 수 있다** — 레드팀이 강정구를 *"원문에 없다"*고 했을 때
7월판을 봤다면 그 표본은 **청크 0건**이었다. 다만 레드팀이 어느 인덱스를 썼는지는
보고서에 없어 **확인할 수 없다**(추정으로 적지 않는다).

⚠️ **인덱스를 재생성하지 않았다.** CLAUDE.md가 *"수기 편집 금지 — 재생성은
`run_chunk_incremental.py`로만"*이라 했고, 9축이 이미 최신이므로 **재생성할 이유가 없다.**
문제는 인덱스가 아니라 **내가 어느 파일을 열었는가**였다.

---

## 3. 희박해 보인 2건의 정체 — 둘 다 결함이 아니다

| 표본 | 9축 청크 | 이유 |
|---|---|---|
| `백가은·조윤정` | **0** | 🔴 **엔진 표본명이 합성**이다. 인덱스에는 `백가은` **435** · `조윤정` **435**로 **따로** 있다 — **이름으로 세면 0이 나온다** |
| `구창회` | 26 | 원문이 **`구창회 설계도면_ 최종.pdf` 한 건(7.2MB)**이고 **도면**이다. 견적서가 아니라 청크가 적은 것이 자연스럽다 |

→ **실질 커버리지 결함 0건.**

`백가은·조윤정`은 **131차 「문자열 수는 항목 수가 아니다」·156차 「21곳」과 같은 계열**이다 —
**세는 키가 대상과 일치하지 않으면 0이 나온다.**

---

## 4. 이 차수가 하지 않은 것

- **결정은 하나도 내리지 않았다.** D-1~D-15 전부 ★ 대기다.
- **엔진 상수 값·케이스·견적비교·레지스트리 값 전부 불변.**
- **청킹 인덱스를 재생성하지 않았다**(§2) — 9축이 이미 최신이고, 수기 편집은 금지다.
- **낡은 `_전체.jsonl`을 삭제하지 않았다** — 과거 차수들이 그 파일을 근거로 판정했을 수 있어
  **지우면 그 판정들의 재현이 불가능해진다.** 대신 **어느 것이 최신인지 가드로 고정**했다.
- **146차 [8]에서 레드팀이 어느 인덱스를 썼는지 단정하지 않았다** — 보고서에 없다.
