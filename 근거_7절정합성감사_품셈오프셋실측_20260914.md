# 7절 정합성 감사 + 품셈 인쇄쪽번호 오프셋 실측 (107차)

**작성일** 2026-09-14 (107차) · **계기** 사용자 지시 **"d 진행"**(106차 ⑦후보 (d) —
동결 유지하고 **검증·문서 계열** 진행)
**성격** 값 변경 0. **이미 쓴 문장이 지금도 참인가**를 기계·원문으로 대조한다.

> **왜 7절인가**: 65차가 문서 전체를 실제 상태와 대조하면서 **7절만 명시적으로
> 건너뛰었다** — 그 차수의 「검증하지 않은 것」이 *"7절 미해결 이슈 항목별 최신성
> (U_VALUE·TOTAL_PYEONG_PRICE 등 조사 트랙 — 값 변경 없이 유지 중이라 재조사 대상
> 아님)"*이다. 그 뒤로 68·77·99·101·105·106차가 바로 그 항목들을 움직였다.

---

## 1. 결과 요약

7절 16개 항목 전수 대조. **후행 차수가 뒤집은 서술 10건**을 찾았다. 전부
**"틀린 값"이 아니라 "낡은 문장"**이다 — 산출물 수치에는 영향이 없고, 읽는 사람이
**이미 닫힌 과제를 열린 것으로 오해**하게 만드는 종류다.

| # | 7절의 현재 서술 | 실제 | 뒤집은 차수 |
|---|---|---|---|
| S1 | "극한설계조건용 `u_design`에 넣을 **확정값(1차 출처)이 없어**… 안전마진 감소 위험은 실질적으로 남아있다" | `U_DESIGN = 5.7`, status **공공기준** | **99차** |
| S2 | "원문서(**엔진데이터 A-4/A-5 원본**, 또는 KISTI TRKO202100009930)를 확보하면 그 값을 `u_design`에 주입할 것" | 해소는 **제3의 문서**(신개념온실 [표3-3-30])로 됐고, 엔진데이터 문서는 **리포에 없음이 확정**됐다 | 99차 · **106차** |
| S3 | "다음 세션에서 이 두 논문(2012 핫박스·2013 현장)을 나란히 놓고 어느 쪽이 `u_design`에 더 적합한지 **사용자와 함께 판단할 것**" | 99차가 판단했고 **둘 중 어느 쪽도 아닌** 표3-3-30으로 갔다. ⚠️105차가 **핫박스(Diop) 쪽을 가리키는 반대 신호**를 기록했다 | 99차 · **105차** |
| S4 | "`heating_load()`가 **safety=1.1을 곱하는 구조라 어느 정도 완충은 되나 확정 아님**" | 1.1의 **출처 확정**(PDF p.389/인쇄 353 "온풍난방 10% 안전계수") | **101차** |
| S5 | "`FR_TABLE`도 이 방향(PO단일0.35 < 다겹보온0.5 < **이중커튼0.85**)" · "절감률 수치(**0.35/0.5/0.85**)" | 현행 `FR_TABLE['이중커튼'] = **0.7**` | **68차** |
| S6 | "이 표는 `heating_load()`가 직접 참조하지 않는 **미사용 상수**" | `heating_load(curtain=…)` 경로로 **연결돼 있다** | P1-9(2026-08-17) |
| S7 | "(1) 이 강제는 **코드 주석 수준일 뿐 함수 시그니처가 강제하지 않아**… 방향 반전 버그가 **재발할 수 있다**" | `fr`/`curtain`/`assembly` **정확히 하나**만 받고 아니면 `ValueError` — 시그니처가 강제한다 | P1-9(2026-08-17) |
| S8 | "절감률 수치 자체는 **여전히 1차 출처 미확보**([확인요망] 유지)" | status **부분실측**, 독립 출처 3건(농사로 70%·NIHHS 65.4%·해외 이중 63%) | 68차 |
| S9 | "**남은 실질 공백은 설계감리비의 '요율→실제 CAPEX 계산 적용 방식 설계'뿐**" | 적용 방식 **결정 완료**(2026-08-18 사용자 결정 — 참고 표시 전용, CAPEX 합계 불산입) | 2026-08-18 |
| S10 | "⚠️ **미커밋 작업 유실 리스크(미해소)**: git 커밋 **3건뿐**" | 커밋 **112건**, 인덱스는 `.gitignore` 편입으로 택일 완료(11절 P0-1) — **65차가 11절은 갱신했으나 7절은 두고 갔다** | 65차 |
| S11 | Step6 제외표: "RFQ 다자간 업체비교 ← **실제 멀티벤더 견적 데이터 있는 케이스**" · "현재 **3개 케이스**(원채원·춘천·우민재)" | `견적비교_논산딸기3사.json` **vendor_quotes 3건** 존재(47차 레드팀 대상) · 케이스 **3정식 + 2부분** | 47차 등 |

📌 S10은 **7절 안에서도** 같은 문장이 2군데 있었는데, 2절(라인 356)의 것은
"2026-08-16 최근 작업" 날짜 블록 안이라 **이력으로 정당**하다 — 고치지 않는다.

---

## 2. 🔴 부수 발견 — 품셈 PDF는 리포 안에 있고, 오프셋을 실측했다

### 2-1. 파일 소재

104차 레드팀 F1이 *"품셈 PDF는 리포 밖"*이라는 서술을 잡아냈고 라벨을 되돌렸다.
107차에 **소재를 직접 확인**했다.

```
시설평가/202201_스마트팜 표준화_품셈.pdf                                 89,821,538 B
시설평가/농어촌공사(2022), 스마트팜 표준화를 위한 사전설계 및 온실공사 품셈 정립.pdf   89,821,538 B
```

- **둘 다 git 추적 중**이다(`git ls-files "시설평가/"` 18건에 포함).
- **SHA-256이 동일하다** (`a0908f3139256021…`) — **같은 파일 2부**다.

⚠️ 처음 `git ls-files | grep 품셈`은 0건을 냈는데, 이는 git이 비ASCII 경로를
**8진 이스케이프로 출력**하기 때문이다(`core.quotepath`). `-c core.quotepath=false`로
다시 돌려 확인했다 — **탐색 도구의 기본값이 만든 거짓 음성**이고, 106차에 같은 유형
(너무 좁은 glob)을 한 번 겪었다.

### 2-2. 인쇄 쪽번호 오프셋 = **−24** (실측)

이 PDF는 폰트에 ToUnicode가 없어 `pdfplumber`가 본문을 `(cid:N)` 글리프로만 내준다.
**각 쪽 꼬리말의 글리프 시퀀스**를 뽑아 자리수 패턴으로 역해독했다.

| PDF 쪽 | 꼬리말 cid 시퀀스 | 해독 | 인쇄 쪽 |
|---|---|---|---|
| 38 | `4,3,12,6,3,4` | `- 1 4 -` | 14 |
| 39 | `4,3,12,7,3,4` | `- 1 5 -` | **15** |
| 40 | `4,3,12,8,3,4` | `- 1 6 -` | **16** |
| 41 | `4,3,12,9,3,4` | `- 1 7 -` | 17 |
| 162 | `4,3,12,5,10,3,4` | `- 1 3 8 -` | **138** |
| 184 | `4,3,12,8,14,3,4` | `- 1 6 0 -` | **160** |
| 186 | `4,3,12,8,13,3,4` | `- 1 6 2 -` | **162** |
| 187 | `4,3,12,8,5,3,4` | `- 1 6 3 -` | **163** |
| 188 | `4,3,12,8,6,3,4` | `- 1 6 4 -` | **164** |
| 195 | `4,3,12,9,12,3,4` | `- 1 7 1 -` | **171** |

**자기정합 확인**: 글리프→숫자 대응이 10쪽 전부에서 **모순 없이 하나로 결정**된다 —
`12→1 · 13→2 · 5→3 · 6→4 · 7→5 · 8→6 · 9→7 · 10→8 · 14→0`. 같은 cid가 서로 다른
숫자를 요구하는 경우가 **한 건도 없다**. 그리고 10쪽 전부에서 **인쇄 = PDF − 24**다.

**→ 앞머리(38~41)와 본문(162~195)에서 오프셋이 같으므로, 앞표지·목차 구간에도
같은 규칙이 적용된다.**

### 2-3. 따라서 확정되는 것

| 기존 표기 | 확정 |
|---|---|
| 인쇄 p.138~162 (PDF 162~186) | ✅ **실측 재현** |
| 인쇄 p.163~164 (PDF 187~188) | ✅ **실측 재현** |
| 인쇄 p.160~162 (PDF 184~186) | ✅ **실측 재현** |
| 인쇄 p.171 (PDF 195) | ✅ **실측 재현** |
| **인쇄 p.15~16** (PDF 대응 미기재) | 🔴 **PDF 39~40으로 확정** — 14절의 "품셈 p.15~16 계열 확정" 차단이 **쪽번호 쪽은 풀린다** |

📌 **104차 F1의 판단이 독립적으로 재현됐다.** 인용은 처음부터 **정확한 인쇄
쪽번호**였고, 102차가 그것을 흐린 것을 104차가 되돌린 것이 옳았다. 이제는
"되돌렸다"가 아니라 **"측정했다"**이다.

---

## 3. 조치

**값은 하나도 바꾸지 않았다.** 문장만 현재 상태에 맞췄다.

1. `작업지시서.md` 7절 — **원문 서술은 지우지 않고** 각 항목에 `📌107차 정정` 주석을
   덧붙였다(31차 관례: 이력은 보존, 현재 상태는 위에서 읽게 한다). 절 머리에
   **상태 인덱스**를 넣어 "열린 것 / 닫힌 것 / 14절로 이관된 것"을 먼저 보게 했다.
2. `smartfarm_engine.py`·`엔진데이터_레지스트리.json` — 품셈 인용에 **실측 오프셋
   −24**와 **리포 내 경로**를 기록. `인쇄 p.15~16` → `인쇄 p.15~16(PDF 39~40)`.
3. `test_engine.py` — 107차 가드. 🔴**초판 가드에 구멍이 있었고 스스로 잡았다**: 축약형 경로만 금지해서 같은 항목의 **전체 경로 형태 1건**이 통과했다(생성 HTML 대조 중 발견). 불변식을 **"품셈 파일명은 언제나 `시설평가/` 뒤에 온다"**로 바꿨다 — **열거는 빠뜨리고 불변식은 안 빠뜨린다**(106차 좁은 glob과 같은 유형). 102차 테스트의 도크스트링이 *"리포 밖 품셈"*이라
   적고 있던 것도 함께 정정했다(**테스트 문서도 낡는다**).

---

## 4. 한계

- **7절 16항목 중 대조한 것은 "후행 차수가 건드린 흔적이 있는 항목"이다.** 나머지
  (hwp 스캔본·부지선정 토목·LFS·`dev/null` 훅)는 **도구 가용성만 재확인**했다 —
  `hwp5` 미설치·`tesseract` 부재를 확인했으므로 결론(재시도 불가)은 유지되나,
  **다른 추출 경로를 새로 찾아보지는 않았다.**
- **오프셋 −24는 10쪽 표본의 관찰**이다. 중간에 삽입면지가 있어 구간별로 달라질
  가능성은 배제하지 못했다 — 다만 앞머리와 본문 양쪽에서 같아 개연성은 낮다.
- 글리프 역해독은 **꼬리말 자리수 패턴에 기댄 추론**이다. 각 쪽을 사람이 눈으로
  본 것이 아니다(폰트 제약). 대응이 10쪽에서 모순 없이 하나로 결정된다는 것이
  근거다.
- **인쇄 p.15~16의 *내용*은 확인하지 않았다.** 쪽번호 대응만 확정했다 — 14절의
  해당 차단은 **절반만** 풀린다.
